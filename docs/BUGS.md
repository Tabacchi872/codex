# BUGS.md

Registro dei bug noti. Aggiornare ad ogni intervento (skill `project-memory`, `webapp-testing`).

Formato per ogni voce:

```
## [ID] — [titolo]
- **Stato:** aperto / in corso / risolto
- **Severità:** bloccante / alta / media / bassa
- **Data apertura:**
- **Data chiusura:**
- **Descrizione:**
- **Passi di riproduzione:**
- **Fix applicato:**
```

---

## BUG-001 — ReferenceError "Audio is not defined" sulle schermate con timer di recupero
- **Stato:** risolto
- **Severità:** alta (rendering rotto su ogni schermata Dettaglio esercizio con una scheda assegnata)
- **Data apertura:** introdotto quando è stato aggiunto `RestTimer`/`SoundSettings` (sessione precedente), scoperto il 2026-07-05
- **Data chiusura:** 2026-07-05
- **Descrizione:** `expo-audio` (`useAudioPlayer`) referenzia l'API `Audio` del browser. Con `expo.web.output: "static"`, Expo Router pre-renderizza ogni rotta anche lato server (Node), dove `Audio` non esiste: il render falliva con `ReferenceError: Audio is not defined` su ogni pagina che monta `RestTimer`/`SoundSettings`. Il bug esisteva silenziosamente da quando questi componenti sono stati introdotti: le verifiche precedenti via `curl` controllavano la presenza di testo atteso ma non uno scan esplicito di "ReferenceError" su queste rotte specifiche.
- **Passi di riproduzione:** `curl http://localhost:8081/esercizi/gambe-squat` (o qualsiasi esercizio assegnato a un cliente) con `output: "static"` → risposta HTTP 200 contenente `ReferenceError: Audio is not defined`.
- **Fix applicato:** `mobile/app.json` → `expo.web.output` cambiato da `"static"` a `"single"` (SPA, nessun pre-render server-side). Vedi `docs/DECISIONS.md` per il ragionamento e la conseguenza sul metodo di verifica (non più possibile controllare il contenuto per-rotta via `curl`, serve un browser reale).

## BUG-002 — Tab bar cliente e card Home non navigano (solo Nutrizione raggiungibile)
- **Stato:** risolto (lato web); rischio residuo lato nativo non verificato
- **Severità:** bloccante (l'intero lato cliente, tranne una schermata, era irraggiungibile)
- **Data apertura:** introdotto insieme alla tab bar a 5 voci (sessione precedente, 2026-07-05), segnalato dall'utente il 2026-07-05
- **Data chiusura:** 2026-07-05
- **Descrizione:** `client-tabs.web.tsx` usava le Tabs "headless" di `expo-router/ui` (`<Tabs><TabSlot/><TabList><TabTrigger .../></TabList></Tabs>`). Leggendo il sorgente installato (`node_modules/expo-router/build/ui/common.js`, funzione `triggersToScreens`), questo componente costruisce un navigator React Navigation che conosce **solo** gli schermi elencati esplicitamente come `<TabTrigger>` (e i loro discendenti nel filesystem). Le 5 tab dichiarate (`cliente-home`, `workout`, `nutrizione`, `chat`, `altro`) erano tutte foglie senza figli: qualunque altra rotta cliente — `prenotazioni`, `bacheca`, `questionario`, `cliente-profilo`, `progressi`, e persino `schede/[id]`/`esercizi/[id]` aperti dalla card "Allenamenti" — non era un discendente di nessuna delle 5 e restava quindi **invisibile a quel navigator**: `router.push()` verso quelle rotte non aveva alcuno schermo registrato a cui approdare. "Nutrizione" sembrava funzionare proprio perché, a differenza delle altre, era una delle 5 rotte effettivamente registrate.
- **Passi di riproduzione:** da cliente, toccare una card Home diversa da "Nutrizione" (es. "Allenamenti", "Questionari", "Prenotazioni", "Bacheca") o le voci del menu "Altro": nessuna navigazione avviene.
- **Fix applicato:** `client-tabs.web.tsx` riscritto usando `Slot` (primitivo standard di `expo-router`, non `expo-router/ui`) come contenuto, invece delle Tabs headless. Verificato nel sorgente (`node_modules/expo-router/build/views/Navigator.js`, `SlotNavigator`) che `Slot` costruisce i suoi schermi da `useSortedScreens` sull'**intero albero di file** della rotta corrente, non da una lista chiusa dichiarata a mano: qualunque rotta esistente è quindi raggiungibile. La tab bar in basso resta identica nell'aspetto (stesse icone/colori/layout) ma ora è costruita con `Pressable` + `router.push()` + `usePathname()` per lo stato attivo, senza alcun meccanismo di navigazione "nascosto". Aggiunto `screenOptions={{ headerShown: false }}` allo `Slot` per evitare che lo Stack sottostante mostri un header nativo di default non previsto dal design. Nessuna modifica alla grafica, alle card, alla struttura dati o al lato coach (che usa file separati e non è stato toccato).
- **Rischio residuo:** la tab bar cliente **nativa** (`client-tabs.tsx`, iOS/Android via `NativeTabs`) usa lo stesso meccanismo a "trigger dichiarati" e molto probabilmente ha lo stesso limite strutturale, ma non è stato possibile verificarlo né corregerlo in questa sessione (ambiente Windows senza simulatore, test solo via Expo Web). Va rivisto prima di un test reale su device.

## BUG-003 — WorkoutPlanForm azzerava lo stato di avanzamento sessione in modifica
- **Stato:** risolto
- **Severità:** alta (perdita silenziosa di dati: sessione completata poteva tornare "da fare" salvando una modifica minima)
- **Data apertura:** introdotto quando `WorkoutPlan.sessionStatus`/`startedAt`/`completedExerciseIds`/`durationSeconds` furono aggiunti (sessione 2026-07-05), scoperto il 2026-07-06
- **Data chiusura:** 2026-07-06
- **Descrizione:** `WorkoutPlanForm.handleSave` costruiva l'oggetto `WorkoutPlan` da salvare includendo solo i campi gestiti dal form (nome, cliente, date, esercizi). In modalità "modifica scheda esistente" (`schede/[id].tsx`, mode='edit'), questo significava che `sessionStatus`, `startedAt`, `completedExerciseIds`, `durationSeconds` — tutti presenti su `initialPlan` ma non ripassati a `onSave` — venivano scritti come `undefined` nello store, azzerando silenziosamente lo stato di avanzamento di una sessione già in corso o completata. Mai notato prima perché non era mai stato verificato un giro "completa sessione → poi modifica scheda → controlla se lo stato è ancora completato".
- **Passi di riproduzione:** completare una sessione (sessionStatus='completed'), poi da coach premere "Modifica scheda", cambiare un campo qualsiasi (es. il nome) e salvare: lo stato tornava a "Da fare" e `completedExerciseIds`/`durationSeconds` sparivano.
- **Fix applicato:** `components/workout-plan-form.tsx`, `handleSave` ora ripassa esplicitamente `sessionStatus`, `startedAt`, `completedExerciseIds`, `durationSeconds`, `completedAt` da `initialPlan` (oltre ai nuovi campi `coachId`/`subscriptionId`/`scheduledTime`/`dayLabel`/`weekLabel`), invece di ometterli.
- **Verifica:** corretto per lettura del codice e tramite typecheck; **non verificato con un clic reale in browser** in questa sessione (nessun tool di automazione browser disponibile) — da confermare aprendo l'app di persona: completare una sessione, modificarne il nome, controllare che resti "Completato".

## BUG-004 — Card Abbonamento spariva cambiando lo status (sembrava che il salvataggio non funzionasse)
- **Stato:** risolto
- **Severità:** alta (il coach non riusciva più a vedere/gestire l'abbonamento dopo un cambio di stato, sembrando un salvataggio fallito)
- **Data apertura:** introdotta con `SubscriptionPackage`/`clienti/[id].tsx` (2026-07-06, parte 1), scoperta e segnalata dall'utente il 2026-07-06
- **Data chiusura:** 2026-07-06
- **Descrizione:** la card "Abbonamento" nel dettaglio cliente calcolava `activeSubscription = subscriptions.find(s => s.status === 'active')` e mostrava i dettagli SOLO se questo esisteva; in caso contrario mostrava "Nessun abbonamento attivo". Cambiare lo stato di un abbonamento esistente (es. da Attivo a In pausa/Completato/Scaduto) tramite "Aggiorna abbonamento" salvava correttamente il nuovo stato nello store (`updateSubscription` sostituisce l'intero oggetto), ma la UI smetteva immediatamente di mostrare QUALSIASI informazione su quell'abbonamento — dando l'impressione, dal punto di vista del coach, che la modifica non fosse stata salvata o fosse stata "persa".
- **Passi di riproduzione:** creare un abbonamento (status Attivo), aprire "Aggiorna abbonamento", cambiare lo stato in "In pausa" e salvare: tornati al dettaglio cliente, la card mostrava "Nessun abbonamento attivo" invece del pacchetto con stato "In pausa".
- **Fix applicato:** `app/clienti/[id].tsx` introduce `displaySubscription` = l'abbonamento attivo se esiste, altrimenti il più recente per data di inizio tra tutti quelli del cliente — sempre mostrato con `SUBSCRIPTION_STATUS_LABEL[displaySubscription.status]` esplicito. "Nessun abbonamento" (testo aggiornato, non più "attivo") compare solo se il cliente non ha mai avuto nessun abbonamento. Aggiunto anche il valore `'cancelled'`, mancante da `SubscriptionStatus`.
- **Verifica:** corretto per lettura del codice (la logica di salvataggio nello store è stata riverificata ed è sempre stata corretta) e tramite typecheck; **non verificato con un clic reale in browser** — da confermare: cambiare stato di un abbonamento più volte e controllare che la card rifletta ogni volta il valore corretto.

## BUG-005 — Stato "Saltato" restava bloccato dopo qualunque modifica alla scheda
- **Stato:** risolto
- **Severità:** media (una sessione saltata non poteva più essere riprogrammata modificandola, serviva un giro manuale con i chip di stato)
- **Data apertura:** effetto collaterale del fix di BUG-003 (2026-07-06, parte 1), scoperto e segnalato dall'utente il 2026-07-06
- **Data chiusura:** 2026-07-06
- **Descrizione:** dopo il fix di BUG-003 (che ha reso `WorkoutPlanForm.handleSave` corretto nel preservare `sessionStatus` invece di azzerarlo), modificare una scheda con stato "Saltato" tramite "Modifica scheda" la lasciava bloccata su "Saltato" per sempre, anche cambiando data/ora — prima del fix di BUG-003 questo non si notava perché lo stato veniva azzerato a "Da fare" ad ogni modifica (comportamento sbagliato ma che nascondeva questo problema).
- **Passi di riproduzione:** impostare una sessione su "Saltato" (chip di stato), poi da "Modifica scheda" cambiare data/ora e salvare: lo stato restava "Saltato" invece di tornare "Da fare"/Programmato.
- **Fix applicato:** `app/schede/[id].tsx`, `handleSave`: se la sessione era `'skipped'` e la data (`startDate`) o l'ora (`scheduledTime`) sono cambiate rispetto al valore precedente, lo stato torna automaticamente a `'todo'`. Se non cambiano data/ora, "Saltato" resta invariato (comportamento corretto: cambiare solo il nome non deve riprogrammare la sessione). Il coach può comunque sempre impostare manualmente lo stato tramite i chip in modalità vista, indipendentemente da questo automatismo.
- **Verifica:** corretto per lettura del codice e tramite typecheck; **non verificato con un clic reale in browser** — da confermare: impostare "Saltato", cambiare data, controllare che torni "Da fare"; impostare "Saltato", cambiare solo il nome, controllare che resti "Saltato".

## BUG-006 — Dettaglio Esercizio cliente: input "Ripetizioni" tagliato e Allegati coperto dalla tab bar su schermi stretti (360px)
- **Stato:** risolto
- **Severità:** alta (schermata centrale del logging allenamento illeggibile/inutilizzabile su schermi stretti, es. 360px)
- **Data apertura:** introdotta con `ExerciseSetLogger` (sessione 2026-07-05, "Tab bar cliente a 5 voci... Dettaglio esercizio come logger di sessione"), segnalata dall'utente il 2026-07-06
- **Data chiusura:** 2026-07-06
- **Descrizione:** due problemi distinti. (1) `exercise-set-logger.tsx`: ogni riga "Serie" era `flexDirection: 'row'` con un'etichetta a larghezza fissa (`width: 60`) seguita da due `ThemedTextInput` con `flex: 1` ma senza `minWidth: 0`. Su RN Web, un flex item senza `minWidth: 0` non si restringe sotto la dimensione "di contenuto" del suo elemento nativo (l'input HTML): con card/schermo stretti (360px) lo spazio disponibile per i due input scendeva sotto quella soglia, e il secondo input ("Ripetizioni") veniva letteralmente tagliato dal bordo della cornice `WebPhoneFrame`, che ha `overflow: 'hidden'` (necessario per gli angoli arrotondati del mockup telefono) — quindi il contenuto in eccesso spariva invece di andare in scroll orizzontale visibile. (2) `app/esercizi/[id].tsx`: il `ScrollView` aveva `paddingBottom: Spacing.six` (64, fisso), a differenza delle altre schermate cliente (`cliente-home.tsx`, `workout.tsx`) che usano `insets.bottom + BottomTabInset + Spacing.four`: mancava il margine di respiro extra e, su nativo, l'inset di sicurezza reale del dispositivo — la sezione "Allegati" (ultimo blocco visibile) restava quindi troppo vicina/sotto la tab bar fissa.
- **Passi di riproduzione:** aprire Dettaglio Esercizio lato cliente (es. "Squat") con l'anteprima Expo Web impostata a 360px: l'input "Ripetizioni" di ogni riga Serie appariva tagliato a destra; scorrendo fino in fondo, "Allegati" risultava troppo vicino alla tab bar in basso.
- **Fix applicato:** `components/exercise-set-logger.tsx` — ogni riga Serie ora mostra l'etichetta "Serie N" su una riga propria e Peso/Ripetizioni in una riga sottostante dedicata (`setInputsRow`, `flexDirection: 'row'`, due figli `flex: 1` con `minWidth: 0` come sicurezza aggiuntiva): ciascun input ha sempre a disposizione metà della larghezza della card, indipendentemente dalla larghezza dello schermo, senza fare affidamento sullo shrink dei flex item. Aggiunto anche `width: '100%'`/`maxWidth: '100%'` al contenitore per escludere overflow dal lato della card. `app/esercizi/[id].tsx` — `paddingBottom` del `ScrollView` allineato al pattern già usato nelle altre schermate cliente: `insets.bottom + BottomTabInset + Spacing.five` (margine leggermente più generoso, dato il contenuto lungo di questa schermata).
- **Verifica:** corretto per lettura del codice (calcolo esplicito della larghezza disponibile nella cornice a 360px, che ha bordo `borderWidth: 6` incluso nel `width` — RN usa box-sizing border-box) e tramite typecheck (0 errori) + smoke test HTTP; **non verificato con uno screenshot/clic reale in browser** (nessun tool di automazione/screenshot disponibile in questo ambiente) — da confermare aprendo la preview a 360/390/430px e controllando visivamente Dettaglio Esercizio.

## BUG-007 — Registrazione cliente Supabase faceva login automatico anche con email non confermata
- **Stato:** risolto
- **Severità:** alta (con "Confirm email" attivo su Supabase, un cliente entrava subito nell'app senza aver mai confermato l'indirizzo, e un secondo bypass permetteva login locale con password in chiaro anche a email non confermata)
- **Data apertura:** introdotta con il collegamento Supabase Fase 1 (2026-07-08), scoperta durante l'implementazione della Fase 2 email vere (2026-07-09) — mai innescata prima perché "Confirm email" non era ancora stato provato attivo con un progetto reale
- **Descrizione:** due problemi collegati. (1) `ClientRegistrationScreen.handleRegister` (`registration-screens.tsx`), dopo `signUpClientWithCoachCode`, ignorava `result.data.session`: se Supabase aveva "Confirm email" attivo, `session` tornava `null` (utente non ancora confermato), ma l'app creava comunque il mirror locale e chiamava `loginAsClient` + redirect a `/cliente-home`, facendo finta che l'utente fosse attivo. (2) Anche lato login, se poi l'utente tentava un accesso reale prima della conferma, `signInWithEmail` falliva con "Email not confirmed", ma `login-screen.tsx` non distingueva questo errore dagli altri e ricadeva sui controlli locali (`coachAccounts`/`accounts` con password in chiaro) — record locali scritti in fase di registrazione (sia coach sia cliente) permettevano quindi un secondo bypass di login anche a email non confermata.
- **Passi di riproduzione:** con "Confirm email" attivo su un progetto Supabase reale, registrare un cliente (o un coach) e osservare l'ingresso immediato nell'app senza mai aver cliccato il link di conferma ricevuto via email.
- **Fix applicato:** `registration-screens.tsx` — `ClientRegistrationScreen` ora controlla `result.data.session`: se `null`, mostra una schermata dedicata "Controlla la tua email" (nessun mirror locale scritto, nessun `loginAsClient`, nessun redirect); `CoachRegistrationScreen` mostra un avviso equivalente sulla schermata "Il tuo codice coach" (non faceva comunque login automatico, ma prima non segnalava la conferma email richiesta). `login-screen.tsx` — aggiunto un controllo esplicito sul nuovo codice errore `email_not_confirmed` (`auth-service.ts`, `mapAuthErrorCode`): se presente, mostra un messaggio dedicato e interrompe subito, senza più ricadere sui controlli locali che avrebbero permesso il bypass.
- **Verifica:** `npx tsc --noEmit` pulito, `npx expo-doctor` 20/20, bundle Metro compilato senza errori con le nuove schermate incluse (smoke test HTTP su porta locale di test). **Non ancora verificato end-to-end con un progetto Supabase reale e "Confirm email" attivo** (serve un'inbox email reale, non disponibile in questo ambiente) — da fare dall'utente, vedi `docs/EMAIL_SETUP.md` sezione "Come testare da web".

## BUG-008 — Con "Confirm email" attivo, profiles/client_profiles/coach_clients restavano vuote dopo la registrazione
- **Stato:** risolto
- **Severità:** bloccante (con "Confirm email" attivo su un progetto Supabase reale, coach e clienti registrati non avevano mai le righe database necessarie a far funzionare login/dashboard/collegamento coach-cliente, pur con l'utente creato correttamente in Auth e l'email di conferma inviata)
- **Data apertura:** introdotta insieme alla Fase 2 email vere (2026-07-09), segnalata dall'utente il 2026-07-10 dopo aver attivato "Confirm email" su un progetto reale
- **Descrizione:** due cause distinte. (1) `signUpCoach`/`signUpClientWithCoachCode` (`auth-service.ts`) tentavano di scrivere `coach_profiles`/`billing_profiles`/`registration_codes` (coach) e `client_profiles`/`coach_clients` (cliente) **subito dopo** `supabase.auth.signUp`, nella stessa chiamata — ma con "Confirm email" attivo, `signUp` ritorna `session: null` fino alla conferma, quindi queste chiamate partivano senza alcuna sessione autenticata (solo anon key): le policy RLS che richiedono `auth.uid() = ...` bloccavano l'insert (l'`auth.uid()` lato Postgres è `null` senza un JWT utente), e quei dati (inseriti dalla form di registrazione) non erano più recuperabili in un secondo momento, essendo persi non appena l'utente lasciava la schermata. (2) `profiles`, creata dal trigger `handle_new_user` su `auth.users` (security definer, indipendente dalla sessione), risultava comunque vuota: il trigger installato sul progetto reale dell'utente non stava producendo la riga attesa (verosimilmente non ancora eseguito/aggiornato su quel progetto specifico), e non esisteva alcuna policy che permettesse a un client autenticato di scrivere la propria riga `profiles` come fallback (mancavano `profiles_self_insert`/`profiles_self_update`) — quindi anche `signInWithEmail`, leggendo `profiles` dopo un login riuscito, falliva con "Accesso riuscito ma profilo non trovato" invece di ripararla.
- **Passi di riproduzione:** su un progetto Supabase reale con "Confirm email" attivo, registrare un coach o un cliente, confermare l'email dal link ricevuto, poi fare login: `profiles`/`client_profiles`/`coach_clients` (Table editor) risultavano vuote, e il login falliva o comunque non trovava il profilo cliente/coach atteso.
- **Fix applicato:** `docs/SUPABASE_SCHEMA.sql` — aggiunte le policy `profiles_self_insert`/`profiles_self_update` (mancanti); `handle_new_user` reso più robusto (`on conflict do update` invece di `do nothing`, e un blocco `exception when others` che non lascia mai fallire la creazione dell'utente Auth per un problema nell'insert di `profiles`). `auth-service.ts` — `signUpCoach`/`signUpClientWithCoachCode` ora salvano i dati necessari (`billing_profile`/`business_name` per il coach; `coach_id`/`coach_code` per il cliente) in `user_metadata` al momento di `signUp`, e tentano gli insert dipendenti solo se la sessione esiste già subito; se `session` è `null`, li rimandano a due nuove funzioni (`ensureCoachOnboarding`/`ensureClientOnboarding`) chiamate da `login-screen.tsx` al primo login reale, che rileggono quei dati da `user_metadata` invece di richiederli di nuovo. `signInWithEmail` non fallisce più con "profilo non trovato" se `profiles` manca: la ricrea dal client autenticato (`ensureProfileRow`, nuova funzione interna) usando gli stessi dati passati a `signUp`. Aggiunta anche `ensureProfileForCurrentUser` (fallback riutilizzabile indipendente dal login). Vedi `docs/DECISIONS.md`, voce "2026-07-10 — Onboarding coach/cliente differito al primo login reale quando 'Confirm email' è attivo".
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro compilato senza errori con le nuove funzioni incluse (smoke test HTTP su porta di test locale). **Non ancora verificato contro un progetto Supabase reale** (nessun accesso diretto in questo ambiente): l'utente deve rieseguire lo snippet SQL aggiornato (policy + trigger, vedi `docs/EMAIL_SETUP.md`) sul proprio progetto e ripetere una registrazione/conferma/login completa. Account già registrati PRIMA di questo fix (senza `billing_profile`/`coach_id` in `user_metadata`) non vengono riparati automaticamente da `ensureCoachOnboarding`/`ensureClientOnboarding` — vanno sistemati manualmente o registrati di nuovo.
- **Addendum 2026-07-10 (continuazione) — causa risolta alla radice:** `handle_new_user()` (`docs/SUPABASE_SCHEMA.sql`) ora crea direttamente TUTTE queste righe (non solo `profiles`) `after insert on auth.users`, indipendentemente da "Confirm email" — non serve più attendere il primo login per completarle. `ensureCoachOnboarding`/`ensureClientOnboarding` restano come rete di sicurezza idempotente, non più come percorso primario. Vedi `docs/DECISIONS.md`, voce "2026-07-10 (continuazione) — Trigger DB definitivo sostituisce l'onboarding differito lato app come percorso primario". Resta comunque **non verificato contro un progetto Supabase reale**.

## BUG-009 — `<button>` dentro `<button>` in cliente-home.tsx (errore di hydration React su web)
- **Stato:** risolto
- **Severità:** media (errore di hydration visibile in console/DevTools su web; non impediva l'uso ma è HTML non valido e segnale di un pattern rischioso da ripetere altrove)
- **Data apertura:** introdotto durante la migrazione al nuovo design system (2026-07-10), segnalato dall'utente il 2026-07-11
- **Descrizione:** `AppListRow` (`mobile/src/components/ui/app-list-row.tsx`) diventa un `Pressable`/`role="button"` quando riceve `onPress`. In `mobile/src/app/cliente-home.tsx`, due righe ("Check-in settimanale" e "Le tue prenotazioni") avevano sia `onPress` sulla riga sia un `AppButton` (anch'esso un `Pressable`/`role="button"`) passato come `trailing` — su web questo produce `<button>` annidato in un altro `<button>`, non valido in HTML, causa dell'errore di hydration React.
- **Passi di riproduzione:** aprire `cliente-home.tsx` su web con "Confirm email"/dati cliente presenti, osservare la console del browser durante il render.
- **Fix applicato:** in entrambi i casi l'azione del bottone interno duplicava esattamente quella della riga (stessa route, `/questionario` e `/prenotazioni`), quindi era ridondante. Sostituito l'`AppButton` interno con un nuovo componente locale `TrailingPill` (solo `View`+`Text`, nessun `Pressable`/ruolo bottone) — stesso aspetto visivo coral pieno, ma puramente decorativo: l'intera riga resta l'unico elemento cliccabile. Verificato che nessun altro file migrato avesse lo stesso pattern (`superadmin-shell.tsx`, `login-screen.tsx`, `AppCard onPress` in `app/index.tsx`, `AppEmptyState`/`AppErrorState`).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `expo start --web --clear` riavviato con cache pulita, bundle Metro compilato senza errori. **Non verificato**: nessun clic reale in un browser (nessun tool di automazione disponibile) — l'utente deve riaprire la pagina e controllare che l'errore di hydration sia sparito dalla console.
- **Lezione per il resto della migrazione (vedi `docs/DECISIONS.md`/promemoria):** non passare mai un `AppButton`/altro elemento interattivo come `trailing`/children di un componente che ha già `onPress` proprio (`AppListRow`, `AppCard` con `onPress`) — se serve un'etichetta visiva nello stesso stile di un bottone ma la riga è già cliccabile, usare un badge non interattivo (pattern `TrailingPill`), non un vero componente Pressable.

## Nota — Expo Go su telefono non funziona correttamente
- **Stato:** aperto, non bloccante (Expo Web resta l'ambiente di sviluppo principale, come da indicazione esplicita dell'utente)
- **Severità:** media (blocca solo il test su device fisico, non lo sviluppo)
- **Descrizione:** l'utente segnala che Expo Web funziona ma Expo Go su telefono no. Non è stato possibile riprodurre/diagnosticare il problema in questa sessione (nessun accesso a un device fisico, nessun log di Expo Go disponibile): non è quindi classificabile come un bug puntuale con causa nota, a differenza di BUG-001/002/003. Ipotesi più probabile, da verificare quando sarà possibile testare su device: le dipendenze native del progetto (`expo-audio`, `expo-video`, `react-native-reanimated` 4.x, `react-native-worklets`, `react-native-gesture-handler`) richiedono spesso una build nativa allineata esattamente alla versione installata; Expo Go pubblica un binario generico che può non contenere i moduli nativi giusti o le versioni giuste per queste librerie, soprattutto per `react-native-worklets`/reanimated 4 (più recenti e con requisiti nativi più stringenti). Vedi `docs/TODO_NEXT.md` per il prossimo passo consigliato (development build EAS).

## BUG-010 — Upload video coach: tabella exercise_videos mancante + file orfani su fallimento DB
- **Stato:** risolto (codice); **da verificare** l'esecuzione dello SQL sul progetto reale dell'utente
- **Severità:** bloccante per l'upload video coach (il salvataggio DB falliva sempre, il video restava solo su Storage senza mai comparire nell'app)
- **Data apertura:** segnalata dall'utente il 2026-07-11, nel primo test reale dell'upload dopo l'implementazione della fase 2 (Supabase Storage)
- **Descrizione:** due problemi distinti. (1) Lo snippet SQL della tabella `exercise_videos` (già scritto in `docs/SUPABASE_SCHEMA.sql` nella sessione precedente) non era mai stato eseguito sul progetto Supabase reale dell'utente — l'app tentava l'insert contro una tabella inesistente, con errore PostgREST tipico `Could not find the table 'public.exercise_videos' in the schema cache`. (2) Bug di codice trovato durante la verifica (non solo il sintomo segnalato): `uploadExerciseVideo` (`mobile/src/lib/exercise-video-service.ts`) caricava il file su Storage PRIMA di scrivere la riga DB, ma se l'insert falliva (per questo motivo o per qualunque altro) il file appena caricato non veniva rimosso — restava orfano nel bucket, mai referenziato da nessuna riga.
- **Passi di riproduzione:** su un progetto Supabase senza la tabella `exercise_videos`, effettuare un upload video da coach: il file compare su Storage ma l'app mostra un errore e nessuna riga compare in `exercise_videos`.
- **Fix applicato:** in `uploadExerciseVideo`, se l'insert/upsert su `exercise_videos` fallisce, il file appena caricato viene rimosso da Storage (best-effort) prima di restituire l'errore. Tutte le policy RLS di `exercise_videos` (tabella + Storage) rese idempotenti (`drop policy if exists` prima di ogni `create policy`), aggiunto `notify pgrst, 'reload schema';` per escludere un ritardo di cache PostgREST come concausa. Vedi `docs/SUPABASE_STORAGE_VIDEO.md` per lo snippet aggiornato.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun nuovo test contro un progetto Supabase reale in questo ambiente. L'utente deve eseguire lo snippet SQL aggiornato e ripetere l'upload; eventuali file orfani già presenti nel bucket da test precedenti a questo fix vanno rimossi manualmente (il fix previene solo i futuri, non pulisce retroattivamente).

## BUG-011 — Coach registrato su Supabase non compare nel pannello superadmin
- **Stato:** risolto (codice); **da verificare** con un login superadmin reale su Supabase
- **Severità:** alta (il pannello superadmin, che dovrebbe essere la fonte di verità amministrativa, era in pratica scollegato da qualunque coach reale registrato su un device diverso da quello del superadmin)
- **Data apertura:** segnalata dall'utente il 2026-07-12
- **Descrizione:** l'intero pannello superadmin (dashboard, lista coach, dettaglio coach) leggeva SOLO `useSuperadminStore.coaches`, un array Zustand persistito su AsyncStorage **locale al device/browser del superadmin**, popolato solo dai 4 coach demo seed e da eventuali coach aggiunti manualmente col pulsante "+ Aggiungi coach" dello stesso pannello. Nessun codice interrogava mai Supabase per i coach reali: un coach registrato via `/registrazione-coach` su un device diverso da quello usato per il superadmin (il caso normale — un coach si registra dal proprio telefono, il superadmin guarda da un altro dispositivo/browser) non veniva mai sincronizzato, perché AsyncStorage non è condiviso tra device/browser (stesso limite già noto per i clienti, vedi fix 2026-07-09).
- **Passi di riproduzione:** registrare un coach reale via `/registrazione-coach` con Supabase configurato, poi accedere al pannello superadmin (anche sullo stesso device, se con un profilo AsyncStorage diverso, es. browser diverso) → il coach non compare né in lista né nei KPI né cercandolo per id.
- **Fix applicato:** nuovo `mobile/src/lib/superadmin-coach-service.ts` (`loadSupabaseCoaches`) legge `public.profiles` (role='coach') arricchito con `coach_profiles`/`billing_profiles`/`registration_codes`/`coach_clients` (conteggio reale). Nuovo hook `mobile/src/hooks/use-superadmin-coaches.ts` (`useSuperadminCoaches`) unisce questi coach reali con quelli creati solo localmente (deduplicando per email, vince sempre la versione Supabase), con fallback ai soli dati locali se Supabase non è configurato. Dashboard/lista/dettaglio (`superadmin/index.tsx`, `superadmin/coaches/index.tsx`, `superadmin/coaches/[id].tsx`) usano ora questo hook al posto dello store locale diretto. Log DEV `SUPERADMIN_COACHES_LOOKUP { source, count }` ad ogni caricamento. Poiché i pulsanti di modifica (piano/stato pagamento/blocco/rigenera codice) scrivono solo su `useSuperadminStore` (nessuna scrittura verso Supabase è stata implementata in questo intervento — fuori scope), sono stati disabilitati con una nota esplicita quando il coach visualizzato è di origine Supabase, invece di lasciarli silenziosamente senza effetto (CLAUDE.md regola 3).
- **Causa architetturale confermata (non risolvibile lato codice mobile):** le query a `profiles`/`coach_profiles`/`billing_profiles`/`registration_codes` sono protette da RLS che richiede `is_superadmin()` (già presente in `docs/SUPABASE_SCHEMA.sql`, nessuna nuova policy necessaria) — cioè una sessione Supabase REALE con `profiles.role='superadmin'`. Se il superadmin usa l'account demo locale (`admin@fitcoach.local`, nessuna sessione Supabase), la query restituisce sempre `[]` in modo silenzioso (comportamento corretto e sicuro di RLS, non un errore): serve un utente Supabase reale con `role='superadmin'` per vedere risultati. Vedi "Come testare" sotto.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun test contro un progetto Supabase reale in questo ambiente — l'utente deve creare/promuovere un utente reale a `role='superadmin'` e ripetere il test descritto in `docs/WORKLOG.md`.

## BUG-012 — Il link reset password Supabase apre il login invece di /reimposta-password
- **Stato:** risolto parzialmente (codice); causa esterna probabile da verificare in Supabase dashboard
- **Severità:** alta (blocca il flusso di reset password quando si verifica)
- **Data apertura:** segnalata dall'utente il 2026-07-12
- **Descrizione/causa nel codice trovata:** `auth-gate.tsx` gestiva `/reimposta-password` (render di `ResetPasswordScreen`) solo DENTRO il ramo `if (!isAuthenticated)`. Se il browser ha già una sessione locale autenticata (es. si è rimasti loggati come coach/cliente nello stesso browser e si segue comunque il link email, scenario comune in test), quel ramo viene saltato del tutto e l'utente finisce sulla dashboard/tab normale invece che sul form di reset — la route era di fatto "protetta" dal gate di autenticazione.
- **Causa più probabile per il sintomo esatto "apre il login" (esterna, non risolvibile nel codice mobile):** se in Supabase → Authentication → URL Configuration la "Redirect URLs" non include l'origin/porta attualmente in uso con `/reimposta-password` (es. `http://localhost:8081/**`), Supabase **ignora silenziosamente** il `redirectTo` passato da `resetPasswordForEmail` e usa la "Site URL" di default (di norma la radice `/`) — il link email riporta quindi su `/` invece che su `/reimposta-password`; con nessuna sessione locale attiva (caso comune), `/` mostra il login. Vedi "Come testare" per come verificarlo.
- **Fix applicato (codice):** `mobile/src/components/auth-gate.tsx` — il controllo `pathname === '/reimposta-password'` è stato spostato PRIMA del gate `if (!isAuthenticated)`, così la route è sempre raggiungibile indipendentemente da una sessione locale preesistente. `resetPasswordForEmail`/lettura sessione recovery/`updatePassword`/redirect finale al login erano già implementati correttamente (verificato, nessuna modifica necessaria lì).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun test contro un progetto Supabase reale in questo ambiente — l'utente deve controllare/aggiornare "Redirect URLs" su Supabase (vedi `docs/EMAIL_SETUP.md`) per l'origin/porta che sta usando, poi ripetere il flusso "Password dimenticata" end-to-end.
- **Addendum 1 (2026-07-12) — persisteva dopo il primo fix, causa aggiuntiva trovata e corretta:** l'utente ha confermato che, anche dopo lo spostamento del controllo in `auth-gate.tsx`, il link continuava ad aprire il login. Trovata una seconda causa reale nel codice: il template email "Reset password" di Supabase puo' usare `{{ .TokenHash }}` invece di `{{ .ConfirmationURL }}` (pattern piu' recente, raccomandato da Supabase per reindirizzare direttamente nell'app) — in quel caso il link arriva con `?token_hash=...&type=recovery` nella query string invece di `#access_token=...` nel frammento, e questo formato **non era gestito affatto** dal codice esistente (`detectSessionInUrl` gestisce solo il frammento): nessuna sessione di recovery si stabiliva mai. **Fix**: `reset-password-screen.tsx` ora legge `token_hash`/`type` dalla query string (`useLocalSearchParams`) e chiama esplicitamente `supabase.auth.verifyOtp({ type: 'recovery', token_hash })` quando presenti, prima di ricadere sull'attesa della sessione stabilita automaticamente dal frammento. Resta comunque possibile che la causa originale (Redirect URLs non allow-listate su Supabase → fallback silenzioso alla Site URL) sia concausa o causa alternativa: solo l'utente puo' verificarlo controllando l'URL reale nella barra degli indirizzi subito dopo il click e il contenuto del template email sul proprio progetto (vedi `docs/EMAIL_SETUP.md`, sezione "Flusso 3 — Reset password", aggiornata con entrambi i formati).
- **Addendum 2 (2026-07-12) — dopo l'addendum 1, il reset funziona ma il login successivo con la nuova password mostra "Credenziali non valide":** trovato un problema di visibilita' degli errori in `login-screen.tsx`, non necessariamente un problema del reset stesso. Se Supabase e' configurato, `signInWithEmail` viene tentato per primo; se fallisce (per QUALSIASI motivo — password errata, o un errore Supabase diverso), il codice ricade silenziosamente sui controlli locali/demo (comportamento intenzionale, per non bloccare account solo-locali) — ma se anche questi falliscono, il messaggio finale era sempre il generico "Credenziali non valide", **senza mai mostrare il vero messaggio restituito da Supabase**. Impossibile quindi distinguere "la password e' davvero sbagliata" da un altro errore Supabase (rate limit, utente disabilitato, ecc.). **Fix**: `login-screen.tsx` ora salva il messaggio d'errore reale di Supabase e, se anche il fallback locale fallisce, lo mostra ("Accesso non riuscito: <messaggio Supabase>") invece del generico. Non e' stato toccato il flusso di reset in se' (`updatePassword`/`verifyOtp` restano come da addendum 1): questo cambiamento serve solo a rendere visibile la vera causa al prossimo tentativo di login, cosa necessaria per capire se il problema e' un reset non andato a buon fine o una password diversa da quella attesa.
- **Verifica addendum 2:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Da fare dall'utente**: ripetere il login con la nuova password e riportare il messaggio Supabase esatto ora mostrato, per capire se si tratta di "Invalid login credentials" (password davvero diversa da quella impostata nel reset — riprovare il reset o verificare cosa e' stato digitato) o di un errore diverso.

## BUG-013 — Codice coach mostrato in app diverso da quello reale in registration_codes
- **Stato:** risolto (lato display in Impostazioni)
- **Severità:** alta (un cliente che usa il codice mostrato nell'app riceve "Codice coach non valido" e non riesce a registrarsi)
- **Data apertura:** segnalata dall'utente il 2026-07-12
- **Causa:** `CoachRegistrationScreen.handleRegister` (`registration-screens.tsx`) genera un `coachCode` LOCALE (client-side, `generateCoachCode`) prima ancora di chiamare Supabase, e lo scrive nel mirror locale (`useSuperadminStore.createCoach`) come valore iniziale. Con "Confirm email" attivo (`session === null` dopo `signUp`), questo codice locale non viene MAI sovrascritto con quello reale a quel punto (corretto: la UI mostra "Controlla la tua email", non un codice) — ma resta comunque salvato nel mirror locale con il valore sbagliato. La correzione dovrebbe avvenire al primo login (`login-screen.tsx` chiama `ensureCoachOnboarding` e allinea il mirror), ma questo passaggio è best-effort e silenzioso: se per qualunque motivo non va a buon fine (`onboarding.ok` false, `localAccount?.coachId` mancante, o qualunque altro gap nella sincronizzazione), il mirror locale resta con il codice sbagliato **indefinitamente, senza alcun errore visibile**. `impostazioni.tsx` (dove il coach controlla il proprio codice) leggeva SOLO questo mirror locale, mai la tabella reale.
- **Passi di riproduzione:** registrare un coach con Supabase configurato e "Confirm email" attivo, confermare l'email, fare login, aprire Impostazioni → il codice mostrato può non coincidere con quello in `registration_codes` su Supabase (specialmente se la sincronizzazione al login non si è completata per qualche motivo).
- **Fix applicato:** nuova funzione `getCoachActiveRegistrationCode(coachId)` (`mobile/src/lib/auth-service.ts`) legge il codice REALE da `public.registration_codes` (preferendo uno con `status='active'`). `mobile/src/app/impostazioni.tsx` ora, se il coach ha una sessione Supabase reale (`getCurrentSession()`, mai `useAuthStore().currentCoachId` — id del mirror locale, non `auth.uid()`), chiama questa funzione e mostra SEMPRE il codice reale al posto del mirror locale (fallback al mirror locale solo per coach 100% demo/locali, senza sessione Supabase). Log DEV `COACH_CODE_LOOKUP { source, code }`. Non toccato il registration flow in sé (già corretto: non mostra mai il codice sbagliato AL MOMENTO della registrazione) né la causa di fondo del mirror locale disallineato (bassa priorità, dato che la fonte di verità ora vince sempre in UI).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun test contro un progetto Supabase reale in questo ambiente — l'utente deve aprire Impostazioni da coach loggato con un account Supabase reale e confrontare il codice mostrato con quello in `registration_codes` (Table editor).

## BUG-014 — Tutti i login (coach e cliente) falliscono con "column profiles.must_change_password does not exist"
- **Stato:** risolto (codice reso resiliente); **causa reale è configurazione, non codice**
- **Severità:** critica (bloccava l'accesso di TUTTI gli utenti, non un caso isolato)
- **Data apertura:** segnalata dall'utente il 2026-07-12
- **Causa:** la colonna `must_change_password` aggiunta a `public.profiles` per la feature "credenziali provvisorie via email" (`docs/SUPABASE_SCHEMA.sql`) non è mai stata eseguita sul progetto Supabase reale dell'utente. `signInWithEmail` (`auth-service.ts`) selezionava questa colonna incondizionatamente per OGNI login (coach e cliente): senza la colonna, Postgres/PostgREST rifiuta l'intera query con `column ... does not exist`, e il login falliva sempre, per chiunque — non solo per chi doveva usare la nuova feature.
- **Fix applicato (codice, difensivo):** `signInWithEmail` ora rileva questo errore specifico e riprova la stessa query SENZA `must_change_password`, trattandolo come `false` (nessun blocco cambio password forzato) finché la colonna non è disponibile — il login non resta più bloccato per tutti mentre l'SQL non è stato eseguito o lo schema PostgREST non si è ancora ricaricato.
- **Fix reale richiesto (SQL, da eseguire subito sul progetto):**
  ```sql
  alter table public.profiles add column if not exists must_change_password boolean not null default false;
  notify pgrst, 'reload schema';
  ```
  Già presente in `docs/SUPABASE_SCHEMA.sql` — semplicemente mai eseguito. Finché non viene eseguito, il login funziona (grazie al fix difensivo sopra) ma la feature "Invia via email"/cambio password obbligato resta non operativa (il flag esiste solo lato codice, non lato DB).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Da fare dall'utente**: eseguire lo SQL sopra sul progetto reale, poi verificare che login coach/cliente tornino a funzionare normalmente e che la colonna sia visibile in Table editor.

## BUG-015 — Testo italiano di un esercizio YMove modificabile da QUALUNQUE coach ("wiki-style")
- **Stato:** risolto
- **Severità:** alta (bug di design, non solo di sicurezza: un coach vedeva il proprio testo cambiare a sua insaputa se un altro coach modificava lo stesso esercizio condiviso)
- **Data apertura:** segnalata dall'utente il 2026-07-13, introdotta nella stessa sessione del 2026-07-12/13 (integrazione YMove)
- **Causa:** la policy RLS `exercises_coach_update_ymove_text` permetteva a qualunque coach autenticato di aggiornare `name`/`description`/`technical_notes` di un esercizio `source='ymove'` condiviso tra tutti i coach — una scelta di design "wiki-style" rivelatasi sbagliata: il testo di un esercizio condiviso non deve essere modificabile da chi non ne ha la titolarità, solo dal superadmin (testo globale) o dal singolo coach per il proprio uso (personalizzazione privata).
- **Fix applicato:** rimossa la policy `exercises_coach_update_ymove_text` (ora solo il superadmin può modificare il testo globale, via `exercises_superadmin_all` già esistente). Nuova tabella `public.exercise_text_overrides` (coach_id, exercise_id, name/description/technical_notes, `unique(coach_id, exercise_id)`): ogni coach personalizza il proprio testo senza toccare né essere toccato dal testo condiviso. Lettura (`getFitCoachExerciseById`): l'override del coach (o del proprio coach, per un cliente) ha sempre priorità, altrimenti si ricade sul testo globale. Vedi `docs/DECISIONS.md` (2026-07-13).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro ricompilato. **Non verificato**: nessun test contro un progetto Supabase reale con due coach distinti.

## BUG-016 — Esercizio importato/collegato a YMove spariva dalla lista scheda dopo salvataggio e riapertura
- **Stato:** risolto
- **Severità:** alta (funzione visibilmente rotta: l'esercizio appena aggiunto/collegato scompariva silenziosamente)
- **Data apertura:** segnalata dall'utente il 2026-07-13 come verifica da fare, bug confermato durante la verifica
- **Causa:** `mobile/src/app/schede/[id].tsx` (vista/dettaglio scheda) usava ancora `getExerciseById` diretto (solo i 44 esercizi storici locali, `mobile/src/data/exercise-library.ts`) per risolvere ogni `WorkoutExercise.exerciseId` nella lista "Esercizi" — sia nel blocco superserie sia in quello standalone. Per un esercizio con id Supabase (importato da YMove o con un video YMove collegato), la lookup falliva sempre (`undefined`), e `if (!exercise) return null` faceva sparire la riga dalla lista non appena si tornava alla vista scheda dopo il salvataggio — mentre `workout-plan-form.tsx`/`esercizi/[id].tsx` erano già stati corretti in sessioni precedenti per usare il resolver locale+Supabase, questo file era stato dimenticato.
- **Fix applicato:** sostituite entrambe le chiamate a `getExerciseById(we.exerciseId)` con `resolveExercise(we.exerciseId)` (`hooks/use-exercise-resolver.ts`, già usato altrove) in `schede/[id].tsx`. Verificato anche `schede/modelli/[id].tsx` (dettaglio modello/template): usa solo dati locali statici (`data/workout-plan-templates.ts`), mai raggiungibile con un esercizio YMove — nessuna modifica necessaria lì.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro ricompilato. **Non verificato**: nessun clic reale (stesso limite ambientale di tutte le sessioni). L'utente deve confermare: aprire una scheda, "Modifica scheda", aggiungere un esercizio da "Libreria YMove", salvare, riaprire la scheda in modalità vista → l'esercizio deve comparire nella lista.

## BUG-017 — Card esercizio in scheda mostrava sempre il placeholder con l'iniziale, mai la miniatura del video
- **Stato:** risolto
- **Severità:** media (nessuna funzione rotta, ma la card era visivamente meno informativa di quanto i dati disponibili permettessero: un esercizio con un video reale — importato da YMove o collegato — appariva identico a uno senza alcun video)
- **Data apertura:** segnalata dall'utente il 2026-07-13 (continuazione 3)
- **Causa:** `components/exercise-thumbnail.tsx` risolveva l'immagine SOLO tramite `data/image-registry.ts` (il registro locale di foto statiche degli esercizi, sempre vuoto — nessuna foto reale è mai stata caricata nel progetto). Non veniva mai interrogato né `exercise.ymoveExerciseId` (per un esercizio importato da YMove) né `exercise_videos` (per un esercizio locale/custom collegato a un video YMove), quindi la thumbnail live di YMove — l'unica fonte di immagine reale oggi disponibile — non veniva mai mostrata: ogni card ricadeva sempre sul cerchio con l'iniziale, indipendentemente dal fatto che un video esistesse davvero.
- **Fix applicato:** `ExerciseThumbnail` ora risolve la miniatura con priorità: (1) `exercise.source==='ymove'` → thumbnailUrl live dell'esercizio YMove di origine; (2) esercizio locale/custom con un collegamento YMove in `exercise_videos` → stessa thumbnailUrl live, chiave sempre l'id YMove; (3) video caricato manualmente dal coach → nessuna colonna poster oggi, resta onestamente il placeholder; (4) nessun video → placeholder invariato. Nuova cache globale in memoria (`lib/ymove-thumbnail-cache.ts`) per la thumbnail YMove per `ymoveExerciseId` (dedup di richieste concorrenti, mai una chiamata ripetuta alla Edge Function per lo stesso video nella stessa sessione) e una seconda cache (`lib/exercise-video-info-cache.ts`) per il risultato di `getExerciseVideo` per `exerciseId`, invalidata esplicitamente da `esercizi/[id].tsx` dopo associa/sostituisci/rimuovi/carica video. Nessuna `VideoView` montata, nessun autoplay, nessun audio: solo `expo-image` con `contentFit="cover"` e una piccola icona play sovrapposta, decorativa (non un bottone separato). `getExerciseVideo` esteso a una struttura discriminata `{source:'upload', videoUrl} | {source:'ymove', ymoveExerciseId, ymoveSlug}` cosi' una riga YMove con `video_url` null non viene più letta per errore come "nessun video".
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform web` completato senza errori (bundle ricompilato, verificato via grep che le nuove funzioni di cache sono incluse). **Non verificato**: nessun clic/scroll reale in un browser (stesso limite ambientale di tutte le sessioni); nessun test contro un progetto Supabase/YMove reale. Limite noto segnalato in quel momento (per un esercizio locale/custom collegato a un video YMove, il cliente riceveva `forbidden` dall'azione `detail`): **risolto**, vedi BUG-018.

## BUG-018 — Cliente riceveva `forbidden` dall'azione `detail` per un video YMove collegato (non importato) dal proprio coach
- **Stato:** risolto
- **Severità:** media (nessun dato esposto in modo scorretto — anzi il cliente vedeva MENO di quanto dovesse: il video, regolarmente visibile al coach che lo aveva collegato, restava un placeholder con la lettera lato cliente)
- **Data apertura:** segnalata dall'utente il 2026-07-13 (continuazione 4), residuo del fix BUG-017
- **Causa:** l'azione `'detail'` della Edge Function `ymove-exercises`, per il ruolo `cliente`, autorizzava l'accesso SOLO se l'id YMove risultava importato come riga in `public.exercises` (`source='ymove'`). Un esercizio locale storico o `custom` con un video YMove ASSOCIATO tramite `exercise_videos` (mai importato come riga `exercises` — il caso descritto nella sezione "Associazione video YMove su un esercizio esistente") non soddisfaceva questa condizione: il cliente riceveva sempre `forbidden`, anche quando il proprio coach vedeva il video regolarmente.
- **Fix applicato:** in `supabase/functions/ymove-exercises/index.ts`, l'azione `'detail'` per il ruolo `cliente` ora: (1) recupera il `coach_id` collegato da `coach_clients` (`client_id=callerId`, `status in ('active','invited')`); (2) autorizza se ALMENO una delle due condizioni è vera — esiste una riga `public.exercises` con `source='ymove'` e quel `ymove_exercise_id` (condivisa globalmente, come prima), OPPURE esiste una riga `exercise_videos` con `coach_id` = il coach collegato al cliente e lo stesso `ymove_exercise_id`; (3) un collegamento `exercise_videos` di un coach DIVERSO da quello del cliente continua a NON autorizzare nulla (query esplicitamente filtrata per `coach_id`). Coach e superadmin: comportamento invariato (nessuna restrizione). Nessun URL/chiave stampato nei log (solo `console.error`/`console.log` già esistenti, non toccati da questo fix).
- **Verifica:** revisione manuale del file (nessun CLI Deno disponibile in questo ambiente per una compilazione isolata). **Non verificato**: nessun test contro un progetto Supabase/YMove reale con due coach e relativi clienti distinti — checklist dedicata aggiunta in `docs/YMOVE_INTEGRATION.md` (punto 15) e `docs/TODO_NEXT.md`.

## BUG-019 — Creazione/modifica scheda falliva con "invalid input syntax for type uuid" per id locali testuali/numerici
- **Stato:** risolto
- **Severità:** bloccante (creazione/modifica/eliminazione scheda ed aggiornamento di sessione potevano fallire con un errore Postgres non gestito)
- **Data apertura:** segnalata dall'utente il 2026-07-14, continuazione della migrazione schede a Supabase
- **Causa:** `WorkoutPlan`/`WorkoutExercise` possono avere id locali placeholder testuali/numerici (es. `"1"`, `"we-1"`) prima del primo salvataggio reale — mai un UUID, perché non ancora generato da Postgres. Tre punti diversi tentavano un cast diretto `(testo)::uuid` su un id non validato, sollevando l'eccezione Postgres "invalid input syntax for type uuid" invece di trattare l'id come "riga nuova": (1) `public.save_workout_plan` controllava solo "presente/non vuoto" (`nullif(...,'') is not null`), non il FORMATO, sia per `payload.id`/`coach_id`/`client_id`/`template_id` sia per ogni `exercises[].id`; (2) `deleteWorkoutPlan` (mobile) non aveva alcun controllo prima di `.eq('id', id)`; (3) `updateWorkoutSessionProgress` (mobile) passava `planId` a un parametro RPC tipato `uuid` senza validarlo prima. In più, la migrazione one-time considerava "già remoto" (da escludere) qualunque piano locale con un id GIÀ a forma di UUID — un'euristica sbagliata: la forma dell'id non prova che quella riga esista davvero su Supabase.
- **Fix applicato:** (1) `mobile/src/lib/workout-plan-service.ts`: `isUuid` rinominata `isValidUuid` (stessa regex) e applicata anche a `deleteWorkoutPlan` (id non valido → no-op di successo, nulla da eliminare remotamente) e `updateWorkoutSessionProgress` (planId non valido → no-op di successo, nulla da sincronizzare). (2) `public.save_workout_plan` (SQL): nuova funzione helper `public.is_valid_uuid(text)` (regex, mai un'eccezione anche su input null), usata PRIMA di ogni cast — su `id`/`coach_id`/`client_id`/`template_id` del piano e su `exercises[].id` — un id non valido è sempre trattato come riga nuova, mai un tentativo di cast. `exercise_id` (l'ESERCIZIO, non la riga) resta sempre testo libero, nessuna validazione/cast, nessuna foreign key aggiunta. La funzione ora tocca sempre `workout_plans.updated_at` esplicitamente in coda, anche quando cambiano solo gli esercizi, così la subscription Realtime riceve sempre l'evento. (3) `mobile/src/app/schede/[id].tsx`: dopo un salvataggio strutturale che assegna per la prima volta un id remoto reale, la nuova azione store `replaceWorkoutPlan(oldId, newPlan)` rimuove la voce con il vecchio id placeholder e aggiunge quella con l'id remoto (mai un `.map()` per id che silenziosamente non trova nulla e lascia il doppione) — con redirect dell'URL al nuovo id se cambiato. (4) `mobile/src/lib/workout-plan-migration.ts`: la mappa persistita "già migrato" passa da un semplice `Set<localId>` a un vero `Map<localId, remoteId>` (`fitcoach-workout-migrated-map:<coachId>`), e l'eligibilità di un piano NON considera più la forma dell'id — solo la presenza nella mappa conta come "già migrato".
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform web` completato senza errori (nuove funzioni confermate nel bundle). Test unitario manuale (Node) sulla logica `isValidUuid`/`buildSavePayload` con gli id esatti del bug segnalato ("1", "we-1"/"we-2"/"we-3", `exerciseId` testuale e UUID): tutti i casi passano, `exercise_id` non viene mai alterato. SQL controllato manualmente riga per riga (parentesi/dollar-quoting bilanciati) — nessuna esecuzione reale contro Postgres in questo ambiente.

## BUG-020 — Dashboard coach: i cinque pulsanti "Azioni rapide" si sovrapponevano su Android
- **Stato:** risolto
- **Severità:** alta (funzione raggiungibile ma illeggibile/non cliccabile in modo affidabile su schermi Android reali)
- **Data apertura:** segnalata dall'utente il 2026-07-13
- **Causa:** `mobile/src/app/index.tsx` calcolava la larghezza di ogni card/pulsante con `width:'48.5%'` + `minWidth:136` + `flexGrow:1` insieme. Su Android, l'arrotondamento della percentuale sommato al `gap` del contenitore fa sì che due elementi affiancati superino la larghezza reale della riga; `flexGrow:1` amplifica il problema facendo crescere gli elementi oltre la larghezza percentuale non appena c'è anche un solo pixel di spazio residuo, con un risultato diverso a seconda del rendering engine — la combinazione (mai una sola di queste tre proprietà) produceva la sovrapposizione visibile.
- **Fix applicato (prima versione, SBAGLIATA su web):** larghezza in pixel calcolata da `useWindowDimensions` — corretta su un device nativo, ma su web la preview gira dentro la cornice iPhone `WebPhoneFrame` (360/390/430px): `useWindowDimensions` misurava l'intera finestra del browser (1000+px), le colonne venivano calcolate su quella larghezza e i pulsanti traboccavano dalla cornice (segnalato dall'utente con screenshot nella stessa giornata).
- **Fix definitivo:** larghezza misurata con `onLayout` sul contenitore reale della griglia (`handleGridLayout` su `statsGrid`, stessa larghezza per statistiche e azioni rapide): `columnWidth = floor((containerWidth - gap) / 2)`; sotto 130px di colonna si passa a una singola colonna (`columnWidth = containerWidth`), mai a un valore che farebbe traboccare la riga; prima della prima misurazione (`containerWidth 0`, un solo frame) si usa `'100%'` (una colonna, mai overflow). Funziona identico dentro `WebPhoneFrame` su web e a schermo pieno su iPhone/Android. Nessuna `position:absolute`, nessuna altezza fissa del contenitore: il quinto pulsante va a capo naturalmente in terza riga perché `flexWrap` con larghezze fisse corrette calcola da sé quanti elementi entrano per riga. `AppStatCard` applica internamente `flex:1` al proprio wrapper: senza azzerare esplicitamente `flexGrow`/`flexShrink`/`flexBasis` nello style passato (`gridItemStyle`), quel `flex:1` avrebbe vinto sulla `width` fissa (stesso meccanismo del bug originale) — risolto passando `{width, flexGrow:0, flexShrink:0, flexBasis:width}`.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform android` e `--platform web` completati senza errori. **Non verificato**: nessun rendering reale osservato da questo agente su device/emulatore Android o nella preview web (limite ambientale) — l'utente deve ricontrollare la preview dopo l'hot reload alle tre taglie della cornice (S/M/L) e, quando possibile, su Android 320/360/393/412px.

## BUG-021 — Libreria YMove: il picker partiva vuoto, senza miniature, e scartava esercizi con "name" invece di "title"
- **Stato:** risolto
- **Severità:** media (funzione utilizzabile ma percepita come rotta: nessun risultato visibile finché non si premeva "Cerca", nessuna miniatura mai mostrata, alcuni esercizi validi invisibili)
- **Data apertura:** segnalata dall'utente il 2026-07-13
- **Causa:** (1) `mobile/src/components/ymove-exercise-picker.tsx` inizializzava `results=[]` e non eseguiva alcuna ricerca finché il coach non premeva "Cerca" manualmente; (2) nessuna card mostrava mai una miniatura: la ricerca (`action:'search'`) non include mai `thumbnailUrl` (disponibile solo su `action:'detail'`, per non consumare il limite mensile video), e il picker non richiedeva mai il dettaglio per mostrarla; (3) sia la Edge Function (`supabase/functions/ymove-exercises/index.ts`) sia il servizio mobile (`ymove-service.ts`) scartavano come non validi i risultati con solo `name` e senza `title`, pur avendo un id valido.
- **Fix applicato:** (1) nuovo `useEffect` con `useRef` (`didInitialSearchRef`) in `YMoveExercisePicker` che esegue una ricerca senza filtri al primo render, protetto da doppie chiamate di React Strict Mode; il pulsante "Cerca" resta invariato per i filtri manuali; una ricerca fallita non azzera più i risultati già mostrati (l'errore compare sopra la lista esistente, con un pulsante "Riprova" dedicato), più uno stato "Caricamento esercizi YMove..." distinto per il primo caricamento. (2) nuovo componente `YMoveResultThumbnail` per card di risultato: usa la cache già esistente `lib/ymove-thumbnail-cache.ts` (`fetchYmoveThumbnail`/`getCachedYmoveThumbnail`), quindi richiede il dettaglio solo per i risultati effettivamente renderizzati (mai in blocco su tutta la ricerca), mostra un placeholder `Dumbbell` (icona fitness generica) finché non arriva la miniatura o se manca, un badge "play" quando `hasVideo=true`, e se l'`<Image>` fallisce a caricare passa al placeholder senza mai ritentare in loop (il fetch del dettaglio è comunque già cache-una-volta). La cache è stata estesa con un limitatore di concorrenza globale a 2 richieste di dettaglio contemporanee (`acquireThumbnailFetchSlot`/`releaseThumbnailFetchSlot`), condiviso anche da `ExerciseThumbnail` (schede). Premere la miniatura apre la stessa "Anteprima" già esistente (nessun player nuovo). (3) `normalizeSummary` (sia Edge Function sia `ymove-service.ts`) ora usa `title ?? name` come fallback esplicito prima di scartare un risultato. (4) log temporaneo sicuro `YMOVE_SEARCH_DEBUG {rawCount, normalizedCount, renderedCount, hasError}` in `searchYmoveExercises` — mai chiavi/token/URL firmati.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform android` completato senza errori (bundle contiene `SUPERADMIN_COACH_LOAD`/`YMOVE_SEARCH_DEBUG`, verificato via grep). **Non verificato**: nessuna chiamata reale contro l'API YMove/Azure Translator in questa sessione (nessuna sessione coach reale disponibile) — l'utente deve confermare che il picker mostra esercizi subito all'apertura e che le miniature compaiono. **Operazione manuale ancora necessaria**: `npx.cmd supabase functions deploy ymove-exercises --project-ref rkcecnzvzoigipjliwdk` per pubblicare il fallback `name`→`title` lato server (il fallback lato mobile è già attivo senza deploy).

## BUG-022 — Superadmin "Account coach": coach reali di Supabase mescolati/oscurati da account demo sempre visibili
- **Stato:** risolto
- **Severità:** media (i coach reali erano già visibili da una sessione precedente — `lib/superadmin-coach-service.ts` esisteva già — ma gli account "Solo locale"/demo comparivano sempre, senza badge chiaro né possibilità di nasconderli, e un errore di rete tornava silenziosamente un elenco vuoto invece di un errore)
- **Data apertura:** segnalata dall'utente il 2026-07-13
- **Causa:** `loadSupabaseCoaches()` inghiottiva ogni errore Supabase in un array vuoto (`console.error` + `return []`), indistinguibile da "nessun coach registrato"; `useSuperadminCoaches` mostrava sempre i coach locali/demo insieme a quelli reali (deduplicati solo per email), etichettati "Solo locale" invece di un badge "Demo" esplicito, senza alcuna variabile per nasconderli del tutto; nessun controllo esplicito che la sessione corrente fosse davvero un superadmin prima di interrogare `profiles` globalmente (mitigato dalle RLS, ma non dichiarato in codice); nessun pulsante di refresh manuale nella lista.
- **Fix applicato:** `loadSupabaseCoaches` ora ritorna `{ok:true,data}|{ok:false,message}` esplicito (mai un array vuoto silenzioso su errore reale). `useSuperadminCoaches` (`mobile/src/hooks/use-superadmin-coaches.ts`): nuovo campo `error` restituito al chiamante; i coach locali/demo sono inclusi SOLO se `process.env.EXPO_PUBLIC_ENABLE_DEMO_DATA === 'true'` (stesso pattern `EXPO_PUBLIC_*` di `lib/supabase.ts`), altrimenti nascosti di default; il caricamento da Supabase parte solo se `currentRole==='superadmin'` (`useAuthStore`); nuovo log sicuro `SUPERADMIN_COACH_LOAD {realCoachCount, demoCoachCount, source:'supabase'|'demo'|'mixed'}` (mai email complete/dati fiscali/token). `superadmin/coaches/index.tsx`: badge "Solo locale" rinominato "Demo", nuovo pulsante "Aggiorna" (refresh manuale oltre al refresh automatico su focus/foreground già esistente), blocco errore con "Riprova", stato vuoto "Nessun coach registrato" quando non c'è alcun filtro attivo. `superadmin/coaches/[id].tsx`: lo stesso errore/Riprova propagato anche al dettaglio coach. Le azioni di blocco/pacchetto/modifica profilo per un coach Supabase erano già collegate realmente (sessione precedente, non simulate) — non modificate.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform android` completato senza errori. **Non verificato**: nessun login superadmin reale in questa sessione — l'utente deve confermare che un coach registrato su Supabase compare nell'elenco e che con `EXPO_PUBLIC_ENABLE_DEMO_DATA` assente/false nessun account demo compare.

## BUG-023 — APK Android: molti pulsanti e tab non ricevevano il tocco (cliente e coach)
- **Stato:** risolto (da verificare su un nuovo APK)
- **Severità:** bloccante (l'app appariva "morta" al tocco in molte schermate: voci di Altro, Check-in/Prenota/Bacheca in Home cliente, "Vai ad Abbonamento", quick action)
- **Data apertura:** segnalata dall'utente il 2026-07-13 dopo un test su APK Android reale
- **Causa:** `AnimatedSplashOverlay` (`mobile/src/components/animated-icon.tsx`, montato alla RADICE dell'app in `app/_layout.tsx`, sopra `AuthGate`) è una View `absoluteFill` con `zIndex:1000` SENZA `pointerEvents="none"`. Viene smontata solo dalla callback di fine animazione Reanimated (`withCallback` + `scheduleOnRN`): se quella callback non scatta — `finished=false` o worklet mai eseguito, caso concreto nelle build release Android — l'overlay resta montato per sempre, invisibile (opacity 0 a fine keyframe) ma sopra OGNI pulsante e tab dell'app, intercettando tutti i tocchi. La versione web (`animated-icon.web.tsx`) ritorna `null`: per questo la preview web funzionava e l'APK no. Tutte le route segnalate come "non si aprono" (Check-in→`/questionario`, Prenota→`/prenotazioni`, Bacheca→`/bacheca`, voci di Altro, "Vai ad Abbonamento"→`/abbonamento-coach`) esistono e sono collegate correttamente, con Pressable sull'intera riga: il problema era SOLO il tocco mai consegnato.
- **Fix applicato:** (1) `pointerEvents="none"` su entrambe le fasi dell'overlay (statica e animata) — è puramente decorativo, non deve mai intercettare un tocco nemmeno mentre è visibile; (2) timeout di sicurezza (`DURATION+400ms`) che smonta comunque l'overlay anche se la callback Reanimated non scatta mai. Audit difensivo esteso a tutti gli overlay assoluti dell'app: `FitnessPattern` ora ha `pointerEvents="none"` anche sulla propria root (non solo nel wrapper di `ScreenBackground`), i badge notifiche/supporto della bottom bar superadmin e il poster di caricamento sopra la `VideoView` del picker YMove sono ora `pointerEvents="none"`. Nessun altro overlay assoluto privo di protezione trovato (`app-ring-progress`, `exercise-thumbnail`, footer di `AppScreen` erano già protetti; le tab bar e il badge-rimuovi allegati sono interattivi per design).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform android` completato senza errori. **Non verificato**: nessun APK ricostruito/testato su device in questa sessione — l'utente deve rifare la build e confermare che tab e pulsanti rispondono.

## BUG-024 — APK Android: tab bar superadmin sovrapposta a contenuti e barra di navigazione, card statistiche troppo alte
- **Stato:** risolto (da verificare su un nuovo APK)
- **Severità:** alta
- **Data apertura:** segnalata dall'utente il 2026-07-13
- **Causa:** (1) la bottom bar di `superadmin-shell.tsx` aveva `height: 74` FISSA ma anche `paddingBottom: insets.bottom + 4` DENTRO quell'altezza: su Android la barra di navigazione di sistema (inset 24-48px) veniva sottratta dai 74px, schiacciando icone/label e facendo finire i form sotto la bar. (2) la griglia statistiche della dashboard usava `width:'48.5%'+minWidth:136+flexGrow:1` (stessa combinazione del BUG-020): a 320-412px le righe superavano il contenitore e la card "Alert pagamento scaduto" finiva sovrapposta; le card `size="lg"` (minHeight 104) erano inutilmente alte per 6 metriche.
- **Fix applicato:** (1) rimossa l'altezza fissa: l'altezza della bar è sempre contenuto (~58px) + `insets.bottom`, e il `paddingBottom` della ScrollView dei contenuti usa la stessa formula — niente più sovrapposizioni con form o barra di sistema. (2) griglia migrata al nuovo hook condiviso `hooks/use-two-column-grid.ts` (larghezza colonna misurata con `onLayout`, estratta dal fix BUG-020 di `app/index.tsx`, ora riusata da entrambe le dashboard) e card passate a `size="sm"` (compatte, stesso componente).
- **Verifica:** `tsc`/`expo-doctor`/`expo export android` puliti. **Non verificato** su device reale alle larghezze 320/360/393/412px.

## BUG-025 — Clienti demo (Marco Bianchi/Giulia Verdi/Luca Ferrari) visibili nell'APK normale
- **Stato:** risolto (da verificare su un nuovo APK)
- **Severità:** media (dati finti presentati come reali a un coach vero)
- **Data apertura:** segnalata dall'utente il 2026-07-13
- **Causa:** `client-store.ts` (e analogamente `training-store`/`chat-store`/`board-store`) inizializzava lo stato con i seed demo (`SEED_CLIENTS` ecc.) SEMPRE, senza alcun gate: ogni installazione partiva con 3 clienti finti, relative schede/storico, messaggi chat e post bacheca.
- **Fix applicato:** nuovo modulo condiviso `mobile/src/lib/demo-data.ts` (`DEMO_DATA_ENABLED = process.env.EXPO_PUBLIC_ENABLE_DEMO_DATA === 'true'`, stesso flag già usato per i coach demo del pannello superadmin, ora unificato lì). I 4 store seed-ati partono vuoti senza il flag; in più, al rehydrate, PURGANO dallo storage persistito i soli elementi con id seed noto (per installazioni esistenti che avevano già salvato i seed) — mai dati creati dall'utente o provenienti da Supabase. Con il flag attivo, i clienti seed mostrano un badge "Demo" nella lista clienti (`clienti/index.tsx`). NESSUN dato Supabase viene toccato: il gate governa solo i seed locali Zustand/AsyncStorage.
- **Verifica:** `tsc`/`expo-doctor`/`expo export android` puliti. **Non verificato**: comportamento reale su un'installazione esistente con storage popolato.

## BUG-026 — Registrazione coach Android: tastiera copriva gli ultimi campi e non si chiudeva
- **Stato:** risolto (da verificare su un nuovo APK)
- **Severità:** alta (registrazione di fatto non completabile comodamente su Android)
- **Data apertura:** segnalata dall'utente il 2026-07-13
- **Causa:** `AppScreen` è una ScrollView semplice senza gestione tastiera; con Expo SDK 57 Android è edge-to-edge di default e `adjustResize` NON ridimensiona più automaticamente il contenuto — la tastiera copriva i campi finali del form (20+ campi) senza possibilità di scorrere fino in fondo; nessun `keyboardShouldPersistTaps`, quindi il primo tocco su un bottone con tastiera aperta serviva solo a chiuderla.
- **Fix applicato:** (1) `AppScreen`: `keyboardShouldPersistTaps="handled"` (un tocco su un bottone agisce subito; un tocco su area vuota chiude la tastiera) e `keyboardDismissMode="on-drag"` su OGNI schermata scrollabile; nuova prop opt-in `keyboardAvoiding` che avvolge il contenuto in `KeyboardAvoidingView behavior="padding"` (attivata su registrazione coach e cliente; no-op su web). (2) `AppTextField`: ora `forwardRef` verso il TextInput nativo e `returnKeyType="done"` di default (ogni campo può chiudere la tastiera con "Fine"). (3) `registration-screens.tsx`: chaining "Avanti" sui campi credenziali (coach: nome→email→password→conferma; cliente: codice→nome→email→password→conferma) con `returnKeyType="next"`/`submitBehavior="submit"`/focus del campo successivo.
- **Verifica:** `tsc`/`expo-doctor`/`expo export android` puliti. **Non verificato**: nessuna tastiera Android reale in questa sessione.

## BUG-027 - APK Android cliente: tab bar funziona, ma card e righe interne non aprono le route
- **Stato:** risolto (da verificare su un nuovo APK)
- **Severita:** bloccante
- **Data apertura:** 2026-07-14
- **Data chiusura:** 2026-07-14
- **Descrizione:** nel lato cliente Android la tab bar principale Home/Workout/Nutrizione/Chat/Altro rispondeva, ma le press interne non portavano a nessuna schermata visibile: Home Check-in/Prenota/Bacheca, voci di Altro, e card Workout Da fare/Passati.
- **Causa:** verificata nel codice. `AppCard` e `AppListRow` erano gia' Pressable quando ricevevano `onPress`, e le route target esistevano. Il blocco era `mobile/src/components/client-tabs.tsx`: su Android usava `expo-router/unstable-native-tabs` con soli 5 `NativeTabs.Trigger` (`cliente-home`, `workout`, `nutrizione`, `chat`, `altro`). L'implementazione installata di `expo-router` passa al navigator solo i trigger (`children: triggerChildren`) e le route `hidden` non sono navigabili; quindi le route interne (`/questionario`, `/prenotazioni`, `/bacheca`, `/cliente-profilo`, `/progressi`, `/pacchetti-cliente`, `/schede/[id]`) restavano fuori dal navigator nativo. La variante web era gia' corretta per lo stesso problema storico: usava `Slot`, che costruisce gli screen dall'intero albero route.
- **Fix applicato:** `client-tabs.tsx` riscritto con `Slot` + tab bar React Native custom mantenendo 5 tab visibili, cosi' le route interne cliente restano renderizzabili anche su Android. Aggiunto helper `logClientNavPress` e log dev `CLIENT_NAV_PRESS { source, target }` su tab cliente, Home, Altro e Workout. Nessuna modifica a Supabase, YMove, schede dati, billing, appuntamenti, chat o notifiche push.
- **Verifica:** `npx.cmd tsc --noEmit` pulito; `npx.cmd expo-doctor` 20/20; `npx.cmd expo export --platform android` completato. Il primo `expo-doctor` e il primo export sono falliti rispettivamente per cache npm e `spawn EPERM` Hermes in sandbox; entrambi sono stati rieseguiti con escalation e completati.

## BUG-028 - APK Android coach: route interne fuori da NativeTabs e lista clienti ancora locale
- **Stato:** risolto (da verificare su un nuovo APK)
- **Severita:** alta
- **Data apertura:** 2026-07-14
- **Data chiusura:** 2026-07-14
- **Descrizione:** il lato coach aveva lo stesso rischio strutturale del cliente: tab principali funzionanti ma route interne aperte da dashboard/lista/dettaglio potenzialmente non renderizzabili su Android. Inoltre la lista clienti coach leggeva ancora `useClientStore`, quindi poteva mostrare solo dati locali/demo invece dei clienti realmente collegati su Supabase.
- **Causa:** `mobile/src/components/app-tabs.tsx` usava `NativeTabs` con soli 5 trigger (`index`, `clienti`, `schede`, `appuntamenti`, `chat`), mentre le route interne (`/clienti/[id]`, `/clienti/new`, `/schede/new`, `/appuntamenti/new`, `/supporto`, `/abbonamento-coach`) non erano trigger. La lista `clienti/index.tsx` usava `useClientStore` come sorgente primaria.
- **Fix applicato:** `app-tabs.tsx` riscritto con `Slot` + tab bar React Native, come cliente/web. Aggiunti log dev `COACH_NAV_PRESS { source, target }`. Nuovi `coach-clients-service.ts` e `use-coach-clients.ts`: query su `coach_clients` con `coach_id=auth.uid()` e `status='active'`, dedup per `client_id`, arricchimento da `profiles` e `client_profiles`, sync nello store per il dettaglio. Demo solo se `EXPO_PUBLIC_ENABLE_DEMO_DATA=true`, con badge Demo gia' esistente. Dashboard/lista/dettaglio clienti usano il nuovo hook.
- **Verifica:** `npx.cmd tsc --noEmit` pulito durante lo sviluppo.

## BUG-029 - Superadmin: log navigazione mancanti e assegnazione pacchetto senza date esplicite
- **Stato:** risolto (da verificare su un nuovo APK e con sessione superadmin reale)
- **Severita:** media
- **Data apertura:** 2026-07-14
- **Data chiusura:** 2026-07-14
- **Descrizione:** il superadmin non aveva il problema NativeTabs (usa gia' una shell React Native custom), ma mancavano log di navigazione e l'azione "Assegna/Cambia pacchetto" nel dettaglio coach non permetteva di impostare manualmente data inizio/scadenza.
- **Causa:** `superadmin-shell.tsx` e le card interne usavano `router.push` diretto senza logging. `assignCoachPackage` calcolava data inizio/scadenza automaticamente.
- **Fix applicato:** aggiunti log dev `SUPERADMIN_NAV_PRESS { source, target }` su bottom bar, notifiche, dashboard, lista/detail coach, piani, pacchetti e supporto. `assignCoachPackage` ora riceve `packageId`, `startsAt`, `expiresAt`, valida le date, marca `canceled` gli abbonamenti active precedenti dello stesso coach e inserisce una nuova riga active in `user_subscriptions` con `payment_provider='superadmin_manual'`; il dettaglio coach rilegge Supabase con `reload()`.
- **Verifica:** `npx.cmd tsc --noEmit` pulito durante lo sviluppo. Nessuna service role key aggiunta; nessuna modifica remota Supabase eseguita.
## BUG-030 - Pacchetti coach: checkout non reale e nessuna sincronizzazione RevenueCat/Supabase
- **Stato:** risolto lato codice, da verificare con store reali e webhook deployato
- **Severita:** alta
- **Data apertura:** 2026-07-14
- **Data chiusura codice:** 2026-07-14
- **Descrizione:** il pulsante acquisto pacchetto coach mostrava un messaggio onesto ma non avviava un pagamento reale. Mancavano mapping product id store, prezzo localizzato da store, restore acquisti e webhook che aggiornasse `user_subscriptions`.
- **Causa:** `mobile/src/lib/package-checkout-service.ts` era ancora lo stub progettato nella fase precedente: non usava RevenueCat, Apple IAP o Google Play Billing e non esisteva una Edge Function sicura per scrivere gli abbonamenti dopo ricevuta verificata.
- **Fix applicato:** installato `react-native-purchases`; nuovo `revenuecat-service.ts` con configure una sola volta, public keys Android/iOS, login RevenueCat con UUID Supabase, logout su cambio account, offerings/prodotti/prezzo store, purchase, pending/cancel/error, restore e CustomerInfo listener deduplicato. `abbonamento-coach.tsx` mostra prezzo store e non usa `price_cents`/prezzo Supabase come prezzo finale. Nuova Edge Function `revenuecat-webhook` con secret, service role solo server-side, ledger idempotente `revenuecat_webhook_events`, risoluzione pacchetto da product id/entitlement e upsert di `user_subscriptions` con `payment_provider='revenuecat'`. Manuale superadmin preservato con `payment_provider='superadmin_manual'`.
- **Verifica:** `npx.cmd tsc --noEmit` pulito durante lo sviluppo. Non verificabile end-to-end senza prodotti RevenueCat/Google/Apple configurati, migrazione SQL applicata, function deployata e build nativa reale.

## BUG-031 - Schermate interne senza freccia indietro coerente
- **Stato:** risolto (da verificare su nuovo APK)
- **Severita:** media
- **Data apertura/chiusura codice:** 2026-07-14
- **Causa:** verificata nel codice. I sotto-stack `schede`, `clienti`, `esercizi`, `appuntamenti` usavano ancora header nativi parziali, mentre `client-tabs.tsx`/`app-tabs.tsx` renderizzano le route interne dentro shell custom con `Slot`. Superadmin aveva `headerShown:false` globale, quindi diverse route interne non avevano alcuna freccia nativa.
- **Fix applicato:** nuovo `mobile/src/components/ui/back-header.tsx` con `ArrowLeft`, hit area 44x44, `accessibilityRole="button"`, `accessibilityLabel="Torna indietro"`, safe-area opzionale e fallback coerenti. Se `router.canGoBack()` e' vero usa `router.back()`, altrimenti fa `router.push(fallbackHref)`. Header nativi disabilitati dove e' visibile il custom header per evitare doppie frecce.
- **Verifica:** `tsc`, `expo-doctor`, `expo export --platform android` puliti. Non verificato con navigazione reale su APK.

## BUG-032 - Contatore workout cliente/coach bloccato a 0/12
- **Stato:** risolto (da verificare su dati reali)
- **Severita:** alta
- **Data apertura/chiusura codice:** 2026-07-14
- **Causa:** `getWorkoutCounter` restituiva `subscription.completedWorkouts` appena esisteva un abbonamento attivo. Quel campo legacy viene incrementato solo se la scheda completata ha `WorkoutPlan.subscriptionId`; le schede gia' completate ma non collegate all'abbonamento restavano corrette come `sessionStatus='completed'`, ma il numeratore UI restava 0.
- **Fix applicato:** il numeratore deriva ora dalle `workoutPlans` reali: stesso `clientId`, `sessionStatus='completed'`, deduplica per `plan.id`, `subscriptionId` uguale quando presente. Le schede legacy senza `subscriptionId` vengono contate solo se `completedAt` o `startDate` rientra nel periodo dell'abbonamento attivo. Home cliente, profilo/progressi cliente e dettaglio cliente coach usano la stessa funzione. Le nuove schede selezionano automaticamente l'abbonamento attivo del cliente se il coach non sceglie altro.
- **Verifica:** `tsc`, `expo-doctor`, `expo export --platform android` puliti. Non verificato contro il dataset reale con 2 schede completate.

## BUG-033 - Android: form "Nuova scheda" usciva orizzontalmente dallo schermo
- **Stato:** risolto (da verificare su device 320/360/393/412px)
- **Severita:** alta
- **Data apertura/chiusura codice:** 2026-07-14
- **Causa:** `WorkoutPlanForm` usava coppie di campi sempre in `flexDirection:'row'` senza breakpoint, senza `minWidth:0` sui figli e con input/label che potevano forzare la larghezza. `schede/new.tsx` usava inoltre un padding bottom fisso, non calcolato su tab bar + safe area Android.
- **Fix applicato:** `ThemedTextInput` e `AppTextField` hanno `width:'100%'`/`minWidth:0`; `WorkoutPlanForm` ha contenitori e figli flex con `minWidth:0`, coppie Data/Ora e Giorno/Settimana impilate sotto 390px, pulsanti "Aggiungi esercizio"/"Libreria YMove" impilabili, label con `flexShrink`. `schede/new.tsx` e `schede/[id].tsx` usano padding bottom `safe area + BottomTabInset + spazio extra`. Applicata la stessa correzione leggera alla riga oraria di `appuntamenti/new.tsx`.
- **Verifica:** `tsc`, `expo-doctor`, `expo export --platform android` puliti. Non verificato visivamente su device reale.
## BUG-034 - Redesign avatar/catalogo: asset reali e bucket non ancora applicati su Supabase
- **Stato:** aperto come verifica operativa, codice predisposto
- **Severita:** media
- **Data apertura:** 2026-07-14
- **Descrizione:** il codice ora supporta avatar cliente e catalogo anteprime esercizi, ma il bucket `client-avatars`, `profiles.avatar_preset` e la tabella opzionale `exercise_image_catalog` non esistono finche' la migrazione locale non viene eseguita manualmente sul progetto Supabase reale. Inoltre non sono ancora stati forniti asset fotografici locali per gli esercizi.
- **Comportamento atteso finche' non si applica la migrazione:** la foto profilo puo' essere scelta e mostrata localmente; l'upload remoto mostra errore leggibile se il bucket manca/non e' configurato. Le anteprime esercizi non restano vuote: se manca l'asset reale si vede il placeholder stabile per gruppo muscolare.
- **Verifica necessaria:** eseguire SQL da `docs/SUPABASE_SCHEMA.sql`, testare upload avatar con un cliente reale, popolare il catalogo immagini con asset reali e ricostruire un APK per verifica visiva.

## BUG-035 - Dettaglio cliente coach: "Sospendi per rinnovo" e "Rimuovi cliente" senza alcun effetto su Expo Web
- **Stato:** risolto (codice); **da verificare** con un clic reale in browser (nessun tool di automazione disponibile in questo ambiente)
- **Severita:** alta (due azioni core della gestione clienti coach — introdotte con le RPC `suspend_assigned_client`/`reactivate_assigned_client`/`remove_assigned_client`, commit `b1861a3` — restavano silenziosamente inerti su Expo Web, l'ambiente di sviluppo principale del progetto)
- **Data apertura:** introdotta col commit `b1861a3 feat: gestisce sospensione e rimozione clienti`; segnalata dall'utente il 2026-07-20
- **Descrizione:** `confirmSuspend()`/`confirmRemove()` in `mobile/src/app/clienti/[id].tsx` chiamavano `Alert.alert(title, message, [...])` con i bottoni di conferma/annulla. Su `react-native-web` (verificato in `node_modules/react-native-web/dist/exports/Alert/index.js`), `Alert.alert` e' implementato come no-op assoluto (`static alert() {}`): nessun dialog compare, nessun `onPress` dei bottoni viene mai invocato. Risultato: tap su "Sospendi per rinnovo"/"Rimuovi cliente" su web non produceva alcun effetto visibile, pur essendo `onPress`/RPC/service (`coach-client-status-service.ts`) corretti — la richiesta non partiva mai perche' la conferma non veniva mai raccolta. "Riattiva cliente" non passa da `Alert.alert` (azione diretta), quindi era gia' logicamente corretto ma non raggiungibile/testabile su web perche' non si poteva mai arrivare allo stato "suspended".
- **Passi di riproduzione:** da coach su Expo Web, aprire il dettaglio di un cliente attivo, premere "Sospendi per rinnovo" o "Rimuovi cliente": nessun dialog appare, nessuna richiesta parte, lo stato del cliente resta invariato.
- **Fix applicato:** aggiunta una funzione locale `confirmDestructive(title, message, confirmLabel)` in `[id].tsx` che su `Platform.OS === 'web'` usa `globalThis.confirm(...)` (sincrono, funziona su web) e su nativo usa `Alert.alert` con una `Promise` — stesso pattern gia' presente e funzionante in `mobile/src/components/client-metrics-panel.tsx` (`confirmAction`). `confirmSuspend`/`confirmRemove` sono ora `async` e chiamano `runStatusAction` solo se l'utente conferma. Non toccati: RPC Supabase, `coach-client-status-service.ts`, `app-button.tsx`, "Assegna scheda", "Riattiva cliente" (gia' corretto).
- **Verifica:** `npx tsc --noEmit` pulito, `npx expo export --platform web` completato senza errori di bundling, `git diff --check` pulito. **Non verificato**: nessun clic reale in un browser (nessun tool di automazione browser — Playwright/Puppeteer/MCP — disponibile in questo ambiente, ne' un coach con un cliente reale assegnato per una prova end-to-end contro Supabase). Checklist di verifica manuale in `docs/TODO_NEXT.md`.

## BUG-036 - Dettaglio/lista clienti coach: i clienti "suspended" spariscono da TUTTE le tab (Tutti/Attivi/Sospesi)
- **Stato:** risolto (codice/migration scritti); **BLOCCA la comparsa reale del cliente sospeso finche' la migration non viene eseguita sul progetto Supabase reale**
- **Severita:** alta (il coach non riesce a vedere/gestire un cliente che ha appena sospeso, ne' a riattivarlo dalla lista — la capacita' pacchetto lo conta comunque, dando l'impressione di un cliente "fantasma")
- **Data apertura:** introdotta con `is_coach_for_client()` in `20260718195148_add_client_suspension_and_removal.sql` (2026-07-18); segnalata dall'utente il 2026-07-20 dopo il fix di BUG-035
- **Causa reale (RLS, non codice mobile):** `coach_clients_coach_scope` (RLS su `coach_clients`) include gia' correttamente `status in ('active','suspended')`, quindi `mobile/src/lib/coach-clients-service.ts` ottiene i `client_id` giusti per entrambi gli stati. Ma la query successiva su `public.profiles`/`public.client_profiles` per completare i dati (nome, avatar, obiettivo) e' filtrata dalle policy `profiles_coach_reads_own_clients`/`client_profiles_coach_for_client`, che usano `is_coach_for_client()` — funzione che autorizza SOLO se `coach_clients.status = 'active'`. Per il client_id sospeso, questa RLS torna silenziosamente 0 righe (nessun errore Supabase): `const profile = profiles.find(...); if (!profile) return null;` scarta quindi il cliente dall'array finale, PRIMA che raggiunga l'hook/la lista/i filtri tab — che erano gia' tutti corretti (`clienti/index.tsx`: `visibleClients` filtra correttamente su `clients`, che pero' arrivava gia' privo del sospeso). Stesso identico pattern gia' risolto per l'avatar in `20260720084323_fix_suspended_client_avatar_access.sql` (mai propagato a profiles/client_profiles).
- **Passi di riproduzione:** da coach con un cliente active e uno suspended (capacita' pacchetto "2 su N" corretta, RPC `_coach_capacity` conta active+suspended via SQL diretto, non passa da questa RLS): tab "Tutti" mostra solo l'active, tab "Attivi" corretta, tab "Sospesi" vuota.
- **Fix applicato:** nuova migration `supabase/migrations/20260720171500_fix_coach_profile_read_includes_suspended_clients.sql` — nuova funzione `public.is_coach_for_client_visible(target_client_id)` (identica a `is_coach_for_client` ma `status in ('active','suspended')`), usata SOLO per `profiles_coach_reads_own_clients`/`client_profiles_coach_for_client` (lettura anagrafica). `is_coach_for_client()` originale resta invariata (solo `active`) perche' usata anche da `workout_plans_coach_scope`/`save_workout_plan` (scrittura schede) — "Assegna scheda" non va toccato, come richiesto. Nessuna modifica alla RPC di sospensione/riattivazione/rimozione, ai modal di conferma, al calcolo capacita' o al codice di filtro tab (gia' corretti). Aggiunti solo log diagnostici `__DEV__` in `coach-clients-service.ts` (righe coach_clients ricevute, profili caricati, eventuali link senza profilo, conteggio finale active/suspended) e in `clienti/index.tsx` (tab selezionata + conteggi, in un `useEffect` per non loggare ad ogni render).
- **Verifica:** `npx tsc --noEmit` pulito, `git diff --check` pulito. **Non verificato/BLOCCANTE**: la migration non e' mai stata eseguita su un progetto Supabase reale in questo ambiente (nessun accesso diretto) — finche' non viene applicata (`supabase db push` o incolla lo SQL nell'SQL editor del progetto collegato), il bug resta presente per l'utente esattamente come segnalato, indipendentemente dal codice mobile gia' corretto. Nessun clic reale in browser eseguibile (stesso limite di sempre). Checklist completa in `docs/TODO_NEXT.md`.

## BUG-037 - Dettaglio esercizio coach: con lo stesso esercizio assegnato a piu' clienti, si apriva di default sul cliente sbagliato
- **Stato:** risolto
- **Severita:** alta (il coach, navigando da una scheda specifica di un cliente, poteva finire a vedere/loggare carichi per un cliente diverso senza accorgersene se non notava i chip in alto)
- **Data apertura:** introdotta con la costruzione originale di `allAssignments`/`selectedIndex` in `mobile/src/app/esercizi/[id].tsx` (non datata con precisione, preesistente); trovata durante l'implementazione dell'inserimento carichi lato coach (2026-07-20)
- **Causa:** in `esercizi/[id].tsx`, per `currentRole === 'coach'` la variabile `assignments` include TUTTI gli assignment di quell'esercizio su TUTTI i clienti (a differenza del cliente, filtrato sul proprio `currentClientId`). `selected = assignments[selectedIndex] ?? assignments[0]` con `selectedIndex` inizializzato a `useState(0)` ignorava completamente il parametro `planId` nell'URL (passato da `schede/[id].tsx` -> `openExerciseDetail`): il coach vedeva sempre il PRIMO assignment trovato in assoluto (ordine di `workoutPlans`), non necessariamente quello del cliente/scheda da cui era navigato.
- **Passi di riproduzione:** stesso esercizio assegnato a due clienti diversi (es. Cliente A e Cliente B, con B che compare per primo in `workoutPlans`); da coach, aprire la scheda del Cliente A -> tap sull'esercizio: la schermata mostra di default i dati/serie/storico del Cliente B finche' non si nota e tocca il chip corretto.
- **Fix applicato:** nuovo `planMatchIndex` (indice dell'assignment il cui `plan.id === planId`) e `effectiveSelectedIndex`, che preferisce `planMatchIndex` finche' il coach non ha selezionato manualmente un chip (tracciato con `userSelectedAssignmentRef`, per non sovrascrivere una scelta esplicita successiva). Comportamento cliente invariato (gia' filtrato per `currentClientId`, mai stato soggetto al bug).
- **Verifica:** `npx tsc --noEmit` pulito. **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente). Vedi checklist in `docs/TODO_NEXT.md`.
- **Addendum (2026-07-20, [[BUG-038]]):** il workaround `planMatchIndex`/`userSelectedAssignmentRef` e' stato rimosso e sostituito da una soluzione architetturale (contesto cliente forzato e validato, niente piu' chip multipli in questo flusso) — vedi BUG-038 per il dettaglio, questa voce resta come storico della causa originale.

## BUG-038 - Dettaglio esercizio coach: chip di TUTTI i clienti visibili/selezionabili entrando dalla scheda di un cliente specifico
- **Stato:** risolto (codice); **non verificato con un clic reale**
- **Severita:** alta (rischio concreto di inserire carichi o completare un esercizio per il cliente sbagliato, senza alcun blocco: il coach doveva accorgersi da solo del chip attivo)
- **Data apertura:** stessa origine architetturale di [[BUG-037]] (costruzione non filtrata di `allAssignments` per il coach in `mobile/src/app/esercizi/[id].tsx`); segnalata esplicitamente dall'utente il 2026-07-20 con l'esempio "Rita, luigi, luigi"
- **Causa:** in `esercizi/[id].tsx`, per `currentRole === 'coach'`, `assignments`/`allAssignments` includeva ogni cliente con quell'esercizio assegnato, e la riga di chip in alto li rendeva tutti visibili e selezionabili — anche quando il coach era arrivato da un percorso specifico (`clienti/[id].tsx` -> `schede/[id].tsx` -> esercizio), dove il contesto doveva restare fissato su un solo cliente. Il fix di BUG-037 (`planMatchIndex`) mitigava solo la selezione di default, non il fatto che gli altri clienti restassero comunque presenti e cliccabili.
- **Passi di riproduzione:** coach seleziona un cliente (es. Rita), entra in una sua scheda, apre un esercizio: in alto compaiono chip di altri clienti (es. altri "luigi") con lo stesso esercizio assegnato, tutti selezionabili nella stessa schermata.
- **Fix applicato:** `schede/[id].tsx` (`openExerciseDetail`) passa ora `clientId` in modo esplicito e autorevole (da `plan.clientId`, gia' caricato via Supabase con RLS scoped al coach) oltre a `planId`. In `esercizi/[id].tsx`, quando il coach arriva con un `planId` (`isCoachSessionContext`), `assignments` viene ridotto a un solo elemento, validato incrociando `planId` + `clientId` contro i `workoutPlans` in store; se la validazione fallisce (parametro mancante, manomesso, o cliente/scheda non corrispondenti) la schermata si blocca (`sessionContextInvalid`) con un messaggio esplicito e un bottone "Torna alla lista clienti", **senza mai selezionare automaticamente il primo cliente disponibile**. La riga di chip resta visibile solo fuori da questo contesto (es. catalogo esercizi generico, `esercizi/index.tsx`, dove non esiste `planId`). Aggiunti avatar, nome cliente e bottone "Torna al cliente" nella card di assegnazione. Il workaround di BUG-037 (`planMatchIndex`/`userSelectedAssignmentRef`) e' stato rimosso perche' superfluo con questa soluzione.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito. **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente) — l'utente deve confermare i passi in `docs/TODO_NEXT.md`.

## BUG-039 - `update_workout_session_progress`: nessun controllo di relazione attiva per il percorso COACH (poteva agire su clienti sospesi/rimossi)
- **Stato:** risolto (migration creata); **da applicare sul progetto Supabase reale**
- **Severita:** alta (gap di autorizzazione server-side: un coach poteva completare/modificare lo stato di una sessione anche per un cliente ormai sospeso o rimosso, solo perche' possedeva la scheda)
- **Data apertura:** preesistente da quando `update_workout_session_progress` fu esteso al coach (migration `20260720190000_harden_exercise_progress_completion_lock.sql`, sessione precedente); trovata durante l'audit di sicurezza esplicitamente richiesto per questo intervento (2026-07-20)
- **Causa:** la funzione verificava correttamente `v_client_has_active_link` (stato `active` della relazione `coach_clients`) per il percorso CLIENTE, ma nessun controllo equivalente esisteva per il percorso COACH (`v_plan.coach_id = auth.uid()`): bastava possedere la scheda, indipendentemente dallo stato corrente della relazione con quel cliente.
- **Passi di riproduzione:** coach con un cliente sospeso/rimosso che ha ancora una scheda (`workout_plans.coach_id` invariato); chiamata diretta a `update_workout_session_progress` per quella scheda veniva comunque accettata.
- **Fix applicato:** nuova migration non distruttiva `supabase/migrations/20260721090000_require_active_link_for_coach_session_progress.sql` — `create or replace function` sulla stessa firma, aggiunge `v_coach_client_active` (stesso controllo gia' usato per il cliente, ma su `coach_clients.client_id = v_plan.client_id`) e solleva `CLIENT_NOT_ACTIVE` se il coach agisce e la relazione non e' `active` (nessuna eccezione per il coach, come richiesto esplicitamente). `mobile/src/lib/workout-plan-service.ts` mappa il nuovo codice a un messaggio utente in italiano.
- **Verifica:** `npx.cmd tsc --noEmit` pulito. **Bloccante per l'utente**: questa migration, come le due precedenti (`20260720171500`, `20260720190000`), non e' stata eseguita contro un progetto Supabase reale in questo ambiente — va applicata prima che la protezione sia effettivamente attiva in produzione.

## BUG-040 - Tab globale "Schede" mostrava le sessioni reali di TUTTI i clienti invece di essere una libreria di modelli del coach
- **Stato:** risolto (codice + migration creata); **migration da applicare sul progetto Supabase reale**
- **Severita:** alta (violazione diretta della separazione richiesta tra libreria di modelli e schede realmente assegnate: il tab globale esponeva nome cliente, stato sessione e completamento di OGNI cliente del coach, mai inteso come vista cross-cliente)
- **Data apertura:** preesistente da sempre (comportamento originale di `schede/index.tsx`, mai una libreria di modelli); segnalata esplicitamente dall'utente il 2026-07-20 (continuazione 4) con il requisito "il tab deve essere la libreria globale dei modelli, non deve aprire o selezionare automaticamente alcun cliente"
- **Causa:** `mobile/src/app/schede/index.tsx` leggeva `workoutPlans` (TUTTE le schede reali assegnate, di TUTTI i clienti, via `useTrainingStore`) e le elencava con tab "Da fare"/"Passati", nome cliente e badge di stato — nessun concetto di modello/cartella collegato a Supabase esisteva realmente: `public.workout_templates` era gia' presente da una migrazione precedente (2026-07-14) ma senza cartelle, senza esercizi propri, senza alcuna schermata che la usasse (scaffold morto, zero chiamanti — confermato via grep).
- **Passi di riproduzione:** da coach, aprire il tab "Schede" della bottom bar: comparivano le sessioni reali di tutti i clienti (con nome cliente e stato), non una libreria organizzabile di modelli.
- **Fix applicato:** `schede/index.tsx` sostituito integralmente con la libreria globale (cartelle/sottocartelle -> schede modello -> esercizi), backed da nuove tabelle Supabase (`template_folders`, `workout_template_exercises`, `workout_templates` esteso con `folder_id`/`sort_order`) e tre nuove RPC (`save_workout_template`, `delete_template_folder`, `assign_workout_template_to_client`) — vedi migration `supabase/migrations/20260722090000_workout_template_folders_and_exercises.sql` e commit `9f18fd2`. Le sessioni reali cross-cliente restano visibili SOLO da `Clienti -> [id] -> Schede` (invariato, mai stato oggetto di questo fix).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito. **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente) — vedi gli 11 scenari di test manuale richiesti in `docs/TODO_NEXT.md`. La migration `20260722090000` non e' stata eseguita contro un progetto Supabase reale: finche' non viene applicata, cartelle/modelli/assegnazione non sono operativi in produzione.
- **Effetto collaterale noto, non corretto (fuori scope):** `schede/[id].tsx`, dopo l'eliminazione di una scheda REALE assegnata, fa ancora `router.replace('/schede')` — ora atterra sulla libreria modelli invece che su un elenco di sessioni. Nessun crash, nessun dato perso: solo una destinazione meno pertinente di prima. Non toccato perche' `schede/[id].tsx` e' un'area esplicitamente protetta (schede reali assegnate) e la correzione non era richiesta.

## BUG-041 - Menu a tendina (Seleziona esercizio/Gruppo muscolare/Attrezzatura/Difficolta') sempre neri anche in tema chiaro
- **Stato:** risolto
- **Severita:** alta (pannello opzioni illeggibile in tema chiaro: testo scuro su sfondo quasi nero in tutti e quattro i menu del catalogo esercizi)
- **Data apertura:** preesistente da quando `DropdownList`/`MenuOption` sono stati introdotti in `exercise-catalog-picker.tsx` (non datata con precisione); segnalata esplicitamente dall'utente il 2026-07-21
- **Causa:** i due componenti condivisi `DropdownList` e `MenuOption` (usati da TUTTI e quattro i menu segnalati: "Seleziona esercizio", "Gruppo muscolare", "Attrezzatura", "Difficolta'" — un unico punto, non quattro bug separati) avevano `backgroundColor` con hex hardcoded (`'#05090C'`/`'#173B16'`), praticamente identici ai soli valori della palette DARK (`Colors.dark.background`/`Colors.dark.backgroundSelected`, `constants/theme.ts`) — indipendenti da `theme`, quindi sempre scuri qualunque fosse il tema attivo dell'app. Bordo e testo, gia' letti correttamente da `theme.border`/`theme.text`/`theme.primary` nello stesso componente, restavano invece coerenti col tema: da qui il contrasto illeggibile in chiaro. **Non era** un problema di `colorScheme`/`<select>` nativo del browser o dell'OS (ipotesi di partenza del task): l'app non usa mai `<select>` HTML ne' `@react-native-picker/picker`, e ne' `global.css` ne' alcun layout impostano mai `color-scheme` — verificato esplicitamente.
- **Passi di riproduzione:** in tema chiaro, aprire un qualunque form con "Aggiungi esercizio" (es. da una scheda o da un modello) → aprire "Seleziona esercizio"/"Gruppo muscolare"/"Attrezzatura"/"Difficolta'" → il pannello si apriva nero/quasi nero, testo scuro illeggibile.
- **Fix applicato:** `mobile/src/components/exercise-catalog-picker.tsx` — `DropdownList` e `MenuOption` usano ora `theme.background`/`theme.backgroundSelected` (stessi token gia' usati da `SelectorButton`, il bottone chiuso subito sopra il pannello, e da `MuscleMultiSelect` nello stesso file) invece degli hex hardcoded. Nessun altro Select/Picker/Dropdown trovato nell'app (audit esplicito: `ymove-exercise-picker.tsx` non ha dropdown, solo campi di testo; altri hex hardcoded trovati altrove — `superadmin/index.tsx`, `muscle-map.tsx`, `web-phone-frame.tsx` — sono badge/silhouette/mockup intenzionalmente sempre scuri, non menu, non toccati).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito. **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente) — da confermare aprendo i quattro menu in tema chiaro e scuro, e cambiando tema a runtime da Impostazioni.

## BUG-042 - Filtri esercizi (pannello "Filtri" e menu "Gruppo muscolare") aperti automaticamente in creazione/modifica scheda
- **Stato:** risolto
- **Severita:** media (nessun dato perso/rotto, ma occupava gran parte della schermata di selezione esercizi ad ogni apertura, richiedendo una chiusura manuale ripetuta)
- **Data apertura:** preesistente da quando il pannello filtri/`openMenu` sono stati introdotti in `exercise-catalog-picker.tsx`; segnalata esplicitamente dall'utente il 2026-07-21
- **Causa:** in `mobile/src/components/exercise-catalog-picker.tsx` (unico componente, condiviso senza duplicazione da `workout-plan-form.tsx` e `workout-template-form.tsx`, entrambi montano/smontano l'intero picker ad ogni apertura) gli stati iniziali erano `filtersOpen = useState(true)` e `openMenu = useState('group')` invece di chiusi — non un `useEffect` reattivo al valore selezionato (nessuno presente) ne' uno stato persistito in uno store/route (`selectedGroup` e' un `useState` locale, mai scritto altrove).
- **Passi di riproduzione:** coach → creazione o modifica scheda (o modello) → "+ Aggiungi esercizio" → il pannello "Filtri" e il menu "Gruppo muscolare" comparivano gia' aperti, senza alcun tocco.
- **Fix applicato:** cambiati solo i due valori iniziali in `useState(false)`/`useState(null)`. Comportamento gia' corretto e lasciato invariato: chiusura automatica del menu alla selezione, un solo menu aperto alla volta, valore selezionato visibile come testo a menu chiuso, ricerca/filtri applicati non toccati.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito. **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente) — da confermare su Web, Android e iOS, checklist in `docs/TODO_NEXT.md`.

## BUG-043 - Modelli professionali "assegnati" dall'interfaccia ma invisibili nelle schede del cliente
- **Stato:** risolto
- **Severita:** alta (l'assegnazione riusciva davvero lato server ma il coach non aveva alcuna prova visibile del risultato senza un'azione estranea, es. riavviare l'app o navigare altrove per forzare un refresh)
- **Data apertura:** preesistente da quando "Assegna a cliente" e' stato introdotto per i modelli (2026-07-21, feat completamento libreria professionale); segnalata esplicitamente dall'utente il 2026-07-21
- **Causa:** `mobile/src/app/schede/modello/[templateId].tsx`, dopo un'assegnazione riuscita (`assignWorkoutTemplateToClient`, RPC `assign_workout_template_to_client` — gia' corretta, verificata riga per riga: gestisce `is_system`, verifica `coach_clients.status='active'` da `auth.uid()`, copia ogni giorno in `workout_plans`/`workout_days`/`workout_day_exercises` indipendenti in un'unica transazione), leggeva le nuove schede SOLO per la card di conferma a video, senza mai scriverle nello store locale `useTrainingStore` — l'unica fonte letta da `clienti/[id].tsx` ("Clienti → cliente → Schede"). La scheda esisteva davvero su Supabase da subito, ma restava invisibile nella lista cliente finche' nient'altro non forzava un refresh completo dello store (Realtime, se attivo, o il mount di un'altra schermata). Le altre due schermate di creazione scheda del progetto (`schede/new.tsx`, `schede/modelli/[id].tsx`) chiamano gia' `addWorkoutPlan(result.data)` dopo un salvataggio riuscito, per questo stesso motivo — questa era l'unica a non seguire quel pattern.
- **Passi di riproduzione:** coach → Schede → Allenamenti professionali → categoria → modello → "Assegna a cliente" → scegliere un cliente active → confermare → la conferma a video compariva regolarmente, ma "Clienti → quel cliente → Schede" non mostrava la nuova scheda.
- **Fix applicato:** aggiunta la chiamata `addWorkoutPlan(plan)` (da `useTrainingStore`) per ciascuna scheda restituita, subito dopo il successo — stesso pattern gia' in uso altrove, nessun meccanismo nuovo. Aggiunta diagnostica `__DEV__` (templateId/is_system/clientId/risposta RPC/id piani creati/conteggio giorni-esercizi, mai un token) in `workout-plan-service.ts` e nella schermata. "Vai al cliente" ora apre direttamente la tab Schede (nuovo parametro opzionale `tab` su `clienti/[id].tsx`, letto solo come stato iniziale) invece di atterrare sempre su Panoramica. Nessuna migration nuova: confermato con `supabase migration list`/`db push --dry-run` che lo schema/RPC applicati al progetto reale sono gia' corretti.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito, `supabase migration list` (tutte remote) e `db push --dry-run` ("Remote database is up to date", nessun push reale eseguito). **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente) — 10 scenari di test manuale richiesti, checklist in `docs/TODO_NEXT.md`.

## BUG-044 - Filtro "Petto" selezionato automaticamente all'apertura del catalogo esercizi
- **Stato:** risolto
- **Severita:** media (nessun dato perso, ma il coach vedeva solo un sottoinsieme di esercizi senza aver scelto nulla, dovendo notare e rimuovere un filtro che non aveva impostato)
- **Data apertura:** preesistente da quando il filtro "Gruppo muscolare" e' stato introdotto in `exercise-catalog-picker.tsx`; segnalata esplicitamente dall'utente il 2026-07-21, subito dopo la verifica del fix BUG-042
- **Causa:** `selectedGroup` non aveva mai uno stato "nessun filtro"/"tutti i gruppi", a differenza di `equipmentFilter`/`difficultyFilter` (entrambi gia' con un sentinel `'all'` e relativa opzione "Tutte attrezzature"/"Tutte difficolta'" nel menu). Partiva sempre da `firstGroup = EXERCISE_GROUP_ORDER.find((g) => groupCounts[g] > 0) ?? 'petto'` — e siccome `'petto'` e' il primo elemento di `EXERCISE_GROUP_ORDER` ed e' quasi sempre non vuoto, il filtro risultava di fatto sempre "Petto" fin dal montaggio del componente. La pill in alto (con una "x" visiva) non aveva inoltre un vero comportamento di rimozione: il suo tocco riapriva solo il menu, non azzerava il filtro.
- **Passi di riproduzione:** coach → creazione/modifica scheda (o modello) → "+ Aggiungi esercizio" → il pannello mostrava sempre e solo gli esercizi del petto, con la pill "Petto x" gia' presente prima di qualunque tocco.
- **Fix applicato:** aggiunto lo stesso sentinel `'all'` gia' usato dagli altri due filtri (nuovo tipo `GroupFilterValue`), stato iniziale `'all'`; `groupExercises` mostra tutti gli esercizi quando `selectedGroup === 'all'`; nuova opzione "Tutti i gruppi" nel dropdown; la pill compare solo dopo selezione esplicita e la sua "x" ora chiama `selectGroup('all')` (rimozione reale, non piu' solo apertura del menu). `firstGroup` resta usato solo per inizializzare il gruppo di un nuovo esercizio personalizzato, che deve sempre avere un gruppo reale.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, `git diff --check` pulito. **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente) — 5 scenari di test manuale richiesti, checklist in `docs/TODO_NEXT.md`.

## BUG-045 - Titolo della card scheda (coach) troppo grande, va a capo su 3 righe disordinate
- **Stato:** risolto
- **Severita:** bassa (solo estetica/layout, nessun dato o funzionalita' persa, ma la card risultava sbilanciata e poco leggibile con nomi scheda lunghi/composti)
- **Data apertura:** preesistente da quando la hero card del dettaglio scheda coach e' stata introdotta; segnalata esplicitamente dall'utente il 2026-07-21 con l'esempio "Powerbuilding — Lower Hypertrophy"
- **Causa:** `mobile/src/app/schede/[id].tsx`, hero card lato coach — `styles.planTitle` con `fontSize: 29`/`lineHeight: 35` e `numberOfLines={3}` sul `Text`: un titolo composto (formato `"<nome modello> — <nome giorno>"`, tipico dei modelli assegnati) a quella dimensione finiva facilmente su 3 righe accanto al box fisso "N esercizi", sbilanciando la card.
- **Passi di riproduzione:** coach → aprire una scheda con un nome lungo/composto (es. un giorno di un modello assegnato) → il titolo occupava 3 righe disordinate, la card risultava sbilanciata.
- **Fix applicato:** `numberOfLines` 3→2 (gia' con `ellipsizeMode="tail"`: un titolo ancora piu' lungo tronca ora con ellipsis invece di rompere su una terza riga); `planTitle.fontSize` 29→21, `lineHeight` 35→26; `flexShrink` esplicito su `heroCopy` (blocco titolo) e `heroVisual` (blocco "N esercizi") perche' sia sempre il titolo a restringersi, mai il box esercizi a larghezza fissa.
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito. **Non verificato**: nessun clic reale in browser (nessun tool di automazione disponibile in questo ambiente) — 6 scenari di test manuale richiesti, checklist in `docs/TODO_NEXT.md`.

## BUG-046 - Migrazione locale dei workout ritenta piani remoti gia' completed, bloccata da WORKOUT_LOCKED
- **Stato:** risolto
- **Severita:** alta (errore reale mostrato all'avvio/refresh per il coach — `WORKOUT_REMOTE_SAVE_ERROR` / "questo workout e' gia' completato e non puo' essere modificato" — su schede che in realta' non hanno bisogno di alcuna migrazione)
- **Data apertura:** segnalata esplicitamente dall'utente il 2026-07-22 con stack trace reale (`workout-plan-service.ts:saveWorkoutPlanInternal`, `workout-plan-migration.ts:runMigration` circa riga 130)
- **Causa:** `runMigration` (`mobile/src/lib/workout-plan-migration.ts`) considera "candidati alla migrazione" tutti i piani in `useTrainingStore.getState().workoutPlans` non presenti nella mappa persistita `migratedMap`. Ma quello store viene sovrascritto ad ogni `refresh()` riuscito con l'INTERO elenco remoto (`setWorkoutPlans(result.data)`, `hooks/use-workout-plans-sync.ts`) — quindi anche i piani gia' sincronizzati (id UUID reale, creati o letti da Supabase) ricompaiono li' e, non essendo mai passati per QUESTA procedura, non sono mai in `migratedMap`. Il filtro precedente (fix 2026-07-14) escludeva volutamente la forma dell'id come segnale — corretto per il bug di allora (falsi "gia' migrato" senza mai creare la riga) ma incompleto: non copriva il caso opposto, un piano gia' sicuramente remoto (id UUID) rimandato di nuovo a `createWorkoutPlan`. Il payload (`buildSavePayload`) invia l'id reale quando e' un UUID valido, quindi `save_workout_plan` lo tratta come UPDATE su una riga esistente — bloccato da `WORKOUT_LOCKED` se quella riga e' `completed` (migrazioni `20260727090000`/`20260728090000`, non toccate da questo fix).
- **Passi di riproduzione:** coach con almeno un workout gia' sincronizzato e completato su Supabase → riavviare l'app o rimontare una schermata che chiama `useWorkoutPlansSync` (dashboard, lista schede) dopo che lo store locale e' gia' stato popolato da un refresh precedente → `WORKOUT_REMOTE_SAVE_ERROR` in console, nessuna schermata rossa ma un tentativo di scrittura reale rifiutato dal server ad ogni refresh successivo.
- **Fix applicato:** in `workout-plan-migration.ts`, il filtro `candidates` ora esclude anche ogni piano con `isValidUuid(plan.id) === true` (import da `workout-plan-service.ts`) — un id placeholder locale non e' MAI un UUID (`plan-${Date.now()}`, unico generatore, sempre dietro `!supabaseConfig.isConfigured`, verificato in `schede/new.tsx` e `schede/modelli/[id].tsx`), quindi un id UUID prova che la riga esiste gia' su Supabase, con qualunque `session_status` — non serve interrogare il remoto per saperlo. Nessuna scrittura tentata su questi piani, nessun errore, nessun retry (il piano semplicemente non rientra mai piu' tra i candidati, ad ogni run). Un vero piano locale-only (mai sincronizzato, anche se gia' `completed` offline) resta migrabile: `buildSavePayload` invia sempre `id: null` per un id non-UUID, quindi `save_workout_plan` fa sempre un INSERT, mai raggiungibile da `WORKOUT_LOCKED` (i trigger di immutabilita' sono `BEFORE UPDATE OR DELETE`, mai `BEFORE INSERT`). Aggiunto un contatore diagnostico `skippedAlreadyRemote` nel risultato e un log `console.warn` solo `__DEV__` quando > 0 (mai un `console.error`/schermata rossa per questo caso, ormai atteso).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 1 file. Confermato per lettura che nessun altro percorso di scrittura (`schede/[id].tsx`, salvataggio strutturale e aggiornamenti di sessione) dipende da `runMigration` per sincronizzare modifiche su un piano gia' remoto: ogni edit reale chiama `updateWorkoutPlanRemote`/`updateWorkoutSessionProgress` direttamente, con errore mostrato subito, mai in coda alla migrazione. **Non verificato**: nessun clic reale/avvio app reale in questo ambiente — 10 scenari di test runtime richiesti, checklist in `docs/TODO_NEXT.md`.

## BUG-047 - Eliminazione scheda cliente reindirizza alla libreria globale modelli, perdendo il contesto cliente
- **Stato:** risolto
- **Severita:** media (nessuna perdita di dati, ma il coach perde il filo del cliente su cui stava lavorando subito dopo un'azione distruttiva riuscita — UX confusa, non un errore visibile)
- **Data apertura:** preesistente da quando `/schede` e' diventata la libreria globale dei modelli (migrazione 2026-07-20, commit `9f18fd2`); segnalata esplicitamente dall'utente il 2026-07-22
- **Causa:** `mobile/src/app/schede/[id].tsx`, `handleDelete` — sia nel ramo locale (Supabase non configurato) sia in quello remoto, dopo l'eliminazione riuscita faceva sempre `router.replace('/schede')`. Prima della libreria modelli, `/schede` era un elenco cross-cliente delle sessioni assegnate, quindi il redirect aveva senso; da quando `/schede` e' la libreria dei modelli del coach, atterrare li' dopo aver eliminato la scheda di un cliente specifico (raggiunta da Clienti → cliente → Schede → dettaglio) fa perdere il contesto cliente appena aperto.
- **Passi di riproduzione:** coach → Clienti → un cliente → tab Schede → apri una scheda `todo`/`in_progress` → Modifica scheda → Elimina scheda → conferma → si atterra sulla libreria modelli globale invece che sul dettaglio del cliente di partenza.
- **Fix applicato:** nuova `goToClientSchede(clientId)` in `schede/[id].tsx`, stesso pattern di navigazione tipizzata gia' usato da "Vai al cliente" (`schede/modello/[templateId].tsx`, dopo un'assegnazione riuscita): `router.replace({ pathname: '/clienti/[id]', params: { id: clientId, tab: 'schede' } })`. `clientId` usato sempre da `plan!.clientId` (il piano gia' caricato, RLS-scoped al coach), mai da un parametro URL — la schermata legge oggi solo `id`, quindi non esisteva comunque un clientId da URL di cui fidarsi. Applicato sia al ramo locale sia al ramo remoto. Se `clientId` fosse vuoto (mai in pratica: tipizzato `string` non opzionale), fallback a `/clienti` (l'elenco), mai un salto silenzioso alla libreria modelli. In caso di cancellazione fallita (incluso `WORKOUT_LOCKED`, gia' bloccato anche server-side dal trigger `before update or delete` su `workout_plans`, migration `20260728090000`, non toccata) nessuna navigazione: si resta nel dettaglio con l'errore reale visibile (comportamento gia' esistente in `setSaveError`, non modificato).
- **Contesti verificati gia' corretti, non toccati:** modelli personali del coach (`schede/modello/[templateId].tsx`, `handleDelete` torna gia' a `/schede/cartella/[folderId]` o `/schede`) e modelli professionali di sistema (non eliminabili, nessun `handleDelete` equivalente in `template-library-screen.tsx`) — `/schede/[id].tsx` non gestisce mai template, solo schede reali (confermato via grep, nessun `templateId` nel file).
- **Verifica:** `npx.cmd tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 1 file. Confermato per lettura che `useCoachClients` (usato da `clienti/[id].tsx`) ricarica i clienti nel proprio `useFocusEffect` — non dipende dall'aver visitato prima `/clienti` — e che la lista schede del cliente nella nuova schermata legge dallo stesso store Zustand gia' aggiornato da `deleteWorkoutPlanLocal`, quindi la scheda eliminata non compare piu' senza bisogno di un refresh remoto esplicito. **Non verificato**: nessun clic reale in browser/app (nessun tool di automazione disponibile in questo ambiente) — 8 scenari di test manuale richiesti esplicitamente, checklist in `docs/TODO_NEXT.md`.

## BUG-048 - Menu "Altro" mostra Bacheca/Prenotazioni ai clienti self_guided
- **Stato:** risolto
- **Severita:** media (nessuna fuga di dati — AuthGate rimbalzava comunque il tap in Home — ma voce di menu morta/confusa, in contraddizione col requisito esplicito di non mostrare funzioni coach-only a un cliente autonomo)
- **Data apertura:** trovata dall'audit "Release Gate Android + iOS" (2026-07-22)
- **Causa:** `mobile/src/app/altro.tsx`, `MENU_ITEMS` era un array statico mai filtrato per `client_mode`, a differenza di `cliente-home.tsx` (gia' corretto con `isCoachGuided`).
- **Fix applicato:** aggiunto `hiddenForSelfGuided` a "Bacheca"/"Prenotazioni", filtro `menuItems = MENU_ITEMS.filter(item => !item.hiddenForSelfGuided || isCoachGuided)` con `isCoachGuided = useMyCoach().clientMode === 'coach_guided'` — stesso pattern fail-closed di `cliente-home.tsx` (le voci restano nascoste finche' `coach_guided` non e' confermato, mai mostrate durante il caricamento). Deep link diretti restano bloccati da AuthGate, invariato.
- **Verifica:** `npx tsc --noEmit` pulito, `git diff --check` pulito su 1 file. **Non verificato**: nessun clic reale (nessun tool di automazione disponibile) — checklist in `docs/TODO_NEXT.md`.

## BUG-049 - Store Zustand non azzerati al logout, rischio di dati residui dell'account precedente
- **Stato:** risolto
- **Severita:** media (nessuna corruzione permanente — i setter dei dati remoti fanno sempre full-replace — ma finestra reale in cui, dopo un nuovo login sullo stesso device, potevano restare visibili per un istante clienti/workout/dati Superadmin dell'account precedente prima del refresh post-login)
- **Data apertura:** trovata dall'audit "Release Gate Android + iOS" (2026-07-22)
- **Causa:** `client-store.ts`/`training-store.ts`/`superadmin-store.ts`/`chat-store.ts`/`appointment-store.ts`/`board-store.ts`/`booking-store.ts`/`checkin-store.ts`/`attachment-store.ts`/`ymove-autolink-store.ts` (tutti persistiti via AsyncStorage) non avevano mai un'azione di reset; `logout()` in `auth-store.ts` azzerava solo i propri campi.
- **Fix applicato:** aggiunta un'azione `reset()` a ciascuno dei 10 store sopra (torna al seed demo se `EXPO_PUBLIC_ENABLE_DEMO_DATA=true`, altrimenti stato iniziale vuoto), nuova `resetUserScopedStores()` in `auth-store.ts` chiamata dentro `logout()` prima di azzerare `isAuthenticated`/`currentRole` — stessa chiamata sincrona, un solo re-render batchato, nessun frame intermedio incoerente. `theme-store.ts` e `training-store.soundSettings` intoccati (preferenze del dispositivo, non dati dell'account). Le sottoscrizioni Realtime si disiscrivono da sole (dipendono gia' da `currentRole` nei rispettivi `useEffect`), nessuna disiscrizione manuale duplicata necessaria.
- **Verifica:** `npx tsc --noEmit` pulito, `git diff --check` pulito su 11 file. **Non verificato**: nessun clic reale (cambio account coach→coach, coach→cliente, superadmin→coach) — checklist in `docs/TODO_NEXT.md`.

## BUG-050 - Link Privacy/Termini senza gestione errori su Linking.openURL
- **Stato:** risolto
- **Severita:** bassa (nessun crash osservato, ma tap silenzioso senza feedback in caso di fallimento — inconsistente col pattern gia' stabilito altrove nel codebase)
- **Data apertura:** trovata dall'audit "Release Gate Android + iOS" (2026-07-22) — feature introdotta nella stessa sessione precedente (configurazione iOS)
- **Causa:** `mobile/src/components/developer-info-section.tsx`, i due `Pressable` "Privacy policy"/"Termini di servizio" chiamavano `Linking.openURL(...)` direttamente in `onPress`, senza `.catch()`/try-catch, a differenza del pattern gia' in uso in `revenuecat-service.ts` (`openRevenueCatSubscriptionManagement`).
- **Fix applicato:** nuova `openLegalLink(url)` — verifica URL non vuoto, `Linking.canOpenURL` prima di aprire, try/catch su `Linking.openURL`, messaggio utente chiaro in ogni caso di fallimento (stesso helper `notify()` di `impostazioni.tsx`/`cliente-profilo.tsx`: `Alert.alert` su nativo, `globalThis.alert` su web). URL placeholder non sostituiti (dato esterno mancante, invariato).
- **Verifica:** `npx tsc --noEmit` pulito, `git diff --check` pulito su 1 file. **Non verificato**: nessun clic reale con URL non apribile/assenza rete — checklist in `docs/TODO_NEXT.md`.

## BUG-051 - Edge Function send-push scritta ma mai deployata: notifiche push chat/appuntamenti silenziosamente inattive
- **Stato:** risolto (deploy, nessuna modifica al codice necessaria)
- **Severita:** media (nessun crash — la chiamata e' fire-and-forget con try/catch in `push-notification-service.ts` — ma una feature reale, gia' collegata da chat e appuntamenti, restava completamente inoperante in produzione)
- **Data apertura:** trovata dall'audit "Release Gate Android + iOS" (2026-07-22)
- **Causa:** `supabase/functions/send-push/index.ts` presente e tracciata in git, chiamata da `notifyMessagePush`/`notifyAppointmentPush` (`push-notification-service.ts`, usati da `chat.tsx` e `appuntamenti/{index,new,[id]}.tsx`), ma mai deployata sul progetto Supabase reale.
- **Audit di sicurezza eseguito prima del deploy (nessuna modifica necessaria, codice gia' corretto):** identita' del chiamante solo dal JWT (mai dal body); destinatario risolto SEMPRE da `coach_clients` reale (mai un user_id arbitrario dal client); un cliente self_guided non ha alcuna riga `coach_clients` quindi riceve sempre 403; `suspended`/`removed` esclusi dal filtro `status in ('active','invited')` sia lato coach sia lato cliente; token push mai restituiti al chiamante; nessun segreto nel mobile (service role solo server-side, iniettata dal runtime); nessun secret custom richiesto oltre a `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` (auto-iniettate).
- **Fix applicato:** `npx supabase functions deploy send-push --project-ref rkcecnzvzoigipjliwdk` (nessun `--no-verify-jwt`: verifica JWT attiva di default, coerente con una funzione chiamata da utenti autenticati). Confermata `ACTIVE`/`verify_jwt=true` via `functions list`.
- **Verifica:** nessuna chiamata reale end-to-end eseguita in questo ambiente (nessun utente di test/device con token push disponibile) — checklist in `docs/TODO_NEXT.md`.

## BUG-052 - Cliente self_guided senza abbonamento Client Pro non poteva raggiungere impostazioni/eliminazione account
- **Stato:** risolto
- **Severita:** alta (violava esplicitamente il requisito "non bloccare mai l'eliminazione account/gestione account" — un cliente self_guided senza piano attivo restava intrappolato sulla paywall senza alcun modo di eliminare l'account, solo logout/ripristino acquisti disponibili)
- **Data apertura:** trovata durante l'audit propedeutico all'implementazione di Client Pro (2026-07-23)
- **Causa:** `mobile/src/components/auth-gate.tsx`, `getClientRedirectTarget` — quando `onboarding.mode === 'self_guided'` e `!planAccess.active`, QUALUNQUE route diversa da `/abbonamento-cliente` veniva rediretta li', incluso `/cliente-profilo` (dove vivono impostazioni, tema, logout, eliminazione account). Anche il tab "Altro" (sempre visibile nella bottom bar) rimbalzava immediatamente indietro.
- **Fix applicato:** l'allowlist per self_guided senza piano attivo ora include anche `/cliente-profilo` (oltre a `/abbonamento-cliente` gia' presente); non allargata a `/altro` (esporrebbe voci premium come Metriche/Progressi/Pacchetti). Aggiunto un link diretto "Impostazioni account" su `abbonamento-cliente.tsx` per raggiungere `/cliente-profilo` senza passare dal tab Altro (che resta bloccato).
- **Verifica:** `npx tsc --noEmit` pulito, `git diff --check` pulito. **Non verificato**: nessun clic reale (nessun tool di automazione disponibile in questo ambiente) — checklist in `docs/TODO_NEXT.md`.

## BUG-053 - `signUpClientWithCoachCode` non imposta `emailRedirectTo` (trovato, NON corretto in questo task)
- **Stato:** aperto, segnalato senza fix (vincolo esplicito del task che l'ha trovato: "non cambiare il funzionamento dei link di conferma Supabase")
- **Severita:** bassa/media (nessuna rottura del flusso — l'email di conferma arriva comunque — ma il redirect dopo la conferma usera' sempre la Site URL di Dashboard invece della porta locale corrente su web, a differenza degli altri due flussi di registrazione)
- **Data apertura:** trovata durante l'audit "uniforma le email FitCoach" (2026-07-24)
- **Causa:** `mobile/src/lib/auth-service.ts`, `signUpClientWithCoachCode` (il percorso di registrazione cliente piu' usato dell'app, con codice coach) non passa `emailRedirectTo: getWebRedirectUrl('/')` nelle `options` di `supabase.auth.signUp`, a differenza di `signUpCoach` e `signUpClient` che lo fanno entrambi.
- **Perche' non e' stato corretto qui:** il task esplicito che ha trovato questo problema vietava di modificare il funzionamento dei link di conferma Supabase — aggiungere `emailRedirectTo` cambierebbe il comportamento reale del redirect post-conferma, non solo l'aspetto grafico delle email. Richiede una decisione/verifica dedicata (quale redirect e' desiderato per questo flusso) prima di un fix mirato.
- **Verifica:** nessuna, per costruzione (non toccato). Documentato anche in `docs/email-templates/README.md`.

## BUG-054 - Il trigger di immutabilita' `workout_plans_prevent_completed_edit` blocca la cancellazione account (nessun bypass service_role) — causa reale piu' probabile del fallimento `delete-account` gia' segnalato
- **Stato:** **CORRETTO** (2026-07-27, migration `20260802090000_allow_account_deletion_of_completed_workouts.sql`, applicata al DB reale e verificata end-to-end con account sintetici). Vedi "Fix applicato" in fondo a questa voce per i dettagli e per una correzione importante rispetto al fix originariamente suggerito qui sotto.
- **Severita:** alta (blocca in modo deterministico e permanente l'eliminazione account per QUALUNQUE utente — cliente o coach — con almeno una sessione di allenamento completata; il retry aggiunto al fix precedente di `delete-account` non aiuta perche' l'errore non e' transitorio, si ripete identico ad ogni tentativo)
- **Data apertura:** trovata il 2026-07-27, durante la pulizia di account sintetici di test creati per la verifica RLS del Blocco 1
- **Causa:** `public.prevent_completed_workout_plan_edit()` (trigger `workout_plans_prevent_completed_edit`, `before update or delete on public.workout_plans`, introdotto in `20260728090000_lock_completed_workout_structure.sql`) solleva `WORKOUT_LOCKED` per QUALUNQUE `UPDATE` o `DELETE` su una riga con `session_status='completed'`, **senza nessuna eccezione per `service_role`/superadmin** — a differenza di altri trigger dello stesso progetto (es. `prevent_client_onboarding_unsafe_changes`, che verifica esplicitamente `auth.role() = 'service_role' or is_superadmin()` prima di applicare i propri vincoli). Quando `auth.admin.deleteUser()` (chiamato da `supabase/functions/delete-account/index.ts`, service role) cancella un utente con almeno un `workout_plans` collegato (`coach_id` o `client_id`) e `session_status='completed'`, la cascade FK arriva a quella riga, il trigger la blocca, e l'intera `DELETE FROM auth.users` fallisce con un errore Postgres che GoTrue propaga senza un campo `message` riconoscibile — riproducendo esattamente il sintomo originale `{}` (vedi task precedente in questa stessa giornata, "Correggi il flusso di eliminazione account di FitCoach").
- **Prova raccolta:** (1) query di sola lettura sull'account REALE di Luigi (`b5890c51-5d9c-4897-a2b4-fe62762474f9`) conferma **11 righe `workout_plans` con `session_status='completed'`** collegate al suo id (coach_id o client_id) — condizione sufficiente per riprodurre il blocco. (2) Riprodotto live durante il cleanup dei sintetici di questo task: il coach di test Z aveva un cliente (W) con UNA sessione completata; `admin.deleteUser(Z)` e `admin.deleteUser(W)` sono entrambi falliti con `{"message":"{}"}` — identico, byte per byte, al sintomo segnalato da Luigi.
- **Perche' non e' stato corretto qui:** fuori scope per il task Blocco 1 (verifica RLS/RPC del sistema "Programmi automatici"): e' un cambiamento a un trigger di sicurezza/immutabilita' condiviso con tutto il progetto, non specifico del sistema "Programmi automatici" in verifica.
- **Fix suggerito (non applicato):** aggiungere alla `prevent_completed_workout_plan_edit()` lo stesso bypass gia' usato da `prevent_client_onboarding_unsafe_changes()` (`if auth.role() = 'service_role' or public.is_superadmin() then return ...; end if;` prima dei controlli), cosi' la cancellazione account (service role) e le azioni superadmin restano possibili senza indebolire la protezione per gli utenti normali (che resta piena: nessun coach/cliente autenticato puo' comunque modificare/cancellare una sessione completata).
- **Cleanup dei sintetici usati come prova:** il primo tentativo di rimuovere i 2 account di test rimasti bloccati (coach `f7641d10-...` / `rls-test-30696-z@example.invalid`, cliente `1bca3eaf-...` / `rls-test-30696-w@example.invalid`) tramite un bypass scoped alla sola transazione (`set local session_replication_role = replica`, mai un `ALTER TABLE ... DISABLE TRIGGER` globale) e' stato inizialmente bloccato dal classificatore di sicurezza; l'utente, consultato, ha poi approvato esplicitamente quella singola azione di cleanup (via `/permissions`). Con quell'autorizzazione, l'unica riga `workout_plans` completata di troppo e' stata rimossa in una transazione isolata (`BEGIN; SET LOCAL session_replication_role = replica; DELETE ... WHERE id = ... AND session_status = 'completed'; COMMIT;`), poi entrambi gli account sintetici sono stati eliminati regolarmente tramite l'Admin API. **Nessun residuo rimasto**: verificato con `select id, email from auth.users where email like 'rls-test-%'` → 0 righe.
- **Verifica (fase di scoperta):** causa confermata con prova diretta (conteggio sul dato reale + riproduzione live identica del sintomo, vedi sopra).

- **Fix applicato (2026-07-27):** il fix suggerito sopra (`auth.role() = 'service_role' or public.is_superadmin()`, stesso pattern di `prevent_client_onboarding_unsafe_changes`) **era sbagliato** e non e' stato quello effettivamente applicato. `auth.role()` legge `current_setting('request.jwt.claims', true)->>'role'`, valorizzato solo quando la richiesta passa da PostgREST con un JWT — ma `auth.admin.deleteUser()` non passa da PostgREST: GoTrue si connette a Postgres direttamente con il proprio ruolo dedicato `supabase_auth_admin` (proprietario dello schema `auth`, confermato via `select rolname from pg_roles`) ed esegue li' la cascade DELETE (`auth.users` → `profiles` on delete cascade → `workout_plans` on delete cascade → `workout_days` → `workout_day_exercises`). In quel contesto `request.jwt.claims` non e' impostato: `auth.role() = 'service_role'` sarebbe stato sempre falso e il fix suggerito non avrebbe risolto nulla.
  - **Fix corretto applicato:** `supabase/migrations/20260802090000_allow_account_deletion_of_completed_workouts.sql`. Aggiunta a tutti e 3 i trigger di immutabilita' (`prevent_completed_workout_plan_edit`, `prevent_completed_workout_day_write`, `prevent_completed_workout_day_exercise_write`) una condizione di bypass **volutamente minima**: `if tg_op = 'DELETE' and (current_user = 'supabase_auth_admin' or session_user = 'supabase_auth_admin') then return old; end if;` — attiva SOLO per DELETE, SOLO quando l'esecutore e' il ruolo di sistema di GoTrue. Nessuna eccezione aggiunta per `service_role` generico, `UPDATE`/`INSERT`, o superadmin: l'intento originale ("una scheda completed e' read-only per coach/client/superadmin via API, sempre") resta intatto.
  - **Verifica end-to-end (account sintetici, dominio dedicato `@bug054-test.invalid`, harness Edge Function temporanea con secret casuale, rimossa a fine test):** (1) creato coach+client sintetici, seminata una `workout_plans` con `session_status='completed'`, `admin.deleteUser(client)` → **riuscito** (`{"ok":true,"error":null}`), verificato con query di sola lettura che `workout_plans`/`profiles`/`auth.users` risultino a 0 righe residue per quell'id. (2) Test di non regressione: creato un secondo client sintetico, seminata una nuova scheda completed, tentato `UPDATE`/`DELETE` diretto su quella riga con `set local role service_role` (stesso ruolo usato da PostgREST per le chiamate service-role, diverso da `supabase_auth_admin`) → **bloccato in entrambi i casi** con lo stesso `WORKOUT_LOCKED` di sempre, confermando che il bypass non si estende oltre il caso specifico GoTrue. (3) Cleanup: eliminato il coach sintetico (cascade su client2 + relativa scheda completed tramite `coach_id`) → riuscito; `lookup_residual` → 0 account sintetici residui. Harness ed Edge Function rimosse (`supabase functions delete bug054-test-harness` + file locale), secret `BUG054_HARNESS_SECRET` rimosso dal progetto.
  - **Impatto sull'account reale di Luigi:** le 11 sessioni completate collegate al suo id non sono piu' un ostacolo alla cancellazione dell'account — un suo eventuale, futuro tentativo reale di "Elimina account" ora dovrebbe riuscire (non testato sul suo account reale, per costruzione: mai testare azioni distruttive su un account reale).

## BUG-055 - 3 dei 18 template `auto_eligible` contengono esercizi con attrezzatura/livello incoerenti col template stesso
- **Stato:** **CHIUSO** (2026-07-27) — corretto con la migration `20260807090000_fix_bug_055_auto_template_content.sql`. 18/18 template `auto_eligible` ora PASS. Dettaglio completo del fix, delle sostituzioni e dei test in `docs/BUG_055_TEMPLATE_FIX.md`.
- **Stato (storico, prima del fix):** aperto, non corretto (trovato durante il sotto-blocco 2.2 del Blocco 2 "Programmi automatici" — seed metadati esercizio — mentre si verificava la compatibilità attrezzatura/luogo/livello dei 18 template `auto_eligible` contro i metadati appena classificati; il problema è nei dati del Blocco 1, `workout_template_exercises`, non nel seed di questo sotto-blocco)
- **Severità:** media (non blocca il Blocco 1/il sistema oggi in produzione — nessuna RPC verifica ancora questa coerenza — ma un futuro motore di sostituzione/revisione che si fidasse del flag `auto_eligible` per questi 3 template potrebbe proporre in modo incoerente un esercizio a bilanciere in un contesto "corpo libero"/"manubri ed elastici", o un esercizio avanzato in un template "Principiante")
- **Data apertura:** 2026-07-27, sotto-blocco 2.2
- **Causa:** i template sono stati creati nel Blocco 1 (migration `20260724090000_workout_template_system_programs.sql`) prima che esistesse qualunque dato di attrezzatura/livello con cui verificarli automaticamente. 3 template contengono esercizi incoerenti col proprio nome/location/level dichiarati:
  - **"Corpo Libero"** (Casa, Principiante): include `gambe-squat` (squat con bilanciere, richiede `full_gym`) e `dorso-trazioni` (livello `advanced`).
  - **"Manubri ed Elastici"** (Casa, Intermedio): include `gambe-stacco-rumeno` (stacco rumeno con bilanciere, richiede `full_gym`).
  - **"Tecnica dei Fondamentali"** (Palestra, Principiante): include `gambe-stacco-rumeno` (livello `advanced`).
- **Perché non è stato corretto qui:** modificare `workout_template_exercises` è un cambiamento ai dati del Blocco 1 (contenuto dei template), fuori scope per un sotto-blocco esplicitamente limitato al seed dei metadati esercizio; inoltre la richiesta esplicita del task vieta di disattivare `auto_eligible` automaticamente senza una decisione dell'utente.
- **Verifica:** confermato con query diretta che confronta ogni esercizio dei 18 template contro `exercise_movement_metadata.compatible_locations`/`min_level` — 15/18 template PASS, questi 3 FAIL. Dettaglio completo in `docs/EXERCISE_METADATA_COVERAGE.md`.
- **Fix suggerito (storico, poi applicato):** sostituire `gambe-squat`→`gambe-affondi`/`glutei-squat-sumo` (bodyweight/home_basic) in "Corpo Libero"; sostituire `gambe-stacco-rumeno`→`femorali-hip-hinge`/`gambe-hip-thrust` in "Manubri ed Elastici"; sostituire `gambe-stacco-rumeno`→un esercizio di livello `beginner`/`intermediate` in "Tecnica dei Fondamentali"; oppure, se gli esercizi sono stati inclusi intenzionalmente (es. come "sfida" opzionale), documentarlo esplicitamente e lasciare invariato.
- **Fix effettivamente applicato:** vedi `docs/BUG_055_TEMPLATE_FIX.md` per la tabella prima/dopo completa, i criteri di sostituzione usati e i test (query dirette + account sintetici) che confermano 18/18 template PASS e zero impatto sui dati reali.
- **Riapertura (2026-07-27, stesso giorno):** 2 delle sostituzioni sopra sono risultate semanticamente incompatibili a una verifica più rigorosa (attrezzatura testuale + `substitution_group`) — corrette con la migration `20260808090000_fix_bug_055b_template_semantic_equipment.sql`. Vedi la sezione "Riapertura" in `docs/BUG_055_TEMPLATE_FIX.md` per la causa del falso PASS, le nuove sostituzioni e i test ripetuti. **BUG-055 resta CHIUSO** (il contenuto finale dei 3 template è corretto), ma la chiusura del 2026-07-27 (fix iniziale) descriveva un contenuto intermedio poi ulteriormente corretto lo stesso giorno.

## BUG-056 - `gambe-bulgarian-split-squat` (livello `advanced`) usato nel template "Manubri ed Elastici" (`Intermedio`)
- **Stato:** **CHIUSO** (2026-07-27) — l'utente ha fornito la regola definitiva di compatibilità livello (`beginner=1/intermediate=2/advanced=3`, `exercise.min_level <= template.level`) e corretto con la migration `20260809090000_fix_bug_056_level_hierarchy.sql`.
- **Severità:** bassa (non blocca nulla oggi; nessuna RPC si fida ancora del flag `auto_eligible` per proporre sostituzioni automatiche).
- **Data apertura:** 2026-07-27, durante la riapertura di BUG-055.
- **Causa:** la regola di compatibilità livello finora validata per i 18 template `auto_eligible` copriva solo "nessun esercizio `advanced` in un template `Principiante`" — non era mai stata definita/verificata una regola generale ordinale per `Intermedio`/`Avanzato`. "Manubri ed Elastici" (Casa, Intermedio), giorno "Lower B" posizione 2, includeva `gambe-bulgarian-split-squat` (`min_level='advanced'`, 3 > 2).
- **Fix applicato:** `gambe-bulgarian-split-squat` → `gambe-affondi` (stesso `substitution_group='lunge_bodyweight_quad'`, stesso `movement_pattern='lunge'`, stesso `primary_muscle_group='quadricipiti'`, stessa `is_unilateral=true`, `min_level='intermediate'` — non abbassato artificialmente nei metadati di `gambe-bulgarian-split-squat`, che resta `advanced` per chiunque altro lo usi; livello del template non alzato). Rivalidati i 18 template con il criterio operativo finora usato (nessun `advanced` in `Principiante` + location + completezza): **18/18 PASS confermato**. `plans_from_manubri_elastici=0`, `cycles_real=1`, `reviews_real=0` — invariati, nessuna scheda reale toccata.
- **Verifica:** confermato con query diretta su `workout_template_exercises`/`exercise_movement_metadata` per il template `6376a9e9-6add-4b77-b85d-00e97b7bd725`.
- **Nota importante — nuova regola ordinale non ancora applicata retroattivamente al resto del catalogo**: applicando la regola definitiva (`exercise.min_level <= template.level`, letteralmente, a tutti e 18 i template) emergono **12 template aggiuntivi** con esercizi `advanced` sopra il proprio livello dichiarato (mai scoperti prima perché la regola operativa usata durante 2.2/BUG-055/BUG-055b era più permissiva). Vedi **BUG-057** per il dettaglio — scoperta esplicitamente fuori scope per BUG-056 (che riguardava solo la riga segnalata), non corretta qui.

## BUG-057 - La regola ordinale definitiva dei livelli (`exercise.min_level <= template.level`), applicata a tutti i 18 template, trova 12 template con esercizi `advanced` sopra il proprio livello dichiarato
- **Stato:** **CORRETTO** (2026-07-28, migration `20260814110000_fix_bug_057_template_level_hierarchy.sql`). L'utente ha fornito una decisione esplicita ("correggere tutti i template, stessa metodologia di BUG-055/056"). **18/18 template PASS confermato** con la regola letterale.
- **Discrepanza 12→11 trovata alla riverifica**: applicando la query letterale al momento della correzione, i template realmente FAIL erano **11, non 12** — "Corpo Libero" (Casa, Principiante) risultava PASS quando BUG-057 fu aperto (2026-07-27) perché la validazione di BUG-055b usava una regola più permissiva ("nessun `advanced` in un template Principiante", non l'ordinale completo); con la regola letterale odierna, "Corpo Libero" FALLISCE anch'esso (2 esercizi `intermediate`: `gambe-affondi`, `tricipiti-dip-tricipiti`, entrambi > livello 1 del template). Nessun template diverso dai 10 già elencati + "Corpo Libero" è risultato in violazione — la lista completa e verificata è: Corpo Libero (2), Dimagrimento Principiante (1), Forza Base 5x5 (4), Forza e Resistenza (1), Full Body Metabolico (2), Inizio Palestra 3 giorni (4), Pesi e Cardio (1), Strength & Shape (1), Tecnica dei Fondamentali (3), Upper/Lower + Conditioning (2), Upper/Lower Ipertrofia (5) — 11 template, 26 righe totali.
- **Fix applicato**: nessun livello esercizio abbassato, nessun livello template alzato, nessuna scheda cliente già assegnata toccata (verificato: zero FK tra `workout_days`/`workout_day_exercises` e `workout_template_*`, stessa garanzia di BUG-055). Ogni sostituzione riusa un esercizio esistente nello stesso `substitution_group` quando disponibile a un livello idoneo (es. `gambe-stacco-rumeno`→`gambe-stacco-rumeno-manubri` in 7 template Intermedio), altrimenti nello stesso ruolo/gruppo muscolare/pattern di movimento. **2 nuovi esercizi creati** solo dove nessuna alternativa esistente soddisfaceva livello+attrezzatura+ruolo contemporaneamente: `spalle-shoulder-press-macchina` (nessuna spinta verticale spalle a ruolo `primary` esisteva sotto `intermediate`) e `tricipiti-dip-ginocchia-piegate` (nessuna alternativa tricipiti offriva un'opzione senza manubrio per "Corpo Libero"). Verificati anche eventuali conflitti di duplicato esercizio nello stesso giorno del piano (1 trovato e corretto: "Upper/Lower + Conditioning"/"Upper B" già conteneva `cardio-battle-rope` altrove nello stesso giorno, usato `cardio-salto-corda` al suo posto per l'occorrenza di `cardio-burpees` in quel giorno specifico).
- **Verifica**: 18/18 template PASS confermato con query diretta dopo l'apply. Nessuna scheda cliente reale toccata (0 FK `workout_days`/`workout_day_exercises`→`workout_template_*`, confermato via `information_schema`).
- **Stato storico (prima del fix, 2026-07-27):** aperto, non corretto — scoperta collaterale durante la chiusura di BUG-056, esplicitamente fuori scope per quella correzione (che riguardava una singola riga segnalata dall'utente).
- **Severità:** bassa oggi (nessuna RPC si fida ancora del flag `auto_eligible`/livello per proporre sostituzioni automatiche o filtrare contenuto per il cliente); diventerà rilevante non appena un motore che usa `min_level` per filtrare/sostituire contenuto (sotto-blocco 2.3) userà questa regola sul contenuto esistente degli altri template.
- **Data apertura:** 2026-07-27, durante la chiusura di BUG-056.
- **Causa:** fino a BUG-056, la regola di compatibilità livello usata per validare i 18 template `auto_eligible` era "nessun esercizio `advanced` in un template `Principiante`" — non un confronto ordinale generale. La regola definitiva fornita dall'utente per BUG-056 (`beginner=1/intermediate=2/advanced=3`, `exercise.min_level <= template.level`) è più stretta: un template `Intermedio` non può contenere alcun esercizio `advanced` (non solo `Principiante`). Applicata a tutti e 18 i template `auto_eligible`, risultano **12 FAIL** (di cui 12 diversi da "Manubri ed Elastici", già corretto in BUG-056): `Dimagrimento Principiante` (1), `Forza Base 5x5` (4), `Forza e Resistenza` (1), `Full Body Metabolico` (2), `Inizio Palestra (3 giorni)` (4), `Pesi e Cardio` (1), `Strength & Shape` (1), `Tecnica dei Fondamentali` (3 — righe diverse da quelle già corrette in BUG-055/BUG-055b), `Upper/Lower + Conditioning` (2), `Upper/Lower Ipertrofia` (5). (`Corpo Libero`/`Manubri ed Elastici` risultano PASS dopo i fix già applicati.)
- **Perché non è stato corretto qui:** BUG-056 riguardava esplicitamente e soltanto la riga segnalata dall'utente (`gambe-bulgarian-split-squat` in "Manubri ed Elastici"); estendere la correzione a 12 template diversi, mai menzionati, sarebbe una modifica non richiesta di scala enorme (decine di righe in template mai segnalati come problematici).
- **Verifica:** query diretta che applica la formula ordinale (`exercise.min_level` mappato a 1/2/3, confrontato con `template.level` mappato a 1/2/3) a tutti i 18 template `auto_eligible`.
- **Fix suggerito (non applicato):** decisione esplicita dell'utente su come procedere — correggere tutti i 12 template con la stessa metodologia di BUG-055/BUG-056 (sostituzione con stesso `substitution_group`/gruppo muscolare/categoria/unilateralità), oppure accettare esplicitamente che alcuni template `Intermedio`/`Avanzato` includano deliberatamente esercizi di livello superiore come stretch/accessori (in tal caso la regola andrebbe raffinata, non applicata letteralmente a tutti). Nel frattempo, il sotto-blocco 2.3 userà comunque la regola ordinale corretta per le proprie decisioni (nuove sostituzioni), ma non retroagisce sul contenuto esistente di questi 12 template.

## BUG-058 - `ProgramCycleStatus` mobile e la query di `getMyActiveProgramCycle()` mai aggiornate dopo l'estensione degli stati del ciclo (2.1) — la card "Il tuo programma automatico" non mostrava nulla per qualunque ciclo non letteralmente `active`
- **Stato:** **CORRETTO** (2026-07-28, sotto-blocco 2.6, nessuna migration necessaria — solo mobile).
- **Severità:** alta (in produzione dal 2026-07-27, giorno di rilascio della migration 2.1): qualunque cliente self-guided con ciclo in `checkin_due`/`review_pending`/`pending_subscription`/`paused_subscription`/`pending_safety_review`/`pending_template` non vedeva ALCUNA card, nessun invito al check-in, nessun avviso di blocco — la card semplicemente scompariva, indistinguibile da "nessun ciclo attivo".
- **Data apertura:** trovata il 2026-07-28, durante l'implementazione del sotto-blocco 2.6 (schermate mobile check-in/review), non segnalata dall'utente.
- **Causa:** la migration `20260804090000_block2_cycle_review_schema.sql` (2.1, 2026-07-27) ha esteso `client_program_cycles.status` da un piccolo set pre-2.1 a 11 valori reali (`draft/active/checkin_due/review_pending/pending_subscription/paused_subscription/pending_safety_review/pending_template/completed/replaced/cancelled`). Il tipo TypeScript `ProgramCycleStatus` (`mobile/src/types/client-fitness-profile.ts`) e la query in `mobile/src/lib/auto-program-service.ts` (`getMyActiveProgramCycle()`, filtro `.in('status', ['active','pending_review'])` — quest'ultimo valore non è mai esistito nel CHECK reale) non sono mai stati aggiornati insieme allo schema: qualunque ciclo con uno degli 8 stati non-terminali diversi da `active` veniva escluso dalla query stessa, prima ancora di arrivare al componente.
- **Verifica:** trovato per lettura/confronto diretto tra il CHECK constraint reale (verificato via query su `pg_constraint`) e il tipo TS + la query mobile; non serviva riprodurlo a runtime per essere certo (i due elenchi erano visibilmente diversi).
- **Fix applicato:** `ProgramCycleStatus` aggiornato all'unione reale a 11 valori; `getMyActiveProgramCycle()` ora filtra su un nuovo `OPEN_CYCLE_STATUSES` (le 8 stesse categorie non-terminali del backend, `_cycle_open_statuses()`), ordinato per `cycle_number` (non più `created_at`, più affidabile per individuare il ciclo corrente). `auto-program-card.tsx` riscritta con un ramo esplicito per ciascuno degli 8 stati (vedi sotto-blocco 2.6 in `docs/WORKLOG.md`).
- **Verifica del fix:** `tsc --noEmit` pulito; bundle Expo Web verificato con successo. Nessun clic reale in browser eseguito (nessun tool di automazione disponibile in questo ambiente) — verifica visiva dei singoli stati non fatta, solo la query/type-check.

## BUG-059 - `run_cycle_review`/`submit_monthly_checkin` (codice 2.3, già "chiuso" e committato prima di questa sessione) confondevano un blocco temporaneo/retriable con una decisione definitiva sul programma, in 5 punti distinti — scoperti SOLO dalla campagna di test 2.7 con token REST reali e chiamate concorrenti reali
- **Stato:** **CORRETTO** (2026-07-28, sotto-blocco 2.7), 6 migration mirate, ciascuna riverificata dal vivo dopo l'apply.
- **Severità:** alta — insieme, questi 5 punti rendevano permanentemente irraggiungibile il prosieguo automatico del programma per: (a) qualunque cliente che avesse ricevuto due chiamate ravvicinate a `run_cycle_review` con dati insufficienti (doppio tap, due device, retry dopo timeout); (b) **qualunque cliente il cui abbonamento scadesse e venisse poi rinnovato** — il ciclo restava bloccato in `paused_subscription` per sempre, nessuna funzione lo riportava mai attivo nonostante l'abbonamento tornasse valido; (c) qualunque cliente il cui blocco di sicurezza (`pending_safety_review`) venisse risolto da un Superadmin con l'azione "approve_continue" — stesso blocco permanente al giro di revisione naturale successivo.
- **Data apertura:** 2026-07-28, durante la campagna di test end-to-end del sotto-blocco 2.7 (mai trovati nelle sessioni precedenti perché mai testati con una VERA seconda chiamata concorrente né con un vero ciclo scadenza→rinnovo→nuovo check-in — gli scenari 2.3 originali testavano `blocked_subscription` come stato singolo, mai il rientro).
- **Causa (5 punti distinti, in ordine di scoperta):**
  1. **STEP 10 di `run_cycle_review` (`insufficient_data`) senza la guardia "se esiste già una review per lo stesso check-in, restituiscila" presente invece per `blocked_safety`/`pending_template`**: due chiamate REST concorrenti reali sullo stesso ciclo/check-in creavano due righe `client_cycle_reviews` duplicate con decisione `insufficient_data` (l'indice unico parziale la esclude esplicitamente, per permettere un vero retry dopo nuove sessioni — ma non deduplicava due chiamate sullo stesso check-in). Fix: `20260814090000_fix_2_7_run_cycle_review_insufficient_data_duplicate.sql`.
  2. **Il ripristino di `paused_subscription` introdotto per risolvere (b) sotto impostava `v_cycle.status := 'active'` solo in memoria**, e la chiamata proseguiva nella stessa transazione fino a un `raise exception 'CHECKIN_REQUIRED'` che annullava anche quell'UPDATE. Fix: `20260814094500_fix_2_7_subscription_resume_commit.sql` (il ripristino ora fa l'UPDATE e ritorna subito, `result_code='resumed'`, transazione committata pulita).
  3. **`v_cycle.status <> any(array[...])` invece di `<> all(array[...])`** nello STEP 4 di `run_cycle_review` — stessa classe di bug già corretta in `superadmin_force_cycle_decision` (2.5), qui mai scoperta perché `pending_safety_review`/`pending_template` si risolvono con RPC Superadmin dedicate, non richiamando `run_cycle_review` una seconda volta. `x <> any(array[a,b,c])` è una tautologia sempre vera per un array di 2+ valori — lo short-circuit "già recensito" scattava sempre, indipendentemente dallo stato reale. Fix: `20260814093000_fix_2_7_run_cycle_review_any_all.sql`.
  4. **`submit_monthly_checkin` considerava qualunque review `decision <> 'insufficient_data'` come "decisione definitiva"**, incluse le vecchie righe `blocked_subscription`/`blocked_safety` di un blocco già risolto — un cliente appena rientrato da `paused_subscription`/`pending_safety_review` non poteva inviare il nuovo check-in richiesto per completare la ripresa (`ALREADY_REVIEWED`). Fix: `20260814100000_fix_2_7_submit_checkin_after_subscription_resume.sql` (esclude `blocked_subscription`) + `20260814103000_fix_2_7_submit_checkin_after_safety_resume.sql` (esclude anche `blocked_safety`, trovato ripetendo lo stesso test per il percorso sicurezza).
  5. **La query generale di idempotenza dello STEP 4 di `run_cycle_review`** (quella per i cicli NON in stato terminale) aveva lo stesso problema del punto 4: una vecchia riga `blocked_subscription`/`blocked_safety` veniva trovata come "ultima review non-insufficient_data" e restituita per sempre come esito cache, anche dopo un rientro legittimo e un nuovo check-in. Fix: `20260814101500_fix_2_7_run_cycle_review_step4_blocked_subscription.sql` + `20260814104500_fix_2_7_run_cycle_review_step4_blocked_safety.sql`.
- **Verifica:** ogni fix riprodotto dal vivo PRIMA della correzione (errore/comportamento reale osservato con account sintetici + token REST/JWT reali via login password), poi riverificato PASS dopo l'apply (retry concorrente → 1 sola riga review; scadenza→rinnovo→check-in→review completo → `insufficient_data` fresco, non il cache `blocked_subscription`; blocco sicurezza→`approve_continue`→scadenza naturale→check-in→review → decisione fresca, non il cache `blocked_safety`). In ogni caso: nessun ciclo duplicato, nessuna notifica duplicata, storico review completo e coerente (es. per il client di test abbonamento: `blocked_subscription` + `insufficient_data`, 1 ciclo totale).
- **Non regressione:** i path già testati nel 2.3 (progress/regress/partial_change/maintain/blocked_safety iniziale/blocked_subscription iniziale/manual_review/cross-client/anon/grant) non toccati da queste 6 migration (solo le select di idempotenza sono cambiate, mai la logica di calcolo decisione).

## BUG-060 - `client-monthly-checkin-store.ts` (sotto-blocco 2.6) senza `reset()`, mai incluso nell'azzeramento degli store al logout
- **Stato:** **CORRETTO** (2026-07-28, sotto-blocco Blocco 3 mobile — trovato mentre si replicava il pattern di reset per il nuovo `app-notification-store`).
- **Severità:** bassa-media — stesso principio di BUG-049 (store account-specifici mai azzerati al logout): le bozze di check-in mensile restano in `AsyncStorage` dopo il logout. Rischio pratico limitato (le bozze sono chiavate per `cycleId`, non per `clientId`: un secondo account sullo stesso device non vedrebbe MAI le bozze del primo, perché i suoi `cycleId` sono sempre diversi — nessuna fuga di dati leggibile), ma viola comunque la regola generale "azzera sempre gli store account-specific al logout" e fa crescere indefinitamente lo storage locale su un device condiviso/di test con molti cambi di account.
- **Data apertura:** 2026-07-28, durante l'implementazione del mobile del Blocco 3.
- **Causa:** `client-monthly-checkin-store.ts`, creato nel sotto-blocco 2.6, non è mai stato aggiunto a `resetUserScopedStores()` in `auth-store.ts` (la funzione centralizzata introdotta dal fix di BUG-049) — omissione, non un difetto del meccanismo di BUG-049 stesso, che per il resto continua a funzionare per tutti gli altri store.
- **Fix applicato:** aggiunto un metodo `reset()` allo store (mancava anche quello) e registrata la chiamata in `resetUserScopedStores()`, insieme al nuovo `app-notification-store` (mai persistito, ma incluso per coerenza/pulizia stato).
- **Verifica:** `tsc --noEmit` pulito, bundle Expo Web compilato con successo.

## BUG-061 - Il webhook RevenueCat non gestisce alcun evento di revoca immediata (REFUND)
- **Stato:** **CORRETTO E DEPLOYATO** (2026-07-28, verifica finale pre-release).
- **Severità:** bassa oggi (nessun cliente reale ha ancora subito un refund/chargeback secondo i dati verificati), ma rilevante per la correttezza futura di `effective_active_days` e in generale dell'accesso Client Pro.
- **Data apertura:** 2026-07-28, durante l'implementazione di `effective_active_days`, leggendo `supabase/functions/revenuecat-webhook/index.ts` per capire come sincronizzare il ledger senza creare una seconda fonte di verità.
- **Causa:** `VALID_TYPES` includeva `REFUND_REVERSED` (la riammissione dopo che una revoca è stata annullata) ma non `REFUND` (la revoca immediata stessa) — verificato con la documentazione ufficiale RevenueCat (via context7) che `REFUND` è un tipo di evento realmente distinto ed esistente (payload: `type`, `app_user_id`, `refund_time`, `transaction_id`, `product_id` — nessun campo di scadenza).
- **Fix applicato:** `supabase/functions/revenuecat-webhook/index.ts` — aggiunto `'REFUND'` a `VALID_TYPES`; nuovo case in `statusForEvent('REFUND', ...)` che ritorna sempre `'refunded'` (nuovo valore, migration `20260815090000_fix_bug_061_refund_status.sql`, estensione additiva del CHECK `user_subscriptions_status_check`, nessuna riga reale toccata), **indipendentemente da `expiresAt`/`validNow`** — a differenza di `CANCELLATION` (che rispetta l'accesso fino alla scadenza già pagata), un rimborso restituisce il denaro: non c'è alcun periodo "già pagato" da onorare. `REFUND` **deliberatamente esclusa** da `STALE_CHECK_EVENT_TYPES` (stessa classe di rischio già documentata per `CANCELLATION`/`EXPIRATION`: se `purchased_at_ms` riflettesse la data d'acquisto originale invece della data dell'evento, un rimborso reale arrivato dopo uno o più rinnovi verrebbe scartato come "obsoleto" — esattamente lo scenario "rimborso dopo rinnovo" richiesto — lasciando un cliente rimborsato con l'accesso ancora attivo). Nessuna modifica alla gestione di duplicati/idempotenza (già strutturale, tabella `revenuecat_webhook_events` con `UNIQUE(event_id)`, invariata e verificata applicarsi identicamente a `REFUND`).
- **Verifica**: 6 nuovi test Deno aggiunti a `index.test.ts` (mai eseguiti in questo ambiente: **nessun binario Deno disponibile**, confermato — `which deno` non trovato, nessuna installazione rilevata — limite pre-esistente già documentato nello stesso file da una sessione precedente, non introdotto qui) — scritti seguendo esattamente lo stesso pattern/stile dei test già presenti per `CANCELLATION`/`EXPIRATION`, saranno eseguibili non appena Deno sarà disponibile. **Verificato dal vivo a livello database** con un client sintetico (mai un abbonamento reale toccato): simulata la scrittura esatta che il webhook corretto produce per un evento `REFUND` sulla stessa `external_subscription_id` appena rinnovata (scenario "rimborso dopo rinnovo") → `status='refunded'` confermato, `_has_active_client_pro_entitlement` → `false` confermato, `log_self_guided_exercise_progress` → bloccato con `SUBSCRIPTION_REQUIRED` confermato, `client_program_cycle_active_periods` → intervallo aperto chiuso automaticamente dal trigger già esistente (integrazione con `effective_active_days` confermata), schede/piani/esercizi (`workout_plans`/`workout_day_exercises`) → 3 piani/12 esercizi rimasti intatti, nessuna cancellazione. Testato anche: nuovo abbonamento dopo il rimborso → entitlement ripristinato (`true`), riga rimborsata mai toccata/reintegrata. Idempotenza (evento duplicato) verificata a livello di vincolo strutturale (`UNIQUE(event_id)` su `revenuecat_webhook_events`, invariato): un secondo insert con lo stesso `event_id` rifiutato con `unique_violation`, confermando la garanzia sottostante al controllo `processed` del codice reale.
- **Smoke test HTTP reale (2026-07-28, colma il limite precedente) — PASS**: chiamata HTTP reale (non simulata) alla Edge Function `revenuecat-webhook` **già deployata in produzione**, con l'header/secret realmente configurato in RevenueCat (fornito dall'utente per questo test, mai scritto su disco). Sequenza eseguita con un secondo account sintetico dedicato (`@example.invalid`, mai un account/abbonamento reale):
  1. Seminato profilo cliente + `user_subscriptions` `status='active'`, `external_subscription_id='synt-http-refund-block5-001'`, pacchetto Client Pro Trimestrale reale, `expires_at` futuro (+90gg).
  2. Verificato pre-condizione: `_has_active_client_pro_entitlement` → `true`.
  3. `POST` reale a `https://rkcecnzvzoigipjliwdk.supabase.co/functions/v1/revenuecat-webhook` con payload `{ event: { type: 'REFUND', id: <event_id univoco>, app_user_id, product_id, entitlement_id: 'client_pro', transaction_id/original_transaction_id: 'synt-http-refund-block5-001', purchased_at_ms } }` e header `Authorization: Bearer <secret RevenueCat reale>` → **risposta HTTP 200**, body `{"ok":true,"data":{"processed":true,"eventId":..., "status":"refunded"}}`.
  4. Verificato a DB: `user_subscriptions.status` → `'refunded'` (era `'active'`), `_has_active_client_pro_entitlement` → `false`.
  5. Verificato blocco reale: `log_self_guided_exercise_progress` chiamata come utente autenticato (JWT reale, non service_role) → rifiutata con `SUBSCRIPTION_REQUIRED: nessun piano Client Pro attivo` (HTTP 400/P0001).
  6. Verificato storico conservato: riga in `revenuecat_webhook_events` presente con `processed=true`, `processing_error=null`, `event_type='REFUND'`.
  7. **Idempotenza**: reinviato lo **stesso** `event_id` → risposta HTTP 200, body `{"ok":true,"data":{"duplicate":true,"eventId":...}}`, nessuna scrittura aggiuntiva (`updated_at` di `user_subscriptions` invariato rispetto al passo 4).
  8. **Cleanup**: eliminata la riga `revenuecat_webhook_events`, la riga `user_subscriptions`, il `profile` e l'utente Auth sintetico via Admin API — residuo verificato zero (query su `profiles` con `email like '%@example.invalid'` → 0 righe). Nessun account/abbonamento reale toccato in nessun momento.
  La funzione deployata gestisce quindi correttamente REFUND end-to-end nella realtà, non solo a livello di codice/DB simulato.
- **Cleanup (verifica precedente a livello DB)**: account sintetico + riga di test in `revenuecat_webhook_events` di quella verifica rimossi, residuo zero verificato (invariato da prima).

## BUG-062 - Il cliente non veniva mai avvisato per il blocco di sicurezza (`pending_safety_review`) né per l'assegnazione di un coach
- **Stato:** **CORRETTO** (2026-07-28, migration `20260814130000_block3_notify_safety_and_coach.sql`).
- **Severità:** media — non un blocco funzionale (il cliente vede comunque lo stato bloccato nella card "Il tuo programma automatico", sotto-blocco 2.6), ma un'esperienza silenziosa: nessuna notifica proattiva per due eventi che la richiesta esplicita del Blocco 3 elenca tra le categorie cliente ("sicurezza", "assegnazione coach").
- **Data apertura:** 2026-07-28, trovata implementando il centro notifiche del Blocco 3 (mai un bug segnalato dall'utente — emerso confrontando sistematicamente ogni notifica client-facing richiesta con quelle realmente scritte da `run_cycle_review`/`_handle_client_coach_assignment`).
- **Causa:** (1) `run_cycle_review` STEP 8 (e il ramo di rientro `pending_safety_review`) inseriva la notifica `review_blocked_safety` solo per `recipient_role='superadmin'`, mai per il cliente. (2) `_handle_client_coach_assignment` (trigger del sotto-blocco 2.4) scriveva solo la riga di audit `client_cycle_reviews` (`manual_review`), nessuna riga in `app_notifications` per il cliente.
- **Fix applicato:** notifica gemella `review_blocked_safety_client` per il cliente in entrambi i punti di STEP 8/rientro (stesso `dedup_key` in entrambi, mai due notifiche per lo stesso episodio); notifica `auto_program_coach_assigned` per il cliente nel trigger di coach handoff.
- **Verifica:** testato dal vivo con account sintetici — check-in con segnalazione dolore → `run_cycle_review` → notifica cliente `review_blocked_safety_client` confermata; assegnazione coach sintetico a un cliente con ciclo in `pending_safety_review` → notifica cliente `auto_program_coach_assigned` confermata, ciclo correttamente chiuso a `replaced`.

## BUG-063 - `import_ymove_bulk.py` contiene la API key YMove e la chiave anon Supabase in chiaro, hardcoded
- **Stato:** **CORRETTO** (2026-08-01, commit `11bdc1f`) — decisione lasciata esplicitamente dall'utente a questa sessione ("risolvi tu stesso").
- **Severità:** alta — `YMOVE_API_KEY` è esattamente la credenziale che il progetto ha sempre trattato come "mai esposta" (requisito esplicito dell'utente all'inizio della fase YMove); se il file venisse committato, la chiave resterebbe recuperabile dalla cronologia git anche dopo una rimozione successiva.
- **Data apertura:** 2026-08-01, trovato durante la revisione dei file pendenti prima del commit richiesto esplicitamente ("procedi con tutto").
- **Causa:** script di importazione bulk (righe 7-9) scritto con le credenziali reali incorporate direttamente nel sorgente invece che lette da variabili d'ambiente — di provenienza esterna a questa sessione (mai scritto qui), root del repository (non in `scripts/`).
- **Rischio aggiuntivo, non solo il segreto:** lo script scrive `exercises`/`exercise_external_links` via REST diretto con la chiave anon (`match_status: "auto_imported"`), bypassando interamente la pipeline sicura (`ymove-library-import`, RPC `apply_ymove_safe_create_batch`, verifica contraddizioni/duplicati, revisione umana) costruita apposta in questa sessione — se rieseguito produrrebbe righe fuori da qualunque controllo di qualità/duplicati già in vigore.
- **Fix applicato:** file originale (mai committato) eliminato; ricreato come `scripts/import-ymove-bulk.py` con `YMOVE_API_KEY`/`SUPABASE_ANON_KEY` lette da variabili d'ambiente (stesso pattern già in uso in `scripts/sync-ymove-exercise-catalog.mjs`, che le rifiuta esplicitamente se hardcoded/pubbliche), spostato nella cartella corretta insieme agli altri script YMove, con docstring che segnala: già eseguito con successo, bypassa la pipeline sicura `ymove-library-import`, da non rieseguire senza passare da lì.
- **Verifica:** confermato che il file originale non era mai stato aggiunto/committato in nessun commit di questa sessione (`git status` lo mostrava sempre come non tracciato); grep sull'intero repository per la chiave esatta → nessun risultato, la credenziale non è mai entrata nella cronologia git. **Non fatto, resta da fare dall'utente**: ruotare comunque la chiave YMove per precauzione (è stata in un file in chiaro su disco, anche se mai su git) — non è un'azione che può essere eseguita da questa sessione (richiede accesso al pannello YMove).

## BUG-064 - Flash UUID nelle card esercizio della Scheda modello (`schede/modello/[templateId].tsx`)
- **Stato:** **CORRETTO** (2026-08-01) — codice modificato, non ancora committato né deployato (nessun `git push`/`db push` richiesto per questo intervento).
- **Severità:** alta — UX rotta visibile su ogni apertura di una Scheda modello con esercizi non presenti nei 44 storici locali (in particolare gli 8 modelli "Allenamenti YMove" appena introdotti): l'utente vede per un istante l'UUID grezzo al posto del nome esercizio.
- **Data apertura:** segnalato dall'utente il 2026-08-01, verificato nel browser dall'utente stesso (non da questo agente — nessun tool di automazione browser disponibile in questa sessione).
- **Causa:** `useExerciseResolver.resolve(id)` (`mobile/src/hooks/use-exercise-resolver.ts`) risolve un solo esercizio alla volta: per gli id non presenti nel catalogo locale sincrono (`data/exercise-library.ts`, 44 esercizi storici) ritorna `undefined` al primo render e avvia in background una query Supabase singola (`getFitCoachExerciseById`, una per ciascun id distinto, cache solo `useState` locale al componente — persa ad ogni remount). `schede/modello/[templateId].tsx` usava `const exerciseName = exercise?.name ?? te.exerciseId;`: finché il fetch asincrono non completava, il fallback mostrava direttamente l'UUID/chiave grezza.
- **Fix applicato:** nuova funzione batch `getFitCoachExercisesByIds` (`mobile/src/lib/fitcoach-exercises-service.ts`) che risolve tutti gli id Supabase mancanti con UNA query `.in('id', ids)` (+ una seconda query batch per `exercise_text_overrides`, RLS-scoped come già faceva la versione singola), filtrando prima gli id non a forma UUID (la colonna `exercises.id` è `uuid`: una chiave legacy testuale nel mix avrebbe fatto fallire l'intera query batch). Nuova cache di modulo `mobile/src/lib/exercise-metadata-cache.ts` (mai persistita su disco, mai URL video YMove) condivisa tra mount/unmount della stessa sessione app. `schede/modello/[templateId].tsx`: appena il template carica, raccoglie tutti gli `exercise_id` univoci dei giorni e lancia una sola `ensureExerciseMetadataLoaded(ids)`; ogni riga esercizio ora sceglie tra tre stati — card reale (dato risolto, anche da cache/local sync), skeleton (stessa geometria della card, mentre il batch è ancora in corso), "Esercizio non disponibile" (batch completato ma id irrisolvibile — mai più l'UUID, id loggato solo in `__DEV__`). Rimosso l'uso di `useExerciseResolver` da questo file (lasciato invariato per gli altri usi, fuori scope).
- **Verifica:** `tsc --noEmit` pulito, `expo export --platform web` completato con successo (incluso il bundle `/schede/modello/[templateId]`), `git diff --check` pulito. **Nessuna verifica browser reale eseguita da questo agente** (nessun tool disponibile) — verifica solo per lettura/ragionamento sul codice sorgente; da confermare visivamente nel browser dall'utente sui due percorsi segnalati (Allenamenti YMove → Dimagrimento → Full Body Metabolico YMove; → Massa muscolare → Upper/Lower YMove) e sul giro Scheda modello → dettaglio esercizio → indietro.
- **Trovato ma non corretto (fuori scope, stesso pattern):** lo stesso fallback `exercise?.name ?? id` esiste anche in `mobile/src/components/client-load-history.tsx` (righe 96 e 353) e in `mobile/src/app/schede/modelli/[id].tsx` (riga 175, schermata diversa basata su template locali statici) — non toccati in questo intervento, segnalati per una eventuale correzione futura mirata.
- **Correzione post-verifica (2026-08-01, gate "VERIFICA FINALE FIX UUID FLASH"):** (1) il conteggio "worst case prima" era impreciso — `getFitCoachExerciseById` eseguiva 2 query per id non cachato (1 su `exercises` + 1 su `exercise_text_overrides`), quindi il worst case reale era **fino a 2N query**, non N; dopo il fix restano sempre e solo **2 query totali** (1 batch su `exercises` + 1 batch su `exercise_text_overrides`), indipendentemente da N. (2) Trovato un gap reale in `ensureExerciseMetadataLoaded`: un errore di query (rete/DB) non veniva distinto da "esercizio inesistente" — un fallimento temporaneo avrebbe fatto apparire in modo permanente "Esercizio non disponibile" per tutta la durata del mount, senza possibilità di retry se non uscendo e rientrando dalla schermata. Corretto: la funzione ora ritorna uno stato esplicito `'ok' | 'error'` (mai una scrittura di cache parziale/di comodo su errore); `[templateId].tsx` traccia `metadataStatus: 'loading' | 'ok' | 'error'` e mostra una riga distinta "Errore di caricamento" con pulsante "Riprova" (mai confusa con "Esercizio non disponibile", riservata solo a un batch riuscito che semplicemente non trova quell'id). (3) Verificato che tutte le chiavi legacy (esercizi storici locali, es. `petto-panca-piana`) sono id non-UUID risolti SEMPRE da `getExerciseById` (sincrono, locale) PRIMA che `ensureExerciseMetadataLoaded` le consideri "mancanti" — il filtro UUID introdotto per la query batch Supabase non le tocca mai, nessuna regressione per template professionali/personali/storici. (4) Grep mirato su `[templateId].tsx` e `exercise-thumbnail.tsx`: nessun uso di `exerciseId`/`exercise_id`/id grezzo come testo visibile in UI (solo come `key` React, parametro di route, o log `__DEV__`). (5) Confermato che il fix non chiama mai `ymove-service.ts`/l'API YMove/`YMOVE_API_KEY` — solo query dirette a `exercises`/`exercise_text_overrides` via Supabase.

## BUG-065 - `ExerciseThumbnail`/`YMoveResultThumbnail` chiamavano l'API YMove (consumo cap mensile) al semplice render di una card, non solo su azione esplicita
- **Stato:** **CORRETTO** (2026-08-02) — codice modificato, non ancora committato né deployato.
- **Severità:** alta — consumo non necessario del limite mensile esercizi YMove (finito) su ogni schermata con card esercizio collegate a YMove: Scheda modello (inclusi gli 8 modelli "Allenamenti YMove", fino a 22 esercizi per schermata), liste workout, editor scheda, catalogo esercizi, tab "Cerca live" e "Archivio" del picker YMove.
- **Data apertura:** trovato durante un audit esplicito richiesto dall'utente su `includeVideos=false`/protezione cap mensile (2026-08-02, solo analisi in un primo turno, poi fix su richiesta esplicita "procedi con la correzione priorità alta della thumbnail").
- **Causa:** `mobile/src/components/exercise-thumbnail.tsx` (`ExerciseThumbnail`, usata in 9 file) e `mobile/src/components/ymove-preview-shared.tsx` (`YMoveResultThumbnail`, usata da "Cerca live" e "Archivio") chiamavano automaticamente, al mount di ogni card, `getYmoveExerciseDetail`/`GET /exercises/{id}?includeVideos=true` (azione `detail` di `ymove-exercises`, l'endpoint che conta legittimamente nel cap mensile) — SOLO per ottenere `thumbnailUrl`, perché la risposta LIST/search in browse mode (`includeVideos=false`, già corretta altrove) non la include. Mitigato solo da una cache di sessione + limite di concorrenza 2 (`mobile/src/lib/ymove-thumbnail-cache.ts`), mai azzerato: ogni esercizio YMove distinto visto per la prima volta in sessione generava comunque una chiamata. Confermato che il fix UUID flash (BUG-064) non ha introdotto questo comportamento (preesistente, lasciato deliberatamente invariato in quel task su istruzione esplicita "non riaprire la logica video").
- **Fix applicato:** rimossa ogni chiamata automatica a YMove per la thumbnail in entrambi i componenti. `ExerciseThumbnail`: mostra sempre il placeholder colorato per gruppo muscolare (già esistente) con un badge "ha video" derivato SENZA rete (`exercise.source==='ymove' && ymoveExerciseId` truthy, oppure una query Supabase a `exercise_videos` per il collegamento indiretto — mai YMove). `YMoveResultThumbnail`: mostra sempre l'icona placeholder + badge `hasVideo` (già gratuito, incluso nella risposta di ricerca/archivio in browse mode). Il video/anteprima reale resta disponibile SOLO tramite l'azione esplicita dell'utente (apertura dettaglio esercizio → `YMoveVideoPlayer`, invariato; tap "Anteprima" nel picker → `PreviewVideo`, invariato — entrambi già chiamavano YMove legittimamente e non sono stati toccati). Eliminato `mobile/src/lib/ymove-thumbnail-cache.ts` (diventato codice morto, nessun importatore rimasto dopo il fix).
- **File modificati:** `mobile/src/components/exercise-thumbnail.tsx`, `mobile/src/components/ymove-preview-shared.tsx`, `mobile/src/components/ymove-archive-browser.tsx`, `mobile/src/components/ymove-exercise-picker.tsx` (adeguati alla nuova firma di `YMoveResultThumbnail`, senza più `ymoveExerciseId`). File eliminato: `mobile/src/lib/ymove-thumbnail-cache.ts`.
- **Verifica:** `tsc --noEmit` pulito, `expo export --platform web` completato con successo, `git diff --check` pulito. Grep di conferma: nessun residuo di `fetchYmoveThumbnail`/`getCachedYmoveThumbnail`/`refreshYmoveThumbnail`/`invalidateYmoveThumbnail` in tutto `mobile/src`. **Nessuna verifica browser reale eseguita da questo agente.**
- **Non toccato in questo intervento (fuori scope, segnalato all'utente):** `scripts/import-ymove-bulk.py:45` usa ancora `includeVideos=true` su una chiamata LIST (script già eseguito una volta, non va rieseguito, ma il flag nel codice resta da correggere); nessuno script diagnostico standalone `check-ymove-usage` creato (solo proposto).
- **Correzione (2026-08-02, gate "CHIUSURA FIX CAP YMOVE"):** (1) `scripts/import-ymove-bulk.py:45` corretto da `includeVideos=true` a `false` (solo il flag, nessuna modifica alla logica di import/ID) — effetto collaterale scoperto e segnalato senza correggerlo (fuori dal mandato esplicito "non modificare la logica"): lo script filtra `exercise_external_links` con `if ex.get("videoUrl")` (righe 90-92), quindi se mai rieseguito con `includeVideos=false` quella lista risulterebbe sempre vuota (la risposta browse mode non include `videoUrl`) — non rilevante in pratica, lo script è storico e non va rieseguito. (2) **Bug reale trovato nel fix precedente**: `ExerciseThumbnail`/`ymove-archive-browser.tsx` assumevano `source==='ymove' ⇒ ha video` per mostrare il badge "ha video" — assunzione non solo non verificata ma probabilmente **falsa per un sottoinsieme dei 360**: lo stesso `import-ymove-bulk.py` (righe 80-92) scriveva una riga in `exercise_external_links` SOLO per gli esercizi che avevano davvero un `videoUrl` al momento dell'import, non per tutti i 360. Corretto: il badge "ha video" (icona Play) ora richiede prova locale reale (video caricato dal coach, o link YMove indiretto verificato via `exercise_videos` — query Supabase esistente, non estesa ai 360 esercizi diretti per non sostituire N chiamate YMove con N query Supabase); per gli esercizi `source==='ymove'` diretti, mostrato invece un badge distinto "Y" (sola provenienza, mai "ha video"). Audit completo di tutte le chiamate `/exercises` nel repository: 4 LIST (tutte `includeVideos=false`), 2 DETAIL espliciti per singolo id (`includeVideos=true`, legittimi), 0 chiamate automatiche durante il render di una card.

## BUG-066 - Motore dinamico (`_assemble_dynamic_auto_program`): la funzione di disponibilità e quella di assemblaggio sono disallineate — 120/2100 combinazioni "disponibili" falliscono davvero in assegnazione
- **Stato:** **CORRETTO e riverificato (2026-08-03)** — codice modificato in `supabase/migrations/20260819090000_dynamic_auto_program_engine.sql` (file mai committato, ancora locale), non applicato a produzione.
- **Fix applicato:** (a) `_dynamic_auto_program_available()` riscritta per contare gli esercizi disponibili per famiglia (`count(*) filter`) invece di verificarne solo l'esistenza (`bool_or`), con cardinalità richiesta allineata esattamente a `_assemble_dynamic_auto_program`: piani a 4 giorni richiedono 2 esercizi 'lower' distinti (Lower A/B) e 2 esercizi 'push' distinti (Upper A/B) nello stesso giorno; il giorno conditioning/recovery (p_days=5 o p_days>=6) richiede 2 esercizi 'conditioning' distinti quando il pool 'recovery' è vuoto (lo slot recovery ricade sulla famiglia conditioning, consumando un secondo esercizio nello stesso giorno del slot conditioning diretto). (b) Aggiunta, non in sostituzione, una rete di sicurezza esplicita in `assign_initial_auto_program()` e `run_cycle_review()`: il blocco di attivazione+assemblaggio è avvolto in un `begin...exception when others`, che intercetta `DYNAMIC_AUTO_PROGRAM_SLOT_UNAVAILABLE`/`DYNAMIC_AUTO_PROGRAM_UNAVAILABLE` e retrocede il ciclo a `pending_template` (con `decision_reason` diagnostico) invece di propagare un'eccezione non gestita al chiamante RPC; qualunque altro errore viene rilanciato invariato. **Non implementata** l'opzione di riutilizzare lo stesso esercizio su due slot dello stesso giorno (esplicitamente esclusa dall'utente: avrebbe degradato ulteriormente la varietà già bassa, vedi osservazione di qualità sotto).
- **Verifica post-fix**: rieseguito lo stesso test reale (non simulato) sulle 2100 combinazioni, chiamando `_dynamic_auto_program_available()`/`_assemble_dynamic_auto_program()` reali su DB locale resettato con il fix applicato. Risultato: le 120 combinazioni che prima crashavano ora sono correttamente rilevate come **non disponibili** dal gate (`real_available=false`), **0 eccezioni non gestite su tutte le 2100** (`ASSEMBLE ERRORS: 0`, era 120), **0 violazioni** equipment/livello/luogo sulle 30.120 righe esercizio delle 1980 combinazioni assemblate (invariato rispetto a prima, nessuna regressione di contenuto). La rete di sicurezza (b) non è stata innescata da questo test (atteso: il fix (a) ora previene il crash a monte) — non è stata testata separatamente con fault-injection in questa sessione, solo verificata per lettura/pattern (stesso idioma `begin...exception when others` già usato e verificato più volte in questa sessione, es. Punto 2/4 del giro di test precedente).
- **Stato precedente (per riferimento):**
- **Severità:** alta — per un sottoinsieme reale di clienti (piani a 4 giorni, livello/attrezzatura con un solo esercizio "push" disponibile) `assign_initial_auto_program()`/`run_cycle_review()` solleverebbero un'eccezione Postgres non gestita invece di restituire uno stato controllato.
- **Data apertura:** 2026-08-03, scoperto durante un test di verifica di contenuto reale (non simulato) richiesto esplicitamente dall'utente sulle 2100 combinazioni goal×livello×luogo×attrezzatura×giorni×durata, eseguito contro il DB locale via `public._dynamic_auto_program_available()` e `public._assemble_dynamic_auto_program()` reali (nessuna modifica a produzione).
- **Causa:** `_dynamic_auto_program_available()` verifica solo `bool_or(family='push')` (almeno UN esercizio "push" disponibile). Ma per i piani a 4 giorni, `_assemble_dynamic_auto_program()` richiede DUE slot "push" distinti nello stesso giorno (Upper A/B: `array['push','pull','core','push']`, con `v_used` che impedisce ripetizioni nello stesso giorno). Quando esiste un solo esercizio eleggibile per la famiglia "push" per quella combinazione livello/attrezzatura/luogo, il secondo slot non trova candidati e la funzione solleva `DYNAMIC_AUTO_PROGRAM_SLOT_UNAVAILABLE: push` — un'eccezione non catturata che si propagherebbe fino al chiamante RPC.
- **Passi di riproduzione:** su DB locale con le migration correnti, per qualunque combinazione a 4 giorni dove `equipment_tag='bodyweight_only'` (che ha un solo esercizio "horizontal_push": `petto-push-up`) — es. goal=Massa muscolare, livello=Principiante, luogo=Palestra, attrezzatura=bodyweight_only, giorni=4: `_dynamic_auto_program_available(...)` ritorna `true`, ma `_assemble_dynamic_auto_program(...)` con gli stessi parametri solleva l'eccezione.
- **Portata reale misurata:** 120 combinazioni su 2100 (test reale, non simulato) falliscono in questo modo; le restanti 1980 assemblate correttamente hanno superato tutti i controlli di contenuto (equipment/livello/luogo, 0 violazioni su 30.120 righe esercizio generate).
- **Fix NON applicato in questo intervento** (solo verifica, come richiesto): possibili direzioni, da validare con l'utente prima di scrivere codice — (a) rendere `_dynamic_auto_program_available` consapevole della cardinalità richiesta per slot (contare gli esercizi disponibili per famiglia, non solo la loro esistenza) invece del solo booleano `bool_or`; (b) rendere `_assemble_dynamic_auto_program` tollerante a un pool "push" con un solo elemento riutilizzandolo su entrambi gli slot del giorno Upper, se accettabile lato prodotto; (c) catturare l'eccezione a un livello superiore e retrocedere a `pending_template` invece di propagare l'errore.
- **Nota collegata, AGGIORNATA — non era solo un limite dell'ambiente di test, vedi BUG-067:** in locale nessuno dei 158 UUID YMove referenziati da `20260819090000_dynamic_auto_program_engine.sql` corrisponde a una riga reale in `public.exercises` (0/158), quindi il ramo `e.active and auto_program_eligible` di `_select_dynamic_auto_exercise_candidates` non è mai stato esercitato in locale (sempre bypassato via `e.id is null`). Verificato però con query read-only diretta in **produzione** (2026-08-03): il problema non è solo locale — **0/360 esercizi YMove hanno `auto_program_eligible=true` anche in produzione**. Vedi BUG-067 per il dettaglio e le implicazioni.
- **Osservazione di qualità (non un blocco costituzionale, ma degno di nota, non corretta in questo intervento su richiesta esplicita)**: sulle 1980 (ora 1980, invariato dopo il fix) combinazioni assemblate correttamente, solo **15 esercizi distinti** risultano usati in totale (query diretta sui piani generati). Causa: `_dynamic_auto_program_pick` seleziona sempre il primo candidato eleggibile in ordine di `exercise_id`, con `v_used` azzerato a ogni giorno (nessuna rotazione tra i giorni di uno stesso piano né tra clienti/combinazioni diverse) — nessun esercizio con equipment/livello/luogo scorretto è mai stato generato, ma la varietà dei piani è oggi molto bassa.

## BUG-067 - Il motore dinamico non seleziona MAI un esercizio YMove, in nessun ambiente: `auto_program_eligible=false` su tutte le 360 righe YMove in produzione
- **Stato:** **CHIUSO (2026-08-03)** — causa reale trovata (bug di mapping nella migration, non una decisione di prodotto in sospeso), corretta, applicata a produzione e verificata read-only post-apply. Commit `252d649`.
- **Severità:** alta (non bloccante di per sé — il motore funziona comunque con il catalogo legacy — ma significa che l'intera importazione YMove di ieri è oggi inerte per questa funzionalità specifica).
- **Data apertura:** 2026-08-03, verificato su richiesta esplicita dell'utente ("blind spot più urgente della varietà, prima di qualunque packaging").
- **Verifica iniziale:**
  - **Locale**: `select source, auto_program_eligible, active from exercises where source='ymove'` → 13 righe (subset pilota), 0/13 `auto_program_eligible=true`.
  - **Produzione (read-only, `supabase db query --linked`, nessuna scrittura)**: `select count(*) filter (...) from exercises where source='ymove'` → **360 righe totali**, **360/360 `active=true`**, **0/360 `auto_program_eligible=true`** (360/360 `false`).
- **Causa reale trovata (2026-08-03, sessione successiva)**: la lista `audited_ymove` nella migration (173 UUID distinti) contiene gli **ID ESTERNI YMove** (`exercises.ymove_exercise_id`), NON l'id interno FitCoach (`exercises.id`). Sia l'insert di `exercise_movement_metadata` sia l'`update ... set auto_program_eligible=true` finale facevano `join ... on a.exercise_uuid = e.id` — la colonna sbagliata. Verificato read-only in produzione: **0/173** match su `e.id`, **171/173** match su `e.ymove_exercise_id` (i 2 UUID restanti non corrispondono a nessun esercizio reale sotto nessun campo — sono gli stessi 2 candidati "pull_regression"/fallback discussi in BUG-066, mai realmente importati). Questo spiega perché il meccanismo che avrebbe dovuto popolare `exercise_movement_metadata` e abilitare `auto_program_eligible` non ha mai funzionato, in nessun ambiente: non era un gate deliberato, era un join che non ha mai trovato nulla.
- **Fix applicato:** (1) corretto il join in entrambi i blocchi (`a.exercise_uuid::text = e.ymove_exercise_id` invece di `= e.id`); (2) aggiunta un'esclusione esplicita di sicurezza per 13 esercizi a pattern plyometrico/esplosivo (Box Jump, Broad jumps, Explosive push ups, ecc. — classificati solo `difficulty='intermediate'`, nessun campo `category` separato nello schema per distinguerli in modo affidabile): questi restano `auto_program_eligible=false` deliberatamente, riservati alla scelta manuale del coach nell'archivio; (3) sostituiti i controlli di sanità con conteggi dinamici/informativi (`raise notice`, non `raise exception` su un totale fisso) per non rompere `db reset` in ambienti senza il catalogo YMove completo, mantenendo pero' un invariante di sicurezza sempre valido: i 13 esclusi non devono mai risultare eleggibili.
- **Verifica del fix — metodologia**: dato che l'ambiente locale non ha mai avuto il catalogo YMove reale (0/360 corrispondenze), il fix è stato verificato mirrorando in locale (via query READ-ONLY su produzione, nessuna scrittura a produzione) i 360 esercizi YMove reali (id, ymove_exercise_id, name, equipment, difficulty, muscle_group, active), poi rieseguendo il DML corretto sul DB locale così popolato.
- **Risultato verifica:** `INSERT 0 171` (metadata popolata per tutti i 171 match reali), `UPDATE 158` (171 − 13 esclusi = 158 esercizi resi `auto_program_eligible=true`), controllo di sicurezza sulle 13 esclusioni: 0 violazioni (nessuno dei 13 risulta eleggibile). Numeri esatti confermati, corrispondono all'aritmetica corretta (**non** al "156" inizialmente ipotizzato dall'utente, che sottraeva due volte i 2 UUID non corrispondenti già esclusi dal conteggio 171).
- **Effetto sul test reale delle 2100 combinazioni (stesso harness di BUG-066, rieseguito su questo stato):** **0 combinazioni pending** (tutte e 2100 ora `real_available=true`, erano fino a 840 secondo la vecchia simulazione JS), **0 eccezioni** non gestite, **0 violazioni** equipment/livello/luogo su 31.800 righe esercizio generate (erano 30.120 su 1980 combinazioni prima di questo fix). **Esercizi distinti usati: 27** (erano 15) — l'abilitazione dei 158 YMove migliora misurabilmente la varietà, anche se il motore resta deterministico sul primo candidato per famiglia (non c'è rotazione tra giorni/clienti, vedi osservazione di qualità in BUG-066).
- **Applicazione a produzione (2026-08-03, stessa giornata, su richiesta esplicita dell'utente)**: `supabase db push --linked --include-all` (necessario per sbloccare 3 migration legacy fuori ordine mai applicate — vedi sotto), poi verifica read-only immediata post-apply: `auto_program_eligible=true` → **158** esatti; nessuno dei 13 esclusi risulta eleggibile (0 violazioni); `exercise_movement_metadata` collegata a un esercizio ymove → 201 totali, di cui **171 esattamente riconducibili ai candidati di questa migration** (verificato per ID) e 30 preesistenti da `20260817100000_ymove_auto_engine_pilot_forza_base.sql` (già applicata in produzione da prima di questa sessione, confermato dal tracciamento reale delle migration, non per inferenza dal nome file). Tutti e 3 i controlli richiesti dall'utente confermati.
- **2 migration legacy fuori ordine corrette per sbloccare il push** (bloccavano `db push` perché precedenti per timestamp ma mai applicate a produzione): `20260716080000_legacy_baseline.sql` (43 `create policy` senza il corrispondente `drop policy if exists` — resi tutti idempotenti); `20260816141500_legacy_ymove_catalog_seed.sql` (insert incondizionato di 13 righe placeholder i cui `ymove_exercise_id` risultavano già presenti in produzione sotto id/nomi reali — reso idempotente con un controllo di stato esplicito, stesso pattern già usato per `20260816115000`).
- **Commit `252d649`**, pushato. Nessuna `migration repair` eseguita.

## BUG-068 - Varietà esercizi bassa nei piani generati dal motore dinamico: selezione sempre deterministica sul primo candidato, nessuna rotazione
- **Stato:** **CHIUSO (2026-08-04)** — corretto, applicato a produzione, verificato. Fix in `supabase/migrations/20260820090000_fix_bug_068_seeded_exercise_variety.sql`. Commit `44e93e3` + `b768bd6`.
- **Severità:** media (nessuna violazione di vincolo — equipment/livello/luogo/esclusioni sono sempre rispettati — ma qualità/varietà del piano generato bassa).
- **Data apertura:** 2026-08-03, osservazione emersa durante la verifica reale (non simulata) delle 2100 combinazioni per BUG-066/BUG-067.
- **Descrizione:** `_dynamic_auto_program_pick()` seleziona sempre il primo candidato eleggibile in ordine di `exercise_id` per la famiglia richiesta, con `v_used` azzerato a ogni giorno (nessuna memoria tra i giorni di uno stesso piano né tra combinazioni/clienti diversi). Risultato misurato: solo **15 esercizi distinti** usati in totale su 1980 combinazioni assemblate correttamente (prima di BUG-067), saliti a **27** dopo aver abilitato i 158 esercizi YMove classificati — un miglioramento reale ma ancora contenuto rispetto al pool ora disponibile, perché il motore continua a preferire deterministicamente lo stesso "primo" esercizio per ogni combinazione livello/attrezzatura/luogo, senza mai variare tra i giorni dello stesso piano o tra clienti con lo stesso identico profilo.
- **Impatto pratico:** un cliente che segue lo stesso programma per settimane vede probabilmente lo stesso esercizio ripetuto identico su più giorni del piano (es. "Lower A" e "Lower B" potrebbero usare lo stesso esercizio squat se il pool per quella combinazione è piccolo); due clienti con profilo identico ricevono piani con contenuto esercizio pressoché identico.
- **Direzione scelta e implementata: (a) seed deterministico, su `cycle_id` + indice giorno (non solo `client_id`).** Confrontate esplicitamente (a) seed su client_id/cycle_id e (b) rotazione su storico reale del cliente prima di implementare — vedi `docs/DECISIONS.md`. (b) resta un passo separato futuro, non implementato: da solo non avrebbe avuto effetto misurabile sul test sintetico usato qui, perché i client di test non hanno storico pregresso.
- **Fix applicato:** `_dynamic_auto_program_pick` riceve un nuovo parametro `p_seed text`; il tie-break finale (`c.sort_key`, alfabetico puro) è sostituito da `md5(coalesce(p_seed,'') || c.exercise_id)`. `_assemble_dynamic_auto_program` calcola `v_seed := p_cycle_id::text || v_day::text` a ogni giorno e lo passa a entrambe le chiamate del picker (slot diretto + fallback recovery→conditioning). **`_select_dynamic_auto_exercise_candidates` non toccata in alcun modo** — il filtro equipment/livello/luogo/esclusioni e il conteggio di cardinalità di BUG-066 restano del tutto separati da questo cambiamento (verificato leggendo il diff, non solo per intenzione).
- **Scoperto e corretto un problema di igiene delle migration durante l'implementazione**: `create or replace function` **non sostituisce** una funzione esistente quando si aggiunge un nuovo parametro (anche con default) — crea un secondo overload, lasciando la vecchia versione a 7 argomenti orfana ma ancora presente e chiamabile. Verificato concretamente su `pg_proc` (non per deduzione): dopo il primo tentativo, esistevano DUE `_dynamic_auto_program_pick` (7 e 8 argomenti). Corretto aggiungendo un `drop function if exists ... (7 argomenti)` esplicito prima della `create or replace`. Riverificato: una sola funzione presente dopo il fix.
- **Un secondo problema di igiene, scoperto prima di committare**: il fix è stato inizialmente scritto modificando in-place `20260819090000_dynamic_auto_program_engine.sql` — file **già applicato a produzione e già committato** (BUG-067, commit `252d649`). Corretto: file ripristinato al suo stato committato, fix spostato in una migration nuova e separata (`20260820090000`, non `20260804090000`: usata la convenzione di date del progetto, monotona ma non allineata al calendario reale — un nome con la data reale odierna sarebbe finito PRIMA di `20260819090000` nell'ordine di applicazione, causando un errore "function does not exist" per `_select_dynamic_auto_exercise_candidates" non ancora creata a quel punto — verificato concretamente con un `db reset` fallito prima di correggere il nome).
- **Verifica di sicurezza esplicitamente richiesta dall'utente, fatta in modo concreto (non per deduzione teorica)**: `_assemble_dynamic_auto_program` è definita e chiamata in un solo file, con esattamente 2 punti di chiamata (entrambi legati a un `cycle_id` appena creato: transizione `pending_template`→`active` in `assign_initial_auto_program`, o inserimento di un nuovo ciclo in `run_cycle_review`); il corpo contiene solo `insert`, mai `update`, su `workout_plans`/`workout_days`/`workout_day_exercises`. Test empirico: creato un ciclo con un piano reale, fotografate le righe esatte di `workout_day_exercises` (id, exercise_id, `updated_at` inclusi), generato un secondo ciclo per lo stesso cliente (con seed diverso, esercizi effettivamente diversi scelti), poi ri-fotografate le righe del primo ciclo → **checksum identico, zero differenze riga per riga, `updated_at` invariato**. Conferma: il fix incide solo sulla generazione di piani nuovi, non tocca mai un piano già esistente.
- **Verifica sulle 2100 combinazioni (stesso harness di BUG-066/067, non modificato):** 0 eccezioni non gestite, 0 violazioni equipment/livello/luogo su 31.800 righe esercizio. **Esercizi distinti usati: 149** (erano 27) — miglioramento di quasi 5,5×.
- **Verifiche puntuali richieste dall'utente prima dell'apply a produzione (entrambe con evidenza diretta, non per deduzione)**:
  1. **Cronologia Git reale su `20260819090000`**: un solo commit tocca mai quel file (`252d649`); `git diff HEAD` sul file vuoto; il commit non contiene alcuna occorrenza di `p_seed`/`BUG-068`; l'hash dell'oggetto Git del file di lavoro combacia esattamente con quello del blob committato — byte per byte identico, mai stata applicata a produzione la versione con la modifica in-place fatta per errore e poi corretta.
  2. **Permessi GRANT**: verificato in produzione (read-only) che la vecchia firma a 7 argomenti di `_dynamic_auto_program_pick`, e le altre 3 funzioni sorelle create dalla stessa migration `20260819090000`, avevano tutte `service_role=X` nell'ACL pur non essendoci alcun `grant` esplicito nel file originale (ereditato da un default privilege a livello di ambiente — diverso da locale, dove `db reset` produce solo `postgres=X`). Aggiunto un `grant execute ... to service_role` esplicito sulla nuova firma a 8 argomenti prima di applicare, per non dipendere da un comportamento implicito specifico dell'ambiente.
- **Applicazione a produzione (2026-08-04)**: `supabase db push --linked` (dry-run di conferma prima: solo questa migration, nessuna delle altre già applicate in precedenza). Verifica post-apply immediata (read-only), entrambi i controlli richiesti confermati: **una sola versione** di `_dynamic_auto_program_pick` in `pg_proc` (8 argomenti, nessun overload a 7 residuo); ACL `{postgres=X/postgres,service_role=X/postgres}` corretto, identico alle 3 funzioni sorelle.
- **Commit `44e93e3`** (fix) **+ `b768bd6`** (grant esplicito aggiunto prima dell'apply), entrambi pushati. Nessuna `migration repair` eseguita.

## BUG-069 - Loop di render infinito in `nutrizione.tsx` (tab Oggi/Diario): selettore Zustand con `?? []` inline
- **Stato:** **CHIUSO (2026-08-04)** — trovato dall'utente al primo test interattivo reale in Expo (mai fatto prima per questa feature), corretto e verificato con controprova concreta. **Non ancora committato.**
- **Severità:** bloccante (pagina bianca, schermata Nutrizione inutilizzabile) ma limitata alle tab Oggi/Diario di questa feature, non ancora in produzione.
- **Causa:** `mobile/src/app/nutrizione.tsx` selezionava lo stato con `useNutritionStore((s) => s.diaryByDate[data] ?? [])`. L'operatore `??` istanzia un nuovo array letterale ad ogni esecuzione del selettore quando la chiave è assente/vuota; `useSyncExternalStore` (usato internamente da Zustand) confronta per riferimento e vede quindi un valore "cambiato" ad ogni render anche quando il contenuto è identico (vuoto come prima) → re-render infinito.
- **Fix applicato:** aggiunta una costante module-level `EMPTY_DIARY_ENTRIES: NutritionDiaryEntry[] = []` in `mobile/src/store/nutrition-store.ts` (con commento che spiega perché è obbligatoria), riusata come fallback stabile nei 2 punti di `nutrizione.tsx` che leggevano `diaryByDate[...]` (tab Oggi e tab Diario), al posto di `?? []` inline.
- **Verifica concreta (non teorica), con controprova prima/dopo**: creato un ambiente di test reale — utente cliente + coach locali (Supabase Auth locale via admin API, DB local `docker exec`), collegamento coach-cliente, onboarding `coach_guided` completato via seeding diretto (bypass trigger di sicurezza tramite il path `service_role`/superuser, solo per dati di test locali) — poi avviato un browser Chrome reale (Puppeteer, headless, non uno smoke HTTP) puntato su un'istanza Expo web locale configurata per parlare al Supabase **locale** (non a quello di produzione — il `.env` di progetto punta a un progetto Supabase remoto reale, non toccato). Login reale, navigazione a `/nutrizione`, click reali su tutte e 4 le tab (Oggi/Piano/Diario/Ricette), 2 giri avanti/indietro:
  - **Con il pattern rotto ripristinato temporaneamente**: riprodotto l'errore esatto riportato dall'utente — console `"The result of getSnapshot should be cached to avoid an infinite loop"` + `"Maximum update depth exceeded"`, `pageerror` non gestito, pagina vuota dopo la navigazione a `/nutrizione`.
  - **Con il fix riapplicato**: 0 errori/warning console, 0 page error, tab Oggi renderizzata correttamente (stato vuoto "Nessun alimento registrato"), tutte e 4 le tab navigabili avanti/indietro senza anomalie.
- **Audit esplicito richiesto dall'utente sul resto della codebase**: cercato lo stesso pattern (`useXStore((s) => ... ?? []` o `?? {}` inline nel selettore) in tutti gli altri store del progetto (`app-notification-store`, `appointment-store`, `attachment-store`, `board-store`, `booking-store`, `chat-store`, `checkin-store`, `client-fitness-profile-store`, `client-monthly-checkin-store`, `client-onboarding-store`, `client-store`, `superadmin-store`, `theme-store`, `training-store`, `ymove-autolink-store`, `program-edit-store`, `auth-store`) e in tutti i loro consumer. **Nessun'altra occorrenza trovata**: gli unici store con strutture keyed (`Record<string,...>`) sono `client-fitness-profile-store`/`client-monthly-checkin-store`/`client-onboarding-store` (drafts) e i loro consumer selezionano senza fallback inline (`s.drafts[clientId]`, può restare `undefined` in sicurezza — nessun problema). Il `??`/emptyDraft usato dentro le azioni `set()` di quegli store è innocuo (non è un selettore di render).
- **Scoperti 2 problemi di ambiente locale non correlati, durante la costruzione del test (non bug applicativi)**: (1) dopo un `supabase db reset` locale mancano i GRANT di base (`SELECT`/`INSERT`/`UPDATE`/`DELETE` su `public.*` per `authenticated`/`anon`) che la piattaforma Supabase gestita applica di default in produzione — stessa classe di discrepanza locale-vs-produzione già documentata in BUG-068; applicati manualmente in locale (fuori da migration, solo per sbloccare il test, RLS resta comunque attiva e invariata) — **da NON portare in produzione senza verificare prima se lì sono già presenti**; (2) `mobile/.env` punta al progetto Supabase remoto reale — il test è stato eseguito sovrascrivendo `EXPO_PUBLIC_SUPABASE_URL`/`EXPO_PUBLIC_SUPABASE_ANON_KEY` solo a livello di variabili d'ambiente della sessione di test (mai scritto su file), per non creare utenti di test o toccare in alcun modo l'istanza di produzione.
- **Nessun commit/push eseguito**, come richiesto — resta in attesa di un nuovo giro di test/approvazione sulle 4 tab prima di qualunque commit.

## BUG-070 - "Azione non riconosciuta" su TUTTE le 8 azioni nutrition: container Edge Function locale non riavviato dopo le modifiche
- **Stato:** **CHIUSO (2026-08-04)** — causa reale trovata (non un bug di codice, un problema di processo/ambiente), corretta con un riavvio del container, verificata via chiamata diretta.
- **Severità:** bloccante (nessuna azione nutrition funzionante), solo in ambiente locale.
- **Causa reale trovata (non per intuito — confronto esatto richiesto dall'utente eseguito prima di agire)**: confrontate le 8 stringhe `action` inviate dal client (`mobile/src/lib/nutrition-service.ts`) contro quelle riconosciute dal server (`supabase/functions/ymove-exercises/index.ts`) — **8/8 identiche, zero discrepanze di nome/maiuscole/underscore**. La causa reale era il container Docker locale `supabase_edge_runtime_lavoro`, attivo dal 2026-08-03T14:05:54Z (verificato con `docker inspect`), **ore prima** dell'ultima modifica al file `index.ts` (2026-08-04 14:08:04, verificato con il timestamp del file) — serviva quindi ancora il bundle precedente a tutto il codice nutrition, per cui ogni azione nutrition cadeva nel fallback pre-esistente `invalid_action` (usato in precedenza solo per le 3 azioni esercizi).
- **Fix applicato:** `docker restart supabase_edge_runtime_lavoro`.
- **Verifica concreta (non teorica)**: chiamata diretta (`curl`, non tramite l'app) a `foods_search` contro l'endpoint locale — risposta cambiata da `{"ok":false,"code":"invalid_action",...}` a `{"ok":false,"code":"ymove_not_configured",...}` (azione ora riconosciuta e instradata correttamente; l'errore residuo è solo per l'assenza della vera API key, vedi rischio noto sotto). Ripetuto per le restanti 7 azioni via `curl` diretto: tutte e 8 restituiscono `ymove_not_configured`, mai più `invalid_action`.
- **Rischio residuo, non risolvibile in questo ambiente**: nessuna delle 8 azioni ha mai ricevuto una risposta VALIDA (non di errore) da YMove in questo ambiente, perché non esiste una copia locale di `YMOVE_API_KEY` (secret Supabase write-only, nessun file `.env` per le funzioni nel repository) — stesso limite già segnalato nel report iniziale della Fase 1. Il codice è confermato corretto (routing, whitelist, validazione parametri) ma **mai stato esercitato con una vera risposta YMove**.
- **Nota per il futuro**: questo container NON si riavvia da solo alle modifiche del codice delle Edge Function — dopo ogni modifica a un file in `supabase/functions/`, va riavviato esplicitamente (`docker restart supabase_edge_runtime_lavoro`) prima di testare in locale, altrimenti si ripresenta lo stesso sintomo "azione non riconosciuta"/comportamento non aggiornato per QUALUNQUE funzione modificata, non solo nutrition.

## BUG-071 - Tab "Piano": spinner infinito e silenzioso se il caricamento iniziale del piano fallisce; "Cambia pasto" disattiva il caricamento prima che il lavoro finisca
- **Stato:** **CHIUSO (2026-08-04)** — 2 bug reali distinti trovati nello stesso giro di audit, entrambi corretti e verificati.
- **Severità:** alta (schermata inutilizzabile senza alcun messaggio, nessun modo di uscirne se non ricaricare l'app).
- **Bug 1 (il più grave — vero "caricamento infinito silenzioso")**: `NutrizioneScreen` (in `mobile/src/app/nutrizione.tsx`) caricava il piano attivo con `getActiveNutritionPlan().then((result) => { if (result.ok) setActivePlan(result.data); })` — **nessun ramo per `!result.ok`**. Poiché `activePlanLoaded` nello store diventa `true` SOLO dentro `setActivePlan`, un fallimento qualsiasi di questa chiamata (RLS, rete, GRANT mancante, ecc.) lasciava `activePlanLoaded` per sempre `false` — l'intera tab "Piano" restava bloccata su un semplice `<ActivityIndicator>` per sempre, PRIMA ancora di raggiungere il form "Genera piano" (il cui gestore d'errore, verificato separatamente, era già corretto). Nessuna eccezione necessaria per riprodurlo: bastava un normale esito `{ok:false}` mai gestito.
- **Verifica concreta con riproduzione reale (non teorica)**: revocato temporaneamente `SELECT` su `public.client_nutrition_plans` per il ruolo `authenticated` in locale (per forzare un vero errore backend), aperta la tab Piano in un browser Chrome reale — **prima del fix**: sarebbe rimasta bloccata sullo spinner per sempre (stesso meccanismo, non ri-testato nella forma rotta per non perdere tempo dato che il meccanismo è lo stesso già provato in BUG-069); **dopo il fix**: mostra "Operazione non riuscita. Riprova." con un pulsante "Riprova", e il form di generazione piano resta comunque accessibile sotto il messaggio. Privilegio ripristinato subito dopo il test.
- **Fix applicato**: aggiunto un ramo esplicito su fallimento (`setActivePlan(null)` per sbloccare `activePlanLoaded`, più uno stato `planLoadError` mostrato con un pulsante "Riprova"), più un `.catch()` di sicurezza sulla stessa catena `.then()` per coprire anche un'eccezione imprevista (non solo un `{ok:false}` gestito).
- **Bug 2, correlato ma distinto (in `PlanMealCard.handleSwap`, flusso "Cambia pasto")**: `setSwapping(false)` veniva chiamato subito dopo `getRecipeDetail()`, ma PRIMA dell'ultima chiamata di rete (`swapNutritionPlanMeal`) — il pulsante smetteva di mostrare il caricamento mentre l'operazione era ancora in corso (finestra in cui l'utente poteva percepire l'azione come conclusa, o in teoria ritentare). Fix: spostato `setSwapping(false)` a DOPO l'esito di `swapNutritionPlanMeal` (sia successo che fallimento).
- **Audit di coerenza richiesto dall'utente sulle altre 3 tab**: `AddFoodModal`/`BarcodeModal` (Oggi), `PlanGeneratorForm` (Piano), `RicetteTab` (Ricette) già gestivano correttamente errore+stop-caricamento (verificato via test reale in browser per la ricerca alimenti/generazione piano/ricerca ricette: messaggio d'errore mostrato, nessun caricamento bloccato). Aggiunta comunque, per coerenza e come rete di sicurezza contro eccezioni impreviste (nessuna mai riprodotta, ma lo stesso pattern `.then()` senza `.catch()` era presente): stato d'errore visibile + `.catch()` anche su `OggiTab` (caricamento pasti di oggi) e `DiarioTab` (caricamento diario, e la funzione elimina voce che ignorava silenziosamente un fallimento).
- **Verifica finale**: `tsc --noEmit` pulito; test reale in browser Chrome (Puppeteer) su tutte e 8 le azioni nutrition dopo il fix di BUG-070 — tutte le 8 azioni ora riconosciute e instradate correttamente (nessun `invalid_action`), tutte bloccate solo dalla mancanza della vera `YMOVE_API_KEY` in questo ambiente (nessuna ha mai ricevuto una risposta valida non di errore — vedi BUG-070 per il dettaglio di questo limite).
- **Nessun commit/push eseguito**, in attesa di conferma esplicita dell'utente vista l'impossibilità di soddisfare in questo ambiente il requisito "almeno una risposta valida per ciascuna delle 8 azioni".

## BUG-072 - `RicetteTab`: "r.instructions.map is not a function" su qualunque ricetta reale espansa — mismatch di schema tra /recipes/search e /recipes/{id}, non un dato anomalo isolato
- **Stato:** **CHIUSO (2026-08-04)** — causa reale trovata guardando i dati veri (non ipotizzata), corretta, verificata su un campione ampio di dati reali diversi + test interattivo reale.
- **Severità:** alta (crash del rendering per QUALUNQUE ricetta espansa in Ricette, non un caso raro).
- **Causa reale, trovata guardando la risposta RAW vera (non ipotizzata)**: `/recipes/search` e `/recipes/{id}` condividono quasi tutti i nomi di campo, ma **non** la forma di `instructions`/`ingredients`. `/recipes/search` restituisce `instructions` come un singolo paragrafo (`string`) e `ingredients` come lista testuale separata da virgole (`string`); solo `/recipes/{id}` (dettaglio) li restituisce come array (step di istruzioni; oggetti struttura per ingrediente). Il tipo `YmoveRecipe` e il codice di `RicetteTab`/`handleSwap` assumevano SEMPRE la forma-array (valida solo per il dettaglio), causando il crash per qualunque ricetta aperta dai risultati di ricerca — confermato su **56 ricette reali distinte**, campionate con 7 ricerche diverse (query/diet/cuisine/mealType vari): **tutte e 56** avevano `instructions`/`ingredients` come stringa nei risultati di ricerca, **0 eccezioni** — non è un caso raro, è la forma normale di quell'endpoint.
- **2 bug reali aggiuntivi trovati nello stesso audit, stesso principio ("verificare il dato vero, mai assumere il nome/tipo di un campo")**:
  1. `YmoveRecipe.prepTimeMinutes`/`.cookTimeMinutes`/`.cuisine`/`.diet` erano nomi di campo **inventati per analogia**, mai verificati contro la risposta reale — i nomi veri (su ENTRAMBI gli endpoint, verificato su 56+21 ricette) sono `prepTimeMin`/`cookTimeMin`/`cuisineType`/`dietTags`. Effetto concreto, non solo teorico: la card ricetta in Ricette mostrava sempre **"NaN min"** (somma di due `undefined`); il flusso "Cambia pasto" salvava sempre `cuisineType`/`prepTimeMin`/`cookTimeMin`/`dietTags` come `undefined` nello snapshot del pasto sostituito.
  2. `YmoveRecipeIngredient.amount` (string) non esiste nella risposta reale di `/recipes/{id}` — i campi veri sono `quantity` (number) + `unit` (string) separati. Verificato su **171 ingredienti reali** (21 ricette diverse): 0 eccezioni, sempre `quantity`+`unit`, mai `amount`. Effetto concreto: ogni ingrediente mostrato nel dettaglio ricetta e ogni pasto sostituito via "Cambia pasto" avrebbe mostrato/salvato una quantità `undefined`.
- **Fix applicato**:
  - `types/nutrition.ts`: `YmoveRecipe.instructions`/`.ingredients` ora union (`string[] | string` / `YmoveRecipeIngredient[] | string`, con commento che spiega il perché); `cuisineType`/`dietTags`/`prepTimeMin`/`cookTimeMin` rinominati per corrispondere ai nomi reali; `YmoveRecipeIngredient.amount` sostituito con `quantity: number; unit: string`.
  - `RicetteTab`: rendering di istruzioni/ingredienti con `Array.isArray()` esplicito, fallback a paragrafo di testo semplice se stringa, nessuna sezione se assente/vuoto (mai un crash, mai testo inventato).
  - `PlanMealCard.handleSwap`: `instructions` normalizzata a array (wrap in `[stringa]` se non già array, `[]` se assente) prima di salvarla nello snapshot del pasto; `ingredients` non-array → `foods: []` (mai ingredienti fittizi inventati da una stringa non strutturata); `portion` costruita da `quantity`+`unit` reali invece del campo `amount` inesistente.
  - `PlanMealCard` (rendering pasto attivo): aggiunta la stessa guardia `Array.isArray()` su `meal.recipe.instructions`/`meal.foods` come difesa in profondità, perché questi arrivano da uno snapshot JSONB con un cast non validato a runtime (`client-nutrition-service.ts`).
  - **`tsc --noEmit` usato come strumento di audit, non solo di verifica**: dopo ogni correzione di tipo, ho lasciato che gli errori di compilazione elencassero ESATTAMENTE ogni altro punto del codice che faceva la stessa assunzione sbagliata (2 punti per instructions/ingredients-array, 6 per i nomi di campo, 2 per `amount`) — nessuno trovato per ispezione manuale/a memoria, tutti emersi dal compilatore.
- **Verifica su dati reali, ampia, non limitata al primo risultato funzionante (richiesto esplicitamente)**: 7 ricerche `/recipes/search` diverse (query/diet/cuisine/mealType/maxCalories/minProtein vari) → 56 ricette, 0 anomalie di tipo residue; 21 chiamate `/recipes/{id}` su un campione diversificato di quelle ricette → 171 ingredienti reali, 0 anomalie; test interattivo reale in browser Chrome (Puppeteer) su **5 ricerche diverse** ("chicken","salad","soup","rice","beef", 100 ricette reali mostrate in totale) con **15 espansioni di card diverse**: 0 errori console/page, 0 occorrenze di "NaN", tempi di preparazione/cottura mostrati correttamente per tutte.
- **Verificato `foods`/`mealplan_generate`/`YmoveFood` per lo stesso pattern di rischio**: tutti i nomi di campo già corretti (verificati byte-per-byte contro l'OpenAPI reale in una fase precedente), nessun'altra discrepanza trovata.
- **Nessun commit/push eseguito**, in attesa di conferma dell'utente.
