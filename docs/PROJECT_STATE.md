# PROJECT_STATE.md

**Aggiornamento corrente 52:** 02/08/2026 (continuazione 4) - **YMOVE_AUTO_PILOT_LIVE**: `supabase db push` reale eseguito su produzione (`rkcecnzvzoigipjliwdk`), migration live `20260817090000` + `20260817100000`. Pilota "Forza Base YMove" attivo nel motore automatico. Risultato verificato read-only: 50/50 `exercise_movement_metadata` YMove presenti, 0 duplicati; Forza Base YMove `auto_eligible=true` (unico tra gli 8 YMove, altri 7 confermati `false`); matcher PASS su Forza/Intermedio/Palestra/4/full_gym → Forza Base YMove; casi incompatibili (livello/luogo/giorni/attrezzatura insufficienti) confermati esclusi, nessuna priorità artificiale introdotta; helper `_template_compatible_with_client_exclusions` confermato live, identico alla versione verificata nel gate. Zero chiamate YMove API, nessun cliente reale modificato, nessuna Edge Function deployata. **Rischio residuo noto, non bloccante**: `public.exercises.auto_program_eligible=false` sui 16 esercizi del pilot — non letto da alcun percorso attivo del motore, non modificato. **Prossimo test reale ancora da fare (non dichiarato PASS)**: flusso E2E con cliente sintetico senza coach (Forza/Intermedio/Palestra/4 giorni/full_gym, entitlement Client Pro attivo) → verificare assegnazione automatica reale di Forza Base YMove. Dettaglio in `docs/WORKLOG.md`.

**Aggiornamento corrente 51:** 2026-08-02 (continuazione 3) - gate finale pre-produzione sul pilota YMove nel motore automatico: decisione `SAFE_TO_APPLY_YMOVE_AUTO_PILOT_FINAL` sulle 2 migration preparate nell'aggiornamento 50 (`20260817090000_fix_run_cycle_review_structural_change_exclusions.sql`, `20260817100000_ymove_auto_engine_pilot_forza_base.sql`), **ancora nessuna applicata**. Solo letture read-only su produzione (`pg_get_functiondef`, `migration list`, `db push --dry-run`), nessuna scrittura, nessuna chiamata YMove, nessun commit/push/deploy. Trovato e corretto un secondo bug reale nella migration: `_template_compatible_with_client_exclusions()` restituiva `true` in modo vacuo per un template a zero giorni (fix: `exists(...)` esplicito aggiunto). Diff riga-per-riga confermato tra `run_cycle_review` live e la versione proposta: uniche differenze le due condizioni STEP 12/13 già previste. Verificati: i 16 esercizi di "Forza Base YMove" (0 quarantenati, metadata compatibile), i 4 scenari di non-match del matcher, stato live pre-apply (0/8 template YMove oggi `auto_eligible=true`), safety SQL, `db push --dry-run` (solo i 2 file attesi), `git diff --check` pulito. Scoperta non bloccante portata esplicitamente in decisione all'utente: i 16 esercizi hanno `public.exercises.auto_program_eligible=false`, colonna verificata (grep + lettura integrale dei 3 corpi funzione del motore) non letta da nessun percorso attivo — scritta solo dalla pipeline import YMove per una vista `public.selectable_exercises` mai creata in produzione. L'utente ha scelto di procedere senza modificare le migration; segnalato come rischio residuo futuro in `docs/TODO_NEXT.md`. Prossimo passo: decisione esplicita dell'utente per `supabase db push` reale, poi verifiche post-apply e test A-H con account sintetici. Dettaglio in `docs/WORKLOG.md`.

**Aggiornamento corrente 50:** 2026-08-02 - chiusura gate cap mensile YMove (BUG-065, correzione) + Fase 1 motore automatico preparata. Gate finale: `scripts/import-ymove-bulk.py:45` corretto a `includeVideos=false`; trovato e corretto un secondo bug reale nel fix precedente — il badge "ha video" assumeva `source==='ymove' ⇒ ha video`, smentito leggendo lo stesso script bulk (`exercise_external_links` scritto solo per esercizi con `videoUrl` reale all'import, non per tutti i 360); ora il badge Play richiede prova locale verificata, gli esercizi YMove diretti senza prova mostrano un badge "Y" di sola provenienza. Audit finale: 4 LIST totali tutte `includeVideos=false`, 2 DETAIL espliciti legittimi, 0 chiamate automatiche a render. `tsc`/`expo export`/`git diff --check` puliti, nessuna verifica browser reale dall'agente. In parallelo, preparate (non applicate) 2 migration per la Fase 1 "YMove nel motore automatico": fix di un'asimmetria pre-esistente sulle esclusioni cliente durante il cambio strutturale di template in `run_cycle_review`, seed di 50 `exercise_movement_metadata` per gli esercizi YMove, e `auto_eligible=true` guardato solo per "Forza Base YMove" (pilota, gli altri 7 template YMove restano disabilitati). `db push --dry-run` conferma solo le 2 nuove migration in coda; nessuna applicata. Nessun commit/push/deploy. Checklist completa in `docs/TODO_NEXT.md`. Dettaglio in `docs/BUGS.md` (BUG-065), `docs/WORKLOG.md`.

**Aggiornamento corrente 49:** 2026-08-02 - fix BUG-065, cap mensile YMove consumato dalle thumbnail: `ExerciseThumbnail` (9 file consumer) e `YMoveResultThumbnail` (picker YMove, tab "Cerca live"/"Archivio") chiamavano automaticamente `GET /exercises/{id}?includeVideos=true` (cap-consuming) al render di ogni card, solo per la miniatura — non solo su tap "Anteprima" come da design. Trovato durante un audit esplicito richiesto dall'utente su `includeVideos=false`/protezione cap (confermato: le chiamate LIST di `ymove-exercises`/`sync-ymove-exercise-catalog.mjs`/`ymove-catalog-audit` erano già corrette). Corretto: entrambi i componenti mostrano ora sempre placeholder + badge "ha video" senza rete; video/anteprima reali restano invariati e solo su azione esplicita. Eliminato `mobile/src/lib/ymove-thumbnail-cache.ts` (codice morto). `tsc --noEmit`/`expo export --platform web`/`git diff --check` puliti. **Nessuna verifica browser reale eseguita dall'agente** — checklist in `docs/TODO_NEXT.md`. Nessun commit/push/deploy in questo intervento. Fuori scope, segnalato: `scripts/import-ymove-bulk.py:45` ha ancora `includeVideos=true` su una LIST (script già eseguito, non da rieseguire); script diagnostico `check-ymove-usage` solo proposto. Dettaglio in `docs/BUGS.md` (BUG-065), `docs/WORKLOG.md`.

**Aggiornamento corrente 48:** 2026-08-01 - fix BUG-064, flash UUID nella Scheda modello: `schede/modello/[templateId].tsx` mostrava per un istante l'UUID/chiave grezza al posto del nome esercizio (fallback `exercise?.name ?? te.exerciseId` durante la risoluzione asincrona uno-alla-volta di `useExerciseResolver`, cache solo `useState` locale al componente, persa ad ogni remount). Corretto con una risoluzione batch: nuova `getFitCoachExercisesByIds` in `mobile/src/lib/fitcoach-exercises-service.ts` (UNA query `.in('id', ids)` + una seconda batch per `exercise_text_overrides`, filtrata prima ai soli id a forma UUID — `exercises.id` è `uuid`, un valore legacy testuale nel mix avrebbe fatto fallire l'intera query); nuova cache di modulo `mobile/src/lib/exercise-metadata-cache.ts` (mai persistita su disco, mai URL video YMove, condivisa tra mount/unmount della stessa sessione app). `[templateId].tsx` raccoglie tutti gli `exercise_id` del template appena caricato e lancia una sola query batch; ogni riga esercizio ora sceglie tra card reale, skeleton (stessa geometria, durante il caricamento) o "Esercizio non disponibile" (mai più l'UUID, id loggato solo in `__DEV__`) per id irrisolvibili dopo il batch. `tsc --noEmit`/`expo export --platform web`/`git diff --check` puliti. **Nessuna verifica browser reale eseguita dall'agente** (nessun tool disponibile) — richiesta all'utente, checklist in `docs/TODO_NEXT.md`. Nessun commit/push/deploy/db push in questo intervento. Stesso pattern trovato ma non corretto (fuori scope) in `client-load-history.tsx` e `schede/modelli/[id].tsx`. Dettaglio in `docs/BUGS.md` (BUG-064), `docs/WORKLOG.md`.

**Aggiornamento corrente:** 2026-07-14 (continuazione 2) - fix BUG-027 APK Android cliente: le card/righe interne di Home, Altro e Workout avevano `onPress` corretti e route esistenti, ma su Android erano renderizzate dentro `NativeTabs`, che registra solo i 5 `Trigger` principali e non puo' mostrare le route interne (`/questionario`, `/bacheca`, `/prenotazioni`, `/progressi`, `/pacchetti-cliente`, `/schede/[id]`). `client-tabs.tsx` ora usa `Slot` + tab bar React Native come la variante web. Aggiunti log dev `CLIENT_NAV_PRESS { source, target }`. Verificati `tsc`, `expo-doctor` 20/20, `expo export --platform android`.

**Aggiornamento corrente 2:** 2026-07-14 (continuazione 3) - fix coach/superadmin Android e dati reali: `app-tabs.tsx` coach ora usa `Slot` + tab bar React Native con log `COACH_NAV_PRESS`, quindi le route interne coach (`/clienti/[id]`, `/clienti/new`, `/schede/new`, `/appuntamenti/new`, `/supporto`, `/abbonamento-coach`) restano renderizzabili su Android. Lista/detail clienti coach collegati a Supabase reale tramite `coach_clients` attivi + `profiles` + `client_profiles`, demo solo con `EXPO_PUBLIC_ENABLE_DEMO_DATA=true`. `abbonamento-coach.tsx` mostra pacchetto attivo, scadenza, limite clienti, clienti utilizzati e checkout non simulato. Superadmin resta shell JS (no NativeTabs), aggiunti log `SUPERADMIN_NAV_PRESS`; assegnazione pacchetto coach ora permette date inizio/scadenza, chiude gli active precedenti e scrive `user_subscriptions` via RLS superadmin.

**Aggiornamento corrente 3:** 2026-07-14 (continuazione 4) - acquisto reale pacchetti coach via RevenueCat: installato `react-native-purchases`, aggiunto `revenuecat-service.ts` con configurazione unica per Android/iOS, public keys `EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY`/`EXPO_PUBLIC_REVENUECAT_IOS_API_KEY`, identificazione coach con UUID Supabase Auth, restore acquisti, listener CustomerInfo senza duplicati e messaggi onesti per web/Expo Go. `abbonamento-coach.tsx` usa il prezzo localizzato store/RevenueCat e non attiva mai `user_subscriptions` dal client: dopo purchase/restore attende solo la sincronizzazione webhook. `subscription_packages` ora prevede mapping RevenueCat/store (`revenuecat_entitlement_id`, `revenuecat_offering_id`, `android_product_id`, `ios_product_id`); superadmin puo' modificarli ma il prezzo Supabase resta solo riferimento. Nuova Edge Function `supabase/functions/revenuecat-webhook` con secret condiviso, service role solo server-side, ledger idempotente eventi e update di `user_subscriptions` con `payment_provider='revenuecat'`. Assegnazione manuale superadmin preservata con `payment_provider='superadmin_manual'`. Migrazione SQL idempotente preparata in `docs/SUPABASE_SCHEMA.sql`, non eseguita.

**Aggiornamento corrente 4:** 2026-07-14 (continuazione 5) - fix pre-build Android: nuovo `BackHeader` condiviso con ArrowLeft, hit area 44x44, fallback per ruolo/sezione e header nativi disabilitati nei sotto-stack interni cliente/coach/superadmin dove il custom header e' visibile. Il contatore workout cliente/coach non legge piu' `subscription.completedWorkouts` come numeratore quando esiste un abbonamento: usa le `workoutPlans` reali `sessionStatus='completed'`, deduplicate per id, filtrate per `subscriptionId` quando presente e per periodo dell'abbonamento per schede legacy senza `subscriptionId`. `WorkoutPlanForm` collega automaticamente le nuove schede all'abbonamento attivo del cliente se disponibile. Layout Android dei form corretto con `minWidth:0`, `width:'100%'`, breakpoint sotto 390px per coppie Data/Ora e Giorno/Settimana, pulsanti esercizio/YMove impilabili, padding bottom con tab bar/safe area. Verificati `tsc`, `expo-doctor` 20/20, `expo export --platform android`.

**Aggiornamento corrente 5:** 2026-07-14 (continuazione 6) - redesign dashboard/avatar/catalogo: dashboard cliente/coach/superadmin aggiornate al linguaggio FitCoach Pro dei mockup locali con logo testuale condiviso, palette premium dark/green/coral e avatar cliente dove disponibile, senza aggiungere metriche finte. `Client` supporta `avatarUrl`, `avatarStoragePath`, `avatarPreset`; `profiles.avatar_url` resta la fonte foto Supabase, risolta con signed URL dal bucket `client-avatars`. `exercise-image-catalog.ts` centralizza le anteprime esercizi: usa asset locali solo se registrati in `image-registry`, altrimenti placeholder stabile per gruppo muscolare; YMove live resta supportato. Migrazione SQL locale in `docs/SUPABASE_SCHEMA.sql` per `profiles.avatar_preset`, bucket/policy `client-avatars`, tabella opzionale `exercise_image_catalog`. Verificati `tsc`, `expo-doctor` 20/20, `expo export --platform android`.

**Aggiornamento corrente 6:** 2026-07-20 - fix BUG-035, dettaglio cliente coach: "Sospendi per rinnovo" e "Rimuovi cliente" non producevano alcun effetto su Expo Web perché `Alert.alert` è un no-op assoluto su `react-native-web` (`node_modules/react-native-web/dist/exports/Alert/index.js`, nessun dialog, nessun callback invocato) — RPC (`suspend_assigned_client`/`reactivate_assigned_client`/`remove_assigned_client`), `coach-client-status-service.ts`, `app-button.tsx` e RLS erano già tutti corretti, verificati senza modifiche. `mobile/src/app/clienti/[id].tsx`: nuova `confirmDestructive()` locale, `globalThis.confirm` su web / `Alert.alert` su nativo (stesso pattern già usato in `client-metrics-panel.tsx`). Unico file toccato. Verificati `tsc --noEmit` pulito, `expo export --platform web` senza errori di bundling, `git diff --check` pulito. Commit isolato `c96d734`. **Non verificato con clic reali** (nessun tool di automazione browser disponibile in questo ambiente, nessun coach/cliente reale contro Supabase) — checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 7:** 2026-07-20 (continuazione) - fix BUG-036, lista clienti coach: i clienti "suspended" sparivano da TUTTE le tab (Tutti/Attivi/Sospesi), non solo da "Sospesi", pur con la capacità pacchetto corretta ("2 su N", conta active+suspended via SQL diretto). Causa reale: RLS, non codice mobile — `profiles_coach_reads_own_clients`/`client_profiles_coach_for_client` usano `is_coach_for_client()` (solo `status='active'`); per un client_id sospeso la query coach-side su `profiles`/`client_profiles` tornava 0 righe silenziosamente e `coach-clients-service.ts` scartava il cliente per assenza di profilo, PRIMA di raggiungere hook/lista/filtri tab (già tutti corretti). Stesso pattern già risolto per l'avatar in `20260720084323_fix_suspended_client_avatar_access.sql`, mai propagato a profiles/client_profiles. Fix: nuova migration `supabase/migrations/20260720171500_fix_coach_profile_read_includes_suspended_clients.sql` con funzione dedicata `is_coach_for_client_visible()` (active+suspended) usata SOLO dalle due policy di lettura; `is_coach_for_client()` originale (solo active) invariata perché usata anche da `workout_plans_coach_scope`/`save_workout_plan` (Assegna scheda, non toccato). Nessuna modifica a RPC sospendi/riattiva/rimuovi, modal, capacità pacchetto, logica filtro tab. Aggiunti solo log diagnostici `__DEV__` in `coach-clients-service.ts`/`clienti/index.tsx`. Verificati `tsc --noEmit` pulito, `git diff --check` pulito. Commit isolato `5d02112`. **BLOCCANTE fino all'esecuzione manuale**: la migration non è mai stata eseguita su un progetto Supabase reale in questo ambiente — il bug resta presente per l'utente finché non viene applicata (`supabase db push` o SQL editor). Vedi azione richiesta in cima a `docs/TODO_NEXT.md`.

**Aggiornamento corrente 8:** 2026-07-20 (continuazione 2) - feat inserimento carichi lato coach: `ExerciseSetLogger` (già condiviso col cliente) ora montato anche per il coach in `esercizi/[id].tsx` (nessun logger parallelo), e `schede/[id].tsx` abilita anche al coach il toggle di completamento per singolo esercizio (il completamento dell'intero workout era già raggiungibile via chip di stato). Bug reale trovato e corretto (BUG-037): per il coach, la schermata esercizio ignorava il `planId` in URL e mostrava di default l'assignment del primo cliente trovato in assoluto, non quello della scheda da cui si navigava — fix con `planMatchIndex`/`userSelectedAssignmentRef`. Gap di sicurezza server-side reale trovato e corretto: le RLS di `exercise_progress_history` verificavano relazione attiva e appartenenza scheda ma MAI lo stato di completamento (solo lato app), e `update_workout_session_progress` non rifiutava chiamate su sessioni già completate. Nuova migration `supabase/migrations/20260720190000_harden_exercise_progress_completion_lock.sql`: colonna `workout_exercise_id` su `exercise_progress_history` (mancava, impediva qualunque verifica server-side di completamento esercizio), funzione `exercise_progress_entry_writable()` usata da tutte le policy insert/update/delete, `update_workout_session_progress` ora rifiuta sempre su sessione completata (nessuna eccezione coach). `client-load-history.tsx`/`CoachLoadEntryForm` (form "Aggiungi carichi" preesistente) lasciato invariato — non è un logger parallelo, scrive nella stessa tabella/servizio. Verificati `tsc --noEmit` pulito, `git diff --check` pulito. Commit isolato `f6bb82f`. **BLOCCANTE fino all'esecuzione manuale**: la nuova migration non è mai stata eseguita su un progetto Supabase reale — la protezione server-side aggiuntiva resta inattiva finché non viene applicata. Nessun clic reale in browser eseguibile — 13 verifiche manuali richieste ancora da fare, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 9:** 2026-07-20 (continuazione 3) - fix BUG-038/BUG-039, contesto cliente nelle schede coach: segnalato dall'utente che, entrando dal dettaglio di un cliente specifico (es. Rita), la schermata esercizio mostrava comunque chip selezionabili di altri clienti (es. "Rita, luigi, luigi"), rischio concreto di inserire carichi/completare per il cliente sbagliato. Causa: in `esercizi/[id].tsx`, per il coach `assignments`/`allAssignments` non era mai filtrato per cliente — il fix precedente di BUG-037 (`planMatchIndex`) correggeva solo la selezione di default, non la presenza/selezionabilità degli altri clienti. Fix: `schede/[id].tsx` passa ora `clientId` esplicito e autorevole (da `plan.clientId`, RLS-scoped) oltre a `planId`; in `esercizi/[id].tsx`, nuovo `isCoachSessionContext` forza `assignments` a un solo elemento validato incrociando `planId`+`clientId` contro i `workoutPlans` in store, con blocco totale della schermata (`sessionContextInvalid`, bottone "Torna alla lista clienti") se la validazione fallisce — mai selezione automatica del primo cliente. Chip multi-cliente nascosti in questo contesto (restano invariati nel catalogo esercizi generico, `esercizi/index.tsx`). Rimosso il workaround `planMatchIndex`/`userSelectedAssignmentRef` di BUG-037, ora superfluo. Secondo gap trovato nell'audit di sicurezza esplicitamente richiesto (distinto, BUG-039): `update_workout_session_progress` verificava la relazione `coach_clients.status='active'` solo per il percorso cliente, non per quello coach — un coach poteva completare/modificare la sessione di un cliente sospeso/rimosso. Nuova migration `supabase/migrations/20260721090000_require_active_link_for_coach_session_progress.sql` aggiunge lo stesso controllo anche al percorso coach (`CLIENT_NOT_ACTIVE`, nessuna eccezione). Auditati e confermati già corretti, nessuna modifica: `clienti/[id].tsx`, `client-load-history.tsx`, `back-header.tsx`. Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito. Commit isolato `b1c7221`. **BLOCCANTE fino all'esecuzione manuale**: la nuova migration `20260721090000`, come le due precedenti (`20260720171500`, `20260720190000`), non è mai stata eseguita su un progetto Supabase reale — la protezione server-side aggiuntiva resta inattiva finché non viene applicata. Nessun clic reale in browser eseguibile — 8 verifiche manuali richieste ancora da fare, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 10:** 2026-07-20 (continuazione 4) - fix/feat BUG-040, libreria modelli scheda organizzata in cartelle: il tab globale "Schede" della bottom bar coach mostrava finora TUTTE le sessioni reali assegnate a TUTTI i clienti (nome cliente, stato, badge) — nessuna vera libreria di modelli esisteva, richiesto esplicitamente dall'utente. `public.workout_templates` era già presente da una migrazione precedente (2026-07-14) ma come scaffold morto (nessuna cartella, nessun esercizio proprio, zero schermate collegate, confermato via grep). Costruita la libreria completa: nuova migration `supabase/migrations/20260722090000_workout_template_folders_and_exercises.sql` aggiunge `public.template_folders` (cartelle/sottocartelle per coach, profondità illimitata), `workout_templates.folder_id`/`sort_order`, `public.workout_template_exercises` (stessa forma di `workout_day_exercises`, nessun livello "giorno"), e tre RPC: `save_workout_template` (upsert atomico, `coach_id` sempre da `auth.uid()`), `delete_template_folder` (`move_to_root`/`delete_all`, mai una cancellazione automatica silenziosa), `assign_workout_template_to_client` (copia indipendente in `workout_plans`, richiede esplicitamente `coach_clients.status='active'` — non `is_coach_for_client()`, più permissivo). `schede/index.tsx` sostituito integralmente; nuove route `schede/cartella/[folderId]`/`schede/modello/new`/`schede/modello/[templateId]`. "Assegna a cliente": solo clienti active, conferma esplicita a schermo dopo l'assegnazione, mai un'apertura automatica del logger. Verificato via grep che nessun file della libreria legge `useClientStore`/`clientId` tranne il dettaglio modello (solo per l'azione "Assegna a cliente", nessuna preselezione). Correzioni post-review dell'advisor: sostituito `is_coach_for_client()` con controllo esplicito `status='active'` nell'RPC di assegnazione; corretto il conteggio pre-eliminazione cartella da "solo figli diretti" a ricorsivo sull'intero sottoalbero; corretta l'etichetta del bottone "sposta" che sovrastimava cosa viene effettivamente spostato. Non toccati: logger carichi, sincronizzazione coach-cliente, lista clienti, sospensione/rimozione, RevenueCat, BIA, metriche, tema, migration già applicate, `schede/[id].tsx`/`schede/new.tsx` (schede reali), `schede/modelli/*` (i 7 modelli statici predefiniti, lasciati raggiungibili solo via URL diretto, nessun nuovo link). Effetto collaterale noto non corretto (fuori scope): `schede/[id].tsx` dopo l'eliminazione di una scheda reale fa ancora `router.replace('/schede')`, che ora atterra sulla libreria modelli invece che su un elenco di sessioni cross-cliente (nessun crash, nessun dato perso). Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 14 file. Commit isolato `9f18fd2`. **BLOCCANTE fino all'esecuzione manuale**: la nuova migration `20260722090000` non è mai stata eseguita su un progetto Supabase reale — l'intera libreria modelli (cartelle/RPC) non è operativa in produzione finché non viene applicata. Nessun clic reale in browser eseguibile — 12 verifiche manuali richieste ancora da fare, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 11:** 2026-07-21 - fix BUG-041, menu a tendina neri in tema chiaro: segnalato dall'utente che i pannelli Select/Picker (Seleziona esercizio, Gruppo muscolare, Attrezzatura, Difficoltà) si aprivano sempre neri/quasi neri anche in tema chiaro. Audit esplicito escluso il colorScheme del browser/OS su `<select>` nativo (ipotesi di partenza): l'app non usa mai `<select>` HTML né `@react-native-picker/picker`, né `global.css` né alcun layout impostano `color-scheme`. Causa reale trovata in `mobile/src/components/exercise-catalog-picker.tsx`: i componenti condivisi `DropdownList`/`MenuOption` (usati da tutti e quattro i menu segnalati, un unico punto) avevano `backgroundColor` con hex hardcoded (`#05090C`/`#173B16`) praticamente identici alla sola palette dark (`Colors.dark.background`/`backgroundSelected`), indipendenti dal tema attivo — bordo/testo dello stesso componente erano già corretti. Fix: sostituiti con `theme.background`/`theme.backgroundSelected`, stessi token già usati da `SelectorButton`/`MuscleMultiSelect` nello stesso file — nessun colore nuovo introdotto. Nessun altro Select/Picker/Dropdown trovato nell'app (verificato `ymove-exercise-picker.tsx`: nessun dropdown, solo campi testo; altri hex hardcoded — superadmin/index.tsx, muscle-map.tsx, web-phone-frame.tsx — sono badge/silhouette/mockup intenzionalmente scuri, non menu, non toccati). Nessun Picker nativo nel progetto: fix puro React Native, identico su Web/Android/iOS. Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 1 file. Commit isolato `179c104`. **Non verificato con clic reali** (nessun tool di automazione browser disponibile in questo ambiente) — 5 verifiche manuali richieste in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 12:** 2026-07-21 - feat completamento libreria professionale dei modelli workout: la libreria modelli (introdotta 2026-07-20, commit `9f18fd2`) non aveva alcun livello "giorno/workout" — `workout_template_exercises` collegava gli esercizi direttamente al modello, un programma reale ha invece sempre più sedute (Workout A/B/C). Tre commit isolati. **Commit 1** (`5aae09d`): nuova migration `supabase/migrations/20260723090000_workout_template_days_and_metadata.sql` — tabella `workout_template_days` (nomi neutri, mai giorni della settimana: nessuna colonna weekday di proposito, l'agenda resta fuori scope), `workout_template_exercises.template_day_id`, migrazione dati non distruttiva (ogni modello personale esistente riceve un "Workout A" con gli esercizi già presenti, ordine/serie/reps/recupero/note preservati), metadati professionali su `workout_templates` (goal/level/duration_weeks/sessions_per_week/estimated_session_minutes/equipment/location/training_style/muscle_focus/intensity/progression_notes/deload_week), distinzione sistema/personale (`is_system`, `coach_id` nullable solo per i modelli di sistema, CHECK di coerenza, `source_template_id`), RLS a 4 policy per tabella su `workout_templates`/`workout_template_days`/`workout_template_exercises` (lettura estesa ai modelli di sistema, scrittura mai su di essi), `rpe_rir` anche su `workout_day_exercises` con fix di `save_workout_plan` che lo preserva in update. `assign_workout_template_to_client` riscritta (`drop function` esplicito, richiesto dal cambio di return type `uuid`→`uuid[]`) per copiare TUTTI i giorni del modello in schede reali indipendenti. Propagato in `types/template-library.ts`, `workout-plan-service.ts`, `workout-template-form.tsx` (form multi-giorno), `schede/modello/{new,[templateId]}.tsx`, `template-library-screen.tsx` (sezioni "Professionali"/"La mia libreria"). **Commit 2** (`a7d53b4`): nuova migration `supabase/migrations/20260724090000_workout_template_system_programs.sql`, solo INSERT — popola 18 modelli di sistema completi (richiesti come "16" nel riepilogo ma la lista dettagliata forniva 18 nomi distinti, tutti realizzati) in 7 categorie (Dimagrimento, Massa muscolare, Ricomposizione corporea, Forza, Principianti, Casa, Performance): 62 giorni, 300 esercizi con ordine/serie/range reps/recupero/RPE-RIR/note tecniche. Ogni `exercise_id` verificato via script contro `mobile/src/data/exercise-library.ts` prima del commit (0 id inventati); nessuna serie prescritta al cedimento (verificato via grep). **Commit 3** (`b8523b5`): filtri combinabili e ricerca in `template-library-screen.tsx` (obiettivo/livello/sessioni-settimana/durata/attrezzatura/luogo/stile/focus-muscolare/intensità/sistema-personale/cartella, opzioni derivate dai dati mai hardcoded; cartella scoped solo a "La mia libreria", non nasconde mai "Professionali"); ricerca su nome+esercizio (nuovo campo `WorkoutTemplateSummary.exerciseIds`, la query di `listWorkoutTemplateSummaries` ora seleziona `exercise_id` invece del solo count) +match aggregato su goal/stile/focus/attrezzatura/luogo/intensità come "tag" liberi (nessuna colonna tag esiste in tabella, interpretazione esplicita). Verificati `tsc --noEmit` pulito dopo ogni commit, nessuno script lint configurato (no-op), `git diff --check` pulito, `supabase db push --dry-run` conferma le due migration in coda. **BLOCCANTE fino all'esecuzione manuale**: nessuna delle due nuove migration è mai stata eseguita su un progetto Supabase reale — la libreria professionale (giorni, metadati, 18 programmi) non è operativa in produzione finché non vengono applicate. Nessun clic reale in browser eseguibile — 12 verifiche manuali richieste, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 13:** 2026-07-21 (continuazione) - refactor: raggruppa i modelli professionali in una cartella. La radice `/schede` mostrava tutte e 18 le card dei modelli professionali in una sezione fissa, occupando la maggior parte della schermata prima delle cartelle/schede personali del coach — l'utente ha chiesto di nasconderle dietro una cartella. Soluzione interamente UI, nessuna migration (come richiesto esplicitamente, "preferire una soluzione UI... non creare una migration se la cartella può essere virtuale"): nuova cartella **virtuale** "Allenamenti professionali" — nessuna riga in `template_folders`, nessun `coach_id`, non rinominabile/eliminabile/spostabile — raggiunta da nuove route `schede/professionali/index.tsx` (goal=null, mostra 7 sottocategorie con conteggio) e `schede/professionali/[goal].tsx` (una sottocategoria). Le 7 sottocategorie raggruppano per il valore reale di `workout_templates.goal` (mai modificato): Dimagrimento, Massa muscolare, Ricomposizione corporea, Forza→"Forza e Powerbuilding" (etichetta UI, il goal salvato resta "Forza"), Principianti, Casa→"Allenamento a casa" (idem, goal resta "Casa"), Performance — mapping presentazionale verificato riga per riga contro i 18 record reali della migration `20260724090000`. `template-library-screen.tsx`: la radice non elenca più le 18 card per default, mostra la cartella virtuale (icona Sparkles, "N modelli completi") seguita da cartelle/schede personali; la ricerca dalla radice trova ancora modelli professionali (righe piatte "Professionali" quando ricerca/filtro attivi, mai annidate — selezionarle apre il dettaglio). **Bug trovato e corretto prima del commit (review esplicita)**: il filtro "Cartella" preesistente (salto rapido dentro "La mia libreria") condivideva lo stesso flag "ricerca/filtri attivi" della rivelazione modelli di sistema — selezionarlo da solo (nessun testo, nessun altro filtro) faceva sparire la cartella virtuale e riversava tutte le 18 card in radice. Corretto con un flag dedicato (`systemResultsActive`) che esclude esplicitamente `folderId`. Estratte come esportate da `template-library-screen.tsx` (riuso identico, nessuna duplicazione): `LibraryFilterBar` (ora con `showKindFilter`/`showFolderFilter`), `TemplateRow`, `matchesLibraryFilters`, `templateSearchHaystack`, `uniqueSorted`/`uniqueSortedNumbers`, tipi `LibraryFilters`/`LibraryFilterOptions`. Non toccati: contenuto dei 18 programmi/esercizi/giorni/metadati, `schede/modello/[templateId].tsx` (dettaglio/duplica/assegna, i modelli restano read-only), assegnazione cliente, copie personali, logger/storico, RLS, migration già applicate, cartelle/schede personali (`schede/cartella/[folderId].tsx`, invariato). Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 5 file. Commit isolato `16cb00a`. Nessun clic reale in browser eseguibile — 8 verifiche manuali richieste, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 14:** 2026-07-21 (continuazione 2) - feat: collega gli appuntamenti ai workout del cliente. Richiesto dall'utente: quando il coach crea un appuntamento, deve poter collegare il prossimo workout pianificato del cliente, senza giorni fissi della settimana e senza avvio/completamento automatico. Nuova migration `supabase/migrations/20260726090000_appointments_next_workout_link.sql` (non applicata a un progetto Supabase reale, solo `supabase db push --dry-run`): `workout_plans.sequence_order` (integer, popolato da `assign_workout_template_to_client` — `create or replace`, stessa firma — a partire da `workout_template_days.sort_order` al momento dell'assegnazione) e `appointments.workout_day_id` (uuid, fk `workout_days`, on delete set null) + indice; RLS estesa (stessa policy `appointments_coach_scope`, drop+create, non una nuova policy) con verifica che `workout_day_id` appartenga a un `workout_days` coerente con coach/client dell'appuntamento e, se presente anche `workout_session_id`, che il day appartenga esattamente a quel plan. Nuovo modulo puro `mobile/src/lib/workout-sequence.ts`: raggruppa le `workout_plans` reali di un cliente in "programmi" per `templateId` condiviso (Workout A/B/C creati insieme da una singola assegnazione), ordina per `sequence_order` con tiebreak sull'id (mai un indice locale/data/giorno della settimana), espone il primo workout non completato/saltato/annullato. `appuntamenti/new.tsx` riscritta: cliente limitato ai soli `active`, propone in automatico il primo workout pendente, azione "Cambia workout" per la scelta manuale, titolo autocompilato (nessuna colonna dedicata: default di UI), avviso non bloccante su doppio collegamento allo stesso workout. Nuova `appuntamenti/[id].tsx`: dettaglio/modifica, "Avvia workout" abilitato solo se il workout collegato è ancora `todo` e l'appuntamento non è annullato, naviga a `/schede/[id]` col solo UUID reale (mai un client id separato); annullamento/modifica non toccano mai `workout_plans`/`workout_days` (stato del collegamento derivato, non una colonna duplicata). `appuntamenti/index.tsx`: la riga apre ora il dettaglio appuntamento invece del profilo cliente. Propagati i nuovi campi in `types/training.ts`, `types/appointment.ts`, `appointment-service.ts`, `workout-plan-service.ts`. Non toccati: libreria modelli professionali, cartelle/filtri Schede, load logger, sospensione/rimozione, RevenueCat, BIA, metriche, tema, admin locale. Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito, `supabase db push --dry-run` conferma solo questa migration in coda (non eseguita realmente). Commit isolato `aba760b`. **BLOCCANTE fino all'esecuzione manuale**: la migration non è mai stata applicata a un progetto Supabase reale — il collegamento appuntamento↔workout non è operativo in produzione finché non viene applicata. Nessun clic reale in browser eseguibile — 12 verifiche manuali richieste, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 15:** 2026-07-21 (continuazione 3) - fix BUG-042, filtri esercizi aperti automaticamente: segnalato dall'utente che, durante creazione/modifica scheda, la sezione "Filtri" e il menu "Gruppo muscolare" risultavano già aperti all'ingresso della schermata di selezione esercizi, occupando gran parte dello schermo. Causa: in `mobile/src/components/exercise-catalog-picker.tsx` (unico componente, condiviso da `workout-plan-form.tsx` e `workout-template-form.tsx` — entrambi montano/smontano l'intero picker ad ogni apertura, `showPicker`/`pickerForDay` in `false`/`null` di default) gli stati iniziali erano `filtersOpen = useState(true)` e `openMenu = useState('group')` invece di chiusi. Fix: solo i due valori iniziali corretti in `useState(false)`/`useState(null)` — nessun'altra modifica. Il resto del comportamento richiesto era già corretto e verificato per lettura: selezionare un'opzione chiude già il menu (`setOpenMenu(null)` in ogni `MenuOption`), un solo menu resta aperto alla volta (`openMenu` è un singolo stato condiviso, non uno per menu), il valore selezionato resta visibile come testo sul `SelectorButton` anche a menu chiuso, query/filtri applicati non vengono toccati dal fix. Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 1 file. Commit isolato `8e1b853`. Nessun clic reale in browser eseguibile — 6 verifiche manuali richieste, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 16:** 2026-07-21 (continuazione 4) - fix BUG-043, modelli professionali "assegnati" ma invisibili nelle schede del cliente. Confermato per la prima volta in questa sessione, tramite `npx.cmd --yes supabase@latest migration list`, che TUTTE le migration locali risultano gia' applicate al progetto Supabase reale (colonna "remote" popolata per ognuna) e `db push --dry-run` conferma "Remote database is up to date" — quindi lo schema/RPC descritti nelle sessioni precedenti come "mai applicati" sono ora effettivamente live in produzione. Audit end-to-end confermato: `assign_workout_template_to_client` (gia' corretta, nessuna modifica necessaria) gestisce esplicitamente `is_system`, verifica `coach_clients.status='active'` via `auth.uid()` (mai fidandosi dell'app), copia correttamente ogni giorno del modello in `workout_plans`/`workout_days`/`workout_day_exercises` indipendenti, in un'unica transazione RPC (rollback automatico su eccezione). Causa reale trovata: `mobile/src/app/schede/modello/[templateId].tsx` non scriveva mai le schede appena create nello store locale `useTrainingStore` — l'unica fonte letta da `Clienti → [id] → Schede` — a differenza di `schede/new.tsx`/`schede/modelli/[id].tsx`, che gia' chiamano `addWorkoutPlan(result.data)` dopo un salvataggio riuscito. Fix: aggiunta la stessa chiamata `addWorkoutPlan` dopo l'assegnazione riuscita; aggiunta diagnostica `__DEV__` (templateId/is_system/clientId/risposta RPC/id piani creati/conteggio giorni-esercizi, mai un token) sia nel servizio sia nella schermata; "Vai al cliente" ora apre direttamente la tab Schede (nuovo parametro opzionale `tab` su `clienti/[id].tsx`, letto solo come stato iniziale) invece di atterrare sempre su Panoramica. Nessuna migration nuova necessaria (RPC gia' corretta). Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 3 file. Commit isolato `c3bb1f4`. Nessun clic reale in browser eseguibile — 10 verifiche manuali richieste, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 17:** 2026-07-21 (continuazione 5) - fix BUG-044, filtro "Petto" selezionato automaticamente nel catalogo esercizi: dopo il fix BUG-042 (commit `8e1b853`, menu chiusi all'apertura), l'utente ha segnalato che il catalogo mostrava comunque sempre solo gli esercizi del petto, con la pill "Petto x" gia' presente prima di qualunque scelta. Causa: in `exercise-catalog-picker.tsx`, `selectedGroup` non aveva mai uno stato "nessun filtro" (a differenza di `equipmentFilter`/`difficultyFilter`, entrambi con sentinel `'all'`) — partiva sempre da `firstGroup` (primo gruppo di `EXERCISE_GROUP_ORDER` con almeno un esercizio), che essendo 'petto' il primo elemento dell'array e quasi sempre non vuoto, risultava di fatto sempre "Petto". Fix: aggiunto lo stesso sentinel `'all'` (nuovo tipo `GroupFilterValue`), stato iniziale `'all'`; `groupExercises` mostra tutti gli esercizi quando `selectedGroup === 'all'`; la pill compare solo dopo selezione esplicita e la sua "x" ora azzera davvero il filtro (prima riapriva solo il menu); aggiunta un'opzione "Tutti i gruppi" nel dropdown, stesso pattern di "Tutte attrezzature"/"Tutte difficolta'". Verificati `tsc --noEmit` pulito, `git diff --check` pulito su 1 file. Commit isolato `eb50a77`. Nessun clic reale in browser eseguibile — 5 verifiche manuali richieste, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 18:** 2026-07-21 (continuazione 6) - fix BUG-045, layout titolo nella card scheda coach: l'utente ha segnalato che nella hero card del dettaglio scheda (`mobile/src/app/schede/[id].tsx`, vista coach) un titolo lungo tipo "Powerbuilding — Lower Hypertrophy" andava a capo su 3 righe disordinate accanto al blocco "N esercizi", sbilanciando la card (`planTitle` aveva `fontSize:29`/`lineHeight:35` con `numberOfLines={3}`). Fix mirato: `numberOfLines` 3→2 sul Text del titolo (gia' con `ellipsizeMode="tail"`, ora un titolo piu' lungo tronca invece di rompere su una terza riga); `planTitle` `fontSize` 29→21, `lineHeight` 35→26; `flexShrink` esplicito su `heroCopy`/`heroVisual` per garantire che sia sempre il titolo a restringersi, mai il blocco esercizi a larghezza fissa. Non toccata la hero card lato cliente (`clientPlanTitle`, schermata diversa, nessun "N esercizi"/nome cliente nel layout). Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 1 file. Commit isolato `58d2eea`. Nessun clic reale in browser eseguibile — 6 verifiche manuali richieste, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 19:** 2026-07-21 (continuazione 7) - protezione server-side completa delle schede completate (`session_status='completed'`), due commit isolati su richiesta esplicita di un audit mirato (non un bug segnalato dall'utente in UI). **Commit 1** (`618ac963`): `public.save_workout_plan` non controllava mai lo stato della sessione prima di scrivere nel ramo UPDATE (nome/date/esercizi/serie/ripetizioni/recupero/note) — a differenza di `update_workout_session_progress`, che lo fa gia' da due migration precedenti. Aggiunto controllo `WORKOUT_LOCKED` prima di qualunque scrittura nel ramo UPDATE (nessuna eccezione per il coach); nuovo trigger `workout_plans_prevent_completed_edit` (`BEFORE UPDATE`, non `security definer`) come difesa in profondita' contro un UPDATE diretto via PostgREST che bypassi la RPC. Migration `supabase/migrations/20260727090000_lock_completed_workout_plan_edits.sql`. **Commit 2** (`7c4f807a`): restava possibile modificare direttamente `workout_days`/`workout_day_exercises` (INSERT/UPDATE/DELETE) di un piano gia' completato. Riordinata `update_workout_session_progress` (nessuna logica cambiata, solo l'ordine delle scritture: prima `workout_day_exercises`, POI `workout_plans.session_status='completed'` come ultima scrittura — necessario perche' l'ordine precedente avrebbe fatto vedere ai nuovi trigger un genitore gia' completato nella stessa transazione); esteso il trigger di commit 1 da `BEFORE UPDATE` a `BEFORE UPDATE OR DELETE` (con ramo esplicito `return old` per DELETE, altrimenti un `return new` incondizionato in un trigger DELETE annulla silenziosamente qualunque cancellazione, anche legittima); due nuovi trigger `BEFORE INSERT OR UPDATE OR DELETE` su `workout_days`/`workout_day_exercises` (funzioni non `security definer`, risalgono sempre al piano genitore via `NEW/OLD.workout_plan_id`, controllano anche il piano di provenienza in caso di riassegnazione). Migration `supabase/migrations/20260728090000_lock_completed_workout_structure.sql`. Audit esplicito confermato: uniche tre funzioni che scrivono su `workout_days`/`workout_day_exercises` sono `save_workout_plan` (gia' protetta), `update_workout_session_progress` (riordinata) e `assign_workout_template_to_client` (crea sempre un piano NUOVO con `session_status='todo'`, non tocca mai un piano esistente). **Stato reale verificato con `npx.cmd --yes supabase@latest migration list` (non assunto)**: `20260727090000` risulta APPLICATA al progetto Supabase reale (colonna remote popolata); `20260728090000` risulta ANCORA NON APPLICATA (colonna remote vuota) — l'immutabilita' di `workout_days`/`workout_day_exercises` non e' ancora attiva in produzione, solo il blocco di `save_workout_plan`/UPDATE diretto su `workout_plans` lo e' gia'. Limite noto e documentato nella migration stessa: nessuna protezione equivalente aggiunta a un eventuale DELETE diretto su `workout_plans` stesso oltre a quanto gia' esteso dal trigger di commit 2. Verificati `tsc --noEmit` pulito, `git diff --check` pulito, `db push --dry-run` conferma la sola migration in coda per ciascun commit. Nessun clic reale/chiamata REST diretta eseguibile in questo ambiente — test funzionali ancora da fare, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 20:** 2026-07-21 (continuazione 8) - protezione route coach della libreria schede, commit isolato `89a9701`. Audit mirato ha rilevato che `AuthGate` (`mobile/src/components/auth-gate.tsx`, `COACH_ONLY_PREFIXES`) copriva `/schede/new` e `/schede/modelli` (plurale, i 7 modelli statici predefiniti) ma non `/schede/modello` (singolare — cartella diversa: `modello/new.tsx`, `modello/[templateId].tsx`), `/schede/professionali`, `/schede/cartella`: un cliente (con coach o autonomo) poteva aprire queste route della libreria coach via URL/deep link diretto. Fix: aggiunti i tre prefissi mancanti; aggiunto lo stesso guard locale gia' in uso in `modello/[templateId].tsx`/`modelli/index.tsx` (`isCoach` da `useAuthStore`, `CoachOnlyNotice` se non coach) anche a `schede/modello/new.tsx` e `schede/new.tsx`, come difesa in profondita' oltre al redirect di `AuthGate`. Verificato che `AuthGate` e' montato alla radice assoluta (`app/_layout.tsx`) e che il suo redirect per un cliente su un path coach-only e' sincrono al primo render (nessun flash della schermata protetta). Audit di tutte le route sotto `schede/modello/`, `schede/professionali/`, `schede/cartella/`: `professionali/index.tsx`, `professionali/[goal].tsx`, `cartella/[folderId].tsx`, `modelli/[id].tsx` non hanno un guard locale ma non sono realmente raggiungibili da un cliente dopo questo fix (stesso meccanismo di redirect sincrono di `AuthGate`) — non modificati, per non introdurre difese ridondanti. Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 3 file. Nessun clic reale in browser eseguibile — verifiche manuali ancora da fare, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 21:** 2026-07-21 (continuazione 9) - feat: storico carichi completo per singolo esercizio, commit isolato `0acbeb4`. La card "Storico pesi" nella schermata esercizio (`mobile/src/components/exercise-history.tsx`) mostrava l'intera lista di serie inline con un toggle "Mostra tutti i carichi"/"Mostra solo l'ultimo". Riscritta come card compatta, interamente `Pressable`: Ultimo carico, Miglior carico (rinominato da "Miglior peso recente" — rappresentava gia' il massimo storico, mai una finestra "recente"), data dell'ultima registrazione, stato vuoto "Nessun carico registrato" (verificato nel codice: nessun refuso "Nessun carico repleto" presente, ne' qui ne' altrove nell'app), indicazione "Vedi storico completo" con freccia. Nessuna lista/toggle inline residua: il tap sull'intera card apre lo storico completo su `/storico-carichi` (route esistente riusata, non duplicata), filtrato per clientId+exerciseId (mai workoutPlanId, cosi' compaiono anche sessioni passate/future dello stesso esercizio in schede diverse) tramite due nuovi parametri opzionali su `storico-carichi.tsx` e un nuovo filtro in `client-load-history.tsx`. La sessione corrente resta separata: il salvataggio dei carichi durante l'allenamento aperto continua a usare `workoutDayExerciseId` in `ExerciseSetLogger`, invariato. Ruoli: il cliente non puo' mai cambiare clientId da parametro URL (sempre `currentClientId` di sessione); il coach puo' aprire lo storico di un cliente solo se presente in `useClientStore` (RLS-scoped) — `/storico-carichi` non e' piu' client-only in `AuthGate` (la rimozione non tocca l'accesso del cliente, la classificazione blocca solo il ruolo sbagliato). Lo storico globale (menu Altro, nessun parametro) resta invariato. Le sessioni completate restano in sola lettura: il controllo di immutabilita' (`canManageEntry`) e' calcolato per singola serie sul proprio `workout_plan_id` reale (bug trovato e corretto prima del commit: non riusa piu' il piano della prima serie della sessione). Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 5 file. **Completata solo lato codice — NON verificata su Web/Android/iOS con clic reali** (nessun tool di automazione browser disponibile in questo ambiente): 7 scenari di test runtime restano da eseguire di persona, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 22:** 2026-07-21 (continuazione 10) - fix: usa la riga esatta della sessione esercizio (workoutDayExerciseId), commit isolato `08adfc9`. Richiesto esplicitamente dall'utente per irrobustire il fix precedente (BUG risolto in `52c06c9`, planId+exerciseId): con lo stesso esercizio inserito due volte nella stessa scheda (es. due varianti/superserie), il match per `exerciseId` trovava sempre la prima occorrenza del piano, non necessariamente la riga realmente aperta — rischio concreto di carichi loggati sulla riga sbagliata. Fix: `schede/[id].tsx` ora passa `workoutExerciseId` (= `WorkoutExercise.id`, corrisponde a `workout_day_exercises.id` reale) verso `/esercizi/[id]`, oltre a `planId`/`clientId`/`exerciseId` di sola validazione. In `esercizi/[id].tsx`, il blocco di narrowing del contesto sessione cerca ESCLUSIVAMENTE per `we.id === workoutExerciseId` dentro il piano indicato da `planId`, poi valida incrociando `exerciseId` e il `clientId` reale del piano; introdotta la variabile dedicata `sessionEntry` che elimina il fallback silenzioso `assignments[0]` in contesto sessione — se una qualunque validazione fallisce, `sessionContextInvalid=true` e la schermata mostra l'errore di blocco invece di montare `ExerciseSetLogger`. Navigazione sequenziale prev/next aggiornata per usare `workoutExerciseId` come chiave di posizione. Compatibilità verificata su tutti i 5 punti che aprono `/esercizi/[id]`: `workout-plan-form.tsx` corretto per NON passare più `planId` nel picker (gli esercizi in bozza locale hanno id temporanei `we-${Date.now()}-...`, mai una vera riga Supabase — prima di questo fix quel punto d'ingresso finiva comunque su `sessionContextInvalid`, nessuna regressione, solo un comportamento più coerente); `workout-template-form.tsx`/`esercizi/index.tsx` già corretti, non toccati (i template non sono sessioni reali, nessun `workoutDayExerciseId` inventato per loro, come vincolato esplicitamente dall'utente). Non toccati: database, `ExerciseHistory`/storico carichi globale (continua a usare clientId+exerciseId), completamento workout, RevenueCat, BIA, agenda. Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 3 file. Nessun clic reale in browser eseguibile — 8 scenari di test manuale richiesti esplicitamente dall'utente ancora da fare, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 23:** 2026-07-22 - fix BUG-046, migrazione locale dei workout ritenta piani remoti gia' completed. Segnalato dall'utente con stack trace reale: `WORKOUT_REMOTE_SAVE_ERROR`/`WORKOUT_LOCKED` in console ad ogni refresh, causato da `runMigration` (`mobile/src/lib/workout-plan-migration.ts`) che trattava come "candidati alla migrazione" anche piani gia' sincronizzati (id UUID reale) — lo store locale (`useTrainingStore`) viene sovrascritto ad ogni `refresh()` riuscito con l'intero elenco remoto (`setWorkoutPlans`), quindi piani gia' su Supabase ricomparivano tra i "locali" e, non essendo mai passati da questa procedura, non erano mai nella mappa persistita `migratedMap`; rimandati a `createWorkoutPlan`, il loro id reale finiva nel payload e `save_workout_plan` li trattava come UPDATE su una riga esistente, bloccata da `WORKOUT_LOCKED` se `completed`. Fix mirato: `candidates` esclude ora anche ogni piano con `isValidUuid(plan.id)` — verificato che l'unico generatore di id placeholder locali (`plan-${Date.now()}`, in `schede/new.tsx`/`schede/modelli/[id].tsx`) e' sempre dietro `!supabaseConfig.isConfigured`, quindi un id UUID prova sempre che la riga esiste gia' su Supabase, con qualunque `session_status` — nessuna interrogazione remota necessaria per escluderlo. Un piano realmente locale-only (mai sincronizzato, anche gia' completed offline) resta migrabile: id non-UUID implica sempre `id:null` nel payload, quindi sempre INSERT, mai raggiungibile da `WORKOUT_LOCKED` (trigger di immutabilita' solo `BEFORE UPDATE OR DELETE`). Aggiunto contatore diagnostico `skippedAlreadyRemote` e log `console.warn` solo `__DEV__`. Verificato per lettura che nessun percorso di scrittura reale (`schede/[id].tsx`) dipende da `runMigration` per sincronizzare edit su piani gia' remoti — ogni edit chiama `updateWorkoutPlanRemote`/`updateWorkoutSessionProgress` direttamente. Non toccati: trigger di immutabilita', `save_workout_plan`, `update_workout_session_progress`, migration gia' applicate, regola `WORKOUT_LOCKED`, database (nessun `db push`). Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 1 file. Commit isolato `3c5fe60`. Nessun avvio app reale eseguibile in questo ambiente — 10 scenari di test manuale richiesti esplicitamente, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 24:** 2026-07-22 (continuazione) - fix BUG-047, navigazione dopo l'eliminazione di una scheda cliente. Segnalato dall'utente: nel percorso Clienti → cliente → Schede → dettaglio scheda, eliminare la scheda reindirizzava a `/schede` — dalla migrazione della libreria modelli (2026-07-20, commit `9f18fd2`) quella route e' la libreria globale dei modelli del coach, non piu' un elenco cross-cliente, quindi il coach perdeva il contesto del cliente appena aperto. Fix mirato in `mobile/src/app/schede/[id].tsx`: nuova `goToClientSchede(clientId)` (stesso pattern di "Vai al cliente" gia' in uso in `schede/modello/[templateId].tsx`) → `router.replace({ pathname: '/clienti/[id]', params: { id: clientId, tab: 'schede' } })`, `clientId` sempre da `plan!.clientId` (mai da parametro URL, la schermata legge solo `id`), applicato sia al ramo locale sia al remoto; fallback documentato a `/clienti` (mai alla libreria modelli) se `clientId` fosse vuoto. Cancellazione fallita (incluso `WORKOUT_LOCKED`, gia' bloccato server-side dal trigger su `workout_plans`, migration `20260728090000`, non toccata): nessuna navigazione, errore reale mostrato, comportamento gia' esistente. Audit preliminare confermato: `/schede/[id].tsx` non gestisce mai template (solo schede reali); gli altri due contesti richiesti dall'utente (modelli personali del coach, modelli professionali di sistema non eliminabili) erano gia' corretti, non toccati. Verificato che nessun refresh remoto esplicito serve per aggiornare la lista schede del cliente (stesso store Zustand gia' aggiornato da `deleteWorkoutPlanLocal`) e che `useCoachClients` si autoalimenta al focus, indipendentemente dall'aver visitato prima `/clienti`. Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `git diff --check` pulito su 1 file. Commit isolato `10f0383`. Nessun clic reale in browser/app eseguibile in questo ambiente — 8 scenari di test manuale richiesti esplicitamente, checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 25:** 2026-07-22 (continuazione 5) - pulizia finale del progetto: rimosso interamente il flusso BIA (upload/parsing/revisione referti PDF), backup obsoleti, script superati, file di scratch. Cinque commit isolati. **Commit 1** (`8746335`): fix residuo salvato, `loadClientProfile` (`auth-service.ts`) non selezionava/mappava mai `profiles.avatar_preset` al login cliente — perso ad ogni nuovo login pur restando salvato su Supabase. **Commit 2** (`45dbc5e`): rimozione BIA — eliminata l'intera Edge Function `supabase/functions/parse-bia-report` (incluso il parser InBody120 mai committato), rimosse da `client-metrics-service.ts`/`client-metrics-panel.tsx`/`use-client-metrics.ts`/`types/client-metrics.ts` tutte le funzioni/tipi/UI BIA (upload, bucket `bia-reports`, revisione dati estratti); conservate intatte le metriche manuali (peso/altezza/BMI/composizione/idratazione/circonferenze incluse quelle degli arti, grafici, confronti, storico). Preservata e ricostruita una modifica valida trovata mescolata nello stesso lavoro non committato: `resolveMetricsActor` abilita l'auto-inserimento cliente delle proprie misure manuali (RLS gia' presente in `20260717100000_client_metrics_client_entry.sql`, applicazione al progetto reale da verificare), esposta tramite la nuova prop `allowClientActions` di `ClientMetricsPanel` usata da `metriche.tsx`. Colonna `client_measurements.bia_report_id`/valore `source='bia_pdf'` mai eliminati (dati storici, mai piu' scritti). **Commit 3** (`736b88b`): altro lavoro residuo valido, non collegato a BIA — card "Il mio profilo di allenamento" in `cliente-profilo.tsx` (legge `getCompletedClientOnboardingProfile()`, gia' esistente) + auto-dismiss del messaggio avatar. **Commit 4** (`3860ba9`): rimossi `mobile_backup_20260715/` (52M, copia obsoleta del progetto + un progetto Flutter estraneo annidato), `mobile_git_backup/` (1.5M, copia della struttura `.git`), `fix-workout-uuid.sql` (versione SUPERATA di `save_workout_plan` priva della protezione `WORKOUT_LOCKED` gia' applicata in produzione — pericoloso se rieseguito per errore), `mobile/src/components/status-dot.tsx` (componente orfano, zero riferimenti verificati), due file di scratch/audit di sessioni precedenti. `.gitignore` aggiornato: `design-references/` (materiale grafico locale ancora utile, non cancellato, solo escluso dal tracking), `web-build/`/`*.apk`/`*.aab`/`*.log` (nessuno presente ora, esclusione preventiva). Verificato e confermato NON legacy: `schede/modelli/*`/`schede/modello/*` (libreria professionale, attivamente referenziata) e l'intero cluster "vecchio design system" (`card.tsx`/`themed-text.tsx`, ancora in uso dalle schermate editor scheda/esercizio non migrate, deliberatamente deferito). Verificato e confermato NON rimovibili: `expo-glass-effect`/`expo-symbols`, mai importate da `mobile/src` ma dependencies dirette (non peer/dev/optional) di `expo-router` stesso, gia' in uso. **BLOCCATO**: ricreazione di `mobile/.env.example` — le impostazioni di permesso di questo ambiente bloccano qualunque scrittura sotto `mobile/.env*`, incluso un placeholder senza valori reali; contenuto proposto documentato in `docs/TODO_NEXT.md` per creazione manuale. `npx.cmd --yes supabase@latest functions list` conferma che `parse-bia-report` non risulta MAI stata deployata sul progetto reale (solo `send-temporary-credentials`/`ymove-exercises`/`revenuecat-webhook` sono ACTIVE) — nessuna azione remota necessaria oltre alla rimozione dal repository, gia' fatta. RevenueCat verificato invariato (`git diff` vuoto sui file toccati dai commit `b8a8b72`/`4f366e9` di questa sessione). Verificati `tsc --noEmit` pulito, nessuno script lint configurato (no-op), `expo-doctor` 19/20 (un solo check fallito, preesistente e non toccato: 8 pacchetti Expo con la patch version leggermente indietro rispetto a quella attesa dall'SDK 57 — nessuna azione richiesta da questo task), `npm ls --depth=0` pulito, `git diff --check` pulito. Nessun clic reale in browser/app eseguibile in questo ambiente — checklist test runtime in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 26:** 2026-07-22 (continuazione 6) - configurazione iOS del progetto (build EAS, TestFlight, App Store Connect), Android/Web invariati. Nessuna cartella `ios/` creata (Expo Managed/CNG, nessuna gia' presente). `mobile/app.json`: nuova sezione `ios` (`bundleIdentifier: "com.fitcoachapp.mobile"`, stessa reverse-domain di `android.package` mai toccato; `supportsTablet: false`; `config.usesNonExemptEncryption: false`; nessun `buildNumber` manuale, gestito da EAS remote versioning gia' attivo `cli.appVersionSource: "remote"`); plugin `expo-image-picker` con `cameraPermission: false` (fotocamera mai usata, solo `launchImageLibraryAsync`). `mobile/eas.json`: `submit.production.ios.ascAppId` con placeholder esplicito (dato Apple mancante, non inventato). Due gap di compliance App Store reali trovati (nessuna eliminazione account, nessun link privacy/termini) e risolti in questo task, non rimandati (discriminante: l'eliminazione account era nominata esplicitamente nella richiesta con vincolo preciso "non basta disattivare l'utente"): nuova Edge Function `supabase/functions/delete-account` (elimina realmente `auth.users` del chiamante via `auth.admin.deleteUser`, mai di terzi; ripulisce prima `client_notes`/`exercise_progress_history` dove `created_by` = chiamante, le uniche due tabelle con `on delete restrict` invece di `cascade` verso `profiles`, per non dipendere dall'ordine dei trigger FK Postgres; blocca esplicitamente il superadmin) + `deleteOwnAccount()` in `auth-service.ts` + pulsante "Elimina account" (doppia conferma) in `impostazioni.tsx` (coach) e `cliente-profilo.tsx` (reale schermata Impostazioni raggiunta dal cliente via `/altro`, diversa da `/impostazioni` che il cliente non raggiunge mai da nessun link). Link "Privacy policy"/"Termini di servizio" aggiunti in `developer-info-section.tsx` (condiviso coach/cliente) verso `PRIVACY_POLICY_URL`/`TERMS_OF_SERVICE_URL` in `constants/app-info.ts` — **URL placeholder `example.com`, nessun documento reale esiste ancora**, dato mancante da Luigi. Audit completo di compatibilita' iOS (Platform.OS Android-only, BackHandler, ToastAndroid, keyboard, safe area, video fullscreen, RevenueCat, webhook): nessun codice bloccante trovato, tutto gia' cross-platform corretto (confermato, non assunto, per lettura diretta di `revenuecat-service.ts`/`package-checkout-service.ts`/`app-screen.tsx`/`fullscreen-video-modal.tsx`). **Limite noto non risolto, documentato**: `NSMicrophoneUsageDescription` resta il testo inglese generico del plugin invece di essere rimossa — bloccare `microphonePermission` su `expo-image-picker` avrebbe rimosso anche `android.permission.RECORD_AUDIO` gia' dichiarato esplicitamente in `app.json`, violando il vincolo esplicito di non toccare configurazioni Android esistenti; richiede una decisione dedicata (verificare se RECORD_AUDIO serve ancora su Android) prima di poter ripulire anche lato iOS. Creato `IOS_SETUP.md` (root) con bundle id, scheme/callback Supabase da configurare a mano (redirect URL `mobile://` in Supabase Dashboard), variabili ambiente richieste (nuova `EXPO_PUBLIC_REVENUECAT_IOS_API_KEY`), checklist dati Apple mancanti, comandi EAS build/submit/TestFlight. Verificati `npx tsc --noEmit` pulito, `npx expo-doctor` 20/20 (nessun warning privacy manifest sui pacchetti installati), `npx expo config --type public`/`--type prebuild` corretti, nessuno script lint/test configurato nel progetto (no-op). **BLOCCANTE fino a intervento manuale di Luigi**: Edge Function `delete-account` scritta ma non deployata (nessun Supabase CLI in questo ambiente, stesso limite gia' noto per le altre Edge Function di questo progetto) — il pulsante "Elimina account" in app fallira' con un errore leggibile finche' non viene deployata; `ascAppId`/chiave RevenueCat iOS/URL privacy reali ancora tutti placeholder; nessuna build EAS iOS reale tentata (richiede account Apple Developer, non disponibile in questo ambiente). Nessun clic reale in app eseguibile in questo ambiente — checklist test in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 27:** 2026-07-22 (continuazione 9) - stabilizzazione pre-APK: chiusi i 4 finding reali dell'audit "Release Gate Android + iOS" (BUG-048/049/050/051), Web/Android/iOS restano compatibili, nessuna modifica a workout/metriche/RevenueCat/BIA/libreria professionale. Quattro commit isolati. **Commit 1** (`7192a36`, BUG-048): `mobile/src/app/altro.tsx` — il menu "Altro" mostrava sempre Bacheca/Prenotazioni anche a un cliente self_guided; filtrate con lo stesso pattern fail-closed gia' in uso in `cliente-home.tsx` (`isCoachGuided = useMyCoach().clientMode === 'coach_guided'`, mai un coachId dedotto, nascoste finche' `coach_guided` non e' confermato). Deep link diretti restano bloccati da AuthGate, invariato. **Commit 2** (`6ba0bcb`, BUG-049): nessuno store Zustand persistito veniva azzerato al logout oltre ad `auth-store` — aggiunta un'azione `reset()` a `client-store`/`training-store`/`superadmin-store`/`chat-store`/`appointment-store`/`board-store`/`booking-store`/`checkin-store`/`attachment-store`/`ymove-autolink-store` (10 store), nuova `resetUserScopedStores()` in `auth-store.ts` chiamata dentro `logout()` prima di azzerare i propri campi — stessa chiamata sincrona, un solo re-render React batchato. `theme-store`/`training-store.soundSettings` intoccati (preferenze del dispositivo). Le sottoscrizioni Realtime non richiedono disiscrizione manuale: dipendono gia' da `currentRole` nei rispettivi `useEffect` e si disiscrivono da sole subito dopo `logout()`. Nessuna modifica ai 9 punti di logout esistenti (chiamano gia' tutti `logout()`). **Commit 3** (`4ebc779`, BUG-050): `developer-info-section.tsx` — i link Privacy/Termini (aggiunti nella sessione iOS precedente) chiamavano `Linking.openURL` senza gestione errori; nuova `openLegalLink()` con verifica URL/`canOpenURL`/try-catch/messaggio utente chiaro (stesso `notify()` gia' in uso altrove). URL placeholder non sostituiti (dato esterno, invariato). **Nessun commit di codice per il finding 4** (BUG-051): audit di sicurezza della Edge Function `send-push` (locale, mai deployata, usata da chat/appuntamenti) confermato gia' corretto — identita' solo da JWT, destinatario risolto sempre da `coach_clients` reale, self_guided/suspended/removed correttamente esclusi, nessun token esposto, nessun secret custom mancante — deployata con `npx supabase functions deploy send-push` (JWT verification attiva di default, nessun `--no-verify-jwt`), confermata `ACTIVE`/`verify_jwt=true`. **Scoperta rilevante non presente nei finding**: `expo-doctor` e `expo install --check` sono passati da 20/20 a 19/20 (13 pacchetti Expo con patch version leggermente indietro) **tra una verifica e l'altra della stessa sessione**, senza alcuna modifica a `package.json`/`package-lock.json` da parte di questo task (confermato `git diff --stat` vuoto su entrambi) — drift del registro npm upstream nel tempo trascorso, non una regressione introdotta qui; nessuna azione eseguita (vietato installare/aggiornare pacchetti in questo task). Verificati per ogni commit `tsc --noEmit` pulito, `git diff --check` pulito, `git status --short` vuoto; `db push --dry-run` conferma "Remote database is up to date"; `functions list` conferma 5 funzioni `ACTIVE` (`send-temporary-credentials`, `ymove-exercises`, `revenuecat-webhook`, `delete-account`, `send-push`). Nessun clic reale in app eseguibile in questo ambiente — checklist test runtime aggiornata in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 28:** 2026-07-23 - Client Pro, abbonamento self_guided collegato a RevenueCat. Audit preliminare ha trovato che l'infrastruttura esisteva gia' quasi per intero (schema `subscription_packages.target_role`, gate `auth-gate.tsx`, costanti entitlement/offering `client_pro`/`client_plans`, SDK RevenueCat gia' generico): il lavoro reale e' stato collegare il checkout (`package-checkout-service.ts` non blocca piu' `targetRole==='client'`), estendere il webhook (`resolvePackageId` non filtra piu' `target_role='coach'`, sicuro perche' gli unique index sui product id sono gia' globali), ricostruire `abbonamento-cliente.tsx` (ordinamento MONTHLY→THREE_MONTH→ANNUAL, badge "Piu conveniente", risparmio calcolato solo se prezzo+periodo+valuta coincidono, mai hardcoded, stati completi) e correggere `pacchetti-cliente.tsx` (prezzo dallo store, non piu' dal DB). **Trovato e risolto BUG-052**: un self_guided senza piano attivo non poteva raggiungere `/cliente-profilo` (impostazioni/eliminazione account) — vedi `docs/BUGS.md`. Nessuna nuova migration (schema gia' sufficiente); i 3 pacchetti Client Pro vanno creati da Luigi nel pannello superadmin esistente, valori esatti documentati in `docs/CLIENT_SUBSCRIPTIONS_SETUP.md` (nuovo). Test aggiunti solo per il webhook (Deno, gia' in uso) — nessun test runner lato mobile (non installato, vietato dal task). Verificati `tsc --noEmit` pulito, `expo-doctor` 19/20 (drift pre-esistente, non causato da questo task), `npm ls` pulito, `git diff --check` pulito, `supabase migration list` allineato, `db push --dry-run` "up to date". Nessun clic reale eseguibile in questo ambiente — checklist test runtime in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 29:** 2026-07-24 - uniformate e professionalizzate tutte le email FitCoach. Audit: unica email reale da codice era `send-temporary-credentials` (Brevo, testo semplice senza layout); email Supabase Auth realmente usate: solo Confirm signup e Reset Password (le altre 4 con slot Dashboard reale — Invite user/Magic Link/Change Email/Reauthentication — mai usate da nessun codice). Creato `supabase/functions/_shared/email-template.ts` (design system unico, table-based, CSS inline, escape HTML/URL sicuri, versione testo semplice, `color-scheme: light` forzato per la dark mode). **Decisione presa con l'utente (AskUserQuestion)**: `send-temporary-credentials` non genera più una password in chiaro — usa `auth.admin.generateLink('recovery')`, riusando la schermata `/reimposta-password` già esistente (stesso meccanismo di "Password dimenticata"); nessuna modifica al funzionamento dei link di conferma Supabase. Creati 10 template HTML pronti per Dashboard Supabase + README in `docs/email-templates/` (i 4 di notifica sicurezza — password/email modificata, metodo di accesso collegato/rimosso — sono etichettati esplicitamente "nessuno slot Dashboard Supabase esiste per questi", non spacciati per copiabili). Nuovo `docs/EMAIL_SMTP_SETUP.md` (audit Brevo, checklist SPF/DKIM/DMARC/dominio per Luigi). Trovato e documentato (non corretto, per non cambiare il funzionamento dei link) BUG-053: `signUpClientWithCoachCode` non passa `emailRedirectTo`. Test Deno scritti per il componente condiviso, non eseguiti (CLI Deno assente in questo ambiente). Verificati `tsc --noEmit` pulito, `npm ls` pulito, `git diff --check` pulito. Nessun invio email reale eseguibile in questo ambiente — checklist test runtime in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 30:** 2026-07-24 (continuazione) - fix: tema chiaro come default, selettore tema invariato. Richiesto esplicitamente dall'utente: nuove installazioni e utenti senza preferenza salvata devono aprire l'app in tema chiaro, senza rompere la scelta gia' salvata da chi ha scelto scuro. Causa: `mobile/src/store/theme-store.ts` aveva `mode: 'dark'` come stato iniziale dello store Zustand/AsyncStorage (`coachdesk-theme-store`) — su storage vuoto (prima installazione) questo valore restava definitivo, non un semplice flash temporaneo. Fix mirato a un solo file: default cambiato a `mode: 'light'`; aggiunta un'opzione `merge` a `persist()` che valida in un solo passaggio (nessun doppio render) il `mode` proveniente da AsyncStorage contro i tre valori validi (`light`/`dark`/`system`), con fallback a `light` per preferenze vecchie/corrotte/non valide — nessun pattern `migrate` introdotto (non esiste altrove nel progetto). Non toccati: `useEffectiveColorScheme` (gia' corretto: legge il `mode` persistito e ricade sul tema di sistema solo per la scelta esplicita `'system'`), `theme-settings.tsx` (selettore chiaro/scuro/sistema, invariato), `use-color-scheme.web.ts` (gia' con fallback `light` proprio durante l'hydration web), `AnimatedSplashOverlay` (overlay opaco che gia' copre l'avvio nativo, invariato), `app.json` (`userInterfaceStyle: "automatic"` lasciato invariato, non impedisce la scelta dark), `auth-store.ts` (conferma per lettura: `theme-store` e' gia' escluso di proposito da `resetUserScopedStores()`, il logout non tocca la preferenza tema). Verificati `tsc --noEmit` pulito, `expo-doctor` 20/20, `npm ls --depth=0` invariato, `expo config --type public` valido, `git diff --check` pulito su 1 file. Commit isolato (vedi WORKLOG). **Test runtime Android/iOS/Web ancora da eseguire** (nessun tool di automazione app/browser disponibile in questo ambiente): prima installazione, storage vuoto, preferenza `light`/`dark`/`system` salvata, valore persistito invalido, logout, cambio account, kill/reopen, sistema operativo scuro senza preferenza salvata.

Snapshot dello stato attuale del progetto. Questo file va **sovrascritto** ad ogni aggiornamento (non è un log): riflette sempre lo stato presente, non la storia. Per la storia vedi `WORKLOG.md`.

**Ultimo aggiornamento:** 2026-07-13 (continuazione 8) — fix bug APK Android BUG-023/024/025/026: `AnimatedSplashOverlay` (residuo template Expo, absoluteFill zIndex 1000 senza pointerEvents, causa GLOBALE dei tocchi persi su pulsanti/tab in tutta l'app) ora `pointerEvents="none"` + timeout di smontaggio; bottom bar superadmin ad altezza dinamica (contenuto+inset) e griglia dashboard con hook condiviso `use-two-column-grid`; seed demo (clienti Marco/Giulia/Luca, schede, chat, bacheca) dietro `EXPO_PUBLIC_ENABLE_DEMO_DATA=true` (`lib/demo-data.ts`, purge dei seed persistiti, badge "Demo"); tastiera registrazione Android (KeyboardAvoidingView opt-in su AppScreen, keyboardShouldPersistTaps ovunque, returnKeyType "Fine"/"Avanti" con chaining). Precedenti — fix BUG-020/021/022: griglia "Azioni rapide"/statistiche della dashboard coach misurata via onLayout (niente più sovrapposizione su Android), libreria YMove ora carica esercizi subito all'apertura con miniature lazy (concorrenza limitata a 2) e fallback `name`→`title`, pannello superadmin "Account coach" con dati demo dietro `EXPO_PUBLIC_ENABLE_DEMO_DATA` esplicito e gestione errore/refresh reale; fix BUG-019 — "invalid input syntax for type uuid" su id locali testuali/numerici nella migrazione schede, corretto con validazione UUID sicura lato mobile e SQL; migrazione schede/allenamenti da Zustand/AsyncStorage a Supabase, progressiva e non distruttiva; associazione automatica dei video YMove agli esercizi storici/custom, sostituisce la ricerca manuale come flusso principale; fix BUG-018 — autorizzazione cliente corretta nell'azione detail per un video YMove collegato non importato; fix BUG-017 — card esercizio in scheda mostra ora la thumbnail live YMove invece del placeholder con l'iniziale; cambio provider traduzione YMove: DeepL → Azure Translator; fix BUG-015 — testi italiani YMove ora per-coach, non più "wiki-style"; fix BUG-016 — esercizio YMove spariva dalla scheda dopo salvataggio; estensione YMove traduzione+video su esercizi esistenti; integrazione catalogo esercizi YMove opzione B; modifica coach reale nel pannello superadmin; limite clienti coach collegato all'abbonamento; pacchetti acquistabili coach/cliente; provider email Brevo)

## Fase del progetto
Sviluppo MVP. Dopo il lato Coach/Admin e il lato Cliente (sessioni precedenti), il 2026-07-08 è stato costruito un intero pannello **Superadmin**, i flussi di **login/registrazione ufficiali** (coach e cliente, con codice coach e dati di fatturazione) e lo **schema Supabase completo**, poi collegato davvero (Fase 1: client Supabase reale, `auth-service.ts`, dual-write locale+Supabase). Il 2026-07-09 l'utente ha collegato un progetto Supabase reale e testato a mano registrazione/login, trovando due bug reali risolti nella stessa sessione. Il 2026-07-09 (continuazione) è stata completata la **Fase 2 — email vere Supabase**: conferma registrazione, password dimenticata e reset password collegati a `resetPasswordForEmail`/`updateUser`, con un bug reale trovato e corretto (BUG-007: bypass della conferma email tramite il fallback di login locale). **2026-07-10 — fix BUG-008**: l'utente ha attivato "Confirm email" su un progetto reale e trovato che `profiles`/`client_profiles`/`coach_clients` (e per il coach `coach_profiles`/`billing_profiles`/`registration_codes`) restavano vuote dopo la registrazione, pur con l'utente creato in Auth e l'email inviata. Causa duplice: (1) `auth-service.ts` scriveva queste tabelle subito dopo `signUp`, ma senza sessione attiva (Confirm email pendente) la RLS le bloccava e i dati della form andavano persi; (2) mancavano le policy `profiles_self_insert`/`profiles_self_update`, quindi nessun fallback client-side era possibile se il trigger `handle_new_user` non aveva creato la riga `profiles`. Primo fix: i dati di onboarding sopravvivono in `user_metadata` al momento di `signUp` e vengono completati al primo login reale (`ensureCoachOnboarding`/`ensureClientOnboarding`, `auth-service.ts`, chiamate da `login-screen.tsx`). **2026-07-10 (continuazione) — trigger SQL definitivo**: su richiesta esplicita dell'utente, `handle_new_user()` è stato esteso per creare TUTTE queste tabelle direttamente `after insert on auth.users` (security definer, bypassa la RLS, non ha bisogno di sessione), indipendentemente da "Confirm email" — non serve più attendere il login per completarle; `ensureCoachOnboarding`/`ensureClientOnboarding` restano come rete di sicurezza idempotente secondaria. Vedi `docs/BUGS.md` (BUG-008), `docs/DECISIONS.md` (voci 2026-07-10), `docs/EMAIL_SETUP.md` (snippet SQL aggiornato da eseguire). **Non ancora verificato con un progetto Supabase reale** (nessun accesso diretto in questo ambiente) — l'utente deve eseguire lo snippet SQL aggiornato e ripetere una registrazione/conferma/login completa. **2026-07-10 (continuazione) — nuovo design system**: creato un design system unico (`mobile/src/theme/`, palette cream/moss/coral/amber/rust ricavata da un mockup di riferimento fornito dall'utente) e 12 componenti UI riusabili (`mobile/src/components/ui/`), applicati a login, tab bar coach/cliente, shell superadmin e alle tre dashboard principali (cliente/coach/superadmin). Sistema parallelo a `constants/theme.ts`, che resta in uso nelle schermate non ancora migrate. **2026-07-11**: migrazione al nuovo design system estesa a praticamente tutte le schermate cliente/coach/superadmin (fix di un bug reale di hydration React, icone reali `lucide-react-native` al posto delle emoji, tab bar web precedentemente non aggiornate) — resta fuori solo il cluster editor scheda/esercizio. Aggiunto anche il **primo pezzo reale di video esercizi**: video guida remoto (`Exercise.videoUrl`) in fase 1, poi **video reali via Supabase Storage** in fase 2 (bucket `exercise-videos`, tabella `exercise_videos`, upload lato coach, visualizzazione lato cliente scoped al proprio coach). Vedi sezioni dedicate sotto, `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/SUPABASE_STORAGE_VIDEO.md`.

## Tipo di progetto
App mobile iPhone — **FitCoach Pro** (nome definito in `mobile/src/constants/app-info.ts`; "CoachDesk" nome di lavoro superato — vedi `docs/DECISIONS.md`), Expo + React Native + TypeScript. Non è una webapp/PWA. Proprietario/sviluppatore: Luigi Marrano.

> Nota: `PRODUCT.md`/`DESIGN.md` nella root del repo descrivono ancora un progetto precedente (webapp B2B gestione progetti/task) e non sono stati aggiornati — decisione rimandata dall'utente. Non usarli come fonte di verità per FitCoach Pro.

## Stack tecnologico
Expo SDK 57 (managed) + React Native 0.86 + TypeScript + Expo Router (file-based, cartella `mobile/src/app`). Stato applicativo Zustand con persistenza locale su AsyncStorage — resta la fonte di lettura per quasi tutta l'app (schede, appuntamenti, chat, superadmin dashboard). Video locali via `expo-video`, audio via `expo-audio`. **Backend Supabase Fase 1 collegato**: `@supabase/supabase-js` + `react-native-url-polyfill` installati, client reale in `mobile/src/lib/supabase.ts` (creato solo se `EXPO_PUBLIC_SUPABASE_URL`/`EXPO_PUBLIC_SUPABASE_ANON_KEY` sono presenti in `mobile/.env`, altrimenti resta `null` senza crash), servizio auth in `mobile/src/lib/auth-service.ts` (`signUpCoach`, `signUpClientWithCoachCode`, `signInWithEmail`, `signOut`, `getCurrentSession`, `requestPasswordReset`, `updatePassword`, `ensureProfileForCurrentUser`, `ensureCoachOnboarding`, `ensureClientOnboarding`, `loadClientProfile`). Schema SQL completo in `docs/SUPABASE_SCHEMA.md`/`.sql`, aggiornato con trigger `handle_new_user`, funzione `increment_registration_code_usage`, e le policy `profiles_self_insert`/`profiles_self_update` (vedi `docs/DECISIONS.md`). **Design Fase 1: dual-write** — quando Supabase è configurato, la registrazione scrive sia su Supabase sia (in parallelo) sugli store Zustand locali esistenti, perché il resto dell'app non è stato migrato e legge ancora solo da lì. Preview di sviluppo su web via `expo start --web`, layout incorniciato in stile iPhone (`WebPhoneFrame`, solo su web). Dettagli completi in `docs/ARCHITECTURE.md` (da verificare se aggiornato con la parte superadmin/Supabase).

## Ambienti
- Sviluppo: preview web locale (`http://localhost:8081`, Chrome su PC) via `npx.cmd expo start --web`. Typecheck (`npx.cmd tsc --noEmit`) e `npx.cmd expo-doctor` (20/20) puliti al termine di questa sessione (2026-07-08, dopo il collegamento Supabase Fase 1).
- Expo Go su iPhone/Android reale: non affrontato in questa sessione, stato invariato rispetto a prima (vedi `docs/BUGS.md`).
- Supabase: **progetto reale collegato dall'utente** (`mobile/.env` configurato, non presente in questo ambiente di sviluppo isolato — verificato solo indirettamente dai log di `expo-doctor`/`expo start` che mostrano `env: load .env`). Registrazione coach, registrazione cliente e login testati a mano dall'utente con esito positivo dopo i due fix di questa sessione (2026-07-09).
- Staging/Produzione: non previste in questa fase.

## Funzionalità completate (verificate solo via typecheck/expo-doctor, NON con clic reali in browser — nessun tool di automazione browser disponibile in questo ambiente)

### Login ufficiale, registrazione coach/cliente, fatturazione coach, superadmin (2026-07-08)
- **Login** (`mobile/src/components/login-screen.tsx`): solo email/password/Accedi, nessun esempio demo mostrato in UI (gli account demo interni restano solo come credenziali di fallback, non visibili). Link "Registrati come coach", "Registrati come cliente", "Password dimenticata" (quest'ultimo disabilitato, non finto: nessun comportamento nascosto).
- **Registrazione coach** (`mobile/src/components/registration-screens.tsx`, `CoachRegistrationScreen`, montata da `AuthGate` sul pathname `/registrazione-coach` — il file route `mobile/src/app/registrazione-coach.tsx` è uno stub `return null` intenzionale, mai renderizzato): nome/cognome, email, password, **conferma password** (aggiunta in questa sessione, mancava), telefono opzionale, nome attività opzionale, più sezione "Dati attività e fatturazione" (tipo soggetto, ragione sociale, P.IVA, codice fiscale, indirizzo, CAP, comune, provincia, nazione, PEC, codice SDI con placeholder `0000000`, email fatturazione). Validazione: ragione sociale/nazione/email fatturazione obbligatori; se nazione Italia e P.IVA presente, richiede PEC o SDI. Genera codice coach univoco `FC-XXXX-XXXX` (`lib/coach-code.ts`), lo salva nel profilo (`useSuperadminStore.createCoach`) e crea l'account di login (`useAuthStore.addCoachAccount`). Schermata finale "Il tuo codice coach" con pulsante "Copia codice" (rinominata da "Codice coach" in questa sessione per matchare il testo richiesto).
- **Registrazione cliente** (`ClientRegistrationScreen` nello stesso file, pathname `/registrazione-cliente`): codice coach obbligatorio, nome/cognome, email, password, conferma password. Verifica codice via `findCoachByCode`; blocca la registrazione se codice non valido o se `canCoachAcceptClients` (`lib/coach-code.ts`) risulta negativo (codice disattivato, coach bloccato/pagamento scaduto/annullato, piano non attivo, o limite clienti del piano raggiunto). Se valido: crea `Client` (collegato a `coachId`/`linkedByCode`), crea `ClientAccount` con la password scelta dall'utente (non temporanea, `mustChangePassword: false`), registra la relazione in `useSuperadminStore.addCoachClient` (incrementa anche `clientsUsed` del coach) e fa login automatico verso `/cliente-home`.
- **Superadmin** (`mobile/src/app/superadmin/*`): dashboard (`index.tsx`), lista/dettaglio/nuovo coach (`coaches/{index,[id],new}.tsx`), piani (`plans/{index,[id]}.tsx`), notifiche (`notifications.tsx`), eventi di pagamento (`payment-events.tsx`), supporto (`support/{index,[coachId]}.tsx`). Il dettaglio coach (`coaches/[id].tsx`) mostra codice coach, stato codice, dati fatturazione completi, pulsanti "Rigenera codice" e "Attiva/Disattiva codice", oltre a modifica manuale piano/stato pagamento/limite clienti e blocco/sblocco. Store dedicato `store/superadmin-store.ts` (persistito, dati demo seed per 4 coach di esempio) con notifiche automatiche generate sugli eventi principali (nuovo coach, piano cambiato, coach bloccato/sbloccato, pagamento scaduto).
- **Filtri stato coach** (`superadmin/coaches/index.tsx`): chip Tutti/Attivi/In prova/Scaduti/Bloccati/Annullati, compatti (minHeight 32, padding ridotto), ordinati su due righe su schermi 360–430px, colore rosso per il filtro attivo (fix di sessione precedente, verificato di nuovo in questa continuazione).
- **Schema Supabase** (`docs/SUPABASE_SCHEMA.md`, `docs/SUPABASE_SCHEMA.sql`): schema SQL completo con tutte le tabelle previste (`profiles`, `coach_profiles`, `client_profiles`, `coach_clients`, `registration_codes`, `billing_profiles`, `plans`, `coach_billing`, `payment_events`, `invoices` — ora con `xml_url` oltre a `pdf_url`, aggiunto in questa sessione —, `invoice_items`, `subscriptions`, `appointments`, `messages`, `admin_notifications`, `push_tokens`), check constraint SQL reale per "Italia + P.IVA → PEC o SDI obbligatori", policy RLS iniziali descritte (da validare su un progetto reale), e nuova sezione "Email future (Supabase Auth)" (aggiunta in questa sessione: conferma registrazione, reset password, invito cliente, conferma cambio email — nessuna email reale parte oggi).

### Collegamento Supabase Fase 1: registrazione/login reali (2026-07-08, continuazione)
- **Client Supabase reale** (`mobile/src/lib/supabase.ts`): `createClient` da `@supabase/supabase-js`, sessione persistita su AsyncStorage, creato solo se `EXPO_PUBLIC_SUPABASE_URL`/`EXPO_PUBLIC_SUPABASE_ANON_KEY` sono presenti — altrimenti `supabase` resta `null`, nessun crash. `mobile/.env.example` creato (`.gitignore` root aggiornato con `!.env.example`/`!mobile/.env.example`, altrimenti il pattern `.env.*` li avrebbe esclusi da git).
- **Servizio auth reale** (`mobile/src/lib/auth-service.ts`): `signUpCoach`, `signUpClientWithCoachCode`, `signInWithEmail`, `signOut`, `getCurrentSession`. Nessuna funzione lancia eccezioni: ritornano sempre `{ ok: false, code, message }` se Supabase non è configurato o in caso di errore (validazione codice non valido, coach bloccato/limite raggiunto, email già registrata, ecc.).
- **Registrazione/login dual-write** (`registration-screens.tsx`, `login-screen.tsx`): se Supabase è configurato, i dati reali vengono scritti su Supabase Auth + Postgres **e anche** mirrorati negli store locali Zustand esistenti (perché dashboard coach, superadmin, schede, chat non sono ancora migrati e leggono solo da lì); se non è configurato, il flusso resta identico a prima (100% locale, invariato). `signOut()` collegato ai 4 punti di logout esistenti (impostazioni, profilo cliente, cambio password, shell superadmin).
- **Schema aggiornato** (`docs/SUPABASE_SCHEMA.sql`): `profiles.id` ora referenzia `auth.users(id)`; nuovo trigger `handle_new_user()` (security definer) che crea la riga `profiles` dai metadata di `signUp`; nuova funzione `increment_registration_code_usage()` (security definer) per non dare policy UPDATE aperte su `registration_codes`; nuove policy self-insert/lettura pubblica minime per far funzionare la validazione codice coach prima che il cliente abbia un account. Limiti noti e documentati in `docs/SUPABASE_SCHEMA.md`/`.sql` (vedi anche `docs/DECISIONS.md`): validazione lato app non ancora tramite RPC atomica server-side; "Confirm email" va disattivato su Supabase perché il flusso Fase 1 funzioni subito dopo la registrazione; limite clienti reale basato su `registration_codes.max_uses` (non su `plans`, diversamente dalla logica locale demo).
- **Email**: `docs/EMAIL_SETUP.md` creato — spiega cosa configurare su Supabase (Confirm email, redirect URL, SMTP) perché le email di conferma/reset partano davvero. Nessuna email reale è mai stata inviata in questa sessione (nessun progetto Supabase reale disponibile).
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun test contro un progetto Supabase reale (nessuna chiave disponibile in questo ambiente), nessun clic reale in browser di questo flusso.

### Fix su collegamento Supabase reale, trovati con uso reale (2026-07-09)
- **Fix 1 — verifica codice coach**: `signUpClientWithCoachCode` (`auth-service.ts`) falliva con `"Invalid path specified in request URL"` per spazi non gestiti nella lista colonne di `.select('id, coach_id, status, ...')` (problema di networking nativo React Native/Expo, non riproducibile su `fetch` browser puro). Corretto rimuovendo gli spazi e spostando il filtro `status = 'active'` dentro la query. L'incremento di `used_count` ora usa un `update()` diretto invece della RPC `increment_registration_code_usage` (più semplice da far funzionare su un progetto reale); aggiunta la policy `registration_codes_increment_usage` (`docs/SUPABASE_SCHEMA.sql`) perché un cliente (non proprietario del codice) possa comunque incrementarlo.
- **Fix 2 — profilo cliente non trovato al login**: la home cliente mostrava "Nessun profilo cliente collegato a questo account" quando il login avveniva su un device/browser diverso da quello di registrazione (AsyncStorage web e Expo Go non condividono storage — il mirror locale creato in registrazione non esisteva altrove). Nuova funzione `loadClientProfile(userId, email)` in `auth-service.ts`: legge `profiles`+`client_profiles`+`coach_clients` (+ `coach_profiles.business_name` best-effort) da Supabase e ricostruisce un `Client` locale usando l'id reale Supabase. `login-screen.tsx` la chiama sempre (non solo se manca il mirror locale) prima di considerare riuscito il login cliente via Supabase, e fa upsert nello store locale; se `client_profiles`/`coach_clients` mancano davvero, il login viene bloccato con un messaggio specifico ("Profilo cliente non completato. Contatta il coach o l'assistenza." / "Cliente non collegato a nessun coach.") invece di procedere verso una home rotta. `registration-screens.tsx` ora usa l'id reale Supabase come id locale del cliente fin dalla registrazione (non più un id sintetico), così i due percorsi restano coerenti. **Non serviva alcuna modifica RLS** (le policy per leggere il proprio `client_profiles`/`coach_clients` esistevano già dalla Fase 1) né a `workout.tsx`/`nutrizione.tsx`/`chat.tsx`/`altro.tsx`/`auth-gate.tsx` (filtrano già per `clientId` senza richiedere un `Client` esistente, mostrando stati vuoti onesti).
- **Limite noto, non risolto qui**: per un cliente registrato via Supabase reale, workout/nutrizione/chat restano vuoti finché il lato coach (dashboard, assegnazione schede/piani) non è anch'esso migrato a Supabase — resta locale-only in questa fase. Comportamento onesto (stato vuoto reale), non un bug, ma da aspettarsi in test.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20.

### Fase 2 — email vere Supabase: conferma registrazione, password dimenticata, reset password (2026-07-09, continuazione)
- **Conferma email registrazione**: `CoachRegistrationScreen`/`ClientRegistrationScreen` (`registration-screens.tsx`) controllano ora `result.data.session` dopo `signUp*`. Se `null` (Confirm email attivo su Supabase), il cliente non viene più loggato/mirrorato in locale — vede una schermata dedicata "Controlla la tua email" con "Torna al login"; il coach vede la stessa schermata al posto del codice (il codice non esiste ancora in quel caso, vedi BUG-008 sotto — comparirà al primo login reale).
- **BUG-007 trovato e corretto in questo passaggio**: prima, un cliente/coach registrato con email non confermata poteva comunque bypassare la conferma tramite il fallback di login locale (password in chiaro) di `login-screen.tsx`. Ora un errore Supabase "email not confirmed" (nuovo codice `email_not_confirmed` in `auth-service.ts`) interrompe subito il login con un messaggio dedicato, senza ricadere sui controlli locali. Vedi `docs/BUGS.md` BUG-007.
- **Password dimenticata**: link in login collegato (non più `disabled`) a `/password-dimenticata` (`forgot-password-screen.tsx`) → `requestPasswordReset(email, redirectTo)` (nuova funzione in `auth-service.ts`, wrapper di `supabase.auth.resetPasswordForEmail`). Messaggio sempre uguale (non rivela se l'email esiste).
- **Reset password**: nuova schermata `/reimposta-password` (`reset-password-screen.tsx`) aspetta la sessione di recovery stabilita da Supabase leggendo il link email, poi chiama `updatePassword(newPassword)` (nuova funzione, wrapper di `supabase.auth.updateUser`), mostra conferma e fa `signOut()` + redirect al login. Per funzionare su web, `mobile/src/lib/supabase.ts`: `detectSessionInUrl` ora `Platform.OS === 'web'` (era `false` fisso) — legge il frammento `#access_token=...&type=recovery` dell'URL. Su nativo resta disattivato (deep link nativo non gestito in questa fase).
- **Redirect URL dinamico**: nuovo `mobile/src/lib/redirect-url.ts` (`getWebRedirectUrl`) usa `window.location.origin`, funziona su qualunque porta locale (8081/8082/8084/...) senza hardcodarne una.
- **Documentazione**: `docs/EMAIL_SETUP.md` riscritto con istruzioni operative (Site URL, Redirect URLs multi-porta, template "Confirm signup"/"Reset password" con `{{ .ConfirmationURL }}`, checklist test da web/Expo Go).
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro compilato senza errori con le nuove schermate incluse (smoke test HTTP su porta di test locale). **Non verificato**: nessun test end-to-end con un'inbox email reale contro un progetto Supabase con Confirm email attivo (da fare dall'utente); nessun deep link nativo per il reset password su Expo Go.

### Fix BUG-008 — onboarding coach/cliente vuoto con "Confirm email" attivo (2026-07-10)
- **Causa**: `auth-service.ts` scriveva `coach_profiles`/`billing_profiles`/`registration_codes` (coach) e `client_profiles`/`coach_clients` (cliente) subito dopo `signUp`, ma con "Confirm email" attivo non esiste ancora una sessione in quel momento — le policy RLS (`auth.uid()`) bloccavano l'insert e i dati della form andavano persi. In più, mancavano le policy `profiles_self_insert`/`profiles_self_update`: se il trigger `handle_new_user` non creava la riga `profiles` (verificato non accadere sul progetto reale dell'utente), nessun fallback client-side era possibile e il login falliva con "profilo non trovato".
- **Fix**: `signUpCoach`/`signUpClientWithCoachCode` salvano ora i dati necessari (`billing_profile`/`business_name`; `coach_id`/`coach_code`) in `user_metadata` al momento di `signUp`, e scrivono le tabelle dipendenti subito solo se la sessione esiste già. Nuove funzioni `ensureCoachOnboarding`/`ensureClientOnboarding` (`auth-service.ts`), chiamate da `login-screen.tsx` dopo ogni login riuscito (best-effort, non bloccano il login se falliscono), completano gli insert mancanti rileggendo i dati da `user_metadata` — idempotenti. `signInWithEmail` non fallisce più con "profilo non trovato": se `profiles` manca, la ricrea dal client autenticato (`ensureProfileForCurrentUser` esposta anche standalone). `docs/SUPABASE_SCHEMA.sql`: aggiunte le due policy mancanti, `handle_new_user` reso più robusto (`on conflict do update`, blocco `exception` che non blocca mai la creazione dell'utente Auth). `login-screen.tsx` sincronizza anche il codice coach del mirror locale (superadmin-store) con quello reale creato al primo login, per non mostrare in Impostazioni un codice mai esistito su Supabase.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro compilato senza errori. **Non verificato**: nessun test contro un progetto Supabase reale con "Confirm email" attivo (l'utente deve rieseguire lo snippet SQL in `docs/EMAIL_SETUP.md` e ripetere registrazione/conferma/login). Account registrati prima di questo fix non vengono riparati automaticamente (mancano i dati in `user_metadata`).

### Nuovo design system FitCoach (2026-07-10, continuazione) — migrazione quasi completa
- **Design system** (`mobile/src/theme/`): `colors.ts` (palette `light`/`dark` — background/surface/surfaceSubtle/border/ink/inkSoft/inkFaint/moss+mossSoft+onMoss/coral+coralSoft+onCoral/amber+amberSoft+onAmber/rust+rustSoft+onRust — ruoli semantici: moss=brand/positivo, coral=UNICO colore per azioni/CTA, amber=warning/in prova, rust=errore/bloccato/scaduto), `spacing.ts` (scala 4px, chiavi numeriche 1-10), `typography.ts`, `radius.ts`, `shadows.ts`, `index.ts` (barrel + hook `useAppTheme()`). **Parallelo** a `mobile/src/constants/theme.ts` (non toccato, resta solo per il cluster editor scheda/esercizio non ancora migrato). Variante dark generata da questo agente (non nel mockup originale, solo chiaro), da rivedere visivamente.
- **Componenti UI riusabili** (`mobile/src/components/ui/`): `AppScreen`, `AppHeader`, `AppCard`, `AppButton`, `AppIconButton`, `AppTextField`, `AppListRow`, `AppStatCard`, `AppBadge`, `AppEmptyState`, `AppErrorState`, `AppSectionTitle` + `index.ts`.
- **Icone reali (2026-07-11)**: le icone erano emoji/glifi Text, incoerenti e a volte rotte su Windows/Chrome (segnalato dall'utente con screenshot). Installate `lucide-react-native`/`react-native-svg` (stessa libreria del mockup di riferimento) e sostituite tutte le emoji/glifi unicode nei file migrati (icone header/liste/errore, icone `cliente-home.tsx`, bell + voci bottom nav di `superadmin-shell.tsx` ora componenti icona dinamici).
- **Fix bug reale (2026-07-11)**: due `AppListRow` in `cliente-home.tsx` avevano `onPress` sulla riga E un `AppButton` come `trailing` con la stessa identica azione — `<button>` dentro `<button>`, errore di hydration React su web. Risolto sostituendo l'`AppButton` interno con un badge testuale non interattivo (`TrailingPill`, locale al file).
- **Migrazione praticamente completa (2026-07-11)** su registrazione, cliente, coach, superadmin: `registration-screens.tsx`; lato cliente `workout.tsx`/`nutrizione.tsx`/`chat.tsx` (condiviso col coach)/`altro.tsx`/`cliente-profilo.tsx` + componenti condivisi `theme-settings.tsx`/`developer-info-section.tsx`/`sound-settings.tsx`/`coach-only-notice.tsx`/`disabled-action.tsx`; lato coach `clienti/index.tsx`/`clienti/[id].tsx`/`clienti/new.tsx`/`subscription-form.tsx` (+ wrapper abbonamento-nuovo/modifica)/`appuntamenti/index.tsx`/`appuntamenti/new.tsx`/`impostazioni.tsx`/`supporto.tsx`/`schede/index.tsx`; lato superadmin `coaches/index.tsx`/`coaches/[id].tsx`/`coaches/new.tsx`/`plans/index.tsx`/`plans/[id].tsx`/`payment-events.tsx`/`notifications.tsx`/`support/index.tsx`/`support/[coachId].tsx`.
- **Deliberatamente NON migrato** (cluster "editor scheda/esercizio", ~1500+ righe, condiviso coach+cliente, logica complessa — superserie/timer/storico pesi/allegati): `schede/[id].tsx`, `esercizi/[id].tsx`, `schede/new.tsx`, `schede/modelli/*`, e i componenti `workout-plan-form.tsx`/`workout-exercise-editor.tsx`/`exercise-set-logger.tsx`/`superset-block.tsx`/`rest-timer.tsx`/`exercise-history.tsx`/`exercise-video-player.tsx`/`exercise-attachments.tsx`/`workout-session-controls.tsx`/`workout-exercise-row.tsx`. Restano sullo stile vecchio (`Card`/`ThemedText`/`constants/theme.ts`) — unico blocco rimasto con due design system paralleli attivi.
- **Verificato**: `npx.cmd tsc --noEmit` pulito (ripetutamente, dopo ogni batch), `npx.cmd expo-doctor` 20/20, `expo start --web --clear` riavviato con cache pulita, bundle Metro completo compilato senza errori (3269 moduli, HTTP 200, ~12.4MB). **Non verificato**: nessun clic reale/screenshot in un browser reale (nessun tool di automazione browser disponibile in questo ambiente) — l'utente deve aprire la preview di persona su tutte le schermate elencate sopra.

### Video esercizi — fase 1 (demo URL) e fase 2 reale (Supabase Storage) (2026-07-11)
- **Fase 1 (demo)**: `Exercise.videoUrl?: string` aggiunto al tipo (`mobile/src/types/training.ts`), opzionale, non rompe i 44 esercizi esistenti. `ExerciseVideoPlayer` (`mobile/src/components/exercise-video-player.tsx`) riscritto: priorità a `videoUrl` (remoto, via URI diretto), fallback al vecchio sistema locale mai popolato (`videoFile` + `video-registry.ts`), fallback finale onesto con icona `Play` (lucide) se manca tutto. Un esercizio demo (`petto-panca-piana`) ha un URL pubblico di test in `mobile/src/data/exercise-library.ts` (`DEMO_VIDEO_URLS`).
- **Fase 2 (reale, Supabase Storage)**: nuova tabella `public.exercise_videos` (`coach_id`, `exercise_id` testo — gli esercizi NON sono in Supabase, nessuna foreign key —, `video_path`, `video_url`, `mime_type`, `size_bytes`, `unique(coach_id, exercise_id)`) + RLS (coach gestisce solo i propri video, cliente legge solo i video del proprio coach tramite `coach_clients`, superadmin tutto). Nuovo bucket Storage `exercise-videos` (pubblico in lettura, scrittura solo nella propria cartella `{coach_id}/...`). Vedi `docs/SUPABASE_STORAGE_VIDEO.md` per SQL completo e test, `docs/SUPABASE_SCHEMA.{sql,md}` per lo schema.
- **Nuovo `mobile/src/lib/exercise-video-service.ts`**: `validateVideoAsset` (estensione mp4/mov/webm sempre richiesta, mimeType solo se presente, max 100MB solo se noto), `uploadExerciseVideo` (web: `File` diretto; native: base64 via `expo-file-system/legacy` → `ArrayBuffer` via `base64-arraybuffer`, pattern esplicitamente raccomandato da Supabase per React Native — verificato via Context7 prima di scrivere il codice, non indovinato), `getExerciseVideo` (lettura RLS-scoped, funziona per coach e cliente senza bisogno di sapere il ruolo), `deleteExerciseVideo`, `getCurrentCoachIdForUpload` (usa **sempre** l'id reale della sessione Supabase, `getCurrentSession()`, mai `useAuthStore().currentCoachId` che è l'id del mirror locale demo e non coincide con `auth.uid()` — un dettaglio critico verificato leggendo `login-screen.tsx` prima di scrivere la RLS).
- **Nuovo `mobile/src/components/exercise-video-upload.tsx`**: `ExerciseVideoUploadControl`, visibile solo al coach in `esercizi/[id].tsx` (montato solo se `currentRole === 'coach'` e Supabase configurato). Nessuna barra di progresso finta (il client Storage di Supabase non espone un callback di avanzamento reale): stato di caricamento indeterminato (spinner su `AppButton`), messaggio di errore (rust) o successo (moss) inline.
- **`esercizi/[id].tsx`**: recupera il video reale all'apertura (`getExerciseVideo`), che ha sempre priorità sul `videoUrl` demo statico una volta noto; nessun'altra modifica alla schermata (resta sullo stile vecchio, cluster editor scheda/esercizio deliberatamente non migrato al design system).
- **Dipendenze aggiunte**: `expo-file-system` (ora diretta, era transitiva), `base64-arraybuffer` (nuova, piccola utility standard per questo pattern).
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `expo start --web --clear` con cache pulita, bundle Metro compilato senza errori (3278 moduli). **Non verificato**: nessun upload/visualizzazione reale contro un progetto Supabase con il bucket/tabella creati (l'utente deve eseguire lo snippet SQL in `docs/SUPABASE_STORAGE_VIDEO.md` e seguire la checklist di test lì descritta, sia lato coach sia lato cliente); nessun test su Expo Go reale (solo bundle web verificato).

### Fix bug upload video — tabella mancante e file orfani (2026-07-11, continuazione)
- **Bug segnalato dall'utente**: primo test reale dell'upload coach, il video veniva caricato su Supabase Storage con successo ma il salvataggio della riga su `exercise_videos` falliva con `Could not find the table 'public.exercise_videos' in the schema cache"`. Causa: lo snippet SQL della tabella (già scritto nella sessione precedente) non era ancora stato eseguito sul progetto Supabase reale dell'utente — l'errore è il sintomo tipico di PostgREST quando una tabella non esiste ancora (o non è stata ricaricata nello schema cache dopo la creazione).
- **Verifica fatta**: cercati tutti gli accessi `.from('exercise_videos')` nel codice (4, tutti in `mobile/src/lib/exercise-video-service.ts`) — colonne usate (`coach_id`, `exercise_id`, `video_path`, `video_url`, `mime_type`, `size_bytes`) già combacianti con lo SQL esistente in `docs/SUPABASE_SCHEMA.sql`, nessun disallineamento di schema trovato.
- **Bug reale trovato e corretto** (non solo il problema segnalato): `uploadExerciseVideo` non ripuliva il file appena caricato su Storage se il successivo insert sul database falliva — un fallimento parziale lasciava un file orfano nel bucket (mai referenziato da nessuna riga). Ora, se l'insert DB fallisce, il file appena caricato viene rimosso automaticamente (best-effort) prima di restituire l'errore. I file eventualmente rimasti orfani dal test originale dell'utente (prima di questo fix) restano nel bucket e vanno rimossi manualmente — vedi `docs/SUPABASE_STORAGE_VIDEO.md`.
- **SQL reso idempotente**: tutte le policy di `exercise_videos` (tabella e Storage) ora precedute da `drop policy if exists`, così l'intero snippet è rieseguibile senza errori se serve rilanciarlo. Aggiunto anche `notify pgrst, 'reload schema';` per forzare PostgREST a vedere subito la tabella nuova, nel caso il ritardo di ricarica automatica fosse la causa (o concausa) dell'errore originale.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun nuovo test contro un progetto Supabase reale in questo ambiente — l'utente deve eseguire lo snippet SQL aggiornato (`docs/SUPABASE_STORAGE_VIDEO.md`) e ripetere l'upload.

### Credenziali provvisorie via email — Edge Function send-temporary-credentials (2026-07-11, continuazione)
- **Cosa esiste ora**: il bottone "Invia via email" in `mobile/src/app/clienti/[id].tsx` (prima `DisabledAction` finta) chiama una nuova Edge Function Supabase (`supabase/functions/send-temporary-credentials/index.ts`), attiva solo se `supabaseConfig.isConfigured`. La function verifica che il chiamante sia il coach proprietario del cliente o un superadmin, genera una password provvisoria random server-side, la imposta con `auth.admin.updateUserById` (service_role, mai nel client mobile), marca `profiles.must_change_password = true` e invia un'email (Resend) con le credenziali — non restituisce mai la password al client mobile, nemmeno in caso di errore.
- **Gating login**: `mobile/src/lib/auth-service.ts` (`signInWithEmail`) legge ora `must_change_password` da `profiles` e lo propaga; `useAuthStore.mustChangePasswordSupabase` lo persiste; `auth-gate.tsx` blocca sia coach sia cliente mostrando il nuovo `SupabaseChangePasswordScreen` finché l'utente non imposta una password propria (`completePasswordChange`, che chiama `updatePassword` reale + azzera il flag). Questo è distinto e non sostituisce il vecchio meccanismo locale demo (`ClientAccount.mustChangePassword`/`ChangePasswordScreen`), che resta invariato per i clienti aggiunti solo localmente senza un vero account Supabase.
- **Limite noto per design**: funziona solo per clienti con un vero account Supabase (registrati via `/registrazione-cliente`); per un cliente aggiunto solo dal coach senza registrazione reale, la function risponde con un errore chiaro ("Nessun account Supabase reale trovato...") invece di un falso successo.
- **Documentazione**: `docs/SUPABASE_TEMP_CREDENTIALS.md` (architettura, deploy CLI, variabili d'ambiente richieste — `RESEND_API_KEY` obbligatoria, `RESEND_FROM_EMAIL` opzionale —, checklist di test end-to-end). SQL: nuova colonna `must_change_password` su `profiles` in `docs/SUPABASE_SCHEMA.sql` (idempotente).
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun Supabase CLI disponibile in questo ambiente — la Edge Function non è mai stata deployata né chiamata contro un progetto reale; l'utente deve seguire la checklist completa in `docs/SUPABASE_TEMP_CREDENTIALS.md` (SQL, deploy, secrets, invio email reale, login con password provvisoria, cambio password obbligato, verifica del flag azzerato).

### Fix BUG-011 — Coach Supabase invisibili nel pannello superadmin (2026-07-12)
- **Cosa esiste ora**: dashboard, lista e dettaglio coach del pannello superadmin (`mobile/src/app/superadmin/{index,coaches/index,coaches/[id]}.tsx`) non leggono più solo `useSuperadminStore.coaches` (locale al device): un nuovo hook `useSuperadminCoaches()` (`mobile/src/hooks/use-superadmin-coaches.ts`) unisce i coach reali caricati da Supabase (`loadSupabaseCoaches()`, `mobile/src/lib/superadmin-coach-service.ts` — `profiles` role='coach' + `coach_profiles` + `billing_profiles` + `registration_codes` + conteggio reale `coach_clients`) con quelli creati solo localmente dal pulsante "+ Aggiungi coach" (dedup per email). Fallback ai soli dati locali se Supabase non è configurato.
- **Superato dall'intervento "Modifica coach reale" (2026-07-12, vedi sezione dedicata sotto)**: nome/nome attività/telefono, blocco/sblocco, stato pagamento, rigenera/attiva-disattiva codice e assegnazione pacchetto ora scrivono davvero su Supabase per un coach reale. Restano disabilitati solo "Piano"/"Limite clienti override"/"Clienti usati"/periodo app (nessun equivalente Supabase reale) ed email (richiederebbe l'Auth admin API).
- **Prerequisito per testare**: le query rispettano la RLS `is_superadmin()` già esistente — servono un utente Supabase reale con `profiles.role='superadmin'` per vedere risultati. L'account demo locale (`admin@fitcoach.local`) non ha sessione Supabase e vedrà sempre lista vuota lato Supabase.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20. **Non verificato**: nessun test contro un progetto reale con un superadmin Supabase autentico in questo ambiente.

### Edge Function credenziali provvisorie — provider email Brevo (2026-07-12)
- `supabase/functions/send-temporary-credentials` usa ora Brevo (`https://api.brevo.com/v3/smtp/email`) invece di Resend: secret richieste `BREVO_API_KEY`/`BREVO_SENDER_EMAIL` (obbligatorie) + `BREVO_SENDER_NAME` (opzionale). Vedi `docs/SUPABASE_TEMP_CREDENTIALS.md` aggiornato.

### Fix BUG-012 — Reset password apriva il login invece di /reimposta-password (2026-07-12)
- `auth-gate.tsx`: `/reimposta-password` ora è raggiungibile SEMPRE (controllo spostato prima del gate `isAuthenticated`), non solo quando il browser non ha già una sessione locale attiva. `resetPasswordForEmail`/lettura sessione recovery/`updatePassword` erano già corretti, nessuna modifica necessaria lì. Causa esterna possibile e documentata: "Redirect URLs" mancante su Supabase per l'origin/porta in uso fa cadere silenziosamente sulla Site URL di default. Vedi `docs/BUGS.md` BUG-012.
- **Addendum 1**: `reset-password-screen.tsx` ora gestisce anche il formato `?token_hash=...&type=recovery` (template email con `{{ .TokenHash }}`, non gestito da `detectSessionInUrl`), non solo `#access_token=...`.
- **Addendum 2**: `login-screen.tsx` ora mostra il vero messaggio d'errore Supabase ("Accesso non riuscito: ...") se il login fallisce sia su Supabase sia sui controlli locali, invece del generico "Credenziali non valide" che nascondeva la causa reale. **Da verificare con l'utente**: quale messaggio Supabase esatto compare ora dopo un reset password, per confermare se la password non corrisponde davvero o c'è un'altra causa.

### Fix BUG-013 — Codice coach in Impostazioni disallineato da registration_codes (2026-07-12)
- `impostazioni.tsx` ora legge il codice coach REALE da Supabase (`getCoachActiveRegistrationCode`, nuova funzione in `auth-service.ts`) invece di fidarsi solo del mirror locale `useSuperadminStore.coaches`, che può restare disallineato se la sincronizzazione al primo login (`ensureCoachOnboarding`) non si completa per qualunque motivo. Stesso principio già applicato per BUG-011 (superadmin): fonte di verità Supabase sempre preferita alla cache locale. Vedi `docs/BUGS.md` BUG-013.

### Fix BUG-014 — Tutti i login falliti per colonna must_change_password mancante (2026-07-12)
- `signInWithEmail` ora è resiliente se `must_change_password` non esiste ancora su `profiles` (SQL non eseguito sul progetto reale): riprova senza quella colonna invece di bloccare il login per tutti. **Causa reale ancora da risolvere lato utente**: eseguire `alter table public.profiles add column if not exists must_change_password boolean not null default false; notify pgrst, 'reload schema';` sul progetto Supabase reale (già in `docs/SUPABASE_SCHEMA.sql`, mai eseguito). Vedi `docs/BUGS.md` BUG-014.

### Pacchetti acquistabili coach/cliente gestiti dal superadmin (2026-07-12, continuazione)
- **Cosa esiste ora**: due cataloghi separati e letti sempre da Supabase (`subscription_packages` con `target_role` `coach`/`client`, `user_subscriptions`), distinti dai concetti già esistenti (`plans`/`coach_billing` = piano SaaS interno del coach; `subscriptions` = abbonamento sessioni creato dal coach per il proprio cliente). Superadmin → nuova sezione "Pacchetti e abbonamenti" (`superadmin/pacchetti/{index,new,[id]}.tsx`, 6° voce della bottom bar): tab "Pacchetti coach"/"Pacchetti clienti", crea/modifica/attiva-disattiva/elimina (elimina bloccata se esistono abbonamenti collegati, guardia sia app-side sia DB `on delete restrict`), campi nome/descrizione/prezzo/valuta/durata (giorni o mesi)/limite clienti (solo coach)/funzionalità incluse (chip)/ordine. Coach → nuova schermata "Abbonamento" (`abbonamento-coach.tsx`, linkata da Impostazioni): pacchetti coach attivi con prezzo/durata/limite clienti/funzionalità, pacchetto/stato/scadenza correnti, storico; più bottone "Condividi" (Share API nativa) accanto al "Copia" già esistente per il codice registrazione in Impostazioni. Cliente → nuova schermata "Pacchetti" (`pacchetti-cliente.tsx`, linkata dal menu Altro, distinta dalla voce "Abbonamento" già esistente che resta l'abbonamento sessioni del coach): stessa struttura lato coach, bottone "Abbonati".
- **Aggiornamento 2026-07-14/2026-07-23**: `mobile/src/lib/package-checkout-service.ts` non è più uno stub — collega un acquisto reale via RevenueCat sia per i pacchetti coach (2026-07-14) sia per i pacchetti cliente "Client Pro" (2026-07-23, vedi sezione dedicata sotto). Il client mobile non scrive mai direttamente `user_subscriptions`: dopo il purchase aspetta solo che il webhook Supabase (`revenuecat-webhook`) sincronizzi la riga reale, con retry limitato (mai un loop infinito). RLS invariata: nessuna policy insert/update su `user_subscriptions` per coach/cliente, solo il superadmin/webhook (service role) possono scrivere.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro web compilato senza errori (verificato via HTTP/grep sul bundle, tutte le nuove schermate/servizi presenti). **Non verificato**: nessun clic reale in un browser (nessun tool di automazione browser disponibile in questo ambiente, stesso limite di tutte le sessioni precedenti), nessun test contro un progetto Supabase reale (le nuove tabelle/RLS non sono mai state eseguite su un progetto reale) — vedi checklist dedicata in `docs/TODO_NEXT.md`.

### Limite clienti coach collegato all'abbonamento (2026-07-12, continuazione)
- **Cosa esiste ora**: il `max_clients` di un pacchetto coach e' ora realmente applicato, non solo mostrato. Un coach puo' avere al piu' UN abbonamento `active` alla volta (trigger `user_subscriptions_single_active`: il pacchetto attivo corrente determina sempre il limite corrente, nessun accumulo tra rinnovi/cambi). La registrazione cliente e' verificata atomicamente lato server: `public._link_client_to_coach()` (con `pg_advisory_xact_lock` per-coach, no race condition) controlla codice + abbonamento coach attivo + limite prima di collegare, chiamata sia dal trigger `handle_new_user()` (percorso immediato, silenzioso) sia dalla RPC pubblica `register_client_with_code` (percorso esplicito lato app, dopo `signUp`). Pre-check pubblico `can_coach_accept_client` chiamato PRIMA di `signUp` per rifiutare con un messaggio onesto ("Abbonamento necessario" / limite raggiunto) senza creare un account Supabase Auth orfano. Contatore "Clienti utilizzati: X su Y" / "Posti disponibili: Z" (via RPC `get_coach_client_capacity`) visibile nell'area "Clienti" del coach (con stato "Abbonamento necessario" se manca un abbonamento attivo) e nel pannello superadmin (lista + dettaglio coach, nuova sezione "Pacchetto acquistato").
- **Comportamento con abbonamento scaduto o limite raggiunto**: un abbonamento con `status='active'` ma `expires_at` nel passato NON conta come attivo (trattato come nessun abbonamento — blocca nuove registrazioni con "Abbonamento necessario"). Se il coach passa a un pacchetto piu' piccolo con piu' clienti gia' collegati del nuovo limite, i clienti esistenti NON vengono mai rimossi: solo le NUOVE registrazioni restano bloccate finche' il conteggio non torna sotto il limite.
- **Limite noto e accettato**: se due registrazioni concorrenti superano il pre-check pubblico nella stessa finestra strettissima e solo una vince il lock atomico, l'altra ottiene comunque un account Supabase Auth creato ma NON collegato al coach — gestito dal comportamento gia' esistente di `loadClientProfile`/login ("Cliente non collegato a nessun coach"), mai un collegamento silenzioso oltre il limite.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro web ricompilato senza errori con tutto il nuovo codice presente. **Non verificato**: nessun clic reale, nessun test contro un progetto Supabase reale (le nuove funzioni/trigger SQL non sono mai state eseguite su un progetto reale) — vedi checklist dedicata in `docs/TODO_NEXT.md`.

### Modifica coach reale nel pannello superadmin (2026-07-12, continuazione)
- **Cosa esiste ora**: la card "Modifica coach" (`superadmin/coaches/[id].tsx`) scrive davvero su Supabase per un coach reale, non più disabilitata. Nuovo `superadmin-coach-admin-service.ts`: nome/nome attività/telefono (`profiles`/`coach_profiles`), stato pagamento/blocco (`coach_profiles.billing_status`), rigenera/attiva-disattiva codice (`registration_codes`), assegna/cambia/termina pacchetto coach (`user_subscriptions`, con `starts_at`/`expires_at` calcolate dalla durata del pacchetto scelto — il trigger `user_subscriptions_single_active` gestisce automaticamente la sostituzione dell'abbonamento precedente). Nessuna nuova policy RLS necessaria (le policy `_superadmin_all` esistenti coprono tutto). `use-superadmin-coaches.ts` ora espone `reload()` con refresh automatico su focus/foreground, cosi' ogni scrittura riuscita si riflette subito in lista e dettaglio.
- **Restano deliberatamente fuori scope**: email del coach (richiederebbe l'Auth admin API con service role, mai disponibile lato mobile) e i campi legacy "Piano"/"Limite clienti override"/"Clienti usati"/periodo app (`plans`/`coach_billing`, mai collegati a un coach Supabase reale) — questi restano disabilitati con una nota esplicativa nel form.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro ricompilato senza errori. **Non verificato**: nessun clic reale, nessun test contro un progetto Supabase reale — vedi checklist dedicata in `docs/TODO_NEXT.md`.

### Integrazione catalogo esercizi YMove — opzione B (2026-07-12, continuazione)
- **Cosa esiste ora**: prima esercizi/schede erano interamente locali (nessuna tabella esercizi su Supabase). Nuova `public.exercises` (esercizi FitCoach `custom`/`ymove`, quelli `ymove` condivisi globalmente tra coach — riuso automatico via vincolo univoco su `ymove_exercise_id`, mai duplicati) + nuova Edge Function `ymove-exercises` (proxy verso l'API YMove, chiave `YMOVE_API_KEY` mai nell'app). Nell'area coach, durante la creazione di una scheda, nuovo bottone "Libreria YMove": ricerca/filtri (nome/muscolo/attrezzatura/tipo/difficolta', sempre `includeVideos=false` in lista), anteprima con video/istruzioni, "Aggiungi a FitCoach" (crea o riusa l'esercizio, mai video/URL salvati nel database — sempre richiesti live). Il video di un esercizio importato usa lo stesso principio anche lato cliente/coach nella schermata esercizio (`YMoveVideoPlayer`, fallback chiari per limite YMove/non autorizzato/URL scaduto, retry manuale).
- **Limite architetturale onesto, documentato esplicitamente (`docs/YMOVE_INTEGRATION.md`, `docs/DECISIONS.md`)**: il controllo server-side "il cliente puo' vedere il video solo se l'esercizio e' davvero nella sua scheda" NON e' pienamente verificabile, perche' le schede restano un modello dati locale (mai su Supabase). Implementato il controllo piu' forte possibile con i dati reali disponibili (cliente collegato a un coach + esercizio gia' importato in FitCoach), non una garanzia completa.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro ricompilato senza errori con tutto il nuovo codice presente. **Non verificato**: nessun clic reale, nessun test contro un progetto Supabase reale ne' contro l'API YMove reale in questo ambiente. **Aggiornamento 2026-07-13**: la struttura risposta YMove v2 e' stata confermata dall'utente (non piu' un'assunzione) e il parsing corretto di conseguenza (`json.data`, campo `title`, `muscleGroup` stringa, `exerciseType` array) — vedi sezione dedicata sotto.

### Estensione YMove: traduzione italiana + associazione video su esercizi esistenti + anti-duplicati (2026-07-13)
- **Cosa esiste ora**: (1) **Traduzione**: all'import di un nuovo esercizio YMove, `name`/`description`/`technical_notes` (i campi gia' esistenti, sempre quelli mostrati) vengono tradotti in italiano una sola volta (Azure Translator, secret opzionali `AZURE_TRANSLATOR_KEY`+`AZURE_TRANSLATOR_REGION`, risorsa regionale — es. `italynorth`) o, se nessun servizio e' configurato/la chiamata fallisce, copiati dall'originale — mai una traduzione finta. L'originale YMove resta sempre conservato separatamente (`ymove_original_title`/`_description`/`_instructions`, mai piu' toccato). Questo testo diventa il testo GLOBALE condiviso, modificabile SOLO dal superadmin (vedi correzione BUG-015 sotto). (2) **Associazione video su un esercizio esistente**: nuova sezione "Video YMove" nella schermata di QUALSIASI esercizio (locale storico o FitCoach custom, non per quelli gia' `source='ymove'`) con Associa/Sostituisci/Rimuovi collegamento — riusa la tabella `exercise_videos` gia' esistente (mai una struttura parallela), il video resta sempre richiesto live, mai un URL salvato. Nome italiano/serie/ripetizioni/recupero/carico/note esistenti non vengono mai toccati da questa operazione. (3) **Anti-duplicati**: prima di salvare un collegamento, si controlla se lo stesso esercizio YMove e' gia' collegato a un altro esercizio di quel coach — se si', si mostra quale e si offre di aprirlo invece di duplicare silenziosamente (garanzia reale: indice univoco `(coach_id, ymove_exercise_id)` su `exercise_videos`).
- **Verificato**: `npx.cmd tsc --noEmit` pulito (mobile + compilazione isolata Deno per la Edge Function), `npx.cmd expo-doctor` 20/20, bundle Metro ricompilato senza errori. **Non verificato**: nessun clic reale, nessun test contro un progetto Supabase reale ne' contro Azure Translator/YMove reali — checklist completa in `docs/YMOVE_INTEGRATION.md`.

### Fix BUG-015/BUG-016 — testi italiani per-coach + esercizio YMove che spariva dalla scheda (2026-07-13, continuazione)
- **BUG-015 (design sbagliato, corretto)**: la personalizzazione "wiki-style" del punto (1) sopra permetteva a QUALUNQUE coach di modificare il testo condiviso di un esercizio `ymove` — segnalato dall'utente come errore, non solo rischio. Corretto: rimossa la policy `exercises_coach_update_ymove_text` (solo il superadmin modifica il testo globale); nuova tabella `public.exercise_text_overrides` (coach_id, exercise_id, name/description/technical_notes, `unique(coach_id, exercise_id)`) per la personalizzazione PRIVATA di ogni coach, mai visibile ad altri coach — il cliente vede l'override del proprio coach. Lettura (`getFitCoachExerciseById`): override se presente, altrimenti testo globale. `fitcoach-exercises-service.ts`: `updateCustomExerciseText` (solo `source='custom'`, invariato) e `upsertExerciseTextOverride` (solo `source='ymove'`, mai il testo globale).
- **BUG-016 (bug reale trovato durante la verifica richiesta)**: `schede/[id].tsx` (vista scheda) usava ancora `getExerciseById` diretto (solo libreria locale) per gli esercizi della scheda — un esercizio con id Supabase (importato/collegato a YMove) spariva silenziosamente dalla lista dopo il salvataggio. Corretto: entrambe le chiamate usano ora `useExerciseResolver().resolve`. Verificato anche `schede/modelli/[id].tsx` (template): usa solo dati locali statici, nessun rischio.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, bundle Metro ricompilato senza errori. **Non verificato**: nessun clic reale, nessun test con due coach distinti su un progetto reale.

### Fix BUG-017 — thumbnail live YMove nelle card esercizio invece del placeholder con l'iniziale (2026-07-13, continuazione 3)
- **Cosa esiste ora**: `ExerciseThumbnail` (usato da tutte le card esercizio in scheda: coach, cliente, superserie, circuiti) mostra la miniatura reale con priorità: esercizio importato da YMove → thumbnailUrl live dell'esercizio di origine; esercizio locale/custom collegato a un video YMove (`exercise_videos`) → stessa thumbnailUrl live; video caricato manualmente dal coach → placeholder (nessuna colonna poster oggi); nessun video → placeholder invariato. Sempre e solo un'immagine statica (`expo-image`, `contentFit="cover"`, icona play decorativa sovrapposta): nessuna `VideoView` montata, nessun autoplay/audio nella lista — il video vero parte solo nel Dettaglio esercizio dopo un tocco esplicito. Due cache globali in memoria (`lib/ymove-thumbnail-cache.ts` per la thumbnail per `ymoveExerciseId`, `lib/exercise-video-info-cache.ts` per il risultato di `getExerciseVideo` per `exerciseId`, invalidata da `esercizi/[id].tsx` dopo ogni modifica del collegamento video) evitano di richiamare la Edge Function o la tabella `exercise_videos` ad ogni render/rimontaggio della stessa card nella stessa sessione. `getExerciseVideo` ora restituisce una struttura discriminata (`source: 'upload' | 'ymove'`) invece di campi tutti opzionali, cosi' una riga YMove (con `video_url` sempre null) non viene mai letta per errore come "nessun video".
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform web` completato senza errori (bundle ricompilato, nuove funzioni di cache confermate presenti via grep). **Non verificato**: nessun clic/scroll reale in un browser, nessun test contro un progetto Supabase/YMove reale.

### Fix BUG-018 — autorizzazione cliente corretta nell'azione detail per un video YMove collegato non importato (2026-07-13, continuazione 4)
- **Cosa esiste ora**: nell'azione `'detail'` di `supabase/functions/ymove-exercises/index.ts`, per il ruolo `cliente` l'accesso e' autorizzato se ALMENO una delle due condizioni e' vera — l'id YMove e' importato in `public.exercises` (`source='ymove'`, condiviso globalmente, come prima) OPPURE esiste una riga `exercise_videos` con quello stesso `ymove_exercise_id` E `coach_id` uguale al coach a cui il cliente e' davvero collegato (`coach_clients`). Un collegamento di un coach diverso non autorizza mai nulla. Prima veniva verificato solo il primo caso: un esercizio locale/custom con un video YMove associato (mai importato come riga `exercises`) faceva rispondere `forbidden` al cliente pur essendo visibile al coach — causa residua del placeholder con la lettera lato cliente per quel sottocaso (BUG-017). Coach/superadmin: nessuna modifica.
- **Da fare dall'utente**: ridistribuire la Edge Function (`supabase functions deploy ymove-exercises`) perche' il fix abbia effetto — il codice sorgente aggiornato non serve finche' non viene deployato.
- **Verificato**: revisione manuale riga per riga del file (nessun CLI Deno disponibile in questo ambiente). **Non verificato**: nessun test contro un progetto Supabase/YMove reale con due coach e client distinti — checklist dedicata in `docs/YMOVE_INTEGRATION.md` (punto 15).

### Associazione automatica dei video YMove (2026-07-13, continuazione 5)
- **Cosa esiste ora**: `mobile/src/lib/ymove-auto-link-service.ts` (`autoLinkYmoveVideosForCoach`) sostituisce la ricerca/scelta manuale come flusso principale: raccoglie i 44 esercizi storici + i `custom` del coach (esclusi sempre gli esercizi `source='ymove'`, già collegati) e chi ha già un video/collegamento in `exercise_videos`; per ognuno degli altri sceglie un termine di ricerca inglese (dizionario di sinonimi in `mobile/src/lib/ymove-name-matching.ts`, altrimenti Azure Translator IT→EN in batch, altrimenti il nome originale), cerca su YMove, filtra `hasVideo===true` e applica un punteggio pesato (nome 60% / gruppo muscolare 20% / attrezzatura 15% / tipologia 5%). Collega SOLO se il punteggio migliore è `>= 0.85` e il distacco dal secondo candidato è `>= 0.10`; altrimenti resta "non associato"/"ambiguo" (mai una scelta a caso), con anti-duplicati prima di salvare. Avviata automaticamente al primo accesso del coach (`components/auth-gate.tsx`, quando il ruolo diventa `'coach'`, una volta per sessione), con cooldown di 7 giorni persistito in AsyncStorage tra un riavvio e l'altro (i falliti/ambigui vengono ritentati automaticamente a quel punto). Stato live in un banner non bloccante nella dashboard coach (`components/ymove-autolink-banner.tsx`, `store/ymove-autolink-store.ts`, `app/index.tsx`): "Associazione automatica video: N/44" durante l'esecuzione, poi il riepilogo finale. "Associa video YMove" (manuale, `esercizi/[id].tsx`) resta come fallback per i casi ambigui/non trovati. Estesa l'azione `'translate'` della Edge Function per accettare anche un formato array generico `{texts: string[], from?, to}` (oltre al formato legacy invariato), usato per le traduzioni IT→EN.
- **Non ancora agganciato**: `autoLinkYmoveVideoForExercise(exercise)` (variante per un singolo esercizio appena creato) è pronta ma senza un punto di chiamata reale — non esiste oggi nel codice una schermata che crea un esercizio `source='custom'` da zero (solo schema/RLS lo prevedono).
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform web` completato senza errori (nuove funzioni confermate presenti nel bundle via grep). Algoritmo di scoring verificato con un test unitario manuale + una simulazione locale a catalogo YMove **sintetico** (non dati reali): sui 44 esercizi storici, tutti hanno un termine dal dizionario di sinonimi e, nella simulazione, tutti e 44 superano la soglia di collegamento. **Non verificato**: nessuna chiamata reale contro l'API YMove/Azure Translator (nessuna chiave disponibile in questo ambiente) — il tasso di successo su un catalogo VERO resta da verificare dall'utente; nessun clic reale in un browser.

### Migrazione schede e allenamenti a Supabase (2026-07-14)
- **Causa del problema originale**: le schede (`WorkoutPlan`, `store/training-store.ts`) sono sempre state 100% locali (Zustand + AsyncStorage) fin dalla primissima versione dell'app (2026-07-05) — mai toccate quando Supabase fu introdotto (2026-07-08+) solo per auth/registrazione/billing/esercizi. Nessun dato condiviso tra dispositivi, nessuna sincronizzazione reale coach↔cliente.
- **Cosa esiste ora**: Supabase e' la fonte di verita' per le schede quando configurato e l'utente e' autenticato; AsyncStorage resta cache offline (mai piu' fonte definitiva). Nuove tabelle `workout_templates`/`workout_plans`/`workout_days`/`workout_day_exercises` (`docs/SUPABASE_SCHEMA.sql`). Il modello TS `WorkoutPlan` (array piatto di esercizi) resta INVARIATO: `workout_days` e' un solo "giorno" implicito per piano, gestito in modo trasparente dal servizio — nessuna schermata sa che esiste. `exercise_id` e' sempre `text` (mai una foreign key): continua a contenere sia gli id dei 44 esercizi storici locali sia gli UUID Supabase (custom/ymove), risolti da `useExerciseResolver` come sempre. Salvataggio atomico multi-tabella tramite RPC `save_workout_plan` (o tutto o niente, mai una scheda parzialmente salvata) con verifica esplicita che il coach sia proprietario e il cliente sia davvero suo (`is_coach_for_client`). Aggiornamenti di sessione (stato/timer/completamento, anche lato cliente) passano da una seconda RPC dedicata (`update_workout_session_progress`): e' li', non con privilegi di colonna, che si applica "il cliente non puo' toccare serie/esercizi/peso/struttura". RLS: coach CRUD pieno sulle proprie schede/per i propri clienti, cliente solo lettura delle proprie, superadmin completo.
  Nuovo servizio `mobile/src/lib/workout-plan-service.ts` (list/get/create/update/delete piano, `updateWorkoutSessionProgress`, CRUD modelli). Migrazione una tantum (`workout-plan-migration.ts`) sposta su Supabase le schede locali di un coach reale, escludendo sempre: piani gia' migrati (mappa persistita per-coach `localId -> remoteId`, idempotente — **mai** la sola forma dell'id, corretto in BUG-019 sotto), piani demo/seed (`SEED_WORKOUT_PLANS`, MAI verso un account reale), piani il cui cliente locale non sia davvero collegato al coach su Supabase. Realtime scoped (`workout-plan-realtime.ts`, singleton per ruolo+utente, niente sottoscrizioni globali/duplicate, cleanup su unmount) su `workout_plans` filtrato per `coach_id`/`client_id`. Hook unico `hooks/use-workout-plans-sync.ts` (migrazione+lettura+realtime) usato da `auth-gate.tsx` (primo sync al login) e dalle schermate (refresh-on-focus: `schede/index.tsx`, `schede/[id].tsx`, `workout.tsx`).
  Schermate collegate: `schede/new.tsx`, `schede/[id].tsx` (salvataggio strutturale/eliminazione awaited con errore reale, sostituzione sicura dell'id locale con quello remoto via `replaceWorkoutPlan`; aggiornamenti di sessione ottimistici con sync in background), `schede/modelli/[id].tsx` (crea N schede in sequenza), `schede/index.tsx`/`workout.tsx` (loading/errore+Riprova).
- **Fix BUG-019 (2026-07-14, continuazione)**: id locali placeholder testuali/numerici (es. `"1"`, `"we-1"`) facevano fallire creazione/modifica/eliminazione scheda con "invalid input syntax for type uuid" — un cast diretto a `uuid` veniva tentato senza validare il formato in tre punti (`save_workout_plan`, `deleteWorkoutPlan`, `updateWorkoutSessionProgress`). Corretto con `isValidUuid` (mobile, ex `isUuid`) applicata ovunque + nuova funzione SQL `public.is_valid_uuid(text)` usata prima di ogni cast nella RPC — mai piu' un'eccezione da un id locale, sempre trattato come riga nuova. `exercise_id` (l'esercizio, non la riga) resta sempre testo libero, invariato. Corretto anche il rischio di doppioni locale/remoto (`replaceWorkoutPlan`, nuova azione store) e l'euristica sbagliata "id gia' UUID = gia' migrato" nella procedura one-time (ora una vera mappa persistita). Vedi `docs/BUGS.md` (BUG-019).
- **Da fare dall'utente**: eseguire la nuova sezione di `docs/SUPABASE_SCHEMA.sql` su un progetto Supabase reale; tutti i test funzionali (coach crea/modifica/elimina scheda, cliente collegato la vede e uno non collegato no, cliente completa esercizio e il coach vede il progresso via Realtime, superserie/circuito, logout/login, secondo dispositivo, errore di rete, nessuna duplicazione durante la migrazione) restano da verificare dal vivo — nessuna chiave Supabase reale disponibile in questo ambiente.
- **Dati che restano locali (fuori scope)**: `progressHistory` (storico pesi per-serie, `ExerciseProgressHistory`) e `soundSettings` — nessuna tabella richiesta per questi nello schema fornito. I 7 modelli predefiniti (`data/workout-plan-templates.ts`) restano dati statici locali: `workout_templates` e' pronta ma senza una UI di gestione dedicata (non richiesta).
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform web` completato senza errori (nuove funzioni/log confermati nel bundle via grep). Test unitario manuale (Node) sulla logica `isValidUuid`/`buildSavePayload` con gli id esatti del bug segnalato. SQL controllato manualmente (parentesi bilanciate, dollar-quoting pari) — nessuna esecuzione reale contro Postgres in questo ambiente.

### Preesistenti (2026-07-05/06, invariate salvo dove indicato)
- Navigazione base, libreria 44 esercizi, struttura dati allenamento, timer/suoni/storico, tema chiaro/scuro, gestione clienti reale, lato cliente completo (Nutrizione/Questionario/Prenotazioni/Bacheca/Chat), abbonamenti/sessioni multiple/agenda coach — vedi `docs/WORKLOG.md` per i dettagli.

### Fix BUG-020/021/022 — dashboard Android, libreria YMove, account coach reali nel superadmin (2026-07-13, continuazione 6)
- **Dashboard coach** (`mobile/src/app/index.tsx`): larghezza colonna misurata in pixel con `onLayout` sul contenitore reale della griglia (`floor((containerWidth - gap)/2)`, fallback a una colonna sotto 130px, `'100%'` per il singolo frame pre-misura), applicata a card statistiche e "Azioni rapide" — niente più `width:'48.5%'+minWidth+flexGrow` (la combinazione causava la sovrapposizione su Android). Una prima versione con `useWindowDimensions` era sbagliata su web (misurava la finestra del browser invece della cornice `WebPhoneFrame`, segnalato dall'utente con screenshot) ed è stata sostituita nella stessa giornata. Nessuna `position:absolute`, nessuna altezza fissa: il quinto pulsante va a capo da solo in terza riga.
- **Libreria YMove** (`ymove-exercise-picker.tsx`): ricerca automatica senza filtri all'apertura (protetta da Strict Mode con un `useRef`), risultati già mostrati non vengono più azzerati da una ricerca fallita successiva, nuovo componente `YMoveResultThumbnail` con miniatura lazy (cache `ymove-thumbnail-cache.ts`, ora con limite di concorrenza globale a 2 richieste di dettaglio), placeholder `Dumbbell` se assente/fallita, badge play se `hasVideo`. `normalizeSummary` (Edge Function e mobile) accetta `name` come fallback quando `title` manca. Nuovo log `YMOVE_SEARCH_DEBUG`. **Operazione manuale ancora necessaria**: `npx.cmd supabase functions deploy ymove-exercises --project-ref rkcecnzvzoigipjliwdk` per il fallback lato server.
- **Superadmin "Account coach"**: il caricamento reale da Supabase esisteva già (BUG-011, 2026-07-12); ora `loadSupabaseCoaches` ritorna un risultato esplicito `{ok,...}` (mai più un array vuoto silenzioso su errore), i coach demo/locali sono nascosti di default e compaiono solo con `EXPO_PUBLIC_ENABLE_DEMO_DATA=true` (badge "Demo"), il caricamento globale richiede `currentRole==='superadmin'`, aggiunti errore+Riprova e pulsante "Aggiorna" manuale, nuovo log `SUPERADMIN_COACH_LOAD`.
- **Verificato**: `npx.cmd tsc --noEmit` pulito, `npx.cmd expo-doctor` 20/20, `npx.cmd expo export --platform android` completato senza errori (bundle verificato via grep). **Non verificato**: nessun rendering reale su device/emulatore Android, nessuna chiamata reale contro l'API YMove/Azure Translator, nessun login superadmin reale con un coach registrato.

### Fix bug APK Android — BUG-023/024/025/026 (2026-07-13, continuazione 8)
- **BUG-023 (causa globale tocchi persi)**: `AnimatedSplashOverlay` ora `pointerEvents="none"` + timeout di smontaggio di sicurezza; audit difensivo su tutti gli overlay assoluti (`FitnessPattern`, badge superadmin, poster player YMove). Tutte le route "che non si aprivano" erano già corrette: era solo il tocco mai consegnato.
- **BUG-024**: bottom bar superadmin ad altezza contenuto+`insets.bottom` (mai fissa), griglia dashboard con `hooks/use-two-column-grid.ts` (condiviso con la dashboard coach) e card `size="sm"`.
- **BUG-025**: seed demo locali (clienti/schede/storico/chat/bacheca) SOLO con `EXPO_PUBLIC_ENABLE_DEMO_DATA=true` (`lib/demo-data.ts`), purge dallo storage persistito degli id seed noti, badge "Demo" in lista clienti. Nessun dato Supabase toccato.
- **BUG-026**: `AppScreen` con `keyboardShouldPersistTaps="handled"`/`keyboardDismissMode="on-drag"` ovunque e `keyboardAvoiding` opt-in (registrazioni); `AppTextField` `forwardRef` + `returnKeyType="done"` default; chaining "Avanti" sulle credenziali di registrazione coach/cliente.
- **Verificato**: `tsc` pulito, `expo-doctor` 20/20, `expo export --platform android` senza errori. **Non verificato**: nessun APK ricostruito/testato su device in questa sessione.

## Funzionalità in corso / non ancora verificate visivamente
- **Tutto il blocco Superadmin + login ufficiale + registrazione coach/cliente + fatturazione (2026-07-08) non è mai stato cliccato in un browser reale**: verificato solo con `tsc --noEmit` e `expo-doctor` (entrambi puliti). Nessun clic reale su: creazione coach da superadmin, rigenerazione/attivazione codice, registrazione coach end-to-end (form → codice mostrato → copia), registrazione cliente end-to-end (codice valido/non valido/coach bloccato → messaggi d'errore corretti), filtri stato coach su schermo stretto reale, dashboard superadmin/piani/notifiche/supporto/eventi pagamento.
- **Collegamento Supabase reale ora testato a mano dall'utente** per registrazione coach, registrazione cliente e login (2026-07-09, con due bug reali trovati e corretti — vedi sopra). Non ancora verificato: logout/re-login ripetuti, un cliente che cambia dispositivo più volte, coach con più clienti reali, scadenza/disattivazione di un codice mentre un cliente lo sta usando.
- **Fase 2 email vere (2026-07-09, continuazione) + fix BUG-008 (2026-07-10) collegate ma non testate contro un'inbox reale**: conferma registrazione, password dimenticata, reset password, e l'onboarding coach/cliente differito sono implementati e passano typecheck/expo-doctor/smoke test bundle, ma nessuno è stato verificato cliccando davvero un link email su un progetto Supabase reale con "Confirm email" attivo — priorità alta per il prossimo test manuale dell'utente: rieseguire lo snippet SQL (`docs/EMAIL_SETUP.md`), registrare coach/cliente, confermare, fare login, e controllare in Table editor che `profiles`/`coach_profiles`/`billing_profiles`/`registration_codes`/`client_profiles`/`coach_clients` esistano davvero.
- Nessuna fattura reale generata.
- Verifiche ereditate dalle sessioni precedenti (workout/abbonamenti/agenda/lato cliente, vedi `docs/TODO_NEXT.md`): ancora da fare con clic reali.
- Test su iPhone/Android reale via Expo Go: non eseguibile al momento (vedi Rischi).
- **Video esercizi Supabase Storage (2026-07-11) mai testati contro un progetto reale**: bucket/tabella/policy documentati in `docs/SUPABASE_STORAGE_VIDEO.md` ma non ancora creati/eseguiti dall'utente — nessun upload coach né visualizzazione cliente verificati end-to-end. Priorità alta per il prossimo test manuale.
- **Credenziali provvisorie via email (2026-07-11, continuazione) mai testate contro un progetto reale**: Edge Function `send-temporary-credentials` scritta ma non deployata (nessun Supabase CLI in questo ambiente), colonna `must_change_password` non ancora eseguita su un progetto reale, nessun invio email reale né login con password provvisoria verificati. Vedi `docs/SUPABASE_TEMP_CREDENTIALS.md` per la checklist completa.
- **Superadmin: coach Supabase (2026-07-12) mai testati contro un progetto reale con un superadmin autentico**: richiede un utente Supabase con `role='superadmin'` (da creare/promuovere manualmente); con l'account demo locale la lista Supabase resta sempre vuota per design RLS. Vedi `docs/BUGS.md` BUG-011.
- **Pacchetti acquistabili coach/cliente (2026-07-12, RevenueCat collegato 2026-07-14 coach / 2026-07-23 cliente "Client Pro") mai testati con un acquisto reale end-to-end**: tabelle/RLS gia' eseguite su un progetto reale, checkout collegato per entrambi i ruoli, ma nessun acquisto sandbox reale (Android/iOS) mai eseguito in questo ambiente — richiede prima la configurazione esterna Google Play/App Store Connect/RevenueCat/pannello superadmin (vedi `docs/CLIENT_SUBSCRIPTIONS_SETUP.md` per Client Pro). Vedi checklist dedicata in `docs/TODO_NEXT.md`.

## Rischi/blocchi attivi
- **Debito di documentazione riconosciuto in questa sessione**: 9 commit del 2026-07-08 (prima di questa sessione) hanno introdotto tutto il sistema superadmin/registrazione/fatturazione senza mai aggiornare `PROJECT_STATE.md`/`WORKLOG.md`/`TODO_NEXT.md`/`DECISIONS.md`. Questo file è stato riallineato ora al codice reale; se emergono altri dettagli non documentati in quei commit, verificare sempre il codice sorgente, non solo questi doc.
- **Expo Go su telefono non funziona correttamente** (invariato, vedi `docs/BUGS.md`). Expo Web resta l'ambiente principale.
- `PRODUCT.md`/`DESIGN.md` non allineati al progetto.
- **Persistenza doppia in Fase 1** (nuovo): quando Supabase è configurato, coach/cliente registrati vengono scritti sia su Supabase sia sugli store locali — se in futuro le due fonti divergono (es. un coach modificato da superadmin locale ma non su Supabase), non c'è ancora sincronizzazione: la migrazione delle altre parti dell'app (schede, appuntamenti, chat, superadmin) a leggere da Supabase resta il prossimo passo architetturale importante.
- **RLS Fase 1 volutamente semplificata** (nuovo, vedi `docs/SUPABASE_SCHEMA.md`/`.sql` e `docs/DECISIONS.md`): la validazione del codice coach e il collegamento `coach_clients` avvengono lato app con policy pubbliche/self-insert, non tramite una funzione server-side atomica; da rafforzare prima di avere clienti paganti reali.
- **Nessuna scritta "demo"/limite tecnico è visibile in UI**: non distribuire a clienti reali finché i flussi email (conferma/reset, ora collegati ma non testati con un'inbox reale — vedi Fase 2 sopra) non sono stati verificati davvero su un progetto Supabase reale.
- **Reset password senza deep link nativo**: funziona solo su Expo Web in questa fase (`detectSessionInUrl` attivo solo su web); su Expo Go il link email non stabilisce automaticamente una sessione di recovery.
- Ambiente Windows senza Mac/iOS Simulator.
- Il blocco Superadmin/registrazione (2026-07-08) è il più grande blocco di funzionalità mai aggiunto senza verifica visiva in questo progetto: priorità alta per il prossimo giro di test manuali (vedi `docs/TODO_NEXT.md`).
- **Video esercizi: bucket/tabella Supabase da creare** (2026-07-11, confermato con un test reale dell'utente): finché lo snippet SQL di `docs/SUPABASE_STORAGE_VIDEO.md` non viene eseguito, l'upload coach fallisce con `Could not find the table 'public.exercise_videos' in the schema cache` — comportamento atteso, non un bug di per sé (il codice controlla `supabaseConfig.isConfigured` ma non può verificare l'esistenza della tabella prima di provare). **Possibili file orfani su Storage** da test precedenti a questo fix (2026-07-11, continuazione): il bug della mancata pulizia in caso di fallimento DB è stato corretto, ma i file già caricati prima del fix vanno rimossi manualmente — vedi `docs/SUPABASE_STORAGE_VIDEO.md`.
- **Video: 100MB per file, nessuna compressione lato server** (nuovo, 2026-07-11): un coach può caricare file grandi senza ottimizzazione; da rivedere se lo storage/banda Supabase diventa un costo rilevante.
- **Due sistemi di design paralleli, ormai residuale** (2026-07-10, aggiornato 2026-07-11): dopo la migrazione quasi completa, lo stile vecchio (`constants/theme.ts`, `Card`, `ThemedText`) resta attivo SOLO nel cluster editor scheda/esercizio (`schede/[id].tsx`, `esercizi/[id].tsx`, `schede/new.tsx`, `schede/modelli/*` e i loro componenti) — vedi sezione dedicata sopra. Non mescolare `AppCard`/`AppButton`/ecc. con `Card`/`ThemedText` nella stessa schermata quando si migra anche quel cluster.
- **Variante dark del nuovo design system non validata visivamente** (nuovo, 2026-07-10): generata da questo agente per coerenza col toggle tema esistente, ma non presente nel mockup originale (solo chiaro) né mai vista dall'utente in un browser reale.
- **Migrazione design system (2026-07-11) non verificata con clic reali**: `tsc`/`expo-doctor`/bundle Metro puliti, ma nessuna delle ~30 schermate migrate in questa sessione è stata cliccata in un browser reale da questo agente (nessun tool di automazione disponibile) — priorità alta per il prossimo giro di test manuali dell'utente, vedi `docs/TODO_NEXT.md`.
- **Nessun Supabase CLI in questo ambiente di sviluppo** (nuovo, 2026-07-11 continuazione): la Edge Function `send-temporary-credentials` è scritta e documentata ma non può essere deployata da qui — l'utente deve usare `supabase functions deploy`/`supabase secrets set` da un ambiente con il CLI installato (vedi `docs/SUPABASE_TEMP_CREDENTIALS.md`). Stesso limite si applicherebbe a qualunque futura Edge Function.
- **Fix BUG-020/021/022 (2026-07-13, continuazione 6) mai verificati con clic/rendering reali**: dashboard Android non vista su un device/emulatore alle larghezze 320/360/393/412px; picker YMove non testato contro l'API YMove reale (nessuna chiave disponibile in questo ambiente) — fallback `name`→`title` lato server richiede il deploy della Edge Function `ymove-exercises` prima di avere effetto; elenco coach reali del superadmin non testato con un login superadmin reale.
- **Invio email delle credenziali provvisorie dipende da Resend** (nuovo, 2026-07-11 continuazione): richiede una `RESEND_API_KEY` valida impostata come secret della Edge Function; senza, "Invia via email" fallisce sempre con un errore esplicito (nessun invio silenzioso). Il mittente di default (`onboarding@resend.dev`) è solo per test, va sostituito con un dominio verificato prima di un uso con clienti reali.
- **Migration `20260728090000` (immutabilità `workout_days`/`workout_day_exercises` per schede completate) NON ancora applicata al progetto Supabase reale** (verificato 2026-07-21 con `migration list`, colonna remote vuota): finché non viene applicata, un UPDATE/INSERT/DELETE diretto via PostgREST su giorni/esercizi di una scheda completed resta tecnicamente possibile, nonostante `save_workout_plan`/UPDATE diretto su `workout_plans` siano già bloccati (`20260727090000`, questa sì applicata). Nessun test funzionale reale (RPC/REST) eseguito contro Postgres in questo ambiente per nessuna delle due migration — solo verifica per traccia di codice.
- **Configurazione iOS (2026-07-22, continuazione 6/7/8)**: Edge Function `delete-account` **deployata, ACTIVE e verificata end-to-end** sul progetto reale `rkcecnzvzoigipjliwdk` (deploy in continuazione 7 — CLI disponibile via `npx supabase@latest`, contrariamente a quanto assunto nelle sessioni precedenti; test end-to-end reale in continuazione 8 con 2 account temporanei, mai dati reali: eliminazione confermata su `auth.users`+tabelle collegate, sicurezza confermata — `userId` falsificato ignorato, 401 su sessione non valida — login vecchio account confermato impossibile). **Unico limite rimasto**: nessuna verifica UI reale (nessun tool di automazione browser/app in questo ambiente). Nessuna build EAS iOS reale possibile (nessun account Apple Developer/Mac disponibile in questo ambiente). Dati Apple ancora tutti placeholder: `eas.json` → `submit.production.ios.ascAppId`, `EXPO_PUBLIC_REVENUECAT_IOS_API_KEY`, `PRIVACY_POLICY_URL`/`TERMS_OF_SERVICE_URL` (`constants/app-info.ts`, oggi `example.com`). Vedi `IOS_SETUP.md` per la checklist completa.
- **Stabilizzazione pre-APK (2026-07-22, continuazione 9)**: chiusi i 4 finding reali dell'audit "Release Gate" (BUG-048/049/050/051 in `docs/BUGS.md`) — menu Altro self_guided, reset store al logout, error handling link Privacy/Termini, deploy `send-push`. **Android: pronto per APK preview** (nessun blocco residuo noto lato codice/config). **iOS: repository pronto per development build**; TestFlight/App Store restano dipendenti solo da configurazioni esterne (ASC App ID, chiave RevenueCat iOS, privacy policy reale — mai dati di codice mancanti). `expo-doctor`/`expo install --check` sono scesi a 19/20 durante questa sessione per drift del registro npm (13 pacchetti Expo con patch version leggermente indietro), **senza alcuna modifica di questo task a `package.json`/`package-lock.json`** (verificato `git diff --stat` vuoto) — nessuna azione eseguita, vietato installare/aggiornare pacchetti in questo task; da rivalutare con un `npx expo install --check` la prossima volta che si toccano le dipendenze.
- **Elenco aperto dall'audit di stabilizzazione (2026-07-21), aggiornato 2026-07-22 (continuazione 5)**: risolti in questa sessione — commit separato modifiche RevenueCat (`b8a8b72`, poi fix PRODUCT_CHANGE `4f366e9`); ritorno corretto dopo eliminazione scheda cliente (BUG-047, `10f0383`); tab Chat nascosta ai clienti autonomi (`924489f`/`48a0dd1`); cartelle di backup `mobile_backup_20260715`/`mobile_git_backup` eliminate (`3860ba9`); dipendenze npm verificate, nessuna da rimuovere (`expo-glass-effect`/`expo-symbols` sono dependencies reali di `expo-router`); flusso BIA e parser InBody120 rimossi interamente dal repository (mai committato prima, `45dbc5e`) invece di essere commitati. Ancora aperto: test runtime reali delle migration `20260727090000`/`20260728090000`; migration del bucket Storage `exercise-videos` (versionata in `20260729090000`, mai eseguita su un progetto reale); `mobile/.env.example` ancora mancante (creazione bloccata dalle impostazioni di permesso di questo ambiente, contenuto proposto in `docs/TODO_NEXT.md`); test di un APK preview reale; build AAB di produzione. Vedi checklist dedicata in `docs/TODO_NEXT.md`.
- **Pacchetti coach Starter/Pro/Studio non esistono ancora come righe reali in `subscription_packages`** (nuovo, 2026-07-24): lo schema/logica upgrade (`abbonamento-coach.tsx`) e' pronta e basata su `max_clients`, ma finche' il superadmin non crea questi 3 pacchetti da `/superadmin/pacchetti` (con `max_clients` 10/25/50) la sezione "Abbonamento"/upgrade dell'hub Area coach mostrera' solo "Nessun pacchetto disponibile" o i pacchetti legacy eventualmente gia' presenti. Non e' un bug di codice, e' dato mancante.
- **Nessun test runner configurato lato mobile** (confermato di nuovo 2026-07-24, hub Area coach): nessun Jest/Vitest/RNTL, nessun file `*.test.ts(x)` fuori da `node_modules` — i test richiesti per Area coach/Clienti/Appuntamenti/Abbonamento/Account/Navigazione sono stati sostituiti da una checklist di verifica manuale (vedi `docs/TODO_NEXT.md`), non installato un runner per non violare il vincolo "non installare pacchetti senza necessità" del task.
- **BUG-054 — CORRETTO (2026-07-27)**: il trigger `workout_plans_prevent_completed_edit` non aveva eccezione per la cancellazione account — bloccava in modo permanente l'eliminazione di qualunque account (cliente o coach) con almeno una sessione di allenamento completata, incluso il flusso `delete-account`. Corretto con `20260802090000_allow_account_deletion_of_completed_workouts.sql`: bypass mirato solo a `DELETE` eseguito dal ruolo di sistema `supabase_auth_admin` (non `service_role`, come inizialmente ipotizzato: verificato che non e' quello il contesto usato da GoTrue per la cascade delete). Verificato end-to-end con account sintetici; nessuna regressione (`UPDATE`/`DELETE` come `service_role` restano bloccati). Vedi `docs/BUGS.md` BUG-054 per i dettagli completi. L'account reale di Luigi (11 sessioni completate) non dovrebbe piu' incontrare questo blocco a un futuro tentativo di eliminazione (non testato direttamente sul suo account, per costruzione).

**Aggiornamento corrente 47:** 2026-07-28 (quindicesima continuazione) - **Smoke test HTTP reale sul webhook RevenueCat (colma il limite dell'Aggiornamento 46)**: eseguita una chiamata HTTP reale (non simulata a livello DB) contro la Edge Function `revenuecat-webhook` già deployata in produzione, con il secret RevenueCat realmente configurato (fornito dall'utente, usato solo runtime, mai scritto su disco/commit). Con un account sintetico dedicato (`@example.invalid`, mai un abbonamento reale): payload `REFUND` valido inviato via `POST` reale → **HTTP 200**, `subscription.status` passato a `'refunded'`, `_has_active_client_pro_entitlement` → `false`, `log_self_guided_exercise_progress` bloccata con `SUBSCRIPTION_REQUIRED` (chiamata come utente autenticato reale, non service_role), storico conservato in `revenuecat_webhook_events`. Stesso `event_id` reinviato → HTTP 200 `{"duplicate":true}`, nessuna scrittura aggiuntiva (idempotenza confermata via HTTP reale, non solo via vincolo DB come nell'Aggiornamento 46). Cleanup completo, residuo zero. **BUG-061 ora PASS anche a livello HTTP reale**, non più solo a livello database — il limite "non verificabile in questo ambiente" dell'Aggiornamento 46 è superato per questo scenario specifico (resta non eseguibile solo la suite Deno `index.test.ts`, che richiede un binario Deno mai disponibile in questo ambiente). Nessuna modifica di codice in questo intervento (solo documentazione). **Nota residua non bloccante**: una cartella `supabase/functions/revenuecat-webhook-test-harness/` (mai deployata, non tracciata da git, residuo di un approccio esplorato e abbandonato in una sessione precedente) resta sul filesystem — 2 tentativi di rimozione bloccati dal permesso Bash, segnalata all'utente per rimozione manuale se desiderata. Nessun test UI eseguito né dichiarato PASS. Dettaglio completo in `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-061).

**Aggiornamento corrente 46:** 2026-07-28 (quattordicesima continuazione) - **Verifica finale pre-release**: BUG-061 corretto (webhook RevenueCat ora gestisce `REFUND` — revoca immediata indipendente da `expiresAt`, deliberatamente esclusa dal controllo di staleness per non rischiare di scartare un rimborso reale dopo un rinnovo; nuovo status `refunded`, migration additiva sul CHECK). Funzione **deployata in produzione**. Testato dal vivo a livello database con client sintetico (rimborso dopo rinnovo → entitlement revocato, ledger `effective_active_days` chiuso automaticamente, logging progressi bloccato, schede/storico intatti; nuovo abbonamento dopo rimborso → entitlement ripristinato senza toccare la riga rimborsata; idempotenza confermata a livello di vincolo DB). **Non verificabile in questo ambiente**: chiamata HTTP reale alla function (nessun Docker/Deno disponibile) — 6 nuovi test Deno scritti ma non eseguibili, stesso limite pre-esistente già documentato in una sessione precedente. **Verifica bootstrap `client_program_cycle_active_periods`**: controllato l'unico ciclo reale attualmente in stato aperto (cliente reale, non sintetico) — `effective_active_days=0` risulta **corretto**, non un bug: il cliente non ha alcun abbonamento Client Pro attivo (tutte le righe `user_subscriptions` scadute/disdette da prima dell'inizio di questa sessione), quindi il ciclo non è mai stato "effettivamente utilizzabile" da quando il ledger esiste — nessuna migrazione correttiva necessaria. **Test UI manuali**: nessun tool di automazione disponibile (verificato di nuovo) — checklist aggiornata in `docs/TODO_NEXT.md` con 2 scenari aggiuntivi (logout/login con store puliti, rimborso), tutti dichiarati esplicitamente aperti. **Correzione riepilogo commit**: il report della sessione precedente dichiarava 6 commit elencandone 5 — la sessione precedente aveva effettivamente creato 5 commit (`041d1c0`...`3910851`), non 6; correzione riportata nel report finale di questa sessione. Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito, cleanup sintetici zero residui. Dettaglio completo in `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-061 chiuso), `docs/TODO_NEXT.md`.

**Aggiornamento corrente 45:** 2026-07-28 (tredicesima continuazione) - **Chiusura dei punti residui del Blocco 2 (BUG-057, effective_active_days) + Blocco 3 "Notifiche" completo**, in un'unica esecuzione continua come richiesto esplicitamente. **BUG-057**: applicata la regola ordinale definitiva dei livelli (`exercise.min_level <= template.level`) a tutti i 18 template `auto_eligible` — 11 template/26 righe incompatibili trovati (non 12 come stimato: "Corpo Libero" risultava PASS con la regola più permissiva usata durante 2.2/BUG-055b, fallisce con la regola letterale — discrepanza documentata in `docs/BUGS.md`), corretti riusando esercizi esistenti nello stesso `substitution_group`/ruolo/gruppo muscolare (2 nuovi esercizi creati solo dove nessuna alternativa esistente bastava: `spalle-shoulder-press-macchina`, `tricipiti-dip-ginocchia-piegate`), **18/18 PASS confermato**, nessuna scheda cliente reale toccata (verificato: zero FK dirette). **effective_active_days**: nuovo ledger `client_program_cycle_active_periods` (intervalli di reale utilizzabilità: abbonamento valido + nessun coach attivo + stato ciclo aperto), sincronizzato in tempo reale da 2 trigger (uno su `client_program_cycles`, cattura ogni transizione di stato da qualunque funzione; uno su `user_subscriptions`, cattura scadenza/rinnovo/cancellazione/revoca indipendentemente da quando viene richiamato `run_cycle_review`) — nessuna seconda fonte di verità sull'abbonamento, riusa `_has_active_client_pro_entitlement` già esistente. `_compute_cycle_progress_metrics` ora usa i giorni effettivamente utilizzabili (non i giorni di calendario) per calcolare le sessioni attese: un cliente non è più penalizzato per giorni di abbonamento scaduto/sospeso/con coach attivo. Testato dal vivo: scadenza a metà ciclo, rinnovo dopo pausa, più sospensioni nello stesso ciclo, cancellazione con accesso valido fino a scadenza vs revoca immediata, coach assegnato durante la sospensione, cleanup account con storico ledger reale. **Scoperta collaterale non corretta** (fuori scope, **BUG-061**): il webhook RevenueCat non gestisce l'evento `REFUND` (revoca immediata), solo `REFUND_REVERSED`. **Blocco 3 "Notifiche"**: 4 nuove RPC condivise cliente/Superadmin (`list_my_notifications`, `count_my_unread_notifications`, `mark_notification_read`, `mark_all_notifications_read`), RLS irrigidita (rimossa la vecchia policy UPDATE diretta che permetteva scrivere qualunque colonna, non solo `read_at`). **BUG-062** trovato e corretto: il cliente non veniva mai avvisato per il blocco di sicurezza né per l'assegnazione di un coach (solo il superadmin per il primo, nessuno per il secondo) — 2 notifiche client-facing aggiunte. Mobile: nuovo componente condiviso `AppNotificationList`, nuove schermate `/notifiche` (cliente) e `/superadmin/notifiche-programmi` (Superadmin, file nuovo e distinto dal registro locale preesistente `/superadmin/notifications`), icona campanella con badge nella Home cliente e nuova Azione rapida nella Home Superadmin. **BUG-060** trovato e corretto: `client-monthly-checkin-store.ts` (2.6) non aveva mai un `reset()` né era incluso nell'azzeramento degli store al logout (stesso pattern di BUG-049, omissione non propagata). Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito, bundle Expo Web compilato con successo, cleanup sintetici zero residui. **Test UI reali non eseguibili** (nessun tool di automazione browser disponibile, verificato esplicitamente in questa sessione) — checklist completa in `docs/TODO_NEXT.md`, scenari dichiarati esplicitamente aperti, non PASS. Dettaglio completo in `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-057 chiuso, BUG-060/061/062 nuovi), `docs/DECISIONS.md`. **Non si procede oltre il Blocco 3** (nessuna nuova funzionalità iniziata), come richiesto esplicitamente.

**Aggiornamento corrente 44:** 2026-07-28 (dodicesima continuazione) - **Blocco 2 "Programmi automatici" COMPLETO**: verifica runtime `pending_template` (mai testata dal vivo nel 2.3), sotto-blocchi 2.4 (coach handoff), 2.5 (7 RPC Superadmin), 2.6 (schermate mobile check-in/review), 2.7 (campagna di test end-to-end finale con account sintetici + **token REST reali**). Eseguito in autonomia continua senza fermarsi per bug/fix/documentazione, come richiesto esplicitamente. **18 migration applicate** in questa sessione (10 per pending_template/2.4/2.5, 8 per la campagna 2.7). Riepilogo bug: 1 fix pending_template (metrics_snapshot vuoto nel ramo "template svuotato dalle esclusioni"), 1 fix 2.4 (conflitto review sul trigger coach handoff), 4 fix 2.5 (transizione senza review valida, `<>any`/`<>all` in `superadmin_force_cycle_decision`, vocabolario `origin` invertito, retry dopo `superadmin_cancel_pending_review`), 1 fix mobile 2.6 (**BUG-058**: `ProgramCycleStatus`/query card mai aggiornati dopo il 2.1 — la card spariva per 8 stati su 9), **6 fix 2.7 in codice 2.3 già "chiuso"** (**BUG-059**: doppia chiamata concorrente duplicava review `insufficient_data`; catena di 5 bug sul rinnovo abbonamento/risoluzione sicurezza — un cliente rinnovato o un blocco sicurezza risolto da Superadmin restavano bloccati PER SEMPRE al giro di revisione successivo, mai scoperto perché mai testato un vero ciclo scadenza→rinnovo→check-in). Nuova convenzione esplicita documentata in `docs/DECISIONS.md`: `insufficient_data`/`blocked_subscription`/`blocked_safety` sono blocchi temporanei/retriable, mai decisioni definitive — ogni punto della funzione che li tratta come "già recensito" deve escluderli tutti e tre, non solo `insufficient_data` come faceva il codice originale. **Campagna 2.7 completa**: rivalidazione decisione+idempotenza `run_cycle_review` dopo tutti i fix; sicurezza (anon 401, cliente normale 400 `FORBIDDEN` su tutte le 8 RPC Superadmin, cross-client `FORBIDDEN`, scrittura diretta via REST bypass RPC → 0 righe, lettura RLS corretta); doppia chiamata concorrente (`Promise.all` reale); scadenza/rinnovo abbonamento end-to-end; risoluzione sicurezza `approve_continue` end-to-end; **cancellazione account con storico Blocco 2 reale** tramite l'Edge Function di produzione `delete-account` (non solo Admin API) → residuo zero su tutte le tabelle, nessuna FK `RESTRICT` introdotta dalle nuove tabelle del Blocco 2 (verificato, nessun rischio tipo BUG-054). Fix mobile collaterale: `AutoProgramCard` tenta ora un `runCycleReview` silenzioso in background quando rileva `paused_subscription`, cosi' un rinnovo reale si riflette senza richiedere un bottone che non esiste in quello stato. Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito, cleanup sintetici zero residui. Dettaglio completo in `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-058/BUG-059), `docs/DECISIONS.md`. **Scenari #15/#16 (pausa abbonamento breve/lunga di sezione 18) restano non testati con granularità fine**, bloccati dal gap noto `effective_active_days` mai popolato (documentato, non un nuovo bug). Nessun clic reale in browser/app eseguito — checklist in `docs/TODO_NEXT.md`. **Blocco 2 dichiarato completo. Non si procede al Blocco 3**, come richiesto esplicitamente due volte dall'utente.

**Aggiornamento corrente 43:** 2026-07-28 (undicesima continuazione) - Blocco 2, sotto-blocco 2.3 implementato e testato: eleggibilità/check-in/motore atomico di revisione del ciclo. **Sezione 2 (prerequisito) prima di tutto**: `assign_initial_auto_program()` aveva due difetti dalla stessa causa (lessico di stato non aggiornato dopo il 2.1) — la lista hardcoded `status in ('draft','active','pending_review')` (sostituita con il nuovo helper centralizzato `_cycle_open_statuses()`) e, scoperta più grave, **un bug live**: la funzione inseriva ancora `status='pending_review'`, valore non più ammesso dal CHECK a 11 stati del 2.1 — qualunque cliente reale con `requires_professional_supervision=true` o "nessun piano dopo esclusioni" avrebbe ottenuto un errore DB invece di un'assegnazione in attesa. Corretto mappando ai due stati giusti (`pending_safety_review`/`pending_template`). Migrazione `20260810090000_block2_cycle_review_engine.sql` (+ 3 fix isolati successivi, vedi sotto): helper di stato centralizzati (`_cycle_open_statuses`/`_cycle_terminal_statuses`/`_cycle_blocked_statuses`), motore di matching condiviso `_match_auto_template` (estratto da `assign_initial_auto_program`, riusato anche dal motore di revisione), estensione additiva di `client_cycle_reviews_eligibility_result_check` (+`no_active_cycle`/`forbidden`), chiusura della policy RLS `client_monthly_checkins_owner_all` (ALL troppo permissiva, sostituita con sola lettura: da qui l'unico percorso di scrittura è `submit_monthly_checkin`), colonna+indice di dedup per `app_notifications`. Tre nuove RPC: `check_cycle_review_eligibility` (pura, 11 result_code con ordine di priorità documentato), `submit_monthly_checkin` (upsert autorevole, validazione esercizi liked/disliked contro il piano del ciclo), `run_cycle_review` (motore atomico a 17 passi: lock riga+advisory lock, idempotenza su stati bloccati/terminali, ordine di decisione fisso `blocked_safety→blocked_subscription→manual_review→insufficient_data→pending_template→regress→progress→partial_change→maintain`, progressione/regressione con arrotondamento equipaggiamento-coerente, sostituzione parziale con cap 40%/pavimento 60%, audit completo per esercizio). **3 bug reali trovati SOLO eseguendo le RPC con account sintetici** (mai dalla validazione sintattica BEGIN/ROLLBACK, che verifica solo che `CREATE FUNCTION` sia sintatticamente valido): riferimenti a colonna ambigui (`cycle_id`/`decision` come parametri OUT di `returns table` in conflitto con le stesse colonne di `client_cycle_reviews` nelle query interne — fix `20260810094500`); il ciclo nuovo veniva inserito `active` PRIMA di chiudere il vecchio, violando l'indice unico "un solo ciclo corrente per cliente" (fix `20260810100000`, chiusura del vecchio ciclo anticipata); un ramo di cambio-modello strutturale non replicava il controllo "giorno vuoto dopo esclusioni" già presente in `assign_initial_auto_program` (fix incluso in `20260810093000`, individuato dall'advisor prima ancora della matrice di test). **Test con 10 account sintetici** (dominio `@example.invalid`, prefisso `block23-test-`, SQL con ruolo `authenticated` reale via `set_config('request.jwt.claims',...)` + REST reali con token da login): PASS confermati — `checkin_required`→`eligible` in sequenza corretta; `progress` (aderenza 100%, 11 esercizi progrediti, verificato idempotente su retry: stesso `next_cycle_id`, 0 righe duplicate); `regress` (aderenza 67%, RPE alto, 12 esercizi regrediti); `partial_change` (esercizio sgradito sostituito, cap 4/12=40% rispettato esattamente, esercizio escluso mai più presente nel piano nuovo); `blocked_safety` (dolore nel check-in → `pending_safety_review`, 1 sola notifica superadmin, check-in bloccato); `blocked_subscription` (abbonamento scaduto tra check-in e revisione); `manual_review` (coach assegnato dopo il check-in); cross-client REST → `forbidden`/`FORBIDDEN` su entrambe le RPC; doppia chiamata REST reale → risultato identico, 0 duplicati (tranne `insufficient_data`, per progettazione **multiplo per giorno** — l'indice univoco parziale lo esenta esplicitamente, solo la notifica è dedupata per giorno). **Non testato dal vivo**: `pending_template` (il classificatore di permessi ha bloccato il test perché richiedeva disattivare temporaneamente 2 template di sistema condivisi/reali senza garanzia transazionale sufficiente — verificato solo per lettura del codice, disclosure esplicita). Cleanup completo: 10 account sintetici + 2 notifiche superadmin di test rimossi, **residuo zero verificato su tutte le tabelle coinvolte**, ciclo reale del cliente reale confermato **byte-per-byte invariato**. Verificati `db push --dry-run` pulito, `git diff --check` pulito. Dettaglio completo in `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/BLOCK_2_DESIGN.md`. **Fermato dopo il commit del 2.3, come richiesto esplicitamente — sotto-blocco 2.4 (coach handoff) non iniziato.**

**Aggiornamento corrente 42:** 2026-07-27 (decima continuazione) - BUG-056 chiuso: l'utente ha fornito la regola ordinale definitiva di compatibilità livello (`beginner=1/intermediate=2/advanced=3`, `exercise.min_level <= template.level`). Migration `20260809090000_fix_bug_056_level_hierarchy.sql`: `gambe-bulgarian-split-squat` (`advanced`) in "Manubri ed Elastici" (Intermedio) sostituito con `gambe-affondi` (stesso `substitution_group`/pattern/gruppo muscolare/unilateralità, realmente `intermediate`). 18/18 PASS confermato col criterio operativo, non regressione dati reali confermata. **Scoperta collaterale fuori scope**: la regola ordinale applicata letteralmente a tutti i 18 template trova 12 template aggiuntivi con contenuto `advanced` sopra il proprio livello — registrato come **BUG-057**, non corretto (decisione utente futura necessaria). Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito. Dettaglio in `docs/WORKLOG.md`, `docs/BUGS.md`, `docs/DECISIONS.md`. **Procedo ora col sotto-blocco 2.3** (motore di revisione), come richiesto esplicitamente.

**Aggiornamento corrente 41:** 2026-07-27 (nona continuazione) - riapertura e correzione semantica di BUG-055: l'utente ha segnalato che 2 delle 4 sostituzioni del fix precedente (Aggiornamento 40) non preservavano davvero attrezzatura/funzione tecnica del template, nonostante il "18/18 PASS" automatico. Causa del falso PASS: il validatore controllava solo il tag grezzo a 3 livelli dell'attrezzatura (mai il testo dichiarato dell'esercizio nel catalogo mobile) e solo `movement_pattern` per le sostituzioni (mai `substitution_group`, il campo più preciso già presente nei dati dal 2.2). Migration `20260808090000_fix_bug_055b_template_semantic_equipment.sql`: aggiunti 3 nuovi esercizi al catalogo (`gambe-air-squat`, `dorso-rematore-corpo-libero`, `gambe-stacco-rumeno-manubri`), ciascuno nello stesso `substitution_group` dell'esercizio che sostituisce; corrette di nuovo le 4 righe (2 in Corpo Libero — `glutei-squat-sumo`→`gambe-air-squat`, `dorso-rematore-manubrio`→`dorso-rematore-corpo-libero`; 1 in Manubri ed Elastici — `femorali-hip-hinge`→`gambe-stacco-rumeno-manubri`; 1 in Tecnica dei Fondamentali — `gambe-hip-thrust`→`gambe-stacco-rumeno-manubri`). Riesaminato `tricipiti-dip-tricipiti`: il suo testo ("Parallele o panca") offre già un'alternativa a bassa attrezzatura coerente con la nota del template — non toccato, non era rotto. **Rivalidati tutti i 18 template** (non solo i 3 toccati) con la regola corretta: 18/18 PASS confermato, nessun'altra incompatibilità trovata altrove. **Scoperta collaterale fuori scope**: `gambe-bulgarian-split-squat` (`advanced`) usato in "Manubri ed Elastici" (`Intermedio`) — registrato come **BUG-056** (non corretto, richiede prima una decisione di prodotto sulla regola di compatibilità livello per template `Intermedio`/`Avanzato`, mai definita finora). Test con account sintetici ripetuto: 3/3 template PASS (10 piani totali), cleanup e residuo zero verificati. Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito. Dettaglio completo in **`docs/BUG_055_TEMPLATE_FIX.md`** (sezione "Riapertura"), `docs/BUGS.md`, `docs/BLOCK_2_DESIGN.md` sezione 26, `docs/DECISIONS.md`. **Resto del Blocco 2 (2.3 in poi) ancora non iniziato**, come richiesto esplicitamente dall'utente.

**Aggiornamento corrente 40:** 2026-07-27 (ottava continuazione) - BUG-055 corretto: contenuto incompatibile dei 3 template automatici (Corpo Libero, Manubri ed Elastici, Tecnica dei Fondamentali), scoperto nel sotto-blocco 2.2. Migration `20260807090000_fix_bug_055_auto_template_content.sql` applicata al DB reale: 4 righe di `workout_template_exercises` corrette (2 in "Corpo Libero" — `gambe-squat`→`glutei-squat-sumo`, `dorso-trazioni`→`dorso-rematore-manubrio`; 1 in "Manubri ed Elastici" — `gambe-stacco-rumeno`→`femorali-hip-hinge`; 1 in "Tecnica dei Fondamentali" — `gambe-stacco-rumeno`→`gambe-hip-thrust`), più 2 righe additive in `exercise_alternatives`. Verificata prima la regola di immutabilità (nessuna FK tra `workout_template_*` e le tabelle reali dei piani, copia per valore, 0 utilizzi reali dei 3 template) — corretto in-place, non versionato. Migration con verifica di pre-condizione esplicita (`raise exception` se il contenuto non corrispondeva a quello analizzato) e verifica di non-regressione su una riga adiacente non toccata. **Risultato: 18/18 template `auto_eligible` PASS** (era 15/18), 0 duplicati, 0 esercizi senza alternativa tra i nuovi inseriti, `plans_from_these_templates=0` sia prima che dopo (nessuna scheda cliente reale toccata). **Test con account sintetici via percorso server reale** (`assign_workout_template_to_client`, mai clienti reali): coach e cliente sintetici creati tramite il vero trigger `handle_new_user()` (non insert manuali), 3/3 template assegnati con copie esatte (10 piani totali su 3+4+3 giorni), cleanup via `auth.admin.deleteUser` con zero residuo verificato. **BUG-055 chiuso in `docs/BUGS.md`**. Il motore di revisione (sotto-blocco 2.3, non ancora iniziato) può ora fidarsi del flag `auto_eligible` per tutti e 18 i template. Dettaglio completo (tabella prima/dopo, criteri di sostituzione) in **`docs/BUG_055_TEMPLATE_FIX.md`** (nuovo documento dedicato), `docs/EXERCISE_METADATA_COVERAGE.md` aggiornato, `docs/BLOCK_2_DESIGN.md` sezione 25. Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito, `git diff --check` pulito.

**Aggiornamento corrente 39:** 2026-07-27 (settima continuazione) - Blocco 2, sotto-blocco 2.2: seed completo e verificato dei metadati esercizio. Migration `20260805090000_block2_exercise_metadata_seed.sql` (+ `20260806090000_block2_exercise_alternatives_gaps.sql` per colmare lacune trovate dalla validazione automatica): `exercise_movement_metadata` popolata al 100% (96/96 esercizi del catalogo `mobile/src/data/exercise-library.ts`, 78/78 esercizi usati dai 18 template `auto_eligible`, 0 riferimenti orfani/mancanti — verificato anche contro tutte le schede reali assegnate, non solo i template); `exercise_alternatives` popolata con 114 relazioni curate, direzionali (mai simmetria assunta), con priorità/difficoltà relativa/motivo. `movement_pattern` esteso con 3 valori (`core_flexion`/`core_extension`/`core_rotation`, necessità dimostrata durante la classificazione — crunch/hyperextension/russian-twist sono movimenti dinamici, non tenute isometriche come plank/pallof-press) — non adottati gli altri 21 valori suggeriti, già coperti da categorie esistenti senza necessità dimostrata. Tassonomia di attrezzatura/luogo/livello/categoria/gruppi muscolari riusata identica dal 2.1 (nessuna reinvenzione). **Scoperta rilevante**: verificando i 18 template `auto_eligible` contro i metadati appena classificati, 3 template (Blocco 1) risultano contenere esercizi con attrezzatura o livello incoerenti col template stesso (es. uno squat a bilanciere in un template "Corpo Libero") — registrato come **BUG-055** in `docs/BUGS.md`, non corretto né disattivato (decisione dell'utente richiesta, come esplicitamente previsto dal task). Un solo esercizio dei template (`gambe-leg-extension`) resta senza alcuna alternativa, per scelta esplicita documentata (nessuna alternativa credibile esiste senza cambiare schema di movimento). Test RLS confermano nessuna regressione: `authenticated` generico e `anon` senza alcun accesso (lettura/scrittura), Superadmin reale con accesso completo. Nessun dato reale di cicli/clienti/schede toccato (verificato). Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito. Dettaglio completo tassonomia/copertura/eccezioni in **`docs/EXERCISE_METADATA_COVERAGE.md`** (nuovo documento dedicato). **Resto del Blocco 2 (2.3 motore di revisione, 2.4 coach handoff, 2.5 RPC Superadmin, 2.6 UI, 2.7 verifica end-to-end) non ancora iniziato**, in attesa di ulteriore via libera esplicita e della decisione utente su BUG-055. Dettaglio completo in `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/BLOCK_2_DESIGN.md` sezione 24.

**Aggiornamento corrente 38:** 2026-07-27 (sesta continuazione) - Blocco 2, sotto-blocco 2.1: schema per cicli/check-in/revisioni implementato e chiuso (solo schema, nessuna RPC/UI). Migration `20260804090000_block2_cycle_review_schema.sql`: `client_program_cycles` esteso a 11 stati (`draft/active/checkin_due/review_pending/pending_subscription/paused_subscription/pending_safety_review/pending_template/completed/replaced/cancelled`, rename `superseded_at`→`replaced_at`, +5 colonne), `client_monthly_checkins` esteso con 10 campi periodici + trigger di lock post-review, `client_cycle_reviews` esteso a 9 decisioni + 15 colonne, nuove tabelle `auto_program_review_config` (versionata per riga, seed 14 soglie approvate, 1 sola versione attiva), `exercise_movement_metadata` (schema pronto, 1 riga minima, seed completo rimandato al 2.2), `exercise_alternatives`, `client_cycle_exercise_transitions` (audit esercizi con trigger anti cross-client), `superadmin_program_overrides` esteso (13 azioni, audit completo, `notes` reso obbligatorio). **Scoperta rilevante durante l'analisi**: il vincolo `UNIQUE(cycle_id)` esistente su `client_cycle_reviews` avrebbe impedito i tentativi multipli `insufficient_data` richiesti dal design — sostituito con un indice univoco parziale (`where decision <> 'insufficient_data'`), verificato con test reali. **Nessuna tabella ledger pause abbonamento creata** (non approvata da `docs/BLOCK_2_DESIGN.md`, decisione esplicita documentata). Verificato read-only prima e dopo: 1 riga reale in `client_program_cycles` (cliente reale, invariata, compatibile con tutte le nuove colonne/CHECK). Test PASS: colonne/CHECK/FK/indici/default/RLS/grant/seed tutti verificati; comportamentali (soglia fuori range, doppia review definitiva, tentativi insufficient_data multipli, self-reference esercizio, esercizio non registrato, modifica check-in dopo lock, transizione cross-client — tutti respinti come previsto); per ruolo (`authenticated` generico e `anon` → 0 accesso alle tabelle amministrative, cliente proprietario reale → invariato, Superadmin reale → accesso completo). **Nota di compatibilità segnalata per il 2.3** (non risolta, fuori perimetro schema): `assign_initial_auto_program()` ha un controllo hardcoded sui vecchi 3 stati non terminali, da aggiornare insieme al motore di revisione. Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito, `git diff --check` pulito. **Resto del Blocco 2 (2.2 seed completo, 2.3 motore di revisione, 2.4 coach handoff, 2.5 RPC Superadmin, 2.6 UI, 2.7 verifica end-to-end) non ancora iniziato**, in attesa di ulteriore via libera esplicita. Dettaglio completo in `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/BLOCK_2_DESIGN.md` sezione 23.

**Aggiornamento corrente 37:** 2026-07-27 (quinta continuazione) - Blocco 2 "Programmi automatici": progettazione **approvata** con 4 decisioni esplicite dell'utente (soglie di default in tabella server-side versionata `auto_program_review_config.config_version`; estensione/rename CHECK solo via unica migration transazionale con mappatura documentata; stato dedicato `pending_template` mai sovrapposto a `pending_safety_review`; procedere subito solo col sotto-blocco 2.0). **Sotto-blocco 2.0 implementato, testato, chiuso**: migration `20260803090000_enable_self_guided_exercise_progress_logging.sql` aggiunge 2 RPC `SECURITY DEFINER` (`log_self_guided_exercise_progress`, `delete_self_guided_exercise_progress`) che abilitano il logging progressi (serie/ripetizioni/carico/RPE/note) per clienti self-guided, impossibile fino ad oggi (`resolveProgressActor` in `exercise-progress-service.ts` richiedeva sempre una riga `coach_clients` attiva, che un self-guided non ha mai). **Nessuna nuova policy RLS aggiunta** (scelta deliberata: nessuna policy esistente permette comunque un bypass diretto per un self-guided, verificato empiricamente con un tentativo di insert diretto via REST → `42501`); percorso coach invariato (verificato con un insert diretto reale, riuscito come prima). `mobile/src/lib/exercise-progress-service.ts` esteso con un ramo self-guided esplicito in `createExerciseProgressEntries`/`deleteExerciseProgressEntry`. **Test**: 15/15 scenari della matrice richiesta PASS (SQL con ruolo `authenticated` reale + REST API reali, 7 account sintetici dominio `@block20-test.invalid`, harness Edge Function temporanea rimossa a fine test), più verifiche di difesa in profondità (bypass RLS bloccato, `anon` senza execute, rollback su payload parzialmente invalido, comportamento deterministico su doppia chiamata). Cleanup completo verificato (0 righe residue), incluso il cleanup di un account con una sessione completata (ulteriore conferma indiretta che BUG-054 funziona). Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito, `git diff --check` pulito. **Resto del Blocco 2 (sotto-blocchi 2.1-2.7: schema cicli/review/check-in, motore di revisione, variazione esercizi, RPC Superadmin, UI) non ancora iniziato**, in attesa di ulteriore via libera esplicita, come richiesto. Dettaglio completo in `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/BLOCK_2_DESIGN.md`.

**Aggiornamento corrente 36:** 2026-07-27 (quarta continuazione) - progettazione completa del Blocco 2 "Programmi automatici" (solo documentazione, nessuna migrazione/RPC/schermata): nuovo `docs/BLOCK_2_DESIGN.md` (22 sezioni) dopo analisi approfondita di tutto lo schema/RPC del Blocco 1 e dei servizi mobile collegati. **Verificato read-only sul progetto reale** prima di scrivere il documento: 1 ciclo reale attivo (cliente reale, non sintetico, `status='active'`, avviato oggi), 0 review/check-in, 0 righe di progresso self-guided (`exercise_progress_history` con `coach_id is null`). **Gap bloccante trovato e documentato come prerequisito** ("sotto-blocco 2.0"): `exercise-progress-service.ts`/`resolveProgressActor()` impedisce strutturalmente a qualunque cliente self-guided di loggare un carico/ripetizione (richiede sempre una riga `coach_clients` attiva) — senza questo fix il motore di revisione del Blocco 2 non avrebbe mai dati di carico/RPE su cui operare, solo dati di aderenza. Altri gap documentati: CHECK di `client_program_cycles.status`/`client_cycle_reviews.decision` da estendere (verificato sicuro sui dati reali: 1 riga/0 righe); `client_monthly_checkins` manca campi fatica/recupero/soddisfazione/disponibilità/attrezzatura/motivo-skip; nessun metadato strutturato di esercizio (schema di movimento/tag attrezzatura/alternative) in nessuna tabella o tipo TS esistente; nessun ledger pause abbonamento; `app_notifications` senza consumer UI (Blocco 3). Documento propone: macchina a 10 stati con transizioni esplicite, funzione di eleggibilità a 9 esiti, motore di decisione a 8 esiti, gerarchia di progressione a 5 livelli con soglie tutte in una nuova tabella di configurazione, algoritmo di sostituzione esercizi con nuova tabella metadati, RPC atomica `run_cycle_review()` (pattern identico ad `assign_initial_auto_program`), 5 nuove RPC Superadmin, matrice di test a 23 scenari. Ogni soglia numerica marcata esplicitamente [PROPOSTA DA APPROVARE], ogni scelta di modellazione non univoca marcata [INTERPRETAZIONE — da confermare]. **Nessun codice/schema applicato**: `git status`/`git diff --check` toccano solo file di documentazione. Prossimo passo: revisione utente del documento, poi implementazione a partire dal sotto-blocco 2.0. Dettaglio completo in `docs/BLOCK_2_DESIGN.md`, `docs/WORKLOG.md`, `docs/DECISIONS.md`.

**Aggiornamento corrente 35:** 2026-07-27 (terza continuazione) - fix BUG-054 (chiusura): trovato l'errore nel fix inizialmente suggerito nell'Aggiornamento 34 (`auth.role() = 'service_role'`) prima di scriverlo in migration — verificato che `auth.admin.deleteUser()` non passa da PostgREST, quindi `request.jwt.claims` non e' mai valorizzato in quel contesto e quella condizione non avrebbe mai potuto attivarsi. Verificato invece (query su `pg_roles`) che GoTrue si connette con il proprio ruolo dedicato `supabase_auth_admin`. Migration `supabase/migrations/20260802090000_allow_account_deletion_of_completed_workouts.sql` (applicata al DB reale dopo conferma esplicita dell'utente): bypass aggiunto a tutti e 3 i trigger di immutabilita' (`workout_plans`/`workout_days`/`workout_day_exercises`), attivo SOLO per `DELETE` SOLO quando l'esecutore e' `supabase_auth_admin` — nessuna eccezione per `service_role` generico, `UPDATE`/`INSERT`, o superadmin: la protezione verso coach/client/superadmin via API resta piena. Verificato end-to-end con harness Edge Function temporanea e account sintetici (dominio `@bug054-test.invalid`, rimossa a fine test): eliminazione di un account con scheda completata riuscita; test di non regressione (`UPDATE`/`DELETE` diretti come `service_role` su una scheda completed) confermano che restano bloccati con lo stesso `WORKOUT_LOCKED` di sempre. Nessun file applicativo mobile toccato (fix interamente lato trigger DB). Verificati `db push --dry-run` pulito, `tsc --noEmit` pulito, `git diff --check` pulito. **BUG-054 chiuso.** Prossimo passo: Blocco 2 del sistema "Programmi automatici", non ancora iniziato. Dettaglio completo in `docs/WORKLOG.md` e `docs/BUGS.md`.

**Aggiornamento corrente 34:** 2026-07-27 (seconda continuazione) - Sistema "Programmi automatici" BLOCCO 1: **PASS DEFINITIVO**, ri-verificato con metodologia RLS corretta dopo che l'utente ha segnalato che la verifica precedente (Aggiornamento 33) usava `supabase db query --linked` senza `set local role authenticated`/`row_security=on` — quella query si connette come `postgres` (`rolbypassrls=true`, verificato con `pg_roles`), quindi nessuno dei test RLS di quel giro era probante. Ripetuti tutti e 7 gli scenari RLS come `authenticated`/`anon` reali (row_security=on, current_user/session_user/role loggati ad ogni test): tutti PASS. Aggiunti 5 test end-to-end via REST API reale (`/rest/v1/rpc/...`, `Authorization`+`apikey` veri, non solo SQL): Client Pro attivo→200, nessun piano→400 `CLIENT_PRO_REQUIRED`, cliente con coach→400 `FORBIDDEN`, cross-client→400 `FORBIDDEN`, doppia chiamata→stesso `cycle_id` (nessun duplicato). **Bug reale trovato e corretto in questa verifica** (migration `20260801090000`, applicata dopo conferma esplicita dell'utente): la funzione entitlement del fix precedente (`client_has_active_self_guided_plan`) era leggibile da qualunque cliente autenticato per l'id di un ALTRO cliente (information leak) e bloccava indebitamente un'azione superadmin; sostituita con `_has_active_client_pro_entitlement`, zero grant a `public`/`anon`/`authenticated` (verificato), `search_path` esplicito, nuovo codice `CLIENT_PRO_REQUIRED`. Non regressione percorso coach confermata esplicitamente (cliente coach-guided senza alcun piano Client Pro completa una sessione assegnata dal coach → riuscito, il controllo si applica solo al ramo self-guided). Trigger di immutabilità confermati tutti abilitati. **Scoperta collegata, fuori scope, documentata come BUG-054**: il trigger `workout_plans_prevent_completed_edit` non ha eccezione per `service_role`, quindi blocca in modo permanente (non transitorio) l'eliminazione di qualunque account con almeno una sessione completata — riprodotto live lo stesso identico sintomo `{}` del fix `delete-account` di una sessione precedente lo stesso giorno, e confermato che l'account reale di Luigi ha 11 sessioni completate (condizione sufficiente a riprodurlo). Il fix di `delete-account` di prima (retry) NON risolve questo caso specifico. Metodologia di test: account sintetici creati/autenticati tramite una Edge Function harness temporanea con Admin API reale (mai insert diretto in `auth.users`, bloccato dal classificatore di sicurezza), protetta da un segreto di sessione casuale e da un controllo rigido sul prefisso email sintetico; rimossa a fine sessione insieme a tutti gli account di test (confermato con query `auth.users` a 0 righe). Verificati `tsc --noEmit` pulito, `db push --dry-run` pulito, `git diff --check` pulito. Dettaglio completo in `docs/WORKLOG.md` e `docs/BUGS.md` (BUG-054).

**Aggiornamento corrente 33:** 2026-07-27 (continuazione) - Sistema "Programmi automatici" BLOCCO 1: migration applicate al progetto Supabase reale e verificate end-to-end. **Nota (2026-07-27, vedi Aggiornamento 34): la metodologia di verifica RLS di questo aggiornamento è risultata invalida** (query eseguite come `postgres`, che bypassa RLS, non come `authenticated` reale) — tutti i risultati "PASS" relativi a RLS qui sotto sono stati ri-verificati e sostituiti nell'Aggiornamento 34. Il resto (struttura DB, migration applicate, bug entitlement lato server individuato) resta valido come cronologia. Le 3 migration (`20260730090000`, `20260730100000`, più una nuova `20260731090000`) sono ora attive in produzione (`supabase migration list` confermato). Verifica completa via `supabase db query --linked` (struttura DB/RLS/indici/constraint tutti confermati corretti) e test funzionali reali usando 6 account sintetici temporanei (creati/eliminati nella stessa sessione, zero dati reali toccati), simulando sessioni utente vere (`set local request.jwt.claims`) per chiamare le RPC esattamente come farebbe l'app. **Bug reale trovato e corretto**: nessuna RPC verificava lato server un piano Client Pro attivo (il gate esistente è solo lato client/UI) — una chiamata diretta al backend poteva ottenere un programma automatico senza alcun entitlement; corretto con `client_has_active_self_guided_plan(uuid)` + controllo `SUBSCRIPTION_REQUIRED` in `assign_initial_auto_program`/`update_workout_session_progress`/`superadmin_assign_program_template` (migration `20260731090000`), verificato PASS dopo il fix. Tutti gli altri scenari testati sono risultati PASS: cliente senza coach (match esatto del motore, schede reali leggibili via RLS, completamento sessione, blocco post-completamento), idempotenza su doppia chiamata, cliente con coach (`FORBIDDEN` + non regressione di `assign_workout_template_to_client` confermata con un'assegnazione coach reale), cliente con necessità di parere professionale (nessuna scheda, notifica a tutti i superadmin, sblocco manuale con audit log), RLS cross-client, gestione errori (`INVALID_STATE`/`SUBSCRIPTION_REQUIRED`). Finding non bloccante: i trigger di immutabilità post-completamento (pre-esistenti) bloccano correttamente anche la cancellazione a cascata di schede completate quando l'utente viene eliminato (comportamento voluto, non un bug) — gestito nel cleanup disabilitando temporaneamente i 3 trigger solo per quell'operazione, poi riattivati e verificati. Verificati `tsc --noEmit` pulito, `git diff --check` pulito. **Non ancora verificato**: nessun clic reale in browser/app (nessun tool di automazione disponibile), checklist UI residua in `docs/TODO_NEXT.md`. Dettaglio completo in `docs/WORKLOG.md`.

**Aggiornamento corrente 35:** 2026-08-02 - Libreria "Allenamenti YMove" progettata, sottoposta a un gate di revisione critica dedicato (2 problemi reali di programmazione trovati e corretti prima dell'apply: volume ridondante su pattern squat in 2 modelli Forza, esercizio troppo avanzato in 1 modello Principianti) e **applicata in produzione** (`supabase db push` reale, commit `4f2456b`). 8 modelli di sistema (`workout_templates.is_ymove` nuovo campo additivo, `default false`, constraint `not is_ymove or is_system`) costruiti sui 360 esercizi YMove già importati, riusando integralmente `is_system`/`coach_id`/RLS/RPC esistenti (nessuna modifica a `save_workout_template`/`assign_workout_template_to_client`: una copia personale di un modello YMove riceve `is_ymove=false` per costruzione, mai valorizzato dalla RPC). `professional-library-screen.tsx` generalizzato con `variant='professional'|'ymove'` invece di duplicare il componente; nuove route `schede/allenamenti-ymove/{index,[goal]}.tsx`. Migration con 8 ID template fissi noti in anticipo (non `gen_random_uuid()`), per permettere un rollback che opera solo su quegli ID esatti (preparato, non eseguito, in `reports/ymove/output/rollback-ymove-workout-templates.sql`) — verificato che `workout_plans.template_id` è `ON DELETE SET NULL`, quindi un eventuale rollback non tocca mai schede clienti reali. Verifiche post-migration in sola lettura tutte PASS (8/8 modelli, 29 giorni, 148 esercizi, 0 mancanti/duplicati/quarantinati, 0 sovrapposizioni tra le 3 librerie). Verificati `tsc --noEmit` pulito, `expo export --platform web` OK, `git diff --cached --check` pulito. **Non verificato con clic reali** — checklist in `docs/TODO_NEXT.md`. Nessun deploy Edge Function, nessuna chiamata YMove, nessun `git push`.

**Aggiornamento corrente 34:** 2026-08-01 - Archivio esercizi YMove (360 gia' importati) nel picker area coach: nuova tab "Archivio" in `mobile/src/components/ymove-exercise-picker.tsx` (accanto a "Cerca live", stesso bottone "Libreria YMove" gia' esistente in creazione scheda, zero modifiche a `workout-plan-form.tsx`/`workout-template-form.tsx`). Verificato prima di scrivere codice: lo stack e' Expo/React Native (non Kotlin/Jetpack Compose come descritto nel comando ricevuto), e l'Edge Function proxy richiesta esisteva gia' (`ymove-exercises`, azione `detail`, deployata) — nessuna nuova Edge Function creata. Nuovi file `mobile/src/components/ymove-archive-browser.tsx` (lista/filtri/video lazy sui 360 esercizi, un solo caricamento, zero chiamate YMove al render lista) e `mobile/src/components/ymove-preview-shared.tsx` (miniatura+anteprima video estratte da `ymove-exercise-picker.tsx` per evitare un import circolare tra i due componenti tab). `mobile/src/lib/fitcoach-exercises-service.ts` esteso con `listYmoveArchiveExercises()` (riusa il mapper esistente). Verificati `tsc --noEmit` pulito, `expo export --platform web` completato senza errori, `git diff --check` pulito. **Non verificato con clic reali** — checklist in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 33:** 2026-07-31 (Parte 2) - YMove Legacy Link, Fase 1 completata in due parti (staging/codice, nessun deploy). Parte 1: `supabase/functions/ymove-library-import/index.ts` riscritto per collegare video YMove a esercizi legacy via `exercise_external_links.exercise_key` testuale, rimosso il bridge UUID morto. Tra le due parti, un revert dell'albero di lavoro (non causato da questo intervento) ha annullato tutte le modifiche non committate ai file tracciati — inclusi il fix di `mobile/src/lib/exercise-video-service.ts` e un meccanismo di fallback video piu' ampio (lookup `exercise_identity_keys`) di provenienza precedente e sconosciuta, e tutte le voci di memoria di progetto scritte nella Parte 1 (ora riscritte). Il file non tracciato `ymove-library-import/index.ts` e' sopravvissuto intatto. Parte 2: ricostruito `getExerciseVideo` con lo scope minimo richiesto (helper `normalizeLegacyKey`, fallback `exercise_external_links` senza prefisso) — il meccanismo piu' ampio perso nel revert NON e' stato ricostruito, fuori scope di entrambi i comandi. Le due migration "legacy bridge" in conflitto architetturale sono state spostate dall'utente in `supabase/pending_migrations/` (commit `32426dd`), risolvendo il conflitto segnalato nella Parte 1. Verificati `tsc --noEmit` pulito, `git status` con un solo file tracciato modificato, nessuna migration in coda. **Nessun deploy, nessun link applicato su Supabase, nessuna verifica end-to-end reale.** Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/TODO_NEXT.md` (raccomandazione: committare presto per non perdere altro lavoro non tracciato).

**Aggiornamento corrente 32:** 2026-07-30 - feat: Sistema "Programmi automatici" per clienti senza coach, **BLOCCO 1 di N** (richiesta a 12 punti, l'utente ha scelto esplicitamente di procedere a blocchi verificabili). Questo blocco copre: schema DB completo per l'intero sistema (anche le parti che verranno popolate/usate solo nei blocchi successivi), questionario fitness iniziale obbligatorio, motore di assegnazione automatica della prima scheda. Riusata integralmente l'infrastruttura esistente: `workout_templates`/`workout_template_days`/`workout_template_exercises` (18 template di sistema già pronti, ora resi `auto_eligible`) e `assign_workout_template_to_client` (rifattorizzata in una funzione condivisa `_copy_template_days_to_plans`, non riscritta — stesso comportamento osservabile). Nuove tabelle: `client_fitness_profile` (questionario iniziale, separata da `client_onboarding` che resta invariata), `client_excluded_exercises`, `client_program_cycles` (wrapper "ciclo" bozza/attiva/completata/sostituita/sospesa/in-revisione, MAI dentro `workout_plans`, indice unico parziale "un solo ciclo in corso per cliente"), `client_program_cycle_plans`, `client_monthly_checkins`/`client_cycle_reviews` (schema pronto per il Blocco 2, non popolati ora), `app_notifications` (centro notifiche in-app multi-ruolo, nuovo), `superadmin_program_overrides`. Alter additivi: `workout_templates.is_active`/`auto_eligible`/`auto_assignment_rules`/`auto_progression_rules`/`next_template_id`; `workout_plans.coach_id` reso nullable + `origin` (CHECK di coerenza, stesso precedente già usato per `workout_templates.coach_id`); `exercise_progress_history` schema-prep (`coach_id` nullable + `has_pain`/`pain_notes`, nessuna scrittura ancora). Nuova funzione `client_has_no_active_coach(uuid)`. **Bug reali scoperti e corretti durante la progettazione stessa** (verificati leggendo il codice reale, non assunti): le RLS `workout_plans_client_read`/`workout_days_client_read`/`workout_day_exercises_client_read` richiedevano un JOIN a `coach_clients` che con `coach_id` NULL non avrebbe mai permesso al cliente di leggere le proprie schede automatiche (corretto con un ramo esplicito aggiunto, nessuna rimozione); `update_workout_session_progress` aveva un controllo FORBIDDEN che con `coach_id` NULL collassava silenziosamente a `NULL` (mai `true`) invece di sollevare, disabilitando l'autorizzazione per un piano automatico (corretto con un ramo esplicito `coach_id is null`). Nuove RPC: `save_initial_fitness_profile`, `assign_initial_auto_program` (idempotente con lock, blocca l'assegnazione e notifica i superadmin se il questionario segnala `requires_professional_supervision=true` — mai un allenamento intenso automatico —, motore di matching deterministico a 4 stadi con fallback su goal/livello/luogo/giorni/durata, rimuove gli esercizi esclusi dalla copia), `superadmin_assign_program_template` (escape hatch RPC-only, nessun pannello UI in questo blocco). UI: `questionario-fitness.tsx` (nuova route multi-step), `auto-program-card.tsx` (in `cliente-home.tsx`, solo self_guided), nuovo store/servizi dedicati, gating in `auth-gate.tsx` (solo `self_guided`, dopo onboarding+paywall Client Pro esistenti, zero modifiche al ramo `coach_guided`). `WorkoutPlan.coachId` reso `string | null` in TS + mapping servizio aggiornato. Verificati `tsc --noEmit` pulito, `git diff --check` pulito, `supabase migration list` conferma tutte le migration precedenti applicate al progetto reale e le 2 nuove in coda, `supabase db push --dry-run` conferma la coda **senza applicare nulla** (scelta deliberata, coerente con la prassi del progetto). Rilettura manuale riga per riga di entrambe le migration per la correttezza sintattica (nessun validatore SQL offline disponibile). **BLOCCANTE fino all'esecuzione manuale**: nessuna delle due nuove migration è mai stata applicata al progetto Supabase reale — l'intero sistema non è operativo in produzione finché non vengono eseguite. Nessun clic reale eseguito (nessun tool di automazione browser/app disponibile). Vedi `docs/WORKLOG.md`, checklist di verifica e prossimi blocchi in `docs/TODO_NEXT.md`.

**Aggiornamento corrente 31:** 2026-07-24 (continuazione) - feat: nuovo hub "Area coach". Creato `mobile/src/app/area-coach.tsx` (7 sezioni: Clienti/Appuntamenti/Abbonamento/Account coach/Impostazioni app/Assistenza e informazioni/Esci, riepiloghi reali via hook gia' esistenti `useCoachClients`/`useAppointmentsRealtime`/`useMySubscription`, nessuna nuova query duplicata). La quick action "Impostazioni" della Home coach (`index.tsx`) ora punta a `/area-coach`. Nuove `account-coach.tsx` (dati personali con modifica nome/cognome/telefono/avatar riusando il pattern di `cliente-profilo.tsx`+`client-avatar-service.ts`, cambio email `auth.updateUser({email})`, cambio password da loggato — mai esistito prima, solo forgot/forced —, codice coach con nuovo pulsante "Condividi" su `CoachInviteCodeCard`, eliminazione account e logout spostati da Impostazioni) e `assistenza.tsx` (riga Assistenza→`/supporto` + `DeveloperInfoSection` riusato). `impostazioni.tsx` ridotta a Tema+Suoni (le altre sezioni rilocate, non eliminate — vedi `docs/DECISIONS.md` per la motivazione di questa scelta). Nuove funzioni in `auth-service.ts`: `loadOwnProfile`/`updateOwnProfile`/`updateEmail` (self-service, id sempre da sessione, coperte dalla RLS `profiles_self_update` gia' esistente — verificata, non nuova), `splitFullName` esportata. `clienti/index.tsx`: ricerca nome/email + filtro "Rimossi" (query estesa a `status in (active,suspended,removed)`, fix collaterale a `index.tsx`/dashboard per non contare i rimossi come attivi). `appuntamenti/index.tsx`: filtro Prossimi/Passati + cliente + tipo + data (client-side, nessuna modifica a `appointment-service.ts`/RLS). `abbonamento-coach.tsx`: badge "Assegnato dall'amministratore" per `superadmin_manual`, conferma esplicita prima di un secondo abbonamento, badge "Upgrade" calcolato su `max_clients` (mai `sort_order`), storico con data/provider/nota "Importo gestito dallo Store" (nessun importo inventato, colonna non esiste). `auth-gate.tsx`: `/area-coach`/`/account-coach`/`/assistenza` aggiunte a `COACH_ONLY_EXACT_ROUTES`. **Nessuna migration** (2 decisioni prese con l'utente via AskUserQuestion: data di nascita coach omessa, mantenuti tutti e 4 i tipi di appuntamento reali). Verificati `tsc --noEmit` pulito, `expo-doctor` 20/20, `npm ls --depth=0` pulito, `expo config --type public` valido, `git diff --check` pulito, `migration list`/`db push --dry-run` invariati. Bundle Web verificato con successo (`expo start --web`, 3388 moduli, nessun errore) — **nessun clic reale eseguito**, nessun tool di automazione browser/app disponibile in questo ambiente. Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/TODO_NEXT.md`.
