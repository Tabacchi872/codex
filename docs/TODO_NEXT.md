# TODO_NEXT.md

Prossimi passi prioritizzati per **FitCoach Pro** (nome definito in `mobile/src/constants/app-info.ts`; "CoachDesk" era il nome di lavoro superato) — app iPhone personal trainer, proprietario Luigi Marrano. Non è uno storico (per quello vedi `WORKLOG.md`).

## Priorità alta

- [x] **Configurazione iOS del progetto — EAS Build/TestFlight/App Store Connect (2026-07-22):** `mobile/app.json`/`eas.json` completati per iOS (bundle id `com.fitcoachapp.mobile`, `supportsTablet:false`, export compliance, `cameraPermission:false`), eliminazione account reale (nuova Edge Function `supabase/functions/delete-account`, pulsante in Impostazioni coach/cliente), link privacy/termini (placeholder). Audit compatibilità iOS: nessun blocco trovato, RevenueCat/webhook già corretti multipiattaforma. Dettagli completi in `IOS_SETUP.md` (root) e `docs/WORKLOG.md` (voce 2026-07-22). **Azioni ancora da fare, in ordine:**
  1. **Deploy Edge Function `delete-account`** (`supabase functions deploy delete-account` da un ambiente con Supabase CLI — non disponibile in questo ambiente): finché non è deployata, il pulsante "Elimina account" in app fallisce con un errore leggibile (nessun crash, ma la feature non è operativa).
  2. **Account Apple Developer + App Store Connect**: creare l'app con bundle id `com.fitcoachapp.mobile`, recuperare l'ASC App ID e sostituirlo in `mobile/eas.json` → `submit.production.ios.ascAppId` (oggi placeholder).
  3. **RevenueCat iOS**: creare/collegare l'app iOS su RevenueCat Dashboard, impostare `EXPO_PUBLIC_REVENUECAT_IOS_API_KEY` come EAS secret + `mobile/.env` locale.
  4. **Privacy policy e termini di servizio reali**: pubblicarli, sostituire `PRIVACY_POLICY_URL`/`TERMS_OF_SERVICE_URL` in `mobile/src/constants/app-info.ts` (oggi `example.com`) e nel questionario App Privacy di App Store Connect.
  5. **Prima build reale**: `eas device:create` (registrare l'iPhone) → `eas build --platform ios --profile development` → verificare su device reale login, RevenueCat, eliminazione account, notch/Dynamic Island, tastiera sui form (nessuna di queste verifiche è mai stata eseguita in questo ambiente, nessun Mac/account Apple disponibile).
  6. **Decisione su `android.permission.RECORD_AUDIO`** (Android, non iOS): verificare se serve ancora a qualcosa; se no, rimuoverlo in un task Android dedicato per poter poi ripulire anche `NSMicrophoneUsageDescription` su iOS (oggi lasciata al testo generico inglese del plugin per non toccare la configurazione Android esistente — vedi `IOS_SETUP.md` sezione 3).
- [x] **Login ufficiale, registrazione coach/cliente, fatturazione coach, superadmin, schema Supabase completo (2026-07-08):** pannello superadmin (dashboard/coach/piani/notifiche/eventi pagamento/supporto), login solo email/password senza esempi demo in UI, registrazione coach con dati fatturazione e codice coach univoco generato, registrazione cliente con validazione codice coach obbligatoria, schema `docs/SUPABASE_SCHEMA.{md,sql}` completo (incluse tutte le tabelle billing/fatturazione). Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`. **Non verificato con clic reali in un browser** — nessuno di questi flussi è mai stato cliccato in un browser reale, né in questa sessione né (a quanto risulta) in quella dei 9 commit precedenti non documentati. Vedi checklist dedicata sotto.
- [ ] **Verifica clic reali del blocco Superadmin/registrazione (priorità assoluta, mai fatta):**
  1. Da login, "Registrati come coach": compilare tutti i campi incluso "Conferma password" (nuova), provare password diverse tra loro (deve bloccare con errore), poi corrette → verificare comparsa schermata "Il tuo codice coach" con codice `FC-XXXX-XXXX` e che "Copia codice" copi davvero.
  2. Provare a registrare un coach con nazione "Italia" + Partita IVA compilata ma PEC e Codice SDI vuoti → deve bloccare con l'errore dedicato; compilando uno dei due deve passare.
  3. Da login, "Registrati come cliente": provare un codice coach inesistente (deve bloccare), poi il codice di un coach demo bloccato/scaduto se presente nei dati seed (deve bloccare con il motivo corretto), poi un codice valido → verificare login automatico su `/cliente-home` e che il cliente compaia nel dettaglio coach lato superadmin ("Clienti attivi").
  4. Da superadmin → Coach → dettaglio: "Rigenera codice" e "Attiva/Disattiva codice" → verificare che il nuovo stato si rifletta subito e che un vecchio codice disattivato blocchi davvero una nuova registrazione cliente (punto 3).
  5. Superadmin → dashboard, piani, notifiche, eventi pagamento, supporto: verificare che tutte le sezioni carichino dati reali (non vuote/rotte) e che i filtri stato coach (`/superadmin/coaches`) restino leggibili e a due righe su 360–430px.
- [x] **Step 0 — Setup progetto e preview:** progetto Expo in `mobile/`, navigazione a tab, preview web in cornice iPhone-style.
- [x] **Struttura dati allenamento reale + timer/storico + redesign rosso + superserie/circuito** (vedi `docs/WORKLOG.md` per le voci precedenti).
- [x] **Tema chiaro/scuro/sistema, gestione clienti reale, autenticazione demo coach/client (2026-07-05):** vedi sezione dedicata sotto e `docs/WORKLOG.md`.
- [x] **Rinomina in FitCoach Pro, sfondo grafico leggero, sezione Sviluppatore, rimozione scritte "demo" dalla UI (2026-07-05):** vedi `docs/WORKLOG.md`.
- [x] **Completamento lato Cliente: Workout live, Nutrizione, Questionario, Prenotazioni, Bacheca, Chat (2026-07-05):** vedi `docs/WORKLOG.md`.
- [x] **Tab bar cliente a 5 voci con icone, Home con icone su ogni card, Dettaglio esercizio come logger di sessione (2026-07-05):** vedi `docs/WORKLOG.md`.
- [x] **Fix navigazione cliente — BUG-002, tab bar web sostituita con `Slot` (2026-07-05):** vedi `docs/WORKLOG.md` e `docs/BUGS.md`. **Non verificato con clic reali in un browser** (nessun tool di automazione browser disponibile): la correzione è validata da lettura del sorgente della libreria + `curl`/bundle, ma va confermata di persona (vedi prossimo step sotto). Tab bar nativa non ancora corretta (stesso rischio, non testabile in questo ambiente).
- [x] **Abbonamento cliente reale (`SubscriptionPackage`), sessioni multiple con data/ora, agenda coach reale con anti-sovrapposizione (2026-07-06):** vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`. **Non verificato con clic reali in un browser** (stesso limite delle voci precedenti) — vedi "Prossimo step operativo immediato" sotto per la checklist completa da eseguire di persona.
- [x] **Ultimo aggiornamento Coach/Admin (2026-07-06, parte 2) — fix status abbonamento, fix stato saltato, modelli allenamento predefiniti, icone tab coach, suono Sirena:** vedi `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-004, BUG-005), `docs/DECISIONS.md`. **Non verificato con clic reali in un browser** — vedi checklist aggiornata sotto. **Prossimo passo dichiarato dall'utente: lato cliente**, non toccato in questa sessione.
- [ ] **Lato coach per Nutrizione e Bacheca personale:** oggi nessuna schermata coach per assegnare un piano nutrizionale a un cliente o scrivere un annuncio personale — le sezioni corrispondenti lato cliente restano vuote finché non viene costruita.
- [ ] **Caricare i video/immagini reali degli esercizi** in `mobile/assets/videos/` e `mobile/assets/images/exercises/`, registrandoli nei rispettivi registri (oggi tutto "mancante" per onestà).
- [x] **Step 1 (Fase 1) — Backend reale, registrazione/login collegati a Supabase (2026-07-08, continuazione):** `@supabase/supabase-js`/`react-native-url-polyfill` installati, client reale (`mobile/src/lib/supabase.ts`) e servizio auth (`mobile/src/lib/auth-service.ts`: `signUpCoach`, `signUpClientWithCoachCode`, `signInWithEmail`, `signOut`, `getCurrentSession`) creati. `registration-screens.tsx`/`login-screen.tsx` collegati con logica dual-write (Supabase + mirror locale) quando le variabili ambiente sono presenti, fallback 100% locale invariato altrimenti. Schema (`docs/SUPABASE_SCHEMA.sql`) aggiornato con `profiles.id → auth.users(id)`, trigger `handle_new_user`, funzione `increment_registration_code_usage`, policy minime per validazione codice/coach pre-login. `docs/EMAIL_SETUP.md` creato. **Non ancora fatto/non verificato** (vedi checklist dedicata sotto): nessun progetto Supabase reale creato/testato in questo ambiente; `auth-store.ts`/`client-store.ts` NON sono stati sostituiti (restano l'unica fonte di lettura per dashboard/schede/appuntamenti/chat/superadmin); nessuna migrazione delle altre tabelle (workout, subscriptions, appointments, messages) oltre a registrazione/login; upload foto questionario resta `blob:` locale; nessun collegamento Stripe/RevenueCat (esplicitamente fuori scope). Vedi `docs/DECISIONS.md` per i compromessi RLS/Confirm-email di questa fase.
- [x] **Verifica reale del collegamento Supabase Fase 1, registrazione+login coach/cliente (2026-07-09):** l'utente ha collegato un progetto Supabase reale e testato a mano registrazione coach, registrazione cliente e login — trovando e facendo correggere due bug reali (verifica codice coach rotta per spazi in una `.select()`; profilo cliente non trovato al login su un device/browser diverso da quello di registrazione, causa `login-screen.tsx` non ricaricava mai da Supabase). Vedi `docs/WORKLOG.md` (voce 2026-07-09) e `docs/DECISIONS.md`.
- [ ] **Verifica reale residua Supabase Fase 1 (priorità alta, non ancora fatta):**
  1. Verificare in Supabase (Table editor) che dopo una registrazione coach/cliente esistano davvero le righe attese: `profiles`, `coach_profiles`, `billing_profiles`, `registration_codes` (coach); `profiles`, `client_profiles`, `coach_clients` con `registration_codes.used_count` incrementato (cliente).
  2. Provare un codice inesistente e un codice di un coach con `coach_profiles.billing_status` impostato manualmente a `blocked`/`past_due`/`canceled` → verificare che la registrazione cliente venga bloccata con il messaggio corretto.
  3. Fare login con lo stesso cliente su un secondo device/browser (es. registrato su Expo Web, poi login da Expo Go, o viceversa) → verificare che la home cliente mostri i dati corretti invece di "Nessun profilo cliente collegato" (fix 2026-07-09, mai verificato su un vero secondo dispositivo).
  4. Verificare che con `mobile/.env` assente/vuoto l'app si comporti esattamente come prima (login/registrazione locale, nessun errore Supabase in console).
  5. Verificare che un cliente reale (registrato via Supabase) veda correttamente stati vuoti onesti in workout/nutrizione/chat/bacheca/prenotazioni (atteso: vuoto, perché il lato coach resta locale-only in questa fase — non un bug).
- [ ] **Verifica reale Fase 2 — email vere Supabase (priorità alta, mai fatta contro un'inbox reale, vedi `docs/EMAIL_SETUP.md`):**
  1. Su un progetto Supabase reale con "Confirm email" attivo: registrare un coach e un cliente con email vere → verificare che compaia la schermata/avviso "controlla la tua email" (non un ingresso immediato), che il login prima della conferma dia "Email non ancora confermata...", e che dopo aver cliccato il link ricevuto il login funzioni normalmente.
  2. "Password dimenticata" da login → inserire l'email di un account confermato → verificare l'arrivo dell'email di reset.
  3. Aprire il link di reset ricevuto → verificare che `/reimposta-password` mostri il form (non "Link non valido"), impostare una nuova password, salvare, verificare la conferma, poi fare login con la nuova password.
  4. Ripetere il punto 3 avviando `expo start --web` su una porta diversa (es. 8082 invece di 8081) per confermare che il redirect dinamico (`getWebRedirectUrl`) funzioni su qualunque porta, non solo quella di default — servono le porte aggiunte a "Redirect URLs" su Supabase (vedi `docs/EMAIL_SETUP.md`).
  5. Verificare in Supabase → Authentication → Email Templates che "Confirm signup" e "Reset password" contengano `{{ .ConfirmationURL }}` (senza, i link nelle email non funzionano).
- [ ] **Verifica reale trigger DB definitivo — onboarding coach/cliente con "Confirm email" attivo (priorità alta, mai fatta contro un progetto reale, vedi `docs/EMAIL_SETUP.md`):**
  1. Rieseguire sul progetto Supabase reale lo snippet SQL aggiornato (`docs/EMAIL_SETUP.md`, sezione "Onboarding coach/cliente..."): `random_coach_code_segment` + `handle_new_user` esteso (crea TUTTE le tabelle, non solo `profiles`) + policy `profiles_self_insert`/`profiles_self_update`.
  2. Registrare un coach (con "Confirm email" attivo) → **prima ancora di confermare l'email**, verificare in Table editor che esistano già `profiles`, `coach_profiles`, `billing_profiles` (con gli stessi dati fiscali inseriti in registrazione, non placeholder), `registration_codes` (un codice `FC-XXXX-XXXX` attivo) → poi confermare l'email → fare login → verificare che il codice mostrato in Impostazioni coincida esattamente con quello già presente in `registration_codes` (creato dal trigger, non da un nuovo giro di `ensureCoachOnboarding`).
  3. Registrare un cliente con un codice coach valido (con "Confirm email" attivo) → prima della conferma, verificare in Table editor `profiles`, `client_profiles`, `coach_clients` (collegato al coach corretto, `used_count` incrementato) → confermare → fare login → verificare che la home cliente mostri i dati corretti (non "profilo non completato"/"non collegato a nessun coach") e che `used_count` non sia salito una seconda volta.
  4. Verificare che un login con un account già confermato/con onboarding già completo NON crei righe duplicate né sposti `used_count` (idempotenza di `ensureCoachOnboarding`/`ensureClientOnboarding`, ora solo rete di sicurezza secondaria rispetto al trigger).
- [ ] **Migrare il resto dell'app a Supabase (prossimo passo dopo la verifica sopra):** dashboard coach, gestione clienti, schede/workout, abbonamenti, appuntamenti, chat, pannello superadmin — oggi restano su AsyncStorage locale anche quando Supabase è configurato (dual-write solo su registrazione/login). Include sostituire `auth-store.ts`/`client-store.ts` come fonte di verità, non solo come mirror.
- [ ] **Rafforzare la RLS Fase 1 prima della produzione:** sostituire la validazione codice coach/collegamento `coach_clients` lato app con una funzione `register_client_with_code()` security definer atomica lato server (oggi si appoggia a policy pubbliche di lettura/self-insert, documentate come semplificazione in `docs/SUPABASE_SCHEMA.md`).
- [x] **Fase 2 — email vere Supabase: conferma registrazione, password dimenticata, reset password (2026-07-09, continuazione):** conferma registrazione ora onesta (nessun login/redirect finto se `session` è `null`), "Password dimenticata" collegato a `resetPasswordForEmail`, nuova schermata `/reimposta-password` collegata a `updateUser`. Vedi `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-007), `docs/EMAIL_SETUP.md`. Ancora mancante (fuori scope esplicito di questa fase): invito cliente via email, conferma cambio email, deep link nativo per il reset password su Expo Go.
- [x] **Fix BUG-008 — onboarding coach/cliente vuoto con "Confirm email" attivo (2026-07-10):** `profiles`/`client_profiles`/`coach_clients`/`coach_profiles`/`billing_profiles`/`registration_codes` restavano vuote perché scritte subito dopo `signUp` senza sessione attiva (RLS le bloccava). Dati salvati ora in `user_metadata`, completati al primo login reale (`ensureCoachOnboarding`/`ensureClientOnboarding`); aggiunte le policy `profiles_self_insert`/`profiles_self_update` mancanti. Vedi `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-008), `docs/DECISIONS.md`, `docs/EMAIL_SETUP.md` (snippet SQL da rieseguire). **Non ancora testato contro un progetto Supabase reale** — vedi checklist dedicata subito sopra.
- [x] **Trigger SQL definitivo — profiles/coach_profiles/billing_profiles/registration_codes/client_profiles/coach_clients create dal database (2026-07-10, continuazione):** `handle_new_user()` ora crea direttamente tutte queste righe `after insert on auth.users`, indipendentemente da "Confirm email" (non serve più il login per completarle) — sostituisce l'onboarding differito del punto sopra come percorso primario, che resta come rete di sicurezza. Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/EMAIL_SETUP.md` (snippet SQL da eseguire). **Non ancora testato contro un progetto Supabase reale** — vedi checklist dedicata sopra (aggiornata).
- [x] **Migrazione al nuovo design system FitCoach — quasi completa (2026-07-10/11, vedi `docs/WORKLOG.md`):** registrazione coach/cliente, tutte le schermate cliente principali (workout/nutrizione/chat/altro/profilo), tutte le schermate coach principali (clienti/abbonamenti/appuntamenti/impostazioni/supporto/lista schede), tutte le schermate superadmin (coaches/piani/pagamenti/notifiche/supporto). Icone reali (`lucide-react-native`, stesso set del mockup) al posto delle emoji. Fix di un bug reale di hydration React (`<button>` dentro `<button>` in `cliente-home.tsx`). **Non ancora verificato con clic reali in un browser** — priorità alta, vedi checklist dedicata sotto.
- [ ] **Verifica reale del nuovo design system (priorità alta, mai fatta con clic reali, ~30 schermate toccate il 2026-07-10/11):**
  1. Aprire `http://localhost:8099` (o riavviare `expo start --web`) e cliccare TUTTE le schermate migrate: login, registrazione coach/cliente, dashboard/workout/nutrizione/chat/altro/profilo cliente, dashboard/clienti/dettaglio-cliente/abbonamenti/appuntamenti/schede/impostazioni/supporto coach, dashboard/coaches/dettaglio-coach/piani/pagamenti/notifiche/supporto superadmin.
  2. Verificare che non ci siano più errori di hydration React in console (il fix del bug `<button>` dentro `<button>` era specifico a `cliente-home.tsx`: controllare se lo stesso pattern esiste altrove non ancora trovato).
  3. Verificare le icone lucide (nessun quadrato vuoto/icona mancante) su schermi 360-430px.
  4. Confrontare tema chiaro e scuro (variante dark mai vista dall'utente, generata da questo agente).
  5. Verificare Expo Go su device reale (mai testato in questa fase — solo web).
- [ ] **Migrare il cluster "editor scheda/esercizio" al nuovo design system (priorità media, unico blocco rimasto sullo stile vecchio dopo il 2026-07-11):** `schede/[id].tsx`, `esercizi/[id].tsx`, `schede/new.tsx`, `schede/modelli/*` e i componenti `workout-plan-form.tsx`/`workout-exercise-editor.tsx`/`exercise-set-logger.tsx`/`superset-block.tsx`/`rest-timer.tsx`/`exercise-history.tsx`/`exercise-attachments.tsx`/`workout-session-controls.tsx`/`workout-exercise-row.tsx` — ~1500+ righe totali, logica complessa (superserie, timer, storico pesi, upload allegati) condivisa coach+cliente, deliberatamente deferito per rischio/dimensione. Riusare `mobile/src/theme/`/`mobile/src/components/ui/` già pronti. (`exercise-video-player.tsx` e il nuovo `exercise-video-upload.tsx` sono già stati migrati al nuovo design system il 2026-07-11, vedi voce video sotto — non serve rifarli.)
- [x] **Video esercizi fase 1 (URL demo) e fase 2 (upload reale coach via Supabase Storage) (2026-07-11):** `Exercise.videoUrl`, `ExerciseVideoPlayer` riscritto (priorità video remoto), nuova tabella `exercise_videos` + bucket Storage `exercise-videos` + RLS scoped per coach, `mobile/src/lib/exercise-video-service.ts`, `mobile/src/components/exercise-video-upload.tsx` (upload coach-only). Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/SUPABASE_STORAGE_VIDEO.md`. **Non ancora testato contro un progetto Supabase reale** — vedi checklist dedicata subito sotto.
- [ ] **Verifica reale video esercizi Supabase Storage (priorità alta, mai fatta contro un progetto reale, vedi `docs/SUPABASE_STORAGE_VIDEO.md`):**
  1. Eseguire sul progetto Supabase reale lo snippet SQL completo (tabella `exercise_videos` + RLS + bucket `exercise-videos` + policy Storage), tutto documentato in `docs/SUPABASE_STORAGE_VIDEO.md`.
  2. Login come coach con account Supabase reale (non demo locale) → aprire un esercizio → verificare comparsa bottone "Carica video" sotto il player.
  3. Caricare un file `.mp4`/`.mov`/`.webm` sotto i 100MB → verificare stato di caricamento, poi messaggio di successo e video visibile subito senza refresh manuale.
  4. Su Supabase: Table editor → `exercise_videos` deve avere una riga con `coach_id`/`exercise_id`/`video_url` corretti; Storage → bucket `exercise-videos` → cartella `{coach_id}/{exercise_id}/` deve contenere il file.
  5. Ricaricare un video sullo stesso esercizio (bottone diventa "Sostituisci video") → verificare che il vecchio file su Storage sia stato rimosso (nessun file orfano) e che la riga in `exercise_videos` sia aggiornata, non duplicata.
  6. Provare un file non valido (es. `.avi` o `.pdf`) → deve bloccare con messaggio d'errore, nessun tentativo di upload.
  7. Login come cliente collegato al coach del punto 2 → aprire lo stesso esercizio → verificare che il video sia visibile e riproducibile, senza alcun bottone di upload.
  8. Login come cliente collegato a un coach DIVERSO (che non ha caricato nulla per quell'esercizio) → verificare che veda il fallback (video demo statico se presente, altrimenti "Nessun video disponibile") — mai il video del primo coach.
  9. Ripetere il test upload su Expo Go reale (mai fatto in questa sessione, solo bundle web verificato) — percorso native usa base64→ArrayBuffer, mai eseguito davvero.
- [ ] **Fatturazione automatica reale:** oggi solo lo schema (`invoices`/`invoice_items`) e i dati fiscali coach (`billing_profiles`) esistono; nessuna fattura viene generata. Servirebbe, dopo lo Step 1: motore di calcolo IVA/numerazione, generazione PDF/XML (SdI per soggetti italiani), collegamento pagamenti reali (Stripe web, Apple/Google in-app) con riconciliazione — vedi note fiscali in `docs/SUPABASE_SCHEMA.md`. Nessuna di queste integrazioni è stata collegata finora, per scelta esplicita dell'utente.
- [ ] **Modifica anagrafica cliente esistente** (oggi solo creazione + cambio stato; nome/cognome/email/telefono/obiettivo/nota non sono più modificabili dopo la creazione).
- [ ] **Expo Go / development build:** Expo Go su telefono non funziona correttamente al momento (Expo Web sì). Nessuna diagnosi definitiva possibile in questa sessione (nessun device fisico/log disponibile). Prossimo passo consigliato: provare un development build EAS (`eas build --profile development`) invece di Expo Go, che risolve strutturalmente i problemi di compatibilità nativa con librerie come `expo-audio`/`expo-video`/`react-native-reanimated` 4.x/`react-native-worklets` — vedi `docs/BUGS.md` (nota dedicata) e il report tecnico della sessione 2026-07-06.

- [ ] **Verifica reale credenziali provvisorie via email (priorità alta, mai fatta contro un progetto reale, vedi `docs/SUPABASE_TEMP_CREDENTIALS.md`):**
  1. Eseguire su Supabase lo SQL aggiornato (`docs/SUPABASE_SCHEMA.sql`, colonna `must_change_password`).
  2. Deployare `supabase/functions/send-temporary-credentials` col Supabase CLI e impostare il secret `RESEND_API_KEY` (+ `RESEND_FROM_EMAIL` se si ha un dominio verificato).
  3. Come coach, aprire un cliente **registrato davvero su Supabase** (non aggiunto solo localmente) con credenziali locali già generate → premere "Invia via email" → verificare stato "Invio...", poi messaggio di successo o errore leggibile.
  4. Provare lo stesso su un cliente aggiunto solo localmente (mai registrato su Supabase) → deve comparire un errore chiaro ("Nessun account Supabase reale trovato..."), mai un falso successo.
  5. Verificare l'arrivo dell'email reale all'indirizzo del cliente, con la password provvisoria in chiaro.
  6. Fare logout, login con quel cliente usando la password ricevuta → deve comparire subito "Cambia password" (`SupabaseChangePasswordScreen`), che blocca le tab.
  7. Impostare una nuova password → verificare che l'accesso si sblocchi e che un nuovo login con la nuova password non richieda più il cambio.
  8. Verificare in Table editor che `profiles.must_change_password` sia tornato `false`.
  9. Ripetere il test con un superadmin che invia credenziali a un coach (la function supporta già `role: 'coach'`, ma non esiste ancora un bottone dedicato lato superadmin — verificare solo via una chiamata diretta se serve, o rimandare finché non viene costruita una UI apposita).

- [x] **Fix BUG-011 — coach registrati su Supabase invisibili nel pannello superadmin (2026-07-12):** dashboard/lista/dettaglio ora caricano i coach reali (`profiles`+`coach_profiles`+`billing_profiles`+`registration_codes`+conteggio `coach_clients`), uniti a quelli locali. Vedi `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-011), `docs/DECISIONS.md`. **Non ancora testato contro un progetto reale** — vedi checklist sotto.
- [ ] **Verifica reale coach Supabase nel pannello superadmin (priorità alta, mai fatta contro un progetto reale, vedi `docs/BUGS.md` BUG-011):**
  1. Su Supabase, promuovere un utente reale a superadmin: `update public.profiles set role = 'superadmin' where email = '...';` (nessun self-service per questo ruolo, per sicurezza).
  2. Fare login nell'app con QUELL'account (non `admin@fitcoach.local`, che non ha sessione Supabase) → verificare che il ruolo venga riconosciuto come superadmin e si apra il pannello.
  3. Registrare un coach reale via `/registrazione-coach` (anche da un browser/device diverso) → aprire Dashboard superadmin → verificare che i KPI (Coach totali, Attivi, In prova, ecc.) includano il nuovo coach.
  4. Aprire "Coach" → verificare che il coach compaia in lista con i dati reali (nome, email, codice coach, stato pagamento da `coach_profiles.billing_status`), senza badge "Solo locale".
  5. Aprire il dettaglio del coach → verificare che si apra correttamente (non "Coach non trovato"), mostri i dati fatturazione reali se presenti, e che i pulsanti "Salva modifiche"/"Blocca coach"/"Rigenera codice"/"Attiva-Disattiva codice" siano disabilitati con la nota "gestione in arrivo" (comportamento atteso, non un bug: la scrittura verso Supabase non è ancora implementata).
  6. Controllare in console (dev) il log `SUPERADMIN_COACHES_LOOKUP { source: 'supabase', count: N }` con N coerente.
  7. Con Supabase configurato ma il superadmin loggato come `admin@fitcoach.local` (demo locale): verificare che la lista mostri solo i coach locali/demo (nessun coach Supabase, comportamento atteso da RLS, non un bug) — utile per capire se un futuro report "coach non compare" è dovuto a questo.
  8. Aggiungere un coach con "+ Aggiungi coach" (solo locale) → verificare che compaia con badge "Solo locale" e che i suoi pulsanti di modifica funzionino normalmente (comportamento invariato).
- [x] **Modifica coach reale nel pannello superadmin (2026-07-12):** nome/nome attività/telefono, blocca/sblocca e stato pagamento, rigenera/attiva-disattiva codice, assegna/cambia/termina pacchetto coach — tutti scritti davvero su Supabase (`superadmin-coach-admin-service.ts`). Restano fuori scope per scelta esplicita: email del coach (richiede Auth admin API/service role) e i campi legacy "Piano"/"Limite clienti override"/"Clienti usati"/periodo app (`plans`/`coach_billing`, mai collegati a un coach reale). Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`.
- [ ] **Verifica reale modifica coach superadmin (priorità alta, mai fatta contro un progetto reale):**
  1. Login come superadmin reale → Coach → dettaglio di un coach Supabase → cambiare Nome/Nome attività/Telefono → "Salva modifiche" → ricaricare la pagina (o tornare alla lista e rientrare) → verificare che i nuovi valori risultino salvati (Table editor: `profiles.full_name`/`profiles.phone`/`coach_profiles.business_name`).
  2. Premere "Blocca coach" → verificare `coach_profiles.billing_status='blocked'` in Table editor, poi che un cliente NON riesca più a registrarsi con il codice di quel coach (`coach_not_accepting_clients`) → "Sblocca coach" → riprovare, deve funzionare.
  3. "Rigenera codice" → verificare che il vecchio codice risulti `disabled` e il nuovo `active` in `registration_codes`, e che compaia subito nella UI senza refresh manuale.
  4. "Disattiva codice"/"Attiva codice" → verificare lo stato riflesso subito in UI e in Table editor.
  5. Nella card "Pacchetto acquistato": "Assegna pacchetto" → scegliere un pacchetto coach attivo → verificare comparsa immediata di pacchetto/limite/scadenza corretti, e che una eventuale riga `user_subscriptions` precedente sia ora `canceled` (trigger `user_subscriptions_single_active`).
  6. "Cambia pacchetto" con un pacchetto diverso → verificare che il limite/scadenza si aggiornino subito, non si sommino al precedente.
  7. "Termina abbonamento" → verificare che la card torni allo stato "Nessun abbonamento coach attivo" e che nuove registrazioni cliente con il codice di quel coach vengano bloccate.
  8. Verificare che i campi "Piano"/"Limite clienti"/"Clienti usati"/periodo restino visibilmente disabilitati con la nota esplicativa, e che il campo Email resti non modificabile per un coach Supabase.

- [x] **Fix BUG-012 — reset password apriva il login invece di /reimposta-password (2026-07-12):** `auth-gate.tsx` non "protegge" più questa route dietro il gate di autenticazione. Vedi `docs/WORKLOG.md`, `docs/BUGS.md` (BUG-012). **Non ancora testato contro un progetto reale** — vedi checklist sotto.
- [ ] **Verifica reale reset password (priorità alta, vedi `docs/BUGS.md` BUG-012):**
  1. Su Supabase → Authentication → URL Configuration → "Redirect URLs": verificare che sia presente l'origin/porta ATTUALMENTE in uso da `expo start --web` con wildcard, es. `http://localhost:8081/**` (aggiungerla se manca — causa più probabile del sintomo originale, indipendente dal fix di codice).
  2. Da login, "Password dimenticata?" → inserire l'email di un account confermato → verificare l'arrivo dell'email.
  3. Cliccare il link ricevuto in un browser SENZA sessioni locali attive (finestra in incognito) → deve arrivare su `/reimposta-password` con il form attivo (non sul login).
  4. Ripetere restando loggati come coach/cliente nello stesso browser (nuova scheda) → deve comunque arrivare al form di reset, non alla dashboard (comportamento nuovo di questo fix).
  5. Impostare una nuova password → verificare il messaggio di conferma → "Torna al login" → fare login con la nuova password.

- [x] **Fix BUG-013 — codice coach in Impostazioni disallineato da registration_codes (2026-07-12):** `impostazioni.tsx` legge ora sempre il codice reale da Supabase. Vedi `docs/BUGS.md` BUG-013.
- [ ] **Verifica reale codice coach in Impostazioni (priorità alta, vedi `docs/BUGS.md` BUG-013):**
  1. Login come coach con account Supabase reale → Impostazioni → verificare che il codice mostrato coincida esattamente con quello in `registration_codes` (Table editor) per quel `coach_id`.
  2. Se un cliente aveva provato a registrarsi con un codice sbagliato in precedenza, riprovare con quello ora mostrato in Impostazioni → deve funzionare.
  3. Verificare anche con un coach 100% demo/locale (mai registrato su Supabase): il codice mostrato deve restare quello locale, senza errori né schermata bloccata.

- [x] **Pacchetti acquistabili coach/cliente gestiti dal superadmin (2026-07-12):** due cataloghi separati (`subscription_packages` con `target_role` coach/client, `user_subscriptions`), RLS completa, sezione superadmin "Pacchetti e abbonamenti" (CRUD + attiva/disattiva + elimina con guardia), schermata coach "Abbonamento" (`abbonamento-coach.tsx`, linkata da Impostazioni) + bottone "Condividi" per il codice registrazione, schermata cliente "Pacchetti" (`pacchetti-cliente.tsx`, linkata dal menu Altro). Nessun pagamento reale collegato per scelta esplicita (Stripe/Apple/Google fuori scope): il bottone "Acquista"/"Abbonati" chiama uno stub che mostra sempre un messaggio onesto, mai un finto successo. Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/SUPABASE_SCHEMA.{sql,md}`.
- [ ] **Verifica reale pacchetti acquistabili (priorità alta, mai fatta contro un progetto reale):**
  1. Eseguire su Supabase la sezione finale di `docs/SUPABASE_SCHEMA.sql` ("Pacchetti acquistabili coach/cliente") — crea `subscription_packages`/`user_subscriptions`, indici, trigger, RLS.
  2. Login come superadmin reale → "Pacchetti" (bottom bar) → tab "Pacchetti coach" → "Nuovo pacchetto coach": compilare nome/prezzo/durata/limite clienti/funzionalità → salvare → verificare che compaia nella lista come Attivo.
  3. Ripetere per un pacchetto cliente (tab "Pacchetti clienti", senza limite clienti in UI).
  4. "Modifica" su un pacchetto → cambiare prezzo/funzionalità → salvare → verificare che i cambi si riflettano nella lista e (dopo refresh) lato coach/cliente.
  5. "Disattiva" un pacchetto → verificare che sparisca dalla lista lato coach/cliente ma resti visibile (come "Non attivo") lato superadmin.
  6. "Elimina" un pacchetto senza abbonamenti collegati → deve chiedere conferma poi eliminarlo davvero. Su un pacchetto con una riga in `user_subscriptions` (creata manualmente da Table editor per il test, status `active`) → deve bloccare con il messaggio "abbonamenti attivi collegati", mai eliminare silenziosamente.
  7. Login come coach reale → Impostazioni → "Gestisci abbonamento" → verificare lista pacchetti coach attivi, prezzo/durata/limite clienti/funzionalità corretti, bottone "Copia"/"Condividi" sul codice registrazione (Condividi apre lo share sheet del sistema/browser).
  8. Premere "Acquista o cambia pacchetto" → deve comparire il messaggio "pagamento non ancora collegato" (Stripe su web), mai un abbonamento attivo finto.
  9. Login come cliente reale → menu Altro → "Pacchetti" → verificare lista pacchetti cliente attivi, descrizione/prezzo/durata/servizi inclusi, bottone "Abbonati" con lo stesso comportamento onesto del punto 8.
  10. Con una riga `user_subscriptions` creata manualmente (status `active`, `expires_at` futura) per un coach/cliente di test → verificare che la relativa schermata mostri "Il tuo abbonamento"/"Il tuo pacchetto" con nome, stato "Attivo", scadenza corretti, e badge "Attuale" sul pacchetto corrispondente nella lista.
- [x] **Limite clienti coach collegato all'abbonamento (2026-07-12):** verifica atomica lato server (`_link_client_to_coach`/`pg_advisory_xact_lock`, no race condition), RPC `register_client_with_code`/`can_coach_accept_client`/`get_coach_client_capacity`, contatore "Clienti utilizzati: X su Y"/"Posti disponibili: Z" in `clienti/index.tsx` (coach) e nel pannello superadmin (lista + dettaglio coach). Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/SUPABASE_SCHEMA.{sql,md}`.
- [ ] **Verifica reale limite clienti coach (priorità alta, mai fatta contro un progetto reale):**
  1. Eseguire su Supabase la sezione finale di `docs/SUPABASE_SCHEMA.sql` ("Limite clienti coach collegato all'abbonamento").
  2. Creare manualmente (Table editor) una riga `user_subscriptions` per un coach di test: `status='active'`, `package_id` di un pacchetto coach con `max_clients=2`, `expires_at` futura → nel coach, aprire "Clienti" → verificare "Clienti utilizzati: 0 su 2" / "Posti disponibili: 2".
  3. Registrare 2 clienti reali con il codice di quel coach → verificare che il contatore salga a "2 su 2" / "Posti disponibili: 0" senza bisogno di refresh manuale (basta tornare sulla schermata Clienti).
  4. Provare a registrare un 3° cliente con lo stesso codice → deve essere bloccato PRIMA della creazione dell'account con "Il coach ha raggiunto il limite massimo di clienti previsto dal proprio pacchetto" (nessun account Supabase Auth orfano creato).
  5. Impostare `status='canceled'` sulla riga `user_subscriptions` di quel coach (nessun abbonamento attivo) → registrare un nuovo cliente con lo stesso codice → deve essere bloccato con "Abbonamento necessario...".
  6. Creare una NUOVA riga `user_subscriptions` `active` per lo stesso coach con un pacchetto `max_clients` diverso (es. 5) → verificare che la vecchia riga sia stata marcata `canceled` automaticamente (trigger `user_subscriptions_single_active`) e che il contatore rifletta subito il nuovo limite (5), non la somma dei due.
  7. Login come superadmin → "Coach" (lista) e dettaglio coach → verificare che "Pacchetto acquistato"/"Clienti utilizzati"/"Limite massimo"/"Posti disponibili"/"Scadenza abbonamento" coincidano con quanto visto lato coach.
  8. (Opzionale, race condition) Provare due registrazioni quasi simultanee con l'ultimo posto libero (due browser/finestre) → solo una deve riuscire, l'altra deve essere bloccata o — nel caso limite di finestra strettissima — arrivare a un login con "Cliente non collegato a nessun coach" (mai un collegamento oltre il limite).

- [x] **Integrazione catalogo esercizi YMove, opzione B (2026-07-12/13):** nuova tabella `public.exercises` (custom/ymove condivisi), Edge Function `ymove-exercises` (search/detail/translate, secret `YMOVE_API_KEY`+`AZURE_TRANSLATOR_KEY`/`AZURE_TRANSLATOR_REGION` opzionali), "Libreria YMove" nel form scheda, video live lato coach+cliente (mai salvato), traduzione italiana una tantum all'import con originali conservati separatamente, associazione video YMove su qualunque esercizio esistente (riusa `exercise_videos`) con anti-duplicati. Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/YMOVE_INTEGRATION.md`, `docs/SUPABASE_SCHEMA.{sql,md}`.
- [x] **Cambio provider traduzione: DeepL → Azure Translator (2026-07-13, continuazione 2):** `translateWithAzure` sostituisce `translateWithDeepl` nella Edge Function; secret richiesti `AZURE_TRANSLATOR_KEY`+`AZURE_TRANSLATOR_REGION` (risorsa regionale, es. `italynorth`), `AZURE_TRANSLATOR_ENDPOINT` opzionale. Vedi `docs/DECISIONS.md`, `docs/WORKLOG.md`. `npx.cmd tsc --noEmit` pulito dopo la modifica. **Da fare**: compilazione Deno reale (CLI non disponibile in questo ambiente, solo revisione manuale del file) e impostare i secret su un progetto Supabase reale.
- [ ] **Verifica reale integrazione YMove (priorità alta, mai fatta contro un progetto/API reali):**
  1. Eseguire su Supabase l'intero `docs/SUPABASE_SCHEMA.sql` (sezioni "Libreria esercizi FitCoach..." e "Traduzione italiana... + associazione video").
  2. Deployare `ymove-exercises` e impostare `YMOVE_API_KEY` (obbligatoria) e, se disponibile, `AZURE_TRANSLATOR_KEY`+`AZURE_TRANSLATOR_REGION` (opzionali ma richieste insieme — vedi `docs/YMOVE_INTEGRATION.md`).
  3. Login coach → creare/aprire una scheda → "Libreria YMove" → cercare per nome/muscolo/attrezzatura/tipologia/difficolta' → verificare risultati corretti, nessun video caricato in questa fase.
  4. "Anteprima" su un risultato → verificare video/miniatura/istruzioni.
  5. "Aggiungi a FitCoach" → verificare comparsa immediata nella scheda, poi impostare serie/ripetizioni/durata/recupero/carico/note normalmente.
  6. Aprire quell'esercizio → "Testi italiani" → verificare che siano in italiano (se `AZURE_TRANSLATOR_KEY`/`AZURE_TRANSLATOR_REGION` configurate) o uguali all'originale (se non configurate) → "Modifica" → cambiare un testo → "Salva" → verificare che resti salvato riaprendo.
  6b. Con un SECONDO coach reale, aprire lo STESSO esercizio (stesso `ymove_exercise_id`) → verificare che veda il testo originale/globale (mai la personalizzazione del primo coach) → verificare in Table editor che `exercise_text_overrides` contenga la riga del primo coach e che `public.exercises` (testo globale) sia rimasta invariata.
  7. Ripetere l'import dello stesso esercizio → verificare in Table editor che non si crei una riga duplicata e che NON avvenga una seconda traduzione.
  8. Aprire un esercizio LOCALE storico (uno dei 44) → "Associa video YMove" → cercare/scegliere un video → "Usa questo video" → verificare che il video compaia subito.
  9. Ripetere l'associazione dello STESSO esercizio YMove su un esercizio diverso della stessa libreria coach → verificare il messaggio "gia' associato a..." invece di un duplicato silenzioso.
  10. "Sostituisci video" → sceglierne un altro → verificare il cambio; poi "Rimuovi collegamento YMove" → verificare il ritorno allo stato senza video/upload manuale.
  11. Salvare e assegnare la scheda a un cliente → login cliente → aprire l'esercizio → verificare che il video sia visibile.
  12. Provare a rompere il player (rete instabile) → verificare messaggio d'errore chiaro + "Riprova", mai un player vuoto/bloccato.
  13. Verificare in Table editor che `public.exercises`/`exercise_videos` non contengano MAI un URL video/thumbnail salvato in modo permanente (solo `ymove_exercise_id`/`ymove_slug` o, per un file caricato, il proprio `video_path`/`video_url` di Storage).
  14. Confermare che login, registrazione, pacchetti/abbonamenti, reset password, credenziali provvisorie e Brevo restano invariati (nessuna regressione).
  15. Coach → "Modifica scheda" → "Libreria YMove" → aggiungere un esercizio → "Salva modifiche" → tornare alla vista scheda: verificare che l'esercizio compaia nella lista "Esercizi" (BUG-016, corretto il 2026-07-13 — prima spariva silenziosamente).
- [x] **Fix BUG-015 — testi italiani YMove ora per-coach, non più "wiki-style" (2026-07-13):** rimossa la policy che permetteva a qualunque coach di modificare il testo condiviso; nuova tabella `exercise_text_overrides` per la personalizzazione privata. Vedi `docs/BUGS.md`, `docs/DECISIONS.md`.
- [x] **Fix BUG-016 — esercizio YMove spariva dalla lista scheda dopo salvataggio/riapertura (2026-07-13):** `schede/[id].tsx` usava ancora `getExerciseById` locale invece del resolver. Vedi `docs/BUGS.md`.
- [x] **Fix BUG-017 — card esercizio in scheda mostra ora la thumbnail live YMove invece del placeholder con l'iniziale (2026-07-13, continuazione 3):** `ExerciseThumbnail` risolve la miniatura da `exercise.ymoveExerciseId` o da `exercise_videos` (con cache in memoria per non richiamare la Edge Function ad ogni render), sempre solo un'immagine statica, mai un video montato in lista. Vedi `docs/BUGS.md`, `docs/WORKLOG.md`.
- [x] **Fix BUG-018 — autorizzazione cliente corretta nell'azione `detail` per un video YMove collegato (non importato) (2026-07-13, continuazione 4):** un cliente ora vede il video/la thumbnail anche per un esercizio locale/custom collegato via `exercise_videos` dal proprio coach, non solo per gli esercizi importati in `public.exercises`. Vedi `docs/BUGS.md`, `docs/YMOVE_INTEGRATION.md`. **Da fare (priorità alta): ridistribuire la Edge Function** (`supabase functions deploy ymove-exercises`) — il fix non ha alcun effetto finché non viene deployato.
- [ ] **Verifica reale anteprime esercizio (priorità alta, mai fatta contro un progetto/API reali):**
  1. Coach → scheda con un esercizio importato da YMove → verificare che la card mostri la thumbnail reale (non l'iniziale) dopo un istante di caricamento, con una piccola icona play sovrapposta.
  2. Coach → esercizio locale storico (uno dei 44) con un video YMove associato tramite "Associa video YMove" → verificare che anche questa card mostri la thumbnail live (stesso comportamento del punto 1).
  3. Coach → esercizio con un video caricato manualmente (upload) → verificare che la card mostri ancora il placeholder con l'iniziale (nessun poster salvato oggi — comportamento atteso, non un bug).
  4. Coach → esercizio senza alcun video → verificare placeholder invariato.
  5. Scorrere una scheda con piu' esercizi che condividono lo stesso video YMove (es. due esercizi collegati allo stesso id) → verificare nei log che la Edge Function `detail` venga chiamata una sola volta per quel `ymoveExerciseId` nella sessione (cache condivisa), non una volta per card.
  6. Ripetere i punti 1-2 lato **cliente** su una scheda assegnata → verificare che il video/la thumbnail siano visibili sia per un esercizio importato da YMove (caso 1) sia per un esercizio locale/custom collegato dal PROPRIO coach (caso 2, corretto da BUG-018) → poi verificare il caso negativo: cliente di un ALTRO coach (o senza coach collegato) → deve restare `forbidden`/placeholder.
  7. Coach → aprire l'esercizio del punto 2 → "Sostituisci video" con un altro video YMove → tornare alla scheda → verificare che la card mostri la NUOVA thumbnail, non quella vecchia (verifica invalidazione cache).
  8. Confermare che serie/ripetizioni/recupero/completamento esercizio/navigazione scheda restano invariati (nessuna regressione).
- [x] **Associazione automatica dei video YMove, sostituisce la ricerca manuale come flusso principale (2026-07-13, continuazione 5):** nuovo servizio `mobile/src/lib/ymove-auto-link-service.ts` (algoritmo di matching pesato nome/muscolo/attrezzatura/tipologia, soglie 0.85/0.10), avviato automaticamente al primo accesso del coach, con banner di stato non bloccante nella dashboard. Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/YMOVE_INTEGRATION.md`.
- [ ] **Verifica reale associazione automatica YMove (priorità alta, mai fatta contro un progetto/API reali — nessuna chiave YMove/Azure disponibile in questo ambiente):**
  1. Configura `YMOVE_API_KEY` + `AZURE_TRANSLATOR_KEY`/`AZURE_TRANSLATOR_REGION` sul progetto reale, ridistribuisci la Edge Function (include l'estensione dell'azione `translate`, vedi comando sotto).
  2. Login come coach reale (primo accesso dopo l'aggiornamento) → verifica che compaia il banner "Associazione automatica video: N/44" nella dashboard, con avanzamento progressivo, poi il riepilogo finale "Video associati: X · Non trovati: Y · Ambigui: Z".
  3. Apri una scheda con esercizi appena collegati automaticamente → verifica che la card mostri la thumbnail reale (non l'iniziale) e che il video parta nel Dettaglio esercizio.
  4. Verifica in Table editor (`exercise_videos`) che le nuove righe abbiano `coach_id` reale, `ymove_exercise_id`/`ymove_slug` valorizzati, `video_path`/`video_url` NULL — mai un URL salvato.
  5. Chiudi e riapri l'app subito dopo → verifica che il banner NON ricompaia (dedup di sessione + cooldown 7 giorni: nessuna nuova scansione).
  6. Prova con un secondo coach → verifica collegamenti indipendenti (stesso video YMove associabile a esercizi diversi da coach diversi, mai in conflitto — vincolo `unique(coach_id, ymove_exercise_id)`).
  7. Verifica manualmente alcuni esercizi rimasti "non associati"/"ambigui" (log `YMOVE_AUTOLINK_SKIPPED`) → confermare che restino collegabili a mano con "Associa video YMove" (fallback, invariato).
  8. Valuta il tasso di successo reale (quanti dei 44 vengono effettivamente collegati) — dipende dal vocabolario `muscleGroup`/`equipment` usato realmente da YMove, sconosciuto a priori e non verificabile in questo ambiente (solo simulato con un catalogo sintetico, vedi `docs/WORKLOG.md`). Se il tasso è basso, il primo punto da rivedere è il dizionario muscolo/attrezzatura in `mobile/src/lib/ymove-name-matching.ts` (facilmente estendibile con le parole chiave osservate nelle risposte reali), non le soglie 0.85/0.10 (fornite esplicitamente dall'utente).
- [ ] **Schermata di creazione esercizio `custom` da zero** (non ancora costruita, solo schema/RLS la prevedono): quando verrà costruita, richiamare `autoLinkYmoveVideoForExercise(exercise)` (già pronta in `ymove-auto-link-service.ts`) subito dopo l'inserimento riuscito, cosi' anche un esercizio custom appena creato ottiene subito un video se il match è sufficientemente affidabile.
- [x] **Migrazione schede e allenamenti da Zustand/AsyncStorage a Supabase (2026-07-14):** nuove tabelle `workout_templates`/`workout_plans`/`workout_days`/`workout_day_exercises`, RPC atomiche `save_workout_plan`/`update_workout_session_progress`, nuovo servizio `mobile/src/lib/workout-plan-service.ts`, migrazione one-shot filtrata (`workout-plan-migration.ts`), Realtime scoped (`workout-plan-realtime.ts`), schermate schede/allenamenti collegate. Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`.
- [x] **Fix BUG-019 — "invalid input syntax for type uuid" su id locali testuali/numerici (2026-07-14, continuazione):** `isValidUuid` (ex `isUuid`) applicata anche a `deleteWorkoutPlan`/`updateWorkoutSessionProgress`; nuova funzione SQL `public.is_valid_uuid(text)` usata prima di ogni cast in `save_workout_plan`; nuova azione store `replaceWorkoutPlan` per sostituire in sicurezza l'id locale con quello remoto senza doppioni; migrazione one-time con mappa persistita `localId -> remoteId` invece di un'euristica sulla forma dell'id. Vedi `docs/BUGS.md` (BUG-019), `docs/WORKLOG.md`.
- [ ] **Verifica reale migrazione schede a Supabase (priorità assoluta, mai fatta contro un progetto reale — nessuna chiave disponibile in questo ambiente):**
  1. Esegui la nuova sezione "Migrazione schede e allenamenti a Supabase" di `docs/SUPABASE_SCHEMA.sql` sul progetto reale (controlla anche che la tabella `workout_plans` compaia nella pubblicazione Realtime: Database → Replication).
  2. Login come coach reale con almeno un cliente REALMENTE collegato (`coach_clients`) → verifica in Table editor che le schede locali pre-esistenti di quel cliente vengano migrate automaticamente (una riga in `workout_plans` + una in `workout_days` con `day_order=1` + una riga in `workout_day_exercises` per ogni esercizio) — MAI le schede demo del cliente seed (Marco).
  3. Coach → "Nuova scheda" → crea una scheda con almeno un esercizio storico locale, uno custom/Supabase se disponibile, e (se già collegato) uno YMove → verifica che venga salvata nelle quattro tabelle correttamente collegate (workout_plans → workout_days → workout_day_exercises).
  4. Coach → modifica serie/ripetizioni/recupero/note di un esercizio esistente → salva → verifica che la riga `workout_day_exercises` corrispondente sia aggiornata (stesso id, non una riga duplicata) e che le righe rimosse dall'editor spariscano davvero dal DB.
  5. Coach → elimina una scheda → verifica che spariscano anche le righe `workout_days`/`workout_day_exercises` collegate (cascade).
  6. Login come il cliente assegnato → verifica che veda esattamente quella scheda; login come un cliente NON collegato a quel coach → verifica che non la veda in nessun modo (RLS).
  7. Lato cliente: "Inizia allenamento" → completa uno o più esercizi (checkbox) → "Fine allenamento" → verifica in Table editor che `session_status`/`started_at`/`completed_at`/`duration_seconds` e il flag `completed` sugli esercizi siano aggiornati — poi login come coach sullo stesso dispositivo o un secondo, aprire la stessa scheda SENZA riavviare l'app → verificare che il progresso compaia (Realtime).
  8. Prova una superserie/circuito (stesso `superset_group_id` su più esercizi) e un esercizio cardio → verifica che il raggruppamento visivo e il bottone Cardio funzionino identici a prima.
  9. Logout/login ripetuti (stesso coach, stesso cliente) → verifica che le schede restino sempre visibili e coerenti, nessuna sparizione né duplicazione.
  10. Se possibile, testa su un secondo dispositivo/browser con lo stesso account → verifica che una scheda creata su un dispositivo compaia sull'altro dopo il refresh-on-focus (o via Realtime senza nemmeno riaprire la schermata).
  11. Disattiva temporaneamente la rete durante un salvataggio → verifica che compaia un messaggio d'errore reale (non un salvataggio silenzioso fallito) e che la scheda NON risulti parzialmente salvata in nessuna delle quattro tabelle.
  12. Conferma che login, registrazione, codice coach, Azure Translator, Edge Function YMove, associazione automatica video, chat, appuntamenti, notifiche push, RevenueCat/Stripe e superadmin restano invariati (nessuna regressione).
  13. **(BUG-019)** Coach → "Nuova scheda" appena dopo l'aggiornamento (nessun piano ancora salvato) → crea/salva → verifica che NON compaia mai "invalid input syntax for type uuid" e che la scheda risulti nelle quattro tabelle con id UUID reali.
  14. **(BUG-019)** Modifica una scheda che era rimasta solo locale (es. Supabase configurato dopo aver gia' creato qualche scheda offline) → salva → verifica in Table editor che esista UNA SOLA riga per quella scheda (mai sia il vecchio id locale sia un nuovo UUID) e che l'URL della schermata sia stato aggiornato al nuovo id.
  15. **(BUG-019)** Elimina una scheda MAI salvata su Supabase (id locale) mentre Supabase e' configurato → verifica che sparisca dalla lista senza errori (nessuna riga remota da eliminare, comportamento no-op atteso).
- [x] **Fix BUG-020 — dashboard coach: i cinque pulsanti "Azioni rapide" si sovrapponevano su Android (2026-07-13, continuazione 6):** griglia ricalcolata in pixel da `useWindowDimensions` invece di percentuali+minWidth+flexGrow. Vedi `docs/BUGS.md`.
- [x] **Fix BUG-021 — libreria YMove: picker vuoto finché non si premeva Cerca, nessuna miniatura, esercizi con `name` scartati (2026-07-13, continuazione 6):** ricerca automatica all'apertura, miniature lazy con concorrenza limitata, fallback `name`→`title`. Vedi `docs/BUGS.md`. **Da fare (priorità alta): ridistribuire la Edge Function** (`supabase functions deploy ymove-exercises`) per il fallback lato server.
- [x] **Fix BUG-022 — superadmin "Account coach": dati demo sempre visibili, nessun errore/refresh reale (2026-07-13, continuazione 6):** dati demo dietro `EXPO_PUBLIC_ENABLE_DEMO_DATA`, badge "Demo", errore+Riprova, pulsante "Aggiorna", verifica ruolo superadmin prima del caricamento. Vedi `docs/BUGS.md`.
- [ ] **Verifica reale dei tre fix BUG-020/021/022 (priorità alta, mai fatta contro device/API/progetto reali):**
  1. Apri la dashboard coach su un device o emulatore Android reale (o DevTools con larghezza forzata) a 320px, 360px, 393px, 412px → verifica che i cinque pulsanti non si sovrappongano mai, due colonne normali, quinto pulsante a capo in terza riga, nessuna sovrapposizione con le card statistiche sopra.
  2. Configura `YMOVE_API_KEY` (se non già fatto) e ridistribuisci la Edge Function → apri "Libreria YMove" dal coach → verifica che gli esercizi compaiano SUBITO (nessun bottone Cerca da premere prima) e che le miniature compaiano dopo un istante di caricamento (placeholder fitness prima, poi l'immagine reale) con l'icona play se il video esiste.
  3. Con la rete disattivata temporaneamente, apri il picker → verifica il messaggio d'errore leggibile con "Riprova"; riattiva la rete e premi "Riprova" → verifica che i risultati compaiano senza dover riaprire il picker.
  4. Cerca un esercizio con filtri che non danno risultati → verifica "Nessun esercizio trovato" (mai insieme a un errore); poi cerca di nuovo senza filtri → verifica che i risultati precedenti non spariscano mai durante il caricamento della nuova ricerca.
  5. Login come superadmin reale (`profiles.role='superadmin'`) → verifica che un coach registrato via `/registrazione-coach` compaia nell'elenco "Account coach" con UUID reale, email, codice coach, pacchetto/scadenza; verifica che senza `EXPO_PUBLIC_ENABLE_DEMO_DATA` impostata nessun account "Demo" compaia; imposta `EXPO_PUBLIC_ENABLE_DEMO_DATA=true` in `mobile/.env` e riavvia → verifica che i coach demo compaiano ora con badge "Demo".
  6. Nell'elenco coach, premi "Aggiorna" → verifica il refresh manuale; disattiva temporaneamente la rete e riprova → verifica errore leggibile con "Riprova".
  7. Apri il dettaglio di un coach reale (UUID nell'URL) → verifica che blocco/sblocco, assegnazione pacchetto e modifica profilo funzionino ancora (invariati da BUG-011/2026-07-12) e che nessuna azione dichiari un falso successo.
- [x] **Fix bug APK Android BUG-023/024/025/026 (2026-07-13, continuazione 8):** overlay splash che bloccava i tocchi, layout superadmin, seed demo dietro flag, tastiera registrazione. Vedi `docs/BUGS.md`.
- [ ] **Verifica reale sul NUOVO APK Android (priorità assoluta — ricostruire l'APK prima, i fix non hanno effetto su quello vecchio):**
  1. Tab bar cliente (Home/Workout/Nutrizione/Chat/Altro) e coach (Home/Clienti/Schede/Agenda/Messaggi): ogni tab apre la pagina giusta al PRIMO tocco.
  2. Home cliente: Check-in apre `/questionario`, Prenota apre `/prenotazioni`, Bacheca apre `/bacheca` (tocco sull'intera riga, non solo sull'icona).
  3. Menu Altro: tutte le 9 voci navigano.
  4. Coach → Clienti: "Vai ad Abbonamento" apre la schermata Abbonamento; la lista NON mostra Marco Bianchi/Giulia Verdi/Luca Ferrari (senza flag); con `EXPO_PUBLIC_ENABLE_DEMO_DATA=true` ricompaiono con badge "Demo".
  5. Superadmin: tab bar non sovrapposta a contenuti/barra di navigazione Android; form interamente raggiungibili; dashboard con card compatte e alert non sovrapposto, a 320/360/393/412px.
  6. Dettaglio coach superadmin: chip "Piano" disabilitati con nota per un coach reale; salvataggio con conferma "Modifiche salvate su Supabase."; "Clienti utilizzati/attivi" coerenti con `coach_clients` reali.
  7. Registrazione coach: con tastiera aperta il form scorre fino all'ultimo campo, "Avanti" passa al campo successivo sui campi credenziali, "Fine" chiude la tastiera, un tocco fuori la chiude, il bottone "Crea account coach" risponde al primo tocco.
- [ ] **UI di gestione "i miei modelli" personalizzati** (facoltativa, non richiesta esplicitamente): `workout_templates` + le funzioni `listWorkoutTemplates`/`createWorkoutTemplate`/`updateWorkoutTemplate`/`deleteWorkoutTemplate` sono pronte in `workout-plan-service.ts` ma senza alcuna schermata che le richiami — i 7 modelli predefiniti (`data/workout-plan-templates.ts`) restano l'unico modo attuale di partire da un modello.

## Priorità media

- [ ] **Step 6 — Vista calendario appuntamenti:** oggi solo lista cronologica; aggiungere vista mensile/settimanale.
- [ ] **Riordino esercizi via drag-and-drop** nell'editor scheda (oggi frecce su/giù, funzionale ma meno comodo).
- [ ] **Selettore data con calendario nativo** per le date (oggi testo libero formato AAAA-MM-GG).
- [ ] **Unicità username** negli account cliente generati (oggi due clienti omonimi genererebbero lo stesso username — accettabile solo in demo).
- [x] **Invio email reale delle credenziali provvisorie (2026-07-11, continuazione):** "Invia via email" ora chiama una Edge Function reale (`send-temporary-credentials`) che genera una password provvisoria server-side, la imposta su Supabase Auth e la invia via email (Resend); `must_change_password` blocca l'accesso finché non viene cambiata. Vedi `docs/WORKLOG.md`, `docs/DECISIONS.md`, `docs/SUPABASE_TEMP_CREDENTIALS.md`. **Non ancora testato contro un progetto Supabase reale** — vedi checklist dedicata sotto.

## Priorità bassa / backlog

- [x] **Redesign visivo v1 + v2 (2026-07-05):** vedi `docs/WORKLOG.md`.
- [x] **Tema, clienti reali, auth demo (2026-07-05):** vedi `docs/WORKLOG.md`. **Non verificato visivamente in un browser reale** — `web.output: "single"` impedisce la verifica del contenuto per-rotta via `curl`: serve aprire `http://localhost:8081` di persona, soprattutto per i flussi interattivi (login, generazione credenziali, cambio tema).
- [ ] **Documentare il nuovo sistema visivo (rosso) in un `DESIGN.md` dedicato a FitCoach Pro** (quello in root resta del progetto B2B precedente).
- [ ] **Test end-to-end su iPhone/Android reali** via Expo Go: timer/suono/vibrazione, video player, superserie/circuito, login/cambio password, tab bar nativa cliente vs coach, condivisione credenziali (Share API nativa) — mai testati su device fisico, solo su preview web.
- [ ] Preparazione futura EAS Build / TestFlight / App Store — non ora.
- [ ] Decisione rimandata dall'utente: se e come aggiornare `PRODUCT.md`/`DESIGN.md`, attualmente ancora relativi a un progetto diverso (webapp B2B gestione progetti/task).

## Prossimo step operativo immediato

**Priorità assoluta**: aprire `http://localhost:8081` e cliccare davvero (nessun tool di automazione browser era disponibile per verificarlo in questa sessione). Checklist abbonamenti/sessioni/agenda (2026-07-06, non ancora verificata con clic reali):
1. Da coach, aprire un cliente (`/clienti/[id]`) → premere "Crea abbonamento", compilare pacchetto (es. 12 allenamenti) e salvare → verificare che la card Abbonamento mostri "0/12" e lo stato Attivo.
2. Premere "Aggiorna abbonamento" → cambiare totale/completati/stato → salvare → verificare che il dettaglio cliente rifletta i nuovi valori.
3. Dallo stesso cliente, premere "+ Nuova scheda" più volte per creare 2-3 sessioni diverse (es. "Petto+Tricipiti", "Dorso+Bicipiti") con data/ora diverse e collegarle all'abbonamento creato → verificare che tutte compaiano nella lista "Schede assegnate" del cliente, ciascuna con la propria data/ora.
4. Premere "+ Nuovo appuntamento" per lo stesso cliente, impostare data/ora, salvare → poi crearne un secondo con **stessa data e orario sovrapposto**: verificare che compaia "Orario non disponibile. Scegli un altro orario." e che il secondo appuntamento NON venga salvato.
5. Verificare che l'agenda (`/appuntamenti`) mostri l'appuntamento creato, ordinato cronologicamente, e che la Dashboard coach (`/`) mostri lo stesso come "Prossimo appuntamento".
6. Da cliente (login `marco.bianchi`/`Forza4821!` o il cliente usato sopra), verificare in Home il contatore aggiornato (es. "0/12"), il prossimo allenamento con data/ora corrette; in tab Workout verificare che tutte le sessioni create compaiano con data/ora; aprire una sessione, premere "Inizia allenamento" → "Fine allenamento" → tornare in Home e verificare che il contatore sia salito a "1/12".
7. Ripetere il punto 6 una seconda volta su un'altra sessione per confermare che il contatore sale a "2/12" (non si blocca, non salta numeri).
8. Verificare che nessuna delle nuove schermate mostri scritte "demo"/"provvisorio" e che tutte le tab coach (Dashboard/Clienti/Esercizi/Schede/Agenda/Impostazioni) restino cliccabili come prima (nessuna regressione da BUG-002-style sulla tab bar coach).

Checklist fix/nuove funzioni Coach/Admin (2026-07-06, parte 2), non ancora verificata con clic reali:
9. Aprire un cliente con abbonamento esistente → "Aggiorna abbonamento" → cambiare stato in "In pausa" → salvare → tornare al dettaglio cliente: la card Abbonamento deve mostrare il pacchetto con stato "In pausa" (NON "Nessun abbonamento"). Ripetere cambiando in "Completato", "Scaduto", "Annullato": la card deve sempre riflettere lo stato corrente.
10. Impostare una sessione su "Saltato" (chip di stato in vista) → aprire "Modifica scheda" → cambiare data o ora → salvare: lo stato deve tornare "Da fare"/Programmato. Poi ripetere ma cambiando solo il nome (non data/ora): lo stato deve restare "Saltato".
11. Aprire "Schede" → "Modelli allenamento" → aprire un modello (es. "Massa muscolare 3 giorni") → "Usa per cliente" → selezionare un cliente (+ abbonamento se disponibile) → confermare: verificare che vengano create le schede nel dettaglio cliente, con nome "Titolo modello — Titolo sessione", poi aprire una di queste e verificare che sia modificabile (esercizi/serie/ripetizioni/recuperi/data/ora) senza che il modello originale in "Modelli allenamento" cambi.
12. Verificare che la tab bar coach in basso mostri un'icona per ogni voce (Dashboard/Clienti/Esercizi/Schede/Agenda/Impostazioni), colorata di rosso sulla tab attiva, e che tutte restino cliccabili.
13. In Impostazioni → Suono selezionato, verificare che compaia "Sirena" tra le opzioni e che il bottone "Prova" la riproduca davvero (non solo silenzio/errore).
14. Aprire "Crea appuntamento" e confermare che non c'è alcun campo "Data fine" (solo Data, Ora inizio, Ora fine).

Poi, checklist ereditata dalla sessione precedente (2026-07-05), ancora da verificare:
15. Login cliente → cliccare tutte e 5 le tab in basso (Home/Workout/Nutrizione/Chat/Altro): ognuna deve cambiare schermata e aggiornare il colore (rosso attivo/grigio inattivo).
16. Da Home, cliccare ogni card e il link "Chat con il coach"; da Altro, cliccare tutte e 8 le voci del menu. Se qualcosa non naviga, vedi `docs/BUGS.md` BUG-002 per il meccanismo di debug già usato.
17. Aprire un esercizio dalla scheda → verificare la sezione "Serie" precompilata, bottone "Rec.", sezione "Allegati" con upload foto, barra "◄ N/M ►".
18. Da cliente, provare a raggiungere `/clienti` o `/schede` via URL: deve comparire "Sezione riservata al coach".
19. Provare un development build EAS al posto di Expo Go per il test su device reale (vedi nota Expo Go sopra e in `docs/BUGS.md`).

**Prossimo intervento consigliato: verifica reale del collegamento Supabase Fase 1** (checklist dedicata sopra, mai eseguita contro un progetto reale) prima di migrare altro. In alternativa: lato coach per Nutrizione/Bacheca personale, caricamento video/immagini reali, o le checklist di clic reali ereditate sopra (Superadmin/registrazione, abbonamenti/agenda, lato cliente).
- [x] **AZIONE RICHIESTA — eseguire la migration del Fix BUG-036 sul progetto Supabase reale:** **CONFERMATO APPLICATO il 2026-07-21** (vedi nota generale sotto, `npx.cmd --yes supabase@latest migration list` mostra questa e tutte le migration successive come "remote"). Restano da fare solo le verifiche manuali con clic reali descritte sotto: login coach reale con 1 cliente active + 1 suspended → tab "Tutti" deve mostrare 2 card, "Sospesi" deve mostrare il sospeso con badge "Sospeso"; aprire il dettaglio del sospeso → "Riattiva cliente"/"Rimuovi cliente" devono comparire e funzionare; verificare in console (DEV) i log `COACH_CLIENTS_LINKS_LOADED`/`COACH_CLIENTS_PROFILES_LOADED`/`COACH_CLIENTS_MAPPED`/`CLIENTI_LIST_FILTER` per confermare che nessun link resti senza profilo (`COACH_CLIENTS_PROFILE_MISSING_FOR_LINK` non deve comparire per un cliente active/suspended). Vedi `docs/BUGS.md` (BUG-036).
- [x] **Fix BUG-035 — sospensione/rimozione cliente coach senza effetto su Expo Web (2026-07-20):** causa reale `Alert.alert` no-op su `react-native-web`; `mobile/src/app/clienti/[id].tsx` ora usa `confirmDestructive()` (`globalThis.confirm` su web, `Alert.alert` su nativo, stesso pattern di `client-metrics-panel.tsx`). Vedi `docs/BUGS.md` (BUG-035), `docs/WORKLOG.md`. Commit `c96d734`.
- [ ] **Verifica reale fix BUG-035 (priorita alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente):**
  1. Login coach reale su Expo Web → dettaglio di un cliente "active" → "Sospendi per rinnovo" → deve apparire il dialog di conferma (`window.confirm` su web) → "Annulla" non deve cambiare nulla.
  2. Ripetere e confermare → lo stato deve diventare "Sospeso" (badge ambra), spariscono "Assegna scheda"/"Sospendi per rinnovo", compaiono "Riattiva cliente"/"Rimuovi cliente" — verificare anche in Table editor (`coach_clients.status='suspended'`, `suspended_at` valorizzato) che il posto nel pacchetto coach NON venga liberato.
  3. "Riattiva cliente" → stato deve tornare "Attivo", ricompaiono "Assegna scheda"/"Sospendi per rinnovo".
  4. "Rimuovi cliente" (su active o suspended) → conferma → "Annulla" non cambia nulla → confermare → deve tornare a `/clienti` e il cliente rimosso non deve più comparire nella lista (verificare che account/profilo/storico/misure/schede/carichi NON vengano cancellati, solo `coach_clients.status='removed'`).
  5. Confermare che "Assegna scheda" su un cliente active continui a funzionare invariato (non toccato da questo fix).
  6. Ripetere su nativo (iOS/Android reale o Expo Go) per confermare che `Alert.alert` mostri il dialog come prima (ramo nativo di `confirmDestructive` invariato rispetto al comportamento precedente).

- [x] **Feat: inserimento carichi lato coach, riuso ExerciseSetLogger (2026-07-20):** coach ora vede e usa lo stesso `ExerciseSetLogger` del cliente in `/esercizi/[id]` (serie/peso/ripetizioni/note/recupero) e puo' completare un singolo esercizio da `/schede/[id]` (completamento workout gia' possibile via chip di stato). Vedi `docs/BUGS.md` (BUG-037), `docs/WORKLOG.md`. Commit `f6bb82f`.
- [x] **AZIONE RICHIESTA — eseguire la migration del blocco carichi lato coach:** **CONFERMATO APPLICATO il 2026-07-21** (vedi nota generale sotto). `exercise_progress_history.workout_exercise_id` esiste e `update_workout_session_progress` rifiuta ora chiamate su sessioni gia' completate — restano da fare solo le verifiche manuali con clic reali gia' elencate per questo fix, checklist sopra.
- [ ] **Verifica reale carichi lato coach (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente):**
  1. Login coach su un cliente "active" con almeno una scheda assegnata → Schede → tap sessione → tap un esercizio → deve apparire "Serie" (ExerciseSetLogger) con peso/ripetizioni/note/bottone recupero, non solo lo storico in sola lettura.
  2. Coach inserisce 2 serie (peso/ripetizioni) e salva → login cliente (o secondo device/browser) sullo stesso esercizio → deve vedere le stesse 2 serie senza refresh manuale (Realtime gia' presente, `EXERCISE_PROGRESS_REALTIME_EVENT` in console DEV).
  3. Cliente modifica/aggiunge una serie → coach deve vedere l'aggiornamento senza refresh manuale.
  4. Coach aggiunge una terza serie → verificare nessun duplicato/sovrascrittura delle prime due.
  5. Coach completa l'esercizio (checkbox in `/schede/[id]`, ora visibile anche a lui) → riaprire l'esercizio: "Serie" deve diventare read-only (card "Esercizio completato") per coach E cliente, "+ Serie"/elimina spariscono per entrambi.
  6. Coach completa l'intero workout (chip "Completata" in `/schede/[id]`) → l'intera sessione deve diventare read-only per entrambi, consultabile ma non modificabile.
  7. Nuova assegnazione della stessa scheda/sessione (nuova "+ Nuova scheda") → deve creare una NUOVA sessione (nuovo `workout_plan_id`), senza toccare lo storico della precedente.
  8. Un coach DIVERSO (non collegato a quel cliente) prova una chiamata diretta a Supabase su `exercise_progress_history` per quel cliente → deve essere rifiutato dalla RLS.
  9. Sospendere il cliente, poi provare a inserire un carico (sia da UI sia via chiamata diretta) → deve essere rifiutato (relazione non `active`); stesso test dopo "Rimuovi cliente" (`removed`).
  10. Dopo aver applicato la migration `20260720190000`: con la sessione gia' completata, chiamare direttamente `supabase.rpc('update_workout_session_progress', ...)` o un insert diretto su `exercise_progress_history` per quella scheda → deve essere rifiutato con `WORKOUT_LOCKED` (RPC) o violazione RLS (insert), non silenziosamente accettato.
  11. Verificare `mobile/src/components/client-load-history.tsx` (tab Carichi, form "Aggiungi carichi" gia' esistente) continua a funzionare invariato per il coach (backfill manuale, non toccato da questa modifica).

- [ ] **RevenueCat pacchetti coach - configurazione e verifica reale (priorita alta):**
  1. Eseguire manualmente la migrazione SQL idempotente aggiornata in `docs/SUPABASE_SCHEMA.sql` (campi RevenueCat su `subscription_packages` + tabella `revenuecat_webhook_events`).
  2. Configurare in RevenueCat entitlement/offering e collegare i prodotti Google Play/App Store; poi salvare in Superadmin -> Pacchetti i campi `revenuecat_entitlement_id`, `revenuecat_offering_id`, `android_product_id`, `ios_product_id`.
  3. Impostare l'unico secret manuale della Edge Function: `REVENUECAT_WEBHOOK_SECRET` (`SUPABASE_URL` e `SUPABASE_SERVICE_ROLE_KEY` sono forniti dal runtime Supabase).
  4. Deployare `revenuecat-webhook` e configurare il webhook RevenueCat con lo stesso secret.
  5. Impostare in EAS le public keys `EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY` e `EXPO_PUBLIC_REVENUECAT_IOS_API_KEY`.
  6. Creare una development/preview build reale Android/iOS (non Expo Go) e verificare acquisto, restore, cambio pacchetto, rinnovo, cancellazione, scadenza, billing issue e logout/cambio coach.
  7. Verificare in Table editor che gli acquisti RevenueCat scrivano `payment_provider='revenuecat'` e che le assegnazioni manuali superadmin restino `payment_provider='superadmin_manual'`.

- [ ] **Verifica nuovo APK Android - back, contatore e layout form (priorita assoluta):**
  1. Cliente: Workout -> scheda -> esercizio -> back torna alla scheda; secondo back torna a Workout. Se si apre la route senza cronologia, fallback coerente a Workout/Altro/Home.
  2. Coach: Clienti -> cliente -> Nuova scheda -> back torna al cliente; Schede -> scheda -> Esercizio -> back torna alla scheda.
  3. Superadmin: Coach -> dettaglio -> back torna alla lista; piani/pacchetti/notifiche/pagamenti/supporto hanno una sola freccia, nessuna doppia freccia nativa.
  4. Contatore: con 0 completati mostra 0/12; completa una scheda -> 1/12; completa una seconda -> 2/12; lato coach stesso cliente mostra 2/12; riaprire una scheda gia' completed non deve diventare 3/12.
  5. Persistenza: chiudere e riaprire l'app dopo completamento sessione; il contatore deve restare coerente perche' deriva da `workout_plans.session_status='completed'`.
  6. Nuova scheda Android 320/360/393/412px: nessun campo tagliato, Data/Ora e Giorno/Settimana impilati quando serve, pulsanti "Aggiungi esercizio"/"Libreria YMove" dentro schermo, form non coperto dalla tab bar.
- [ ] **Verifica redesign/avatar/catalogo esercizi sul nuovo APK:**
  1. Eseguire la sezione SQL "FitCoach Pro visual assets" di `docs/SUPABASE_SCHEMA.sql` su Supabase prima di testare upload foto reale.
  2. Creare/verificare bucket `client-avatars`, caricare una foto da Profilo cliente e confermare che `profiles.avatar_url` contenga il path Storage e che l'immagine ricompaia dopo logout/login.
  3. Registrazione cliente e creazione cliente coach: scegliere avatar uomo/donna/neutro e verificare che dashboard/lista/dettaglio mostrino il preset senza foto.
  4. Aprire Home cliente, Dashboard coach e Superadmin su Android 320/360/393/412px: logo/header leggibili, nessun overflow, nessuna metrica fake.
  5. Aprire una scheda con esercizi locali, YMove e upload manuale: asset locale presente -> immagine; asset mancante -> placeholder gruppo; YMove valido -> thumbnail live; upload senza poster -> placeholder con badge video.
  6. Quando saranno disponibili asset reali, popolare `IMAGE_REGISTRY`/`exercise_image_catalog` e verificare che non venga mai restituita una sorgente immagine non esistente.

- [x] **Fix BUG-038/BUG-039 — contesto cliente non isolato nelle schede coach (2026-07-20, continuazione 3):** `esercizi/[id].tsx` mostrava chip di TUTTI i clienti con lo stesso esercizio assegnato anche quando il coach arrivava dal dettaglio di un cliente specifico. `schede/[id].tsx` ora passa `clientId` esplicito e autorevole; `esercizi/[id].tsx` valida `planId`+`clientId` contro i `workoutPlans` in store e blocca la schermata (nessun cliente selezionato automaticamente) se la validazione fallisce. Trovato anche un secondo gap distinto: `update_workout_session_progress` non verificava `coach_clients.status='active'` per il percorso coach (solo per quello cliente) — nuova migration `20260721090000` lo corregge. Vedi `docs/BUGS.md` (BUG-038, BUG-039), `docs/WORKLOG.md`. Commit `b1c7221 fix: mantiene il cliente selezionato nelle schede coach`.
- [x] **AZIONE RICHIESTA — eseguire la migration del controllo relazione attiva coach:** **CONFERMATO APPLICATO il 2026-07-21** (vedi nota generale sotto). Un coach non puo' piu' completare/modificare la sessione di un cliente sospeso/rimosso — restano da fare solo le verifiche manuali con clic reali gia' elencate per questo fix.
- [ ] **Verifica reale contesto cliente nelle schede coach (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente):**
  1. Login coach → apri il dettaglio di un cliente (es. Rita) → Schede → tap una sessione → tap un esercizio: deve comparire SOLO Rita (nome+avatar), nessun chip di altri clienti.
  2. Con lo stesso esercizio assegnato anche ad altri clienti (es. Luigi) in un'altra scheda: verificare che nel percorso di Rita non compaiano mai chip verso Luigi, ne' sia possibile selezionarlo.
  3. Inserire una serie/peso da questo percorso → verificare in Table editor che `exercise_progress_history.client_id` sia SEMPRE quello di Rita, mai di un altro cliente.
  4. "Torna al cliente" → deve riportare al dettaglio di Rita; tornare alla lista clienti → selezionare Luigi → Schede → esercizio: deve comparire SOLO Luigi, nessun residuo di Rita.
  5. Manomettere manualmente l'URL cambiando `clientId` con l'id di un altro cliente (mantenendo lo stesso `planId`) → deve comparire la schermata di blocco "Cliente non valido per questa sessione" con bottone "Torna alla lista clienti", mai un fallback silenzioso sul primo cliente disponibile.
  6. Aprire la stessa route senza alcun `clientId`/`planId` (es. da un link salvato vecchio) → stesso comportamento di blocco, mai un'apertura automatica su un cliente a caso.
  7. Aprire il catalogo esercizi generico (`/esercizi`, senza passare da un cliente specifico) → verificare che il comportamento a chip multi-cliente resti invariato (accesso legittimo "generico", non regressione).
  8. Dopo aver applicato la migration `20260721090000`: sospendere/rimuovere un cliente che ha ancora una scheda assegnata a quel coach → chiamare direttamente `supabase.rpc('update_workout_session_progress', ...)` su quella scheda → deve essere rifiutato con `CLIENT_NOT_ACTIVE`, non silenziosamente accettato.

- [x] **Fix/feat BUG-040 — libreria modelli scheda organizzata in cartelle (2026-07-20, continuazione 4):** il tab globale "Schede" mostrava TUTTE le sessioni reali assegnate a TUTTI i clienti invece di essere la libreria di modelli del coach. Sostituito integralmente con cartelle/sottocartelle -> schede modello -> esercizi, backed da nuove tabelle Supabase (`template_folders`, `workout_template_exercises`, `workout_templates` esteso) e tre RPC (`save_workout_template`, `delete_template_folder`, `assign_workout_template_to_client` — quest'ultima richiede esplicitamente cliente `status='active'`, copia indipendente mai collegata al modello originale). Vedi `docs/BUGS.md` (BUG-040), `docs/WORKLOG.md`. Commit `9f18fd2 feat: organizza i modelli scheda in cartelle`.
- [x] **AZIONE RICHIESTA — eseguire la migration della libreria modelli:** **CONFERMATO APPLICATO il 2026-07-21** (vedi nota generale sotto). La libreria modelli (cartelle/RPC) e' operativa in produzione — confermato indirettamente anche dal fix BUG-043 (assegnazione modelli professionali), che ha potuto osservare l'RPC `assign_workout_template_to_client` funzionare davvero contro il progetto reale.
- [ ] **Verifica reale libreria modelli scheda (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente):**
  1. Login coach → tab "Schede" (bottom bar) → deve aprire la libreria globale (cartelle/modelli), MAI un cliente selezionato o preselezionato.
  2. "+ Cartella" → crea "Dimagrimento" → deve comparire subito nella lista, ordinata alfabeticamente.
  3. Dentro "Dimagrimento", "+ Scheda modello" → crea 2-3 schede modello con nome ed esercizi (serie/ripetizioni/recupero/note) → verificare che compaiano nella cartella dopo il salvataggio.
  4. Dentro "Dimagrimento", "+ Cartella" → crea una sottocartella → aprire una scheda modello esistente → "Sposta in altra cartella" → scegliere la sottocartella → tornare indietro e verificare che la scheda non sia piu' visibile nel livello superiore ma lo sia nella sottocartella.
  5. Aprire una scheda modello → "Modifica" → aggiungere/rimuovere/riordinare esercizi, cambiare serie/ripetizioni/recupero/note → "Salva modifiche" → riaprire e verificare che i cambi siano persistiti.
  6. Sulla stessa scheda modello, "Assegna a cliente" → verificare che compaiano SOLO clienti `active` (mai sospesi/rimossi) → scegliere Rita → confermare → deve comparire la conferma a schermo ("Scheda assegnata a Rita", bottoni "Vai al cliente"/"Resta qui") — MAI un'apertura automatica del logger/allenamento.
  7. Clienti → Rita → tab Schede → verificare che la nuova scheda assegnata compaia, con i suoi esercizi corretti.
  8. Tornare al modello originale → modificarlo (es. cambiare le ripetizioni di un esercizio) → salvare → riaprire la scheda di Rita (da Clienti → Rita → Schede) → verificare che NON sia cambiata (copia indipendente).
  9. Tornare al tab globale "Schede" → verificare che non resti alcun cliente "selezionato" ne' alcun residuo dell'assegnazione precedente.
  10. Eliminare una cartella VUOTA → conferma semplice → sparisce. Eliminare una cartella con sottocartelle/modelli dentro → deve comparire la scelta "Sposta il contenuto diretto in Senza categoria"/"Elimina la cartella e tutto il contenuto", con il conteggio TOTALE (incluse le sottocartelle) mostrato correttamente prima di scegliere.
  11. "Duplica" una scheda modello → verificare che crei una copia indipendente nella stessa cartella, con lo stesso nome + "(copia)" e gli stessi esercizi, senza toccare l'originale.
  12. Verificare che `/schede/modelli` (i 7 modelli statici predefiniti) resti raggiungibile solo via URL diretto, invariato, non piu' linkato dal tab globale (comportamento atteso, non un bug).

- [x] **Fix BUG-041 — menu a tendina sempre neri in tema chiaro (2026-07-21):** `DropdownList`/`MenuOption` in `exercise-catalog-picker.tsx` (usati da Seleziona esercizio/Gruppo muscolare/Attrezzatura/Difficolta') avevano sfondo con hex hardcoded quasi identico alla palette dark, indipendente dal tema attivo. Sostituiti con `theme.background`/`theme.backgroundSelected`. Vedi `docs/BUGS.md` (BUG-041). Commit `179c104 fix: corregge il tema dei menu a tendina`.
- [ ] **Verifica reale menu a tendina in tema chiaro/scuro (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente):**
  1. Impostazioni → tema chiaro → aprire un form con aggiunta esercizio (es. Nuova scheda, o Nuova scheda modello) → aprire "Seleziona esercizio" → il pannello deve essere chiaro, testo scuro leggibile, opzione selezionata evidenziata con tinta verde chiara leggibile.
  2. Nello stesso form, aprire "Gruppo muscolare", "Attrezzatura", "Difficolta'" (nel pannello Filtri) → stesso risultato: pannello chiaro, mai nero.
  3. Impostazioni → tema scuro → riaprire tutti e quattro i menu → devono restare leggibili (pannello scuro, testo chiaro, coerente con lo stato precedente al fix, che per il tema scuro era gia' visivamente corretto per coincidenza).
  4. Cambiare tema da Impostazioni mentre l'app e' aperta (senza riavviare) → riaprire un menu → verificare che il colore si aggiorni subito, nessun valore residuo del tema precedente.
  5. Ripetere su Android/iOS (development build o Expo Go, se disponibile) → nessuna regressione: nessun Picker nativo e' coinvolto (il progetto non usa `@react-native-picker/picker`), il fix e' lo stesso componente React Native su tutte le piattaforme.

- [x] **Feat: completa la libreria professionale dei modelli workout (2026-07-21):** aggiunge il livello Workout A/B/C mancante ai modelli (`workout_template_days`), metadati professionali (goal/livello/durata/frequenza/attrezzatura/ecc.), distinzione sistema/personale con RLS a 4 policy per tabella, 18 programmi di sistema completi (62 giorni, 300 esercizi, nessuna serie al cedimento) e filtri/ricerca combinabili nella libreria. Vedi `docs/WORKLOG.md` (voce 2026-07-21 "Feat: completa la libreria professionale"). Commit `5aae09d`, `a7d53b4`, `b8523b5`.
- [x] **AZIONE RICHIESTA — eseguire le due nuove migration della libreria professionale:** **CONFERMATO APPLICATO il 2026-07-21** (vedi nota generale sotto). I modelli personali esistenti hanno il loro "Workout A", i 18 modelli di sistema sono presenti — confermato funzionalmente dal fix BUG-043, che ha osservato `assign_workout_template_to_client` copiare correttamente giorni/esercizi di un modello di sistema in schede reali.
- [ ] **Verifica reale libreria professionale dei modelli workout (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 12 scenari richiesti esplicitamente):**
  1. Dopo aver applicato le due migration: verificare in Table editor che ogni modello personale pre-esistente (creato prima di questo intervento) abbia ora un giorno "Workout A" con tutti i suoi esercizi originali (stesso ordine/serie/reps/recupero/note), nessun modello rimasto senza giorni.
     - **[ ] PARZIALE (verificato 2026-07-22, livello di verifica: statico).** Evidenza: la migrazione dati non distruttiva in `supabase/migrations/20260723090000_workout_template_days_and_metadata.sql` righe 54-67 — `insert into workout_template_days (template_id, name, sort_order) select wt.id, 'Workout A', 0 from workout_templates wt where not exists (days) and exists (workout_template_exercises)`, poi `update workout_template_exercises set template_day_id = (...)` — preserva ordine/serie/reps/recupero/note (nessuna colonna toccata, solo `template_day_id` valorizzato). **Gap trovato**: la clausola `and exists (select 1 from workout_template_exercises wte where wte.template_id = wt.id)` esclude dal backfill un eventuale modello personale pre-esistente con ZERO esercizi — quel modello resterebbe senza alcun giorno, contraddicendo il testo del requisito ("nessun modello rimasto senza giorni"). Mitigazione trovata: `workout-template-form.tsx` ha sempre validato lato client `if (exercises.length === 0) setError('Aggiungi almeno un esercizio.')`, sia nella versione corrente sia in quella originale (commit `9f18fd2`, verificato con `git show 9f18fd2:mobile/src/components/workout-template-form.tsx`) — quindi nessun modello con 0 esercizi dovrebbe essere mai stato salvabile via UI. **Gravita: BASSA** (il gap esiste solo per righe create fuori dal normale flusso app, es. SQL diretto/import). **Prossimo intervento consigliato**: query diretta sul progetto reale (`select id from workout_templates where not exists (select 1 from workout_template_days where template_id = workout_templates.id)`) per confermare 0 righe orfane; se ne emergono, migration correttiva mirata. Non eseguibile in questo ambiente: nessun tool di introspezione DB live funzionante (`supabase db query --linked` fallisce su Windows per percorso con spazi, gia' documentato nel punto 11).
  2. Tab "Schede" → cartella "Allenamenti professionali" (vedi checklist dedicata sotto per la nuova struttura a cartella/sottocategorie, 2026-07-21) → una sottocategoria → deve comparire con i suoi modelli, ciascuno con badge obiettivo/livello e meta (settimane, sessioni/settimana, durata).
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: `mobile/src/components/professional-library-screen.tsx` (`PROFESSIONAL_CATEGORIES`, `CategoryRow` con conteggio, `TemplateRow` condiviso da `template-library-screen.tsx`); `TemplateRow` mostra badge `goal`/`level` (`AppBadge`) e meta `durationWeeks`/`sessionsPerWeek`/`dayCount`/`exerciseCount`. Commit `5aae09d`/`16cb00a`.
  3. Aprire un modello Push Pull Legs → deve mostrare 3 giorni (Push/Pull/Legs) ciascuno con i propri esercizi completi (mai vuoti/segnaposto), ordine/serie/reps/recupero/RPE-RIR/note leggibili.
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: `supabase/migrations/20260724090000_workout_template_system_programs.sql` righe 255-294 — modello "Push Pull Legs" con 3 `workout_template_days` (Push/Pull/Legs, sort_order 0/1/2), 6 esercizi ciascuno, tutti i campi popolati (order/sets/reps o reps_min-max/rest_seconds/notes/rpe_rir), nessun placeholder. Spot-check: `petto-panca-piana`/`gambe-squat`/`core-plank`/`dorso-trazioni` confermati presenti in `mobile/src/data/exercise-library.ts`. Mapping client in `mobile/src/lib/workout-plan-service.ts` (`mapTemplateRow`, righe 537-568) preserva `sortOrder`/tutti i campi esercizio. Commit `a7d53b4`.
  4. Provare a modificare un modello di sistema → deve comparire SOLO l'azione "Duplica nella mia libreria", mai "Modifica"/"Elimina"/"Sposta" (RLS deve rifiutare comunque un tentativo diretto).
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: UI — `mobile/src/app/schede/modello/[templateId].tsx` righe 340-359 (`template.isSystem ? <Duplica...> : <>Modifica/Duplica/Sposta/Elimina</>`). RLS — `workout_templates_coach_update`/`workout_templates_coach_delete` (`supabase/migrations/20260723090000_workout_template_days_and_metadata.sql`) richiedono sempre `not is_system and coach_id = auth.uid()`; `save_workout_template` fa inoltre `WHERE id = v_template_id AND coach_id = v_coach_id`, e per una riga di sistema `coach_id` e' `NULL` (CHECK `is_system AND coach_id IS NULL`), quindi la RPC fallisce esplicitamente con `NOT_FOUND` anche a prescindere dalla RLS. Commit `9f18fd2`/`5aae09d`.
  5. "Duplica nella mia libreria" su un modello di sistema → deve creare una copia in "La mia libreria" (`is_system=false`, `source_template_id` valorizzato) → modificarla (es. cambiare le ripetizioni di un esercizio) → verificare che l'originale di sistema resti invariato.
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: `duplicateWorkoutTemplate` (`mobile/src/lib/workout-plan-service.ts` righe 733-778) chiama `saveWorkoutTemplate` senza `id` → sempre ramo INSERT di `save_workout_template`, che scrive `is_system=false`/`coach_id=auth.uid()` incondizionatamente (mai letto dal payload) e `source_template_id` valorizzato dal chiamante. La copia ha un id UUID nuovo, distinto dall'originale: una successiva modifica passa dal ramo UPDATE con `WHERE id = <id copia> AND coach_id = auth.uid()`, che non puo' mai toccare la riga originale (id diverso). Commit `9f18fd2`/`5aae09d`.
  6. Con un secondo coach reale: verificare che non veda ne' la copia personale del punto 5 ne' le cartelle dell'altro coach, ma veda gli stessi 18 modelli professionali.
     - **[x] Completato lato codice, test runtime ancora aperto (richiede due account coach reali, mai disponibili in questo ambiente; verificato 2026-07-22, livello di verifica: statico).** Evidenza: RLS `workout_templates_read: for select using (is_system or coach_id = auth.uid())` (`supabase/migrations/20260723090000_workout_template_days_and_metadata.sql`) — un secondo coach non soddisfa `coach_id = auth.uid()` sulla copia altrui, ma soddisfa sempre `is_system` sui 18 modelli professionali; `template_folders_coach_own_all: for all using (coach_id = auth.uid())` (`supabase/migrations/20260722090000_workout_template_folders_and_exercises.sql`) esclude sempre le cartelle di un altro coach. Commit `9f18fd2`/`5aae09d`.
  7. Nel pannello filtri, impostare Obiettivo="Dimagrimento" + Sessioni a settimana=3 + Luogo="Palestra" → verificare che i risultati si combinino (AND) e includano "Full Body Metabolico"; provare anche la ricerca testuale per nome di un esercizio (es. "squat") → deve trovare i modelli che lo contengono.
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: `matchesLibraryFilters` (`mobile/src/components/template-library-screen.tsx` righe 87-105) applica ogni filtro come `if (filtro && campo !== filtro) return false` indipendente — semantica AND pura, nessun OR. "Full Body Metabolico" confermato con `goal='Dimagrimento'`, `sessions_per_week=3`, `location='Palestra'` esattamente (`supabase/migrations/20260724090000_workout_template_system_programs.sql` righe 27-31). Ricerca per esercizio: `templateSearchHaystack` (stesso file, righe 70-85) include `template.exerciseIds.map((id) => getExerciseById(id)?.name)` nell'haystack di ricerca. Commit `5aae09d`/`b8523b5`.
  8. Assegnare un programma multi-giorno (es. Upper/Lower Ipertrofia, 4 giorni) a un cliente `active` (es. Rita) → deve comparire la conferma con il conteggio corretto di schede create ("4 schede assegnate a Rita"), MAI un'apertura automatica del logger.
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: RPC `assign_workout_template_to_client` (versione live: `supabase/migrations/20260726090000_appointments_next_workout_link.sql` righe 94-170, `create or replace` sulla stessa firma) richiede esplicitamente `coach_clients.status = 'active'` (mai 'invited'/'suspended'/'removed') e rifiuta un modello personale di un altro coach (`FORBIDDEN`) salvo che sia di sistema. `handleConfirmAssign` (`mobile/src/app/schede/modello/[templateId].tsx` righe 162-202) mostra la conferma solo dopo `result.ok === true`, con `dayCount: result.data.length`; le uniche azioni post-conferma sono "Resta qui" (nessuna navigazione) o "Vai al cliente" (`/clienti/[id]?tab=schede`) — mai un'apertura diretta di `/schede/[id]`/`ExerciseSetLogger`. Atomicita': l'intero ciclo per-giorno gira dentro una singola funzione `security definer`, un'eccezione a meta' ciclo fa rollback automatico dell'intera chiamata (nessuna assegnazione parziale). Commit `9f18fd2`/`5aae09d`/`aba760b`.
  9. Clienti → Rita → tab Schede → verificare che Rita abbia ricevuto TUTTI e 4 i giorni come schede reali indipendenti, con esercizi/serie/reps/recupero/RPE-RIR copiati correttamente.
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: lo stesso ciclo `for v_template_day in ... loop` in `assign_workout_template_to_client` crea, per ogni giorno del modello, una riga `workout_plans` indipendente (id proprio, `day_label` = nome del giorno) + `workout_days` + `workout_day_exercises` con TUTTI i campi copiati inclusa `rpe_rir`. `handleConfirmAssign` scrive tutti i piani ritornati nello store locale (`result.data.forEach((plan) => addWorkoutPlan(plan))`, fix BUG-043/commit `c3bb1f4`) — `getClientPlans` (`mobile/src/lib/workout-progress.ts` righe 21-26) filtra per `clientId` senza deduplicare per nome/giorno, quindi tutti e N i piani compaiono distintamente. Commit `c3bb1f4`/`aba760b`.
  10. Tornare al modello originale (di sistema o la copia personale usata al punto 8) → modificarne un esercizio → salvare → riaprire una delle schede di Rita → verificare che NON sia cambiata (copia indipendente, come per i modelli pre-esistenti).
     - **[x] Completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico).** Evidenza: grep sull'intero corpo di `save_workout_template` (righe 367-535 di `supabase/migrations/20260723090000_workout_template_days_and_metadata.sql`) conferma ZERO riferimenti a `workout_plans`/`workout_days`/`workout_day_exercises` — la RPC scrive esclusivamente su `workout_templates`/`workout_template_days`/`workout_template_exercises`. Nessun percorso strutturale puo' propagare una modifica del modello a una copia gia' assegnata. Stessa conclusione gia' raggiunta per il caso DELETE nel punto 11 (commit `217cdcf`). Commit `9f18fd2`/`5aae09d`.
  11. Eliminare il modello personale usato per l'assegnazione (non quello di sistema, che non e' eliminabile) → verificare che le schede di Rita, la loro cronologia e i carichi eventualmente gia' inseriti restino intatti (`workout_plans`/`workout_days`/`workout_day_exercises` non hanno alcun vincolo di cancellazione a cascata dal modello).
     - **[x] Requisito "nessun vincolo di cancellazione a cascata dal modello" — completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico, non basato su note/dichiarazioni precedenti).** Evidenza: `docs/SUPABASE_SCHEMA.sql` (colonna `workout_plans.template_id uuid references public.workout_templates(id) on delete set null`, MAI `on delete cascade` — nessuna migration successiva la modifica, verificato con grep su tutte le migration); RPC `assign_workout_template_to_client` (`supabase/migrations/20260723090000_workout_template_days_and_metadata.sql`) crea sempre righe `workout_plans`/`workout_days`/`workout_day_exercises` indipendenti, senza riferimento vivo al contenuto del modello; `exercise_progress_history.workout_plan_id` (i "carichi"/cronologia) referenzia solo `workout_plans` con `on delete set null` (`supabase/migrations/20260718120000_exercise_progress_history.sql`, riga 35) — NESSUNA colonna della tabella referenzia `workout_templates`, quindi eliminare un modello non puo' toccarla ne' direttamente ne' transitivamente; `deleteWorkoutTemplate` (`mobile/src/lib/workout-plan-service.ts`) esegue solo `DELETE` su `workout_templates`, nessuna cascata applicativa aggiuntiva. Commit di riferimento: `9f18fd2` (introduce la struttura), `5aae09d`/`a7d53b4`/`b8523b5` (estendono a giorni/metadati/sistema, FK invariata). **Resta aperto**: nessun test con clic reali (assegnare a Rita, eliminare davvero il modello personale, verificare a schermo schede/cronologia/carichi) — nessun tool di automazione browser disponibile in questo ambiente, coerente con gli altri 11 scenari di questa checklist (checkbox padre sopra resta `[ ]`, invariato).
  12. Tab "Schede" (bottom bar) → verificare che non selezioni mai un cliente, in nessun momento di questo percorso (ne' prima ne' dopo un'assegnazione).
     - **[x] Requisito "la libreria globale non seleziona mai un cliente" — completato lato codice, test runtime ancora aperto (verificato 2026-07-22, livello di verifica: statico, non basato su note/dichiarazioni precedenti).** Comportamento richiesto: durante l'intero percorso libreria → cartella/professionali → dettaglio modello → assegnazione, la tab "Schede" (`/schede`) non deve mai portare/mostrare un clientId, ne' prima ne' dopo un'assegnazione. Evidenza — schermate: `mobile/src/app/schede/index.tsx` (`<TemplateLibraryScreen folderId={null} />`, nessun parametro), `schede/cartella/[folderId].tsx` (legge solo `folderId`), `schede/professionali/{index,[goal]}.tsx` (legge solo `goal`), `mobile/src/components/template-library-screen.tsx` e `professional-library-screen.tsx` (nessun `useClientStore`, nessuna lettura di `clientId` — verificato con grep, l'unico riferimento a `useClientStore` nell'intera cartella `schede/` e' in `schede/modello/[templateId].tsx`, dove serve ESCLUSIVAMENTE a popolare la lista chip "Assegna a cliente", mai a preselezionare/filtrare la libreria). Evidenza — navigazione: tab bar (`mobile/src/components/app-tabs.tsx`/`app-tabs.web.tsx`, entry `{ path: '/schede' }`, percorso statico senza parametri) e OGNI `router.replace('/schede')`/`fallbackHref="/schede"` nel codice (`schede/modello/[templateId].tsx`, `schede/modello/new.tsx`, `schede/modelli/index.tsx`, `template-library-screen.tsx`, `professional-library-screen.tsx`, `schede/[id].tsx`, `schede/new.tsx` — verificato con grep su tutto `mobile/src`) e' sempre un path nudo, mai un oggetto con `params`. Dopo un'assegnazione riuscita, `handleConfirmAssign` in `schede/modello/[templateId].tsx` mostra solo "Resta qui" (resta sul dettaglio modello, nessuna navigazione) o "Vai al cliente" (naviga a `/clienti/[id]?tab=schede`, MAI a `/schede`) — nessun percorso porta a `/schede` con uno stato cliente residuo. Commit di riferimento: `9f18fd2` (feat: organizza i modelli scheda in cartelle), `16cb00a` (refactor: raggruppa i modelli professionali in una cartella), `c3bb1f4` (fix BUG-043, "Vai al cliente" introdotto proprio per separare esplicitamente l'assegnazione dalla libreria). **Resta aperto**: nessun test con clic reali (navigare davvero nel percorso completo, incluso dopo un'assegnazione, e osservare a schermo che il tab "Schede" non mostri mai contesto cliente) — nessun tool di automazione browser disponibile in questo ambiente. Checkbox padre della checklist (12 scenari) resta `[ ]`, invariato: restano aperti i punti 1-10 (mai testati con clic reali).

- [x] **Refactor: raggruppa i modelli professionali in una cartella (2026-07-21, continuazione):** la radice `/schede` non mostra piu' direttamente le 18 card dei modelli professionali — sostituite con una cartella virtuale "Allenamenti professionali" (nessuna riga in `template_folders`, non rinominabile/eliminabile/spostabile) con 7 sottocategorie automatiche basate sul valore reale di `goal`. Vedi `docs/WORKLOG.md` (voce 2026-07-21 "Refactor: raggruppa i modelli professionali"). Commit `16cb00a`.
- [ ] **Verifica reale cartella "Allenamenti professionali" (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, dipende dall'esecuzione delle due migration `20260723090000`/`20260724090000` ancora pendenti, altrimenti la cartella e le sottocategorie mostrano 0 modelli):**
  1. Tab "Schede" → deve comparire UNA SOLA cartella "Allenamenti professionali" (icona a stella, sottotitolo "18 modelli completi") seguita dalle cartelle personali del coach e dalle sue schede senza categoria — MAI le 18 card dei modelli direttamente in radice.
  2. Aprire "Allenamenti professionali" → devono comparire le 7 sottocategorie (Dimagrimento, Massa muscolare, Ricomposizione corporea, Forza e Powerbuilding, Principianti, Allenamento a casa, Performance), ciascuna con il conteggio corretto (3/3/2/3/3/2/2).
  3. Aprire "Forza e Powerbuilding" → devono comparire solo "Forza Base 5x5", "Powerbuilding", "Forza e Ipertrofia" (3 modelli, nessun modello di un'altra categoria).
  4. Aprire "Allenamento a casa" → devono comparire solo "Corpo Libero" e "Manubri ed Elastici" (2 modelli).
  5. Da una sottocategoria, aprire un modello → deve restare read-only (nessuna azione "Modifica"/"Elimina"/"Sposta", solo "Duplica nella mia libreria") → duplicarlo → verificare che la copia compaia in "La mia libreria" e sia modificabile normalmente.
  6. Tornare alla radice → verificare che le cartelle personali del coach e le sue schede senza categoria restino visibili e invariate (creabili/rinominabili/eliminabili come prima di questo intervento).
  7. Dalla radice, cercare il nome di un modello professionale (es. "Push Pull") → deve comparire come riga piatta sotto "Professionali" (non dentro la cartella) → toccarla deve aprire il dettaglio del modello.
  8. Dalla radice, aprire il pannello filtri e selezionare SOLO una cartella personale del coach (nessun testo di ricerca, nessun altro filtro) → la cartella virtuale "Allenamenti professionali" deve restare l'unica cosa mostrata riguardo ai modelli di sistema (cioe' NON deve comparire una sezione "Professionali" con tutte le 18 card) — questo era un bug reale trovato e corretto prima del commit, va confermato che il fix regga anche con un clic reale.

- [x] **Verifica (audit codice/RLS/RPC, non clic reali) — regole di proprieta' e immutabilita' dei modelli scheda (2026-07-22):** su richiesta esplicita dell'utente, controllate senza modificare codice le seguenti quattro proprieta' di `schede/modello/[templateId].tsx` e delle tabelle/RPC collegate:
  1. I modelli professionali `is_system=true` sono read-only e non eliminabili — confermato: RLS (`workout_templates_coach_update`/`_coach_delete` e le equivalenti su `workout_template_days`/`workout_template_exercises`) richiedono sempre `not is_system and coach_id = auth.uid()`; inoltre `save_workout_template` fa `WHERE id = v_template_id AND coach_id = v_coach_id`, e per una riga di sistema `coach_id` e' `NULL` (vincolo CHECK `is_system AND coach_id IS NULL`), quindi la RPC fallisce esplicitamente con `NOT_FOUND` anche a prescindere dalla RLS; lato UI "Modifica"/"Elimina"/"Sposta" non sono mai renderizzati per `template.isSystem`.
  2. I modelli personali sono modificabili/eliminabili solo dal coach proprietario — confermato: stesse policy RLS (`coach_id = auth.uid()`) su tutta la gerarchia modello→giorni→esercizi, piu' controllo esplicito `auth.uid()` con eccezione `FORBIDDEN` nelle RPC (`assign_workout_template_to_client`, `delete_template_folder`).
  3. Modificare/eliminare un modello originale non altera le copie `workout_plans` gia' assegnate ai clienti — confermato sui file: `workout_plans.template_id` ha `ON DELETE SET NULL` (non `CASCADE`, definito in `docs/SUPABASE_SCHEMA.sql`, nessuna migration successiva lo modifica, verificato con grep su tutte le migration); `assign_workout_template_to_client` crea righe indipendenti in `workout_plans`/`workout_days`/`workout_day_exercises`, scollegate dal contenuto del modello.
  4. Dopo l'eliminazione di un modello personale la navigazione torna alla cartella di origine o alla libreria generale — confermato: `router.replace(template.folderId ? '/schede/cartella/${folderId}' : '/schede')`, entrambe le route esistono.
  - **Livello di verifica registrato esplicitamente:** controlli completati tramite audit del codice, delle migration, delle policy RLS e delle RPC. **Non** e' stato eseguito un test diretto sul database live del vincolo `ON DELETE SET NULL` (punto 3): il comando `supabase db query --linked "select 1;"` non ha funzionato in questo ambiente Windows per un errore di percorso con spazi (`"C:\Program" non e' riconosciuto...`), riproducibile anche con una query banale — quindi resta una verifica di sola lettura dei file, non un'introspezione del database reale. Nessun bug reale trovato, nessuna modifica al codice e' stata necessaria (nessun commit di fix collegato a questa voce).

- [x] **Feat: collega gli appuntamenti ai workout del cliente (2026-07-21, continuazione 2):** nuovo appuntamento propone in automatico il primo workout pendente del programma del cliente (ordine stabile via `workout_plans.sequence_order`, mai giorno della settimana/data/nome esercizio/indice locale), con scelta manuale ("Cambia workout") e nuovo dettaglio appuntamento (`appuntamenti/[id].tsx`) con "Avvia workout" gated su stato `todo`. Vedi `docs/WORKLOG.md` (voce 2026-07-21 "Feat: collega gli appuntamenti ai workout del cliente"). Commit `aba760b`.
- [x] **AZIONE RICHIESTA — eseguire la nuova migration del collegamento appuntamenti↔workout:** **CONFERMATO APPLICATO il 2026-07-21** (vedi nota generale sotto). `workout_plans.sequence_order` e `appointments.workout_day_id` esistono ora sul progetto reale — le 12 verifiche manuali per questa feature (checklist sopra) possono essere eseguite.

- [x] **NOTA GENERALE — tutte le migration risultano applicate al progetto Supabase reale (verificato 2026-07-21, durante il fix BUG-043):** `npx.cmd --yes supabase@latest migration list` mostra la colonna "remote" popolata per OGNI migration locale fino a `20260726090000` inclusa, e `npx.cmd --yes supabase@latest db push --dry-run` conferma "Remote database is up to date" (nessun push reale eseguito in questa sessione). Questo significa che tutte le voci "AZIONE RICHIESTA — eseguire la migration..." precedenti in questo file, segnalate come BLOCCANTI nelle sessioni 2026-07-20/21, sono ora risolte — l'utente le ha applicate fuori da questa sessione. Restano pero' aperte TUTTE le checklist di verifica manuale con clic reali associate a ciascuna feature (nessuna e' mai stata eseguita in questo ambiente, che non ha alcun tool di automazione browser): sono la prossima priorita' reale, non piu' bloccata da una migration mancante.
- [ ] **Verifica reale collegamento appuntamenti↔workout (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 12 scenari richiesti esplicitamente, dipende dall'esecuzione della migration sopra):**
  1. Coach → "+ Nuovo appuntamento" → selezionare un cliente `active` con un programma assegnato (es. Rita con Upper/Lower Workout A/B/C tutti pendenti) → verificare che il picker cliente mostri SOLO i clienti attivi (nessun sospeso/rimosso).
  2. Con Rita selezionata → verificare che venga proposto automaticamente "Workout A" (il primo pendente in ordine), con la scheda "Upper/Lower" mostrata come Scheda assegnata.
  3. Premere "Cambia workout" → verificare che compaiano solo i workout pendenti reali di Rita (mai un modello della libreria) → scegliere manualmente "Workout B" → verificare che la scelta manuale sostituisca la proposta automatica.
  4. Salvare l'appuntamento → riaprirlo (tocco sulla riga in Agenda) → verificare che il workout collegato scelto al punto 3 sia rimasto quello (non sia tornato alla proposta automatica).
  5. Modificare data/ora dello stesso appuntamento (senza toccare "Cambia workout") → salvare → verificare che il workout collegato resti invariato.
  6. Annullare l'appuntamento (bottone "Annulla" in lista o stato in dettaglio) → verificare che il Workout B in questione resti "Da fare" per Rita (nessun avanzamento di sequenza, nessuna modifica a `workout_plans`/`workout_days`).
  7. Creare un nuovo appuntamento per Rita → verificare che la proposta automatica sia ancora "Workout A" (il B era stato scelto manualmente ma mai completato, l'annullamento del punto 6 non deve averlo consumato).
  8. Su un appuntamento con workout collegato ancora "Da fare" e non annullato → premere "Avvia workout" → verificare che apra la scheda corretta di Rita (non di un altro cliente) usando l'UUID reale, e che NON la segni come completata automaticamente.
  9. Completare davvero "Workout A" di Rita dal logger (checkbox esercizi + fine allenamento) → creare un nuovo appuntamento per Rita → verificare che la proposta automatica sia ora "Workout B" (l'avanzamento e' avvenuto solo al completamento reale, non alla creazione/apertura/Avvia/cambio data di un appuntamento).
  10. Provare a selezionare un cliente sospeso o rimosso nel form nuovo appuntamento → verificare che non compaia affatto nel picker.
  11. (Se possibile via chiamata diretta/devtools) Tentare di salvare un appuntamento con un `workout_day_id` di un workout che NON appartiene al cliente selezionato (id manipolato) → deve essere rifiutato lato server (RLS + validazione applicativa), mai salvato.
  12. Creare un appuntamento SENZA collegare alcun workout ("Nessun workout") → verificare che venga salvato correttamente e che il dettaglio mostri "Nessun workout collegato" invece di un errore, con "Avvia workout" assente/disabilitato.

- [x] **Fix BUG-042 — filtri esercizi aperti automaticamente in creazione/modifica scheda (2026-07-21, continuazione 3):** il pannello "Filtri" e il menu "Gruppo muscolare" in `exercise-catalog-picker.tsx` (condiviso da creazione/modifica scheda e modello) partivano gia' aperti ad ogni apertura del picker (`filtersOpen`/`openMenu` con default sbagliato). Corretti solo i due valori iniziali. Vedi `docs/BUGS.md` (BUG-042), `docs/WORKLOG.md`. Commit `8e1b853`.
- [ ] **Verifica reale filtri esercizi chiusi all'ingresso (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 6 scenari richiesti esplicitamente):**
  1. Coach → creazione scheda (o modello) → "+ Aggiungi esercizio" → verificare che il pannello "Filtri" sia chiuso e nessun menu (Gruppo muscolare/Attrezzatura/Difficolta'/Seleziona esercizio) sia aperto.
  2. Aprire "Gruppo muscolare" e scegliere "Petto" → verificare che il menu si richiuda subito e che il bottone mostri "Petto" come testo.
  3. Aprire "Attrezzatura" → verificare che "Gruppo muscolare" si richiuda automaticamente (un solo menu aperto alla volta).
  4. Chiudere il picker ("Chiudi" o tornare indietro) e riaprirlo ("+ Aggiungi esercizio") → verificare che nessun menu/filtro sia aperto automaticamente, anche se un valore era stato scelto in precedenza nella stessa sessione.
  5. Verificare che il valore selezionato (es. "Petto") resti visibile come testo sul bottone chiuso.
  6. Ripetere i punti 1-5 su Web, Android e iOS (nessuna differenza attesa: fix puro React Native, nessun branch `Platform.select` coinvolto nello stato open/closed).

- [x] **Fix BUG-043 — modelli professionali assegnati ma invisibili nelle schede del cliente (2026-07-21, continuazione 4):** `schede/modello/[templateId].tsx` non scriveva mai le schede appena create (via RPC `assign_workout_template_to_client`, gia' corretta) nello store locale `useTrainingStore` — l'unica fonte letta da `Clienti → [id] → Schede`. Aggiunta la stessa chiamata `addWorkoutPlan` gia' usata da `schede/new.tsx`/`schede/modelli/[id].tsx`; aggiunta diagnostica `__DEV__`; "Vai al cliente" apre ora direttamente la tab Schede (nuovo parametro `tab` opzionale su `clienti/[id].tsx`). Nessuna migration nuova (RPC gia' corretta). Vedi `docs/BUGS.md` (BUG-043), `docs/WORKLOG.md`. Commit `c3bb1f4`.
- [ ] **Verifica reale assegnazione modelli professionali (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 10 scenari richiesti esplicitamente, ORA eseguibile: le migration risultano applicate al progetto reale, vedi nota generale sopra):**
  1. Coach → Schede → Allenamenti professionali → una categoria → un modello → "Assegna a cliente" → scegliere Rita (active) → confermare → verificare la card di conferma con il conteggio giorni corretto.
  2. In console (DEV): verificare i log `TEMPLATE_ASSIGN_RPC_RESPONSE` (planIds valorizzati, errorMessage null) e `TEMPLATE_ASSIGN_SUCCESS` (workoutPlanIds, dayCount, exerciseCount coerenti col modello, isSystem=true).
  3. Toccare "Vai al cliente" → deve aprire direttamente Clienti → Rita sulla tab "Schede" (non "Panoramica") → la scheda/e appena assegnata/e deve/devono essere gia' visibili SENZA alcun refresh manuale ne' riavvio app.
  4. Contare i giorni e gli esercizi mostrati per Rita → devono corrispondere esattamente al modello (stesso numero di giorni, stesso numero di esercizi per giorno, stesso ordine/serie/reps/recupero/RPE-RIR/note).
  5. Tornare al modello di sistema usato → modificarlo (se possibile, altrimenti duplicarlo e modificare la copia) → verificare che le schede di Rita NON cambino (copia indipendente).
  6. Ripetere l'assegnazione con un modello PERSONALE del coach (non di sistema) → verificare che funzioni identicamente.
  7. (Se verificabile) Con un secondo coach reale, tentare di assegnare un modello personale del PRIMO coach → deve essere rifiutato (FORBIDDEN).
  8. Tentare di assegnare un modello a un cliente sospeso o rimosso → il cliente non deve nemmeno comparire nel picker (gia' filtrato lato UI); se forzato via chiamata diretta, la RPC deve rifiutare con CLIENT_NOT_ACTIVE.
  9. (Se possibile via devtools) Forzare un errore a meta' RPC (es. client_id valido ma non piu' active tra la UI e l'invio) → verificare che NON resti alcuna scheda parziale per quel cliente (rollback transazionale).
  10. Verificare in console il log `TEMPLATE_ASSIGN_ERROR` per un tentativo di assegnazione non autorizzato (nessun token registrato, solo templateId/isSystem/clientId/message).

- [x] **Fix BUG-044 — filtro "Petto" selezionato automaticamente nel catalogo esercizi (2026-07-21, continuazione 5):** `selectedGroup` in `exercise-catalog-picker.tsx` non aveva mai uno stato "tutti i gruppi" (a differenza di equipmentFilter/difficultyFilter), partiva sempre da `firstGroup` che risolveva quasi sempre a 'petto'. Aggiunto lo stesso sentinel `'all'` gia' usato dagli altri due filtri, opzione "Tutti i gruppi" nel menu, pill visibile solo dopo selezione esplicita con rimozione reale. Vedi `docs/BUGS.md` (BUG-044), `docs/WORKLOG.md`. Commit `eb50a77`.
- [ ] **Verifica reale filtro gruppo muscolare neutro all'apertura (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 5 scenari richiesti esplicitamente):**
  1. Coach → creazione/modifica scheda (o modello) → "+ Aggiungi esercizio" → verificare che NESSUN filtro sia selezionato (nessuna pill visibile nell'header "Filtri") e che la lista mostri esercizi di TUTTI i gruppi muscolari.
  2. Aprire "Gruppo muscolare" → verificare la presenza di "Tutti i gruppi" in cima, gia' evidenziato come attivo.
  3. Selezionare "Petto" → verificare che compaia la pill "Petto x" nell'header e che la lista si restringa ai soli esercizi del petto.
  4. Toccare la "x" sulla pill "Petto" → verificare che la pill sparisca e che la lista torni a mostrare tutti gli esercizi di tutti i gruppi.
  5. Selezionare di nuovo "Petto", chiudere completamente il picker ("Chiudi" o tornare indietro) e riaprirlo ("+ Aggiungi esercizio") → verificare che il filtro sia tornato neutro (nessuna pill, elenco completo), non "Petto" residuo dalla sessione precedente.

- [x] **Fix BUG-045 — layout titolo nella card scheda coach (2026-07-21, continuazione 6):** nella hero card del dettaglio scheda lato coach (`schede/[id].tsx`), un titolo lungo/composto (es. "Powerbuilding — Lower Hypertrophy") andava a capo su 3 righe disordinate accanto al box "N esercizi". Ridotti `fontSize`/`lineHeight` del titolo, `numberOfLines` 3→2, `flexShrink` esplicito su titolo/box esercizi. Vedi `docs/BUGS.md` (BUG-045), `docs/WORKLOG.md`. Commit `58d2eea`.
- [ ] **Verifica reale layout titolo card scheda (priorita' media, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 6 scenari richiesti esplicitamente):**
  1. Coach → aprire una scheda con un titolo lungo/composto (es. un giorno di un modello assegnato tipo "Powerbuilding — Lower Hypertrophy") → verificare che il titolo resti ordinato, su un massimo di 2 righe, con ellipsis se troppo lungo (mai una parola isolata su una terza riga).
  2. Verificare che il box "N esercizi" a destra resti sempre completamente visibile, mai schiacciato o parzialmente coperto dal titolo.
  3. Aprire una scheda con un titolo corto (es. "Workout A") → verificare che il layout resti bilanciato, nessuno spazio vuoto anomalo.
  4. Confrontare con una scheda dal titolo lungo ma su UNA parola sola lunghissima (edge case, es. un nome esercizio concatenato) → verificare che tronchi con ellipsis invece di causare overflow orizzontale.
  5. Ripetere su uno schermo stretto (~360-375px, layout impilato `stackHero`) → verificare che la card resti pulita, titolo e box esercizi non si sovrappongano.
  6. Verificare che badge stato, nome cliente, data e scadenza restino leggibili e ben distinti dal titolo (gerarchia visiva), sia in tema chiaro sia in tema scuro.

- [x] **Protezione server-side completa delle schede completate (2026-07-21, continuazione 7):** `save_workout_plan` non controllava mai `session_status` prima di scrivere (a differenza di `update_workout_session_progress`) — corretto con controllo `WORKOUT_LOCKED` nel ramo UPDATE + trigger `workout_plans_prevent_completed_edit` (`BEFORE UPDATE`, poi esteso a `OR DELETE`). Restava anche possibile modificare direttamente `workout_days`/`workout_day_exercises` di un piano completato — corretto con due nuovi trigger `BEFORE INSERT OR UPDATE OR DELETE` e riordino di `update_workout_session_progress` (esercizi scritti prima, piano completato scritto per ultimo). Vedi `docs/WORKLOG.md` (voce 2026-07-21 "Protezione server-side completa delle schede completate"). Commit `618ac963`, `7c4f807a`. Migration `20260727090000_lock_completed_workout_plan_edits.sql`, `20260728090000_lock_completed_workout_structure.sql`.
- [ ] **AZIONE RICHIESTA — verificare/applicare la migration `20260728090000` (priorita' alta):** `npx.cmd --yes supabase@latest migration list` (2026-07-21) conferma `20260727090000` gia' APPLICATA al progetto reale, ma `20260728090000` ANCORA NON APPLICATA (colonna remote vuota) — finche' non viene applicata, un UPDATE/INSERT/DELETE diretto via PostgREST su giorni/esercizi di una scheda completed resta tecnicamente possibile. Non dare per scontato lo stato di questa o di future migration senza rieseguire `migration list`.
- [ ] **Verifica reale protezione schede completate (priorita' alta, mai fatta con clic/chiamate reali — nessun tool di automazione browser ne' accesso REST diretto disponibile in questo ambiente, 7 scenari richiesti esplicitamente, dipende dall'applicazione della migration sopra):**
  1. Scheda `todo`/`in_progress` (mai completata) → coach modifica nome/esercizi/serie/ripetizioni/recupero/note da `schede/[id].tsx` → deve salvare normalmente.
  2. Cliente/coach segna progressi (esercizi completati, timer) su una scheda `in_progress` → deve funzionare normalmente.
  3. Completare davvero una sessione (checkbox esercizi + "Fine allenamento") → deve passare a `completed` senza errori, esercizi svolti marcati correttamente.
  4. Sulla stessa scheda ora `completed`: `save_workout_plan` (tentativo di modifica nome/esercizi da UI o chiamata diretta) → deve essere rifiutato con `WORKOUT_LOCKED`.
  5. Sulla stessa scheda: un UPDATE diretto via PostgREST su `workout_plans` (bypassando la RPC) → deve essere rifiutato dal trigger.
  6. Sulla stessa scheda: INSERT/UPDATE/DELETE diretti su `workout_days`/`workout_day_exercises` (bypassando `save_workout_plan`) → devono essere rifiutati dai nuovi trigger, incluso il caso di riassegnazione (`workout_plan_id`/`workout_day_id` cambiato verso un piano ancora aperto).
  7. Consultare lo storico di una scheda completed (sola lettura, sia lato coach sia lato cliente) → deve restare pienamente consultabile; una NUOVA sessione indipendente (nuova assegnazione) deve restare modificabile normalmente.

- [x] **Fix: protegge le route della libreria schede coach (2026-07-21, continuazione 8):** `AuthGate` non copriva `/schede/modello` (singolare, diversa da `/schede/modelli`), `/schede/professionali`, `/schede/cartella` — un cliente poteva aprirle via URL/deep link diretto. Aggiunti i tre prefissi mancanti a `COACH_ONLY_PREFIXES`; aggiunto guard locale (`isCoach`/`CoachOnlyNotice`, stesso pattern gia' in uso in `modello/[templateId].tsx`) a `schede/modello/new.tsx` e `schede/new.tsx`. Vedi `docs/WORKLOG.md` (voce 2026-07-21 "Fix: protegge le route della libreria schede coach"). Commit `89a9701`.
- [ ] **Verifica reale protezione route coach libreria schede (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 6 scenari richiesti esplicitamente):**
  1. Login coach → aprire normalmente tutte le route sotto `/schede/modello/`, `/schede/professionali/`, `/schede/cartella/`, `/schede/new` → nessuna regressione, comportamento invariato.
  2. Login cliente con coach → aprire via URL diretto `/schede/modello/new` → deve comparire l'accesso negato (redirect immediato a `/cliente-home`, mai il form).
  3. Login cliente autonomo (self-guided) → stesso test → stesso risultato.
  4. Login superadmin → aprire le stesse route → deve essere allontanato da `SUPERADMIN_ONLY_PREFIXES` prima ancora di raggiungere il guard locale (comportamento preesistente, non una regressione di questo fix) — verificare che non venga mai trattato come coach.
  5. Verificare che non ci sia alcun flash del form/contenuto reale prima del redirect, su connessione lenta/CPU throttling.
  6. Da una route negata, verificare che back/navigazione riporti a una route consentita (tab bar cliente), non a una schermata vuota o a un loop.

- [ ] **Elenco aperto dall'audit di stabilizzazione (2026-07-21), aggiornato 2026-07-22 (continuazione 5 — pulizia finale del progetto):**
  1. Test runtime reali (REST/RPC dirette, non solo traccia di codice) delle migration `20260727090000`/`20260728090000` — vedi checklist dedicata sopra.
  2. ~~Commit separato per le modifiche RevenueCat~~ — **risolto**: `b8a8b72 feat: aggiunge la gestione abbonamento RevenueCat`, poi `4f366e9 fix: aggiorna il pacchetto dopo un cambio prodotto RevenueCat`.
  3. ~~Verifica e commit del parser BIA InBody120~~ — **il flusso BIA e il parser InBody120 sono stati rimossi interamente dal repository** (mai committati prima, `45dbc5e refactor: rimuove il flusso BIA dall'app`, 2026-07-22 continuazione 5) invece di essere completati/committati, su richiesta esplicita dell'utente. Vedi checklist dedicata "Test runtime dopo la rimozione del flusso BIA" sotto.
  4. ~~Ritorno non corretto dopo l'eliminazione di una scheda cliente~~ — **risolto 2026-07-22, vedi BUG-047 sopra** (`schede/[id].tsx` torna ora al dettaglio del cliente proprietario, tab Schede, mai alla libreria modelli).
  5. ~~Nascondere la tab/voce Chat ai clienti autonomi~~ — **risolto**: `924489f fix: nasconde la chat ai clienti autonomi` (tab bar) e `48a0dd1 fix: nasconde le azioni coach ai clienti autonomi` (Home cliente: Check-in/Prenotazioni/Bacheca/Chat con il coach). Test runtime ancora aperti, vedi checklist dedicata piu' sotto.
  6. Eseguire la migration del bucket Storage `exercise-videos` (versionata in `20260729090000_version_exercise_videos_storage_bucket.sql`, mai eseguita su un progetto reale — vedi anche checklist dedicata sopra).
  7. **Ricreare `mobile/.env.example` — ancora BLOCCATO**: le impostazioni di permesso dell'ambiente usato in questa sessione impediscono qualunque scrittura sotto `mobile/.env*` (incluso un file placeholder senza valori reali). Da creare manualmente con questo contenuto esatto (solo placeholder, nessun valore reale — le uniche 5 variabili `EXPO_PUBLIC_*` effettivamente lette in `mobile/src`, verificato con grep):
     ```
     EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
     EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-public-key
     EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=your-revenuecat-android-public-sdk-key
     EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=your-revenuecat-ios-public-sdk-key
     EXPO_PUBLIC_ENABLE_DEMO_DATA=false
     ```
  8. ~~Eliminare le cartelle di backup residue~~ — **risolto 2026-07-22 (continuazione 5)**: `mobile_backup_20260715/` (52M) e `mobile_git_backup/` (1.5M) rimosse, commit `3860ba9 chore: rimuove backup e file obsoleti`.
  9. ~~Verificare e rimuovere le dipendenze npm inutilizzate~~ — **verificato 2026-07-22 (continuazione 5), nessuna rimossa**: `expo-glass-effect`/`expo-symbols` non sono importate da `mobile/src` ma sono dependencies dirette (non peer/dev/optional) di `expo-router` stesso — rimuoverle dal `package.json` di primo livello non le eliminerebbe realmente (npm le installerebbe comunque come transitive di `expo-router`) e rischierebbe di disallineare la versione pinnata dal resto dell'SDK Expo 57. `@expo/ngrok` confermato intenzionalmente presente (tool CLI di sviluppo, non un import applicativo).
  10. Test di un APK preview reale su device/emulatore Android (build EAS `preview`, mai eseguito in questo ambiente).
  11. Build AAB di produzione (EAS `production`), mai eseguita.
  12. **(nuovo 2026-07-22 continuazione 5)** `npx.cmd expo-doctor` segnala 8 pacchetti Expo con la patch version leggermente indietro rispetto a quella attesa dall'SDK 57 (`@expo/ui`, `expo`, `expo-asset`, `expo-constants`, `expo-dev-client`, `expo-image-picker`, `expo-notifications`, `expo-router`) — preesistente, non toccato in questa sessione (nessuna richiesta esplicita di aggiornare versioni). Valutare `npx expo install --check` in una sessione dedicata.

- [x] **Feat: storico carichi completo per singolo esercizio (2026-07-21, continuazione 9):** la card "Storico pesi" nella schermata esercizio mostrava l'intera lista di serie inline con un toggle. Riscritta come card compatta interamente cliccabile (Ultimo carico/Miglior carico/data ultima registrazione/stato vuoto "Nessun carico registrato" — verificato nel codice, nessun refuso "Nessun carico repleto" presente), il tap apre lo storico completo su `/storico-carichi` (route esistente riusata, non duplicata) filtrato per clientId+exerciseId (mai workoutPlanId). Vedi `docs/WORKLOG.md` (voce 2026-07-21 "Feat: storico carichi completo per singolo esercizio"). Commit `0acbeb4`.
- [ ] **Verifica reale storico carichi per singolo esercizio (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 7 scenari richiesti esplicitamente):**
  1. Esercizio senza alcun carico registrato → la card mostra lo stato vuoto compatto "Nessun carico registrato", nessuna lista/tabella.
  2. Registrare 3 serie su un esercizio → la card deve mostrare Ultimo carico e Miglior carico corretti (Miglior carico = massimo storico, non solo dell'ultima sessione).
  3. Lo stesso esercizio presente in piu' schede (passate/attuali/future) → aprendo lo storico completo, tutte le sessioni compaiono, raggruppate per data/scheda, mai un'unica lista disordinata per singola serie.
  4. Isolamento tra clienti: aprire lo storico di un cliente non deve mai mostrare dati di un altro cliente, ne' un cliente deve poter cambiare `clientId` da parametro URL.
  5. Navigazione indietro dallo storico completo → deve tornare esattamente alla stessa schermata/sessione esercizio da cui si e' partiti (stesso `planId`), non a una route generica.
  6. Coach e cliente: entrambi devono vedere lo stesso storico autorizzato per lo stesso esercizio/cliente (coach solo se il cliente e' realmente suo).
  7. Sessioni completate: lo storico deve restare in sola lettura (nessun bottone Elimina attivo su una serie di una sessione completata), sia per il coach sia per il cliente.

- [x] **Fix: usa la riga esatta della sessione esercizio, workoutDayExerciseId (2026-07-21, continuazione 10):** il match precedente (`52c06c9`, planId+exerciseId) trovava sempre la prima occorrenza del piano che usa quel tipo di esercizio, non necessariamente la riga realmente cliccata — con lo stesso esercizio inserito due volte nella stessa scheda, i carichi finivano sulla riga sbagliata. `schede/[id].tsx` passa ora `workoutExerciseId` (= `workout_day_exercises.id` reale) come chiave di ricerca primaria; `esercizi/[id].tsx` valida incrociando planId/exerciseId/clientId e non usa piu' `assignments[0]` come fallback in contesto sessione. Vedi `docs/WORKLOG.md` (voce 2026-07-21 "Fix: usa la riga esatta della sessione esercizio"). Commit `08adfc9`.
- [ ] **Verifica reale riga esatta sessione esercizio (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser disponibile in questo ambiente, 8 scenari richiesti esplicitamente dall'utente):**
  1. Stesso esercizio presente in quattro schede diverse dello stesso cliente → aprendone una specifica, deve aprirsi esattamente quella scheda/riga, non un'altra a caso.
  2. Stesso esercizio inserito due volte nella stessa scheda (es. due varianti/superserie) → ogni riga deve mantenere carichi separati, mai condivisi/sovrascritti a vicenda.
  3. Modificare manualmente il parametro `workoutExerciseId` nell'URL con l'id di una riga di un ALTRO piano → l'accesso deve essere rifiutato (schermata di errore, mai `ExerciseSetLogger` montato).
  4. Modificare manualmente il parametro con una riga valida ma di un ALTRO cliente → l'accesso deve essere rifiutato allo stesso modo.
  5. Coach e cliente aprono la stessa riga (stesso `workoutExerciseId`) → devono vedere/loggare esattamente la stessa sessione.
  6. "Indietro" dalla schermata esercizio deve tornare alla scheda/sessione corretta da cui si e' partiti.
  7. Lo storico carichi (`/storico-carichi`, feature precedente) deve continuare a funzionare per clientId+exerciseId, non deve essere stato toccato da questo fix.
  8. Verificare che non ci sia alcun fallback silenzioso: un parametro incoerente deve sempre mostrare l'errore di blocco, mai la prima assegnazione trovata a caso.

- [x] **Fix BUG-046: evita di rimigrare i workout completati (2026-07-22):** `runMigration` (`workout-plan-migration.ts`) ritentava piani gia' remoti/completed (ricomparsi nello store locale dopo un `setWorkoutPlans`), venendo bloccato da `WORKOUT_LOCKED` ad ogni refresh. Fix: `candidates` esclude ora ogni piano con `isValidUuid(plan.id)` — un id placeholder locale non e' mai un UUID, quindi un id UUID prova che la riga esiste gia' su Supabase. Vedi `docs/BUGS.md` (BUG-046), `docs/WORKLOG.md` (voce 2026-07-22). Commit `3c5fe60`.
- [ ] **Verifica reale migrazione locale workout / BUG-046 (priorita' alta, mai fatta con avvio app reale — nessun tool di automazione disponibile in questo ambiente, 10 scenari richiesti esplicitamente dall'utente):**
  1. Avvio app con un coach che ha almeno un workout remoto `completed`: nessun errore in console, nessun tentativo di scrittura (`WORKOUT_REMOTE_SAVE_ERROR` non deve comparire).
  2. Secondo avvio/refresh (stessa sessione o dopo restart): lo stesso workout non viene ritentato, nessun log ripetuto.
  3. Workout remoto `todo`: la sincronizzazione/lettura normale continua a funzionare, nessuna regressione.
  4. Workout remoto `in_progress`: idem.
  5. Tentativo manuale di modifica reale di una scheda `completed` (non via migrazione, ma da UI: `schede/[id].tsx`): `WORKOUT_LOCKED` deve restare un errore reale e visibile.
  6. Nessuna duplicazione di piani/giorni/esercizi dopo piu' refresh consecutivi.
  7. Nessuna perdita dello storico carichi/progressi sui piani coinvolti.
  8. Verificare il caso di un piano `completed` presente SOLO in locale (mai sincronizzato, id non-UUID, es. da prima del collegamento Supabase): deve essere inserito correttamente come riga nuova, senza errori.
  9. Nessuna schermata rossa (Web/Android) durante l'intero ciclo di avvio/refresh.
  10. Migrazione idempotente dopo logout/login (nessun ciclo, nessun errore ripetuto).

- [x] **Fix BUG-047: torna alle schede del cliente dopo l'eliminazione (2026-07-22, continuazione):** eliminare una scheda reale dal percorso Clienti → cliente → Schede → dettaglio reindirizzava a `/schede` (libreria globale modelli), perdendo il contesto cliente. Fix: `schede/[id].tsx`, `goToClientSchede(clientId)` → `router.replace({ pathname: '/clienti/[id]', params: { id: clientId, tab: 'schede' } })`, clientId sempre da `plan!.clientId`. Vedi `docs/BUGS.md` (BUG-047), `docs/WORKLOG.md` (voce 2026-07-22 continuazione). Commit `10f0383`.
- [ ] **Verifica reale navigazione dopo eliminazione scheda cliente / BUG-047 (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser/app disponibile in questo ambiente, 8 scenari richiesti esplicitamente dall'utente):**
  1. Elimino una scheda `todo` di un cliente (es. Rita) dal suo dettaglio → torno al dettaglio di Rita, tab Schede (non alla libreria modelli).
  2. La scheda eliminata non compare piu' nella lista schede di Rita.
  3. Elimino una scheda di un altro cliente (es. Luigi) → NON atterro sul dettaglio di Rita (o di un cliente sbagliato) — atterro sempre sul cliente proprietario della scheda eliminata.
  4. Cancellazione fallita (es. rete assente) → resto nel dettaglio scheda, nessuna navigazione, errore reale visibile.
  5. Tentativo di eliminare una scheda `completed` → bloccato (sia lato client con messaggio immediato, sia server-side se dovesse arrivare una richiesta diretta).
  6. Dopo il redirect, il tasto indietro non riapre la scheda eliminata (non deve tornare a `/schede/[id]` della scheda cancellata).
  7. Stesso comportamento su Web e su Android.
  8. La libreria globale dei modelli (`/schede`) resta invariata: aprirla direttamente dal tab bar coach continua a mostrare cartelle/modelli come prima, nessuna regressione.

- [x] **Nasconde chat e azioni coach-only ai clienti self_guided — completato lato codice, verifica statica effettuata (2026-07-22):** tab Chat rimossa dalla bottom bar (`client-tabs.tsx`/`client-tabs.web.tsx` via `visibleClientTabs()`, mode letto da `AuthGate`/`clientOnboarding.mode`, commit `924489f4`); tile Check-in/Prenotazioni/Bacheca e bottone "Chat con il coach" rimossi dalla Home cliente (`cliente-home.tsx`, flag `isCoachGuided` da `myCoach.clientMode`, commit `48a0dd15`). Verifica statica: `tsc --noEmit` pulito, `git diff --check` pulito, audit grep delle rotte coach-only, tracciamento manuale dei render-gate di `AuthGate` per escludere flash. **Nessun clic reale eseguito** — vedi checklist runtime dedicata sotto.
- [ ] **Test runtime navigazione cliente self-guided / coach-guided (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser/Android disponibile in questo ambiente, completato solo lato codice/verifica statica, commit di riferimento `924489f4` e `48a0dd15`):**

  CLIENTE SELF_GUIDED
  - [ ] La tab Chat non compare nella bottom bar.
  - [ ] Nella Home non compare il tile Check-in.
  - [ ] Nella Home non compare il tile Prenotazioni.
  - [ ] Nella Home non compare il tile Bacheca.
  - [ ] Nella Home non compare il pulsante "Chat con il coach".
  - [ ] Il tile Nutrizione resta visibile e funzionante.
  - [ ] La griglia Home resta ordinata senza spazi vuoti.
  - [ ] Un deep link diretto a /chat viene bloccato da AuthGate.
  - [ ] I deep link a /questionario, /prenotazioni e /bacheca vengono bloccati.
  - [ ] Refresh della pagina senza comparsa temporanea delle azioni coach-only.

  CLIENTE COACH_GUIDED
  - [ ] La tab Chat resta visibile.
  - [ ] Check-in, Prenotazioni, Bacheca e Chat con il coach restano visibili.
  - [ ] Tutte le relative route continuano ad aprirsi.
  - [ ] Nessuna regressione dopo refresh.

  CAMBIO ACCOUNT
  - [ ] Logout self_guided → login coach_guided aggiorna correttamente tab e Home.
  - [ ] Logout coach_guided → login self_guided nasconde immediatamente le azioni.
  - [ ] Nessun flash della configurazione dell'account precedente.

  PIATTAFORME
  - [ ] Verifica completa su Expo Web.
  - [ ] Verifica completa su Android dev build/APK.
  - [ ] Controllo layout su schermo stretto.

- [x] **Pulizia finale del progetto: rimozione flusso BIA, backup, script obsoleti — completato lato codice, verifica statica effettuata (2026-07-22, continuazione 5):** flusso BIA (upload/parsing/revisione PDF) rimosso interamente, incluso il parser InBody120 mai committato; metriche manuali/storico pesi/workout/RevenueCat verificati invariati; auto-inserimento cliente delle misure manuali (`resolveMetricsActor`/`allowClientActions`) e card "Profilo di allenamento" preservati come modifiche valide residue; backup obsoleti (`mobile_backup_20260715/`, `mobile_git_backup/`), script superato (`fix-workout-uuid.sql`), componente orfano (`status-dot.tsx`) rimossi. Verifica statica: `tsc --noEmit` pulito, `expo-doctor` 19/20 (1 issue preesistente non collegata), `npm ls --depth=0` pulito, `git diff --check` pulito, grep esaustivo zero riferimenti BIA residui. **Nessun clic reale eseguito** — vedi checklist dedicata sotto. Commit: `8746335`, `45dbc5e`, `736b88b`, `3860ba9`.
- [ ] **Test runtime dopo la rimozione del flusso BIA (priorita' alta, mai fatta con clic reali — nessun tool di automazione browser/Android disponibile in questo ambiente, completato solo lato codice/verifica statica, commit di riferimento sopra):**

  METRICHE MANUALI (invariate, da riconfermare dopo la rimozione BIA)
  - [ ] Coach: "Aggiungi misurazione" su un cliente salva correttamente peso/altezza/BMI/composizione/circonferenze.
  - [ ] Modifica ed eliminazione di una misurazione esistente funzionano ancora (icone matita/cestino, solo lato coach).
  - [ ] Grafici temporali (peso/massa grassa/girovita) e confronti ("Ultimo controllo"/"Dall'inizio") si popolano correttamente con almeno 2 rilevazioni.
  - [ ] Una riga storica con `source='bia_pdf'` (dati da una vecchia sessione, se presente sul progetto reale) resta visibile nello storico/grafici senza errori.
  - [ ] Nessun bottone o testo "BIA"/"PDF" visibile in nessuna schermata metriche (coach o cliente).

  AUTO-INSERIMENTO CLIENTE (funzionalita' preservata, mai testata dal vivo)
  - [ ] Cliente collegato a un coach attivo, su `/metriche`: vede il bottone "Aggiungi misurazione" e riesce a salvare una propria rilevazione.
  - [ ] La riga creata dal cliente ha `coach_id` corretto (il coach realmente collegato) e `created_by` uguale al cliente.
  - [ ] Cliente su `/metriche` NON vede le icone di modifica/eliminazione sulle rilevazioni esistenti (restano riservate al coach).
  - [ ] Cliente senza coach collegato (o senza `coach_clients` attivo): il salvataggio viene rifiutato con un messaggio chiaro, nessun errore silenzioso.
  - [ ] Verificare se la migration `20260717100000_client_metrics_client_entry.sql` (segnala "Not applied automatically by Codex") e' davvero applicata sul progetto reale — se non lo e', l'auto-inserimento cliente fallira' con un errore RLS.

  PROFILO DI ALLENAMENTO CLIENTE (`cliente-profilo.tsx`, funzionalita' preservata)
  - [ ] Cliente con onboarding completato: la card "Il mio profilo di allenamento" mostra obiettivi/peso/altezza/BMI/giorni/esperienza/aree di interesse corretti.
  - [ ] Cliente senza onboarding completato: messaggio "Profilo di allenamento non ancora compilato" (o "Percorso gestito dal tuo coach" per coach_guided), nessun errore.
  - [ ] Messaggio di conferma avatar ("Avatar aggiornato") scompare da solo dopo circa 2-3 secondi.

  REGRESSIONE GENERALE
  - [ ] Nessun import rotto/schermata bianca su nessuna route toccata (`metriche.tsx`, `cliente-profilo.tsx`, dettaglio cliente coach tab Metriche).
  - [ ] RevenueCat (acquisto/gestione/webhook), workout, storico carichi, avatar upload restano invariati — nessuna regressione.
  - [ ] Web e Android coerenti.
